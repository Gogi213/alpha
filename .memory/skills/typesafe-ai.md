---
title: TypeSafe (`typesafe-ai`) — установленный скилл: где лежит, что делает, где ключ
date: 2026-09-21
type: skill
salience: 2
last_access: 2026-09-21
tags: [skills, typesafe, ai, environment]
---

# Скилл `typesafe-ai` (TypeSafe / System One / Jev)

Зачем страница: скилл поставлен по просьбе владельца 21.09 — чтобы следующая сессия знала,
что он есть, откуда взялся и где брать ключ, не перечитывая README и не гадая.

## Что поставлено и куда

- Источник: `github.com/typesafe-ai/skills.git`, в пакете ровно один скилл `typesafe-ai`
  (MIT, `SKILL.md` + `LICENSE`).
- Способ установки (один, путь «для другого агента», т.к. работаю не в Claude Code):
  `npx skills add typesafe-ai/skills --skill typesafe-ai --agent claude-code --global --copy --yes`
  (`--agent claude` не существует — валидное имя `claude-code`; полный список выдаёт сам CLI).
- Лежит: `C:\Users\Георгий\.claude\skills\typesafe-ai\` — **глобально**, user-level.
  Каталог скиллов DSH читает из Claude-совместимых каталогов (`~/.claude/skills`,
  `~/.claude/plugins/cache/**/skills`), поэтому установка туда видна и в DSH.
- **В сессии установки скилл в каталоге не появляется** — каталог впрыскивается на старте
  сессии; он доступен со следующей. Проверка: `npx skills list` (проектные) и
  `Test-Path ~\.claude\skills\typesafe-ai`.
- Скан реестра при установке: Gen — Safe, Socket — 0 alerts, Snyk — Low Risk. Ручное чтение
  `SKILL.md` до установки: только guidance (какие примитивы, как строить вопросы, читать живые
  доки по `docs.typesafe.ai`), без исполняемых шагов и без указаний что-либо отправлять наружу.

## Что это по сути

TypeSafe — «единицы ИИ-суждения» как примитивы: модель **Jev** (`jev-latest`) принимает `state`
и карту типизированных `questions`, возвращает типизированные `answers` и вероятности вместо
свободного текста. Три примитива: `noul` (вероятность «да»), `choice` (одно из заданных
вариантов + распределение), `score` (взвешенная позиция на шкале уровней). Код владеет
workflow, модель даёт «программируемый здравый смысл» — маршрутизация, ранжирование, извлечение,
проверка. Применимо к alpha как источник типизированных суждений о состоянии рынка/уровня
(например, отнести касание к режиму), но это **не решено** — владелец такого решения не давал.

## API и ключ

```http
POST https://api.typesafe.ai/v1/systemone
Authorization: Bearer <API_KEY>
Content-Type: application/json
```

`model: "jev-latest"`, `questions` — карта с ключами по выбору вызывающего (ключ не уходит в
модель). Ответ: `answers` под теми же ключами + `usage`. Ошибки: `401` (ключ), `422` (валидация),
`429`/`529` — экспоненциальный backoff.

**Ключ — только в переменной окружения, значение в репозиторий и в память не пишется**
(правило проекта: секреты — только имена переменных). 21.09 владелец прислал ключ открытым
текстом в чат — он попал в историю сессии, поэтому его следует **отозвать и выпустить новый**,
а новый положить в окружение:

```powershell
[Environment]::SetEnvironmentVariable('TYPESAFE_API_KEY','<новый ключ>','User')
```

Имя переменной у SDK не подтверждено по докам (страница `api.md` задаёт только заголовок) —
при первой реальной интеграции сверить со страницей SDK, а не догадываться.
