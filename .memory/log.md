# Memory Log

> append-only 时间线。每条：`## [YYYY-MM-DD] <kind> | <title>`，可 `grep '^## \[' log.md` 解析。

## [2026-09-15] install | dsh-plugin-memory 初始化

- 记忆库脚手架已创建。Владелец отказался от «铸魂»-диалога: BOOTSTRAP.md → `status: complete`.

## [2026-09-15] seed | Память проекта заведена (dsh-plugin-memory)

- Каталог: `C:\visual projects\alpha\.memory` (внутри репо alpha), бэкенд — markdown+git, без LLM.
- Владелец отказался от «души»/персоны: BOOTSTRAP.md → `status: complete`, SOUL.md переписан в рабочую рамку.
- MEMORY.md сжат с 4.5 КБ (китайский) до ~1.6 КБ — бюджет boot-блока 8000 символов.
- index.md несёт блок «Состояние» — то, что переносится между сессиями.
- Настройки: `~/.dsh/memory.json` (автоинъекция вкл, активная追忆 выкл); trial-профиль: digest-nudge и autoCommit выключены.

## [2026-09-15] verify | Память сверена с реальностью: сутки 09-14 на сервере закрылись

- Сессия «новый чат»: поднята память (index + `projects/active/alpha.md`), затем сверена с фактами.
- Совпало: git (`dev/wave-4-record-verify`, HEAD `a8dd7d8`, дерево чистое), сервер (`active`,
  `NRestarts=0`, 60 binlog, `gaps.csv` пуст, `verify-*.status` — 0, 40 ГБ свободно), локально
  (коллектор не запущен, три каталога `data/always-on/*`).
- Единственный сдвиг: файлы `*-2026-09-14.binlog` отротированы 00:00 UTC, 436 МБ за 18:20→24:00Z
  (≈1.85 ГБ/сутки). Сутки можно закрывать `binlog-stats`/`verify`, но `verify` ещё не гонялся.
- Детали записаны в `projects/active/alpha.md` (раздел «Проверка состояния — 2026-09-15 00:15Z»).

## [2026-09-15] fact | Дрейф `docs/COMMANDS.md`: абзац «Состояние» (строки 29–40) устарел

- Там боевой пул из **десяти** и «идущий» локальный олвейс-он `data/always-on/20260912T122440Z/`.
- Факт: топ-50 заморожен (В-50, `3da0b3f`), местная запись остановлена владельцем (В-41).
- Не правился (правка репозитория, владелец не просил) — отметить в `log.md` при исправлении.

## [2026-09-15] skill | Заведена страница `skills/dsh-task-board.md`

- Плагин `@linxin666/dsh-client-ui-task-board` 0.3.22 (профиль `web`): хост-леджер
  `~/.dsh/task-board/ledger-v2.json` (схема v3, revision 0, `tasks: []`, TZ `Asia/Tbilisi`),
  запуск = новая DSH-сессия с пином workspace/пресет/permission, cron 5 полей без догонов,
  ворота прав, continuation-карточки `<<<FREEZE … >>>FREEZE`, shell-полей в протоколе нет,
  `announceToAgent`/`preventIdleSleep` по умолчанию off, телеметрия 1 heartbeat/сутки.
- Решение владельца: скорее всего не нужно — записано как отказ, не запрет; пакет оставлен.
- Страница cold (salience 3), чтобы не перечитывать README на 17 КБ.

## [2026-09-15] uninstall | Плагин task-board снят с профиля `web`

- Владелец: «убираем этот плагин». `dsh plugin --profile web remove
  @linxin666/dsh-client-ui-task-board` — со второй попытки (первая `EPERM`: песочница DSH не
  пускает запись в `~/.dsh/profiles/web`, пришлось разово расширять доступ).
- Проверено: `package.json` без пакета, `node_modules/@linxin666/dsh-client-ui-task-board` нет,
  `--dump-config` — 606 строк (было 612), ошибок нет; `ui-git-graph`, `ui-skill-explorer`,
  `agent-teams`, `dsh-memory`, `dsh-context` на месте.
- Остаток `~/.dsh/task-board/` (пустой леджер + lock + scheduler) удалён;
  `C:\Users\Георгий\.dsh\PROFILE-STATE.md` обновлён (строка состава помечена как снятая).
- Запись в сайдбаре GUI пропадёт после перезапуска `dsh web` (в живом процессе плагин остаётся
  загруженным).

## [2026-09-15] server | Новый VPS OVH Singapore: доступ восстановлен, ключ настроен

- Пришёл новый сервер: OVH `vps-cab27b0c.vps.ovh.ca`, `139.99.91.22`, регион **os-sgp2**,
  Ubuntu 26.04 LTS, 2 vCPU (Haswell), 3.7 ГБ RAM, 38 ГБ диск (36 свободно), TZ UTC,
  `ubuntu` с NOPASSWD sudo, из тулчейна только curl/python3/rsync/git (rustc/cargo нет).
- **Вход сломал я сам**: OVH отдаёт первичный пароль с флагом «истёк», sshd не даёт сессию
  (`Password change required but no TTY available`). Первый PTY-заход начал принудительную смену,
  новый пароль был сгенерирован внутри процесса и нигде не сохранён — обрыв унёс его, и машина
  осталась запертой (панель показывает старый, он уже не подходит). Проверено с двух IP.
- Владелец переустановил VPS через OVH CLI с предустановленным `id_rsa`; флаг «истёк» всё равно
  блокировал сессию — снят повторной сменой пароля через PTY **по ключу** (значение держалось в
  переменной оболочки, генерация и применение в одном вызове). Теперь: `chage` → expires never,
  `authorized_keys` = 1, sudo NOPASSWD, доступ по ключу проверен.
- Замер против старого сервера: TCP до `api.bybit.com` 3–4 мс против 26–35, полный ответ 26 мс
  против 580, `stream.bybit.com` (CloudFront) 2.6 мс. То есть сингапурская VPS к Bybit ближе;
  старое железо мощнее (4 vCPU / 7.9 ГБ).
- `ovhcloud vps set-password` на этом VPS недоступен (403 `This function is not available`),
  rescue-флага у CLI нет (только Manager). Страница: `skills/ovh-vps-singapore.md`.
- Назначение второго сервера не решено: перенос коллектора / второй пул / узел обработки.
  Гигиена не делалась (пароль по SSH не отключён, ufw и rustup не настраивались).

## [2026-09-15] deploy | Коллектор перенесён на новый сервер, замер 100 монет

- Решение владельца: «переносим коллектор полностью», затем прогон 10 минут на 100 монет и
  замер ресурсов. Диск: у VPS два диска — `sda` 40 ГБ (система) и неразмеченный **`sdb` 50 ГБ**;
  `sdb` → ext4 → `/opt/alpha` (fstab, 47 ГБ свободно). Отсюда «38 вместо 100 ГБ».
- Перенос через эту машину: бинарник (md5 `e1d721a9…`), исходники (`/opt/alpha/src` — **байт-в-байт
  равен репозиторию**, 133 файла `src/`), серверный топ-30 и systemd-юнит. Пересборка не нужна.
- Пул на 100 монет: `lob pick --window-secs 600 --h3-k 1.0 --top 100` → отдельные `--instruments-out`
  / `--candidates-out`, боевые `instruments.csv` и `docs/plan/candidates.csv` не тронуты.
- Прогон (`/opt/alpha/root100`, 600 с): **CPU 12.21 % ядра**, RSS 9.4→38.4 МБ, parse p99 107.5 мкс,
  очередь p99 194.6 мкс, 7.31 млн записей, 34.9 МБ, 2.49 Б/запись (v3), 0 gaps/resyncs/reconnects.
  Бюджет `PLAN.md` 6.1 (<5 %) превышен в 2.4 раза; машина при этом простаивает (2 ядра).
- Диск по фактическим монетам: топ-100 **5.02 ГБ/сутки (9 дней)**, топ-50 2.95 (16), топ-30 2.33 (20);
  топ-5 монет = 24.9 % байтов. Рычагов глубины у `lob session` нет — только размер пула,
  ротация/выгрузка, кодек (заморожен В-49) и профилирование.
- Ждёт решения владельца: размер прод-пула на новом сервере и судьба старого (гасить/вторая половина);
  юнит на новом сервере пока не включён.

## [2026-09-15] tuning | Сервер и коллектор: A/B по ресурсам, профиль, откат busy_poll

- Владелец: «делай что можно по оптимизации коллектора и сервера»; поправка — Bybit **мигрирует**
  серверы в Токио только во второй половине ноября, сейчас мэтчинг в Сингапуре, машина стоит верно,
  переезд — вслед за Bybit.
- Сервер: `/etc/sysctl.d/99-alpha-latency.conf` — `bbr`, `netdev_max_backlog=25000`,
  `tcp_fastopen=3`, `tcp_slow_start_after_idle=0`; `unattended-upgrades` и apt-таймеры выключены;
  `busy_poll/busy_read` **откатаны в 0** (замер: +45 % CPU на запись за p99, который не нужен).
- Четыре прогона по 10 мин на 100 монетах (CPU на млн записей): дефолт 9.99 с → busy_poll 14.51 →
  тюнинг 9.47 → тюнинг + `taskset -c 1 nice -n -5` 9.32. Привязка/приоритет отклонены (p99 хуже),
  sysctl оставлен. Таблица — `skills/ovh-vps-singapore.md`.
- Профиль `perf`: ядро/сисколлы ~25 % (`finish_task_switch`, `_raw_spin_unlock_irqrestore`,
  `iowrite16`), libc memset `rep stos` 9.1 % (вызывающий не символизируется), приложение ~17 %
  (`Book::apply` 5.1 %, serde ~3 %, zstd ~1 %). Вывод: дальше выигрыш — правки кода
  (батчи/пробуждения, memset), отдельным тикетом; сервер уже выжат.

## [2026-09-15] cutover | Прод переехал на OVH-сервер, пул — 100 монет

- Владелец: «топ 100 ставим» и «топ 5 не собираем — туда входит и биткоин», «не блеклистим, просто
  убираем лист»; плюс отладочный пул убрать совсем.
- Пул собран из боевого часового отбора `--window-secs 3600 --h3-k 1.0 --top 105`: вырезан
  **глобальный топ-5 по обороту — BTC, ETH, XRP, SOL, ZEC** (BTC/ETH отбор и так выбивает по имени,
  поэтому вырезались три строки списка). Осталось **100 монет**, первая HYPE, последняя NBIS;
  блеклистов в код не добавлялось. `instruments.csv` в репозитории обновлён (не закоммичен).
- Юнит `alpha-collector.service` включён и активен с **2026-09-15T14:31:04Z**, `NRestarts=0`,
  100 binlog-файлов, `gaps.csv` пуст.
- Старый коллектор `13.140.29.171` погашен штатно (файл `stop`, В-41): `closed=true`,
  342 465 743 записи и 1.13 ГБ за 20.2 ч на топ-30 (1.35 ГБ/сутки — ниже прежней прикидки 2.05).
- Отладочные пулы удалены и на сервере (`root100*`, bench-логи), и локально; `perf`-дампы оставлены.
- Артефакты: `docs/findings/deploy-2026-09-15-ovh-singapore.md`, обновлённый блок «Состояние»
  в `CLAUDE.md`; в памяти — index, проектная страница и `skills/ovh-vps-singapore.md`.
- Первый риск теперь — **диск**: 5.02 ГБ/сутки при 47 ГБ ≈ 9 суток, ротации/выгрузки нет;
  `verify` нового дня ещё не гонялся.

## [2026-09-15] measure | Колоночная раскладка против v3: 3.4–6.8 %, отклонена

- Владелец: «делаем прототип сравниваем» (кандидат «колоночная раскладка» из разбора экономии).
- Инструмент: `examples/columnar_probe.rs` — только чтение, на настоящем `binlog::Reader`, три
  варианта на одних записях (v3+zstd1, колоночно одним блоком, колоночно тремя блоками),
  round-trip обязателен (расхождений 0).
- Результат на живых файлах нового сервера: LSKUSDT (2.61 млн записей) — 2.906 → **2.806 Б/запись
  (96.6 %)**, CYSUSDT (23 тыс.) — 8.334 → **7.768 (93.2 %)**; отдельные zstd-кадры на колонку хуже
  одного блока (98.7 / 95.5 %).
- **Отклонено**: 3.4–6.8 % ниже/на грани порога 5 % (В-49), цена — новая версия формата + читатель +
  поддержка во всех командах. Причина малой экономии: v3 уже берёт главное дельтами цены и размера.
- Побочно: у тихих монет 72 % сырых байт — заголовки групп, то есть обрамление сообщения, а не
  данные; если экономить на малоликвидных — рычаг там.
- Артефакт: `docs/findings/format-columnar-2026-09-15.md`.

## [2026-09-15] measure | Словарь zstd: 1.8–2.7 %, отклонён; тема формата закрыта

- Владелец: «тести словарь» (последний из дешёвых рычагов по байтам).
- Инструмент: `examples/dict_probe.rs` (только чтение; обучение словаря на первой половине кадров,
  замер на второй — иначе словарь видит то, на чём его проверяют; отдельный прогон «словарь с LSK,
  замер на CYS»). Для обучения включена фича `zdict_builder` у `zstd` — только для прототипа.
- Результат: LSK 2.906 → **2.853 Б/запись (98.2 %)**, CYS 8.334 → **8.107 (97.3 %)**; словарь 112 КБ
  хуже 8-килобайтного. «Потолок» со словарём из самих тестовых кадров — 70 % на CYS — artefact
  переобучения (словарь 112 КБ против файла 190 КБ), в бою недостижим, в вывод не берётся.
- **Отклонено**: 2–3 % ниже порога 5 % (В-49). Итог по байтам: три захода — таблица `ev`/уровни
  zstd, колоночная раскладка, словарь — все ниже порога; формат v3 исчерпан, дальше только смена
  класса кодека (brotli, платит процессором) или запись меньшего объёма данных.
- Артефакт: `docs/findings/format-dictionary-2026-09-15.md`. Тема формата закрыта; реальный рычаг
  остаётся один — **ротация/выгрузка суток** (диск ~7 суток на 6.8 ГБ/сутки).

## [2026-09-15] measure | Глубина .50 против .200: живьём, 20 монет, одновременно

- Повод: владелец — «50 уровней это 0.18 % цены, надо тянуть 200».
- Собрал на старом сервере бинарник с `ORDERBOOK_DEPTH=200` и `CAPACITY` книги 128→256
  (md5 `f573e4bc…`), прогнал **одновременно** с боевым `.50` на одном пуле из 20 монет (10
  ликвидных + 10 тихих), 600 с, рядом с идущей записью.
- Результат: записей +3.8 %, **байт −19 %** (10.42 → 8.45 МБ), **CPU 4.17 % → 2.32 % ядра**,
  parse p99 159.7 → 192.5 мкс (бюджет 200), RSS +2 %, нули по gaps/reconnect/resync/frames_failed.
- Механика: `.200` даёт **вдвое меньше сообщений биржи, в каждом втрое больше уровней**
  (LSK: 64 262 → 28 422 сообщений, 12.0 → 33.2 записей на сообщение) — заголовок сообщения
  амортизируется, отсюда и байты, и процессор.
- **Цена: время грубеет.** Интервал между сообщениями: LSK p90 22 → 85 мс, p99 56 → 107 мс;
  у тихой SENT медиана 21 → **97 мс**. Бьёт по самому мелкому горизонту — `m_100ms` и
  `within_touch_100ms` (В-45); горизонты ≥ 1 с не страдают.
- Артефакт: `docs/findings/depth-200-ab-2026-09-15.md`; файлы A/B на сервере — `/opt/alpha/ab50`,
  `/opt/alpha/ab200`. Решение владельца: нужен ли 100-мс горизонт (тогда `.50`) или глубина
  (тогда `.200`); третий вариант — подписка сразу на оба топика, но это правка модели данных.

## [2026-09-15] research | Склейка потоков глубины: есть индустриальный механизм, `seq` — защита от дрейфа

- Вопрос владельца: «нам не надо 200 так же часто, как 50 — есть ли индустриальный механизм склейки
  и защита от дрейфа».
- Ответ: механизм называется **fusing depth feeds**, есть в hftbacktest (туториал + утилита
  `FuseMarketDepth`); оттуда же подтверждение частот Bybit: BBO 10 мс, `.50` — 20 мс, `.200` — 100 мс.
- Защита от дрейфа — поле **`seq` («cross sequence»)**: по документации Bybit оно и предназначено
  для сравнения данных разных глубин (меньше `seq` — раньше сгенерировано); `u` — счётчик внутри
  топика, разрыв = потеря → ресинк. У нас **первый и последний слой уже реализованы**: `u` с
  автоперезапросом снапшота (`conn.rs`) и выравнивание с REST-снапшотом по `seq` (`verify_sidecar`
  каждые 5 минут).
- Индустриальный порядок: своя книга и своя проверка `u` на каждый поток; между потоками порядок по
  `seq`; на уровень — выигрывает новейший по биржевой метке; **держать две книги, а не склеивать в
  одну** (склейка бывает внутренне противоречива — быстрый поток уже показывает лучшую цену, а
  глубокие уровни устарели); периодическая сверка с REST.
- Варианты для нас: A `.50` (сейчас), B `.200` (замерен: −19 % байт, −44 % CPU, время грубеет),
  C `.50`+`.200` со своими книгами и мерджем по `seq`, D `.50` + REST-снапшоты глубины через уже
  существующий сайкар, E `orderbook.full.<symbol>` (200 мс, полная глубина, только дельты).
  Разумны C или D; решение за владельцем.

## [2026-09-15] measure | Архивация суток: сжатие даёт 8–11 %, «в два раза» — только сроком хранения

- Вопрос владельца: «можем предыдущие дни архивировать или сжимать, чтобы в два раза экономнее».
- Закрытые сутки — другой режим (процессор не в дефиците), поэтому замер отдельный: файл ARBUSDT
  (18.44 МБ, 4.03 млн записей) распакован в тела кадров (24.2 МБ) и сжат одним потоком.
- **Повторное сжатие файла — ровно 100 %** (внутри уже zstd-кадры). Архив одним потоком:
  zstd-1 97.7 %, zstd-19 **92.3 %**, `--ultra -22 --long=27` 92.2 %, **xz -9e 89.5 %**;
  round-trip байт-в-байт. То есть экономия **8–11 %**, а не вдвое; причина — формат уже кодирует
  цену и размер дельтами, остаточная энтропия в самих размерах.
- Цена: xz-9e ~15 с на 24 МБ ⇒ **~1.5 часа CPU на сутки данных**; zstd-19 вдвое дешевле по CPU.
- **Настоящие 2× — в сроке хранения**: 3 суток на сервере вместо 7 = в 2.3 раза меньше занято.
  То есть «архивировать предыдущие дни» = выгружать закрытые сутки с сервера и удалять после
  `verify`; сжатие архива — опциональная надстройка (−8…11 %).
- 2× по самим данным — только сокращением записи (фильтр 25 bps: −0.7 % LSK, −20 % DOGE, −64 % CYS),
  решение о данных, отложено осознанно.
- Артефакт: `docs/findings/archive-compression-2026-09-15.md`; прототип `examples/archive_probe.rs`.
  За собой прибрано: A/B-каталоги, сырой поток и выходы кодеков на сервере удалены.

## [2026-09-15] impl | T45 (два потока глубины): реализовано, смоук пройден, ждёт доработки тестов

- Исполнитель реализовал T45 целиком: подписка `.50` + `.200` в одном соединении, **своя книга на
  поток**, свой контроль `u` и ресинк, **два файла** — основной (как раньше) и `deep/<SYM>-<день>.binlog`
  (формат v3, свой снапшот первым кадром), `session.json.streams`. 26 файлов, +1920/−396.
- Ворота капитана: build 0; **717 passed, 0 failed** (+7); `clippy --all-targets -D warnings` чисто
  (после правки моей зацепки в `examples/depth_probe.rs`); `fmt` чисто.
- Живой смоук (10 тяжёлых монет, 300 с, рядом с боевой записью): 10+10 файлов растут,
  `gaps=reconnects=resyncs=frames_failed=0`, `streams` = `.50` 1.51 млн записей / 5.65 МБ,
  `.200` 1.38 млн / 3.89 МБ, суммы сходятся; оба файла — v3; `deep/` резолверу невидим;
  parse p99 149.5 мкс, CPU 5.3 % ядра.
- **Цена глубины: +69 % байт.** Боевые 5.32 ГБ/сутки → **~9 ГБ/сутки** на 100 монет: 3 суток на
  сервере ≈ 27 ГБ (влезает в 46), недельный архив ≈ 63 ГБ вместо 37.
- Ревью (три оси) — `needs_revision`: блокеров в боевом коде нет, находки про тесты/доку
  (нет тестов на сброс `synced`/ротацию по потокам и на кадр-снапшот `.200`; тест ёмкости книги
  проходит при 128; дока `sink.rs` противоречит себе; `remove_file` до `drop`). Отправлено
  исполнителю; после доработки — решение владельца о переводе прода на два потока.

## [2026-09-15] deploy | Двойная глубина (`.50` + `.200`) включена в прод

- Владелец: «делай самый эффективный вариант»; вопрос «сколько уже пишется двойная глубина» показал,
  что в проде её **не было** (только 5 минут смоука, файлы удалены) — переключил сразу.
- 2026-09-15T17:13:53Z: бинарник T45 (md5 `85c1366d…`, собран на старом сервере, смоук пройден)
  поставлен на боевой вместо прежнего (`e1d721a9…`, сохранён как `alpha-collector-prev`), юнит
  перезапущен: active, `NRestarts=0`, MainPID 24139.
- Проверено: **100 глубоких** файлов в `deep/` + 100 новых частей быстрого потока (`-p2`) на текущие
  сутки; оба потока растут (за 4.5 мин: быстрый 8.5 МБ, глубокий 6.3 МБ); `gaps.csv` пуст;
  `session.json.streams` несёт оба потока. Глубина добавляет **+74 %** к быстрому потоку (смоук давал
  +69 %) ⇒ боевой расход **≈9 ГБ/сутки** на 100 монет, 3 суток ≈ 27 ГБ при 46 свободных.
- У сегодняшних файлов глубина есть **с 17:13:53Z** (до этого — только `.50`).
- Доработка T45 (тесты/дока по замечаниям ревью) продолжается отдельно; при её закрытии пересоберу
  и, если код изменится, перезапущу ещё раз.

## [2026-09-15] measure | Дрейфа `.50` против `.200` нет (проверено книгами)

- Вопрос владельца: «а дрейф 50 относительно 200 нет?». Проверено прототипом
  `examples/stream_drift.rs`: две книги по одной монете за один период, быстрая подтягивается ровно
  к метке глубокого сообщения, пачка применяется целиком, сравнение по границам сообщений.
- **LSKUSDT** (самая активная, ~1.5 ч, 8 927 сравнений): верх совпал **97.63 %**, по четвертям окна
  97.25 / 97.24 / 98.15 / 97.87 %, расхождения ≤1 тика — 36/30 случаев, «крупных» (>1 тика) 149,
  максимум 41 тик и в обе стороны. **DOGEUSDT** (17 006 сравнений): **99.99 %**, максимум 1 тик.
- Вывод: **дрейфа нет** — согласие ровное по времени, расхождения ограничены и разнонаправлены;
  это следствие пачечной публикации `.200` раз в ~100 мс, а не потеря уровней.
- Методическая ловушка (записана): сравнение **внутри** пачки глубокого сообщения даёт 0.00 % —
  сравнивать только на границах сообщений.
- Следствие для склейки: состояние глубокого потока отстаёт на ~100 мс, поэтому верх книги брать из
  быстрого потока, глубокий — только за пределами его окна, выравнивание по `seq`.
- Артефакт: `docs/findings/stream-drift-2026-09-15.md`.

## [2026-09-15] impl | T45 закрыт (ревизия по ревью), T46 (архив) в работе

- Ревизия T45: все пять находок ревью закрыты — три теста по потокам (сброс `synced`, ротация и
  переоткрытие глубокой части после потери границы кадра), тест кадра-снапшота `.200` 200×2 с
  оракулом на резерв и нулём аллокаций, тест книги на аллокации, честная дока кадра, возврат
  close-then-remove. Каждая проверка дополнительно проверена мутацией (падает на сломанной логике).
- Гейты капитана: **721 passed, 0 failed**, `clippy --all-targets -D warnings` чисто, `fmt` чисто.
- Прод уже пишет два потока (бинарник до ревизии; отличие — только порядок закрытия при неудачном
  открытии символа). Пересборка и перезапуск — вместе с T46, одним перезапуском.
- **T46 запущен** тому же исполнителю: контейнер `*.binlog.zst` (один zstd-поток над заголовком v3 и
  телами кадров), команда `lob archive`, чтение архива читателем и резолверами, обязательный
  round-trip, удаление оригинала только явным флагом и при `verify-*.status == ok`. xz — вне итерации
  (нужна новая зависимость).

## [2026-09-15] impl | T46 (архив закрытых суток) реализован и проверен

- Контейнер `*.binlog.zst`: маркер `ABLA|версия|уровень` + заголовок v3 + `u32 длина | тело кадра
  без сжатия`*, один поток zstd (по умолчанию 19). Команда `lob archive --path … [--level] [--out]
  [--delete-original]`: запись в `.tmp`, обязательная сверка тел кадров с диска, затем `rename`;
  удаление оригинала только по флагу и при `verify-<SYMBOL>.status == ok`. Чтение контейнера —
  в `Reader` (опознание по магии zstd), резолверы видят `*.binlog.zst`.
- Гейты капитана: build 0; **752 passed, 0 failed** (+31 к 721); `clippy --all-targets -D warnings` и
  `fmt --check` чисто.
- **Независимая проверка на боевом v3** (`CYSUSDT-2026-09-15-p2.binlog`): архив **81.3 %** от
  оригинала, round-trip сошёлся, `binlog-stats` совпал по всем счётчикам, `lob levels` — тот же CSV
  по SHA256 (`1B2AC402…`). Замер исполнителя на файле после `--rewrite-out`: 90.3 % (уровень 19),
  `markout`/`verify` совпали по SHA256.
- Ограничение: контейнер принимает только v3, для боевых v2-суток нужен `binlog-stats --rewrite-out`
  (строку в ранбук вписывает владелец). xz не добавляли (новая зависимость liblzma).
- Бинарник T45-ревизия + T46 собран и поставлен в прод одним перезапуском.

## [2026-09-15] deploy | Объединённый бинарник (T45-ревизия + T46) в проде

- Собран на старом сервере (md5 `f8923a3a75b22a104981f23788ab7574`), поставлен на боевой
  (`alpha-collector`, прежний сохранён как `alpha-collector-prev`), юнит перезапущен **18:36:40Z**:
  active, `NRestarts=0`, **200 глубоких** файлов и 100 новых частей `-p3`, `gaps.csv` пуст,
  `lob archive` в help. На диске 1.1 ГБ, свободно 46 ГБ.
- Локальный мусор убран: `.tmp-ssh` 21.4 МБ → 0.1 МБ; на сервере удалены тестовые бинарники,
  perf-дампы и bench-скрипты; на старом сервере — дерево сборки `src46`.

## [2026-09-15] digest | Память приведена к состоянию после T45/T46

- `index.md` переписан компактно (был раздут до 3.1 КБ и **обрезался в boot-блоке** — держать ~2,3 КБ).
- В `projects/active/alpha.md` добавлены разделы: T45 (два потока), дрейф `.50`/`.200`, T46 (архив);
  блок «Что дальше» переставлен — первым пунктом **скрипт ротации**.
- `skills/ovh-vps-singapore.md`: раздел «Развёрнуто» отражает объединённый бинарник, `deep/`,
  команду `lob archive`, объём ≈7 ГБ/сутки и запас ≈6 суток.
- Открытое: ротация (первое по срочности), `verify` первого боевого дня, коммит правок 15.09,
  ранбук архивации v2-суток через `--rewrite-out`.

## [2026-09-15] decision | Хранение ~3 суток на сервере + замер глубины книги

- Решение владельца: записывать дольше 3 суток смысла нет; **пока пишем всё, что есть**.
- **Оговорка, которую надо держать в голове:** статистика требует **≥ 7 дневных кластеров**
  (`stats::G_MIN = 7`).
  **Уточнено 2026-09-16 (моя ошибка формулировки):** это не значит, что команды «откажутся».
  На одном дне числа **считаются**: `touch-profiles` печатает маргиналы всех осей, а `none` ставит
  только в границах интервала (`src/commands/lob/touch_profiles.rs:524`); `profiles` считает, но
  часть блоков с интервалами не пишет; `shortlist` печатает `недобор` (`shortlist.rs:749`:
  `n < 100 || G < G_MIN`), то есть не подтверждает, а не молчит; `watch` не ставит подтверждающий
  флаг. Отказом (`Err` с `minimum: G_MIN`) отвечает только сам бутстрап (`stats/mod.rs:346`).
  Значит исследовать на одном дне можно — не хватает лишь границ и подтверждающих флагов.
  Про 3 суток на сервере: они возможны только с выгрузкой (сервер — 3 суток, архив — 7+);
  удалять без выгрузки нельзя, иначе вердикты не соберутся никогда.
- Замер «нужно ли всё, что пишем» (`examples/depth_probe.rs`,
  `docs/findings/depth-distribution-2026-09-15.md`): подписка `orderbook.50` — глубже Bybit не
  отдаёт; глубина книги (медиана от середины): LSK 17.5 bps, DOGE 60.8, CYS 383.9; доля записей
  ближе 25 bps: LSK 99.3 %, DOGE 80.4 %, CYS 35.8 %. Фильтр «только ближе 25 bps» сэкономил бы
  0.7 % / 20 % / 64 % — на ликвидных (кто и даёт объём) почти ничего, а у тихих дальние уровни и
  есть книга. **Вывод: сокращать запись нечем, пишем всё; рычаг — срок хранения и выгрузка.**

## [2026-09-15] export | Архив памяти и проектной документации для владельца

- По просьбе владельца собран `C:\Users\Георгий\Downloads\alpha-memory-docs-20260916-0016.zip`
  (758.1 КБ, 150 файлов): `.memory/` (15), `CLAUDE.md`, `docs/` (68), `.autopilot/` (66),
  `examples/` (6), `READ-ME.txt` с описанием содержимого. Данные записи (binlog) в архив не входят —
  они только на сервере `139.99.91.22:/opt/alpha/root`.
- Грабли: запись за пределы рабочего каталога (в `Downloads`) песочница режет — `Compress-Archive`
  падает с `Access to the path ... is denied`; помог разовый `danger-full-access`. Повтор: складывать
  архив через `%USERPROFILE%\Downloads` в одном вызове с расширенными правами.
- Память — карта, а не документация: в архив положены и сами источники правды, чтобы состояние
  читалось без доступа к репозиторию и без грепа проекта.

## [2026-09-16] triage | Внешние источники (HFT Underground): что берём в коллектор

- Владелец принёс две выгрузки: канал HFT Underground (16 500 сообщений, 01.2025–16.09.2026) и
  заметку «Коннектор Bybit» по мотивам того же канала. Триаж целиком —
  `docs/findings/hft-underground-2026-09-16.md`.
- **Фильтр по коду перед разбором:** в `src` нет ни `liquidation`, ни `rpi`, ни `mark_price`, ни
  `external`/`okx`/`binance` — то есть три флага разметки, которые источник называет
  обязательными, у нас отсутствуют целиком. `Outcome` — бинарная разметка 70/20
  (`classify_outcome`, `src/lob/levels.rs:197`) по `traded_lots` против `size_max`.
- **Вердикт: берём три публичных потока Bybit, которых у нас нет** (топики сверены по документации
  биржи, не по пересказу канала): `priceLimit.{symbol}` (300 мс, `buyLmt`/`sellLmt`),
  `allLiquidation.{symbol}` (500 мс, `T`/`S`/`v`/`p`), `orderbook.rpi.{symbol}` (100 мс, 50 уровней,
  **два размера на уровень** — non-RPI и RPI). Формат v3 менять не нужно: `Record` уже несёт
  `ev`-биты, цену в тиках и обе метки времени; ликвидация ложится на `exch_ts`/`price_ticks`/`qty_lots`.
- **Главный смысл RPI:** наша метка «съели» сравнивает объём сделок с видимым размером. RPI-заявки
  в обычном `orderbook.50` не видны, поэтому объём может покрыть уровень, который на самом деле
  сняли. Это систематическая ошибка в целевой метке, а не косметика.
- **В разметку без новых данных:** доля исполнения `traded/size_max` (оба поля уже есть в
  `LevelRecord`) вместо бинарного `Eaten/Pulled/Mixed`; режим тика — `tick_size` в заголовке файла,
  `median_trade_lots` уже в пуле; агрегат по рынку — на чтении.
- **Отвергли:** блочный формат «якорь цены + короткие записи» (наш v3 заморожен замером В-49),
  parquet/ClickHouse/Timescale, внешние биржи внутри коллектора (это второе приложение на 2 vCPU;
  наш пул альт-тяжёлый, поводырь для него слабее, чем в источнике), Hyperliquid.
- **hftbacktest:** версия в `Cargo.toml` — 0.9.4, и это **новейшая** (crates.io, 2025-12-10), то есть
  ~9 месяцев без релиза — утверждение источника «заброшен» верно, но обновляться некуда. Зависимость
  тонкая (адаптер `MarketDepth`/`Bot`), формат данных наш ⇒ риск управляемый, вендорить форк можно.
- **Отдельная дорожка (не коллектор):** требования к живому боту — trigger-limit IOC, свой матчинг
  филлов по ID, защита по тишине ведущей биржи, фильтр токсичных филлов, mark price/ADL-ранг/
  изолированная маржа, перечитывание `instruments-info`, троттлинг по загрузке лимита.
- **Внешнее подтверждение нашего вердикта:** Walshire — полтора года на фронтраннинге от плотностей
  без результата, «плотности снимаются не в твою сторону»; единственное подтверждённое применение
  плотности у профессионала — фильтр импульса (каскад сделок, подпёртый крупной лимиткой). Это
  сходится с нашим T38 (`net_fill < 0` на всех десяти монетах, `docs/archive/artifacts/bounce-backtest-2026-09-13.csv`).
- Не смотрели: топик `full-ob` (Full Orderbook) из той же секции документации — это работа по T45 (глубина).

## [2026-09-16] decision | Владелец: прайс-лимит и ликвидации не берём; замер RPI (В-51, В-52)

- Владелец по трём пунктам разбора: «1 — не берём, 2 — не берём, 3 — не понял разницу с нами».
  Записано: **В-51** (`priceLimit.{symbol}` и `allLiquidation.{symbol}` в коллектор не берём) и
  **В-52** (направление: «мы будем совмещать микроструктуру и то что торгуют пацаны руками»).
- **Пункт 3 объяснён и измерен.** RPI — не «ещё одна глубина» (не как наш `.200`), а второй слой
  ликвидности на **тех же ценах**: заявки маркет-мейкеров, которых нет в `orderbook.50`. В стриме
  `orderbook.rpi.{symbol}` на каждой цене два числа — видимый размер и RPI-размер.
- Замер (клиент WS на stdlib Python, запуск **на сервере** — локально TLS не работает;
  `.tmp-ssh/rpi_probe.py`, `.tmp-ssh/rpi_pool_probe.py`, только чтение, прод не задет), 100 монет
  пула, 20 с, **только снапшоты** (в дельте размер абсолютный на момент — складывать нельзя,
  первые прикидки по всем сообщениям отброшены):
  **50 из 100 монет имеют живую RPI**; **26.6 % уровней снапшотов с RPI>0** (2 661 из 10 000);
  по всем обновлениям 30.1 %. Отношения по символам: AKE **9.5×** (368 500 видимого → 3 494 800 RPI),
  LSK 8.8×, NBIS 8.0×, FIL 7.8×, ETHFI 5.4×, CVC 2.7×, XPL 2.2×. Контроль `orderbook.rpi.BTCUSDT`
  за 25 с — ноль RPI при 3 741 разобранном уровне, то есть RPI живёт по конкретным символам.
- **Почему это важно нам:** `size_max` в `LevelRecord` — максимум **видимого** размера, а
  `traded_lots` — весь объём сделок на цене, включая исполнения об невидимые RPI-заявки. Правило
  70/20 (`classify_outcome`, `src/lob/levels.rs:197`) читает их отношение ⇒ при живой RPI уровень
  помечается `eaten`, хотя видимую плотность могли снять. Ошибка **односторонняя**: всегда сдвигает
  `pulled`/`mixed` в `eaten`.
- Поле доступности RPI в `/v5/market/instruments-info` **не нашлось** (LSKUSDT и BTCUSDT: только
  `riskParameters.priceLimitRatioX/Y`) — утверждение канала про «поле в instruments-info» не
  подтвердилось, рабочая проверка только подпиской.
- **Решение по третьей книге `rpi/` открыто** (ждёт владельца). Стоимость вдвое ниже, чем казалось:
  подписка нужна 50 символам из 100, а не всему пулу. Формат v3 менять не нужно.
- **Пересмотр в тот же день (самопроверка, до кода):** (1) подменять размер уровня на
  «видимое + невидимое» **нельзя** — уровень рождается/умирает по видимой книге, объединённый
  размер поедет не входом метки, а определением уровня и сломает сравнимость всего ряда артефактов;
  правильно — добавлять невидимое отдельным признаком. (2) отбор по видимому размеру — **не дефект**:
  ручные видят видимый стакан, а В-52 велит подстраиваться под них. (3) замер «сделки против
  видимого» (Y > X) — **не доказательство**: внутри окна уровень могли добавить и тут же съесть,
  значит это верхняя граница, а не измерение.
  **Главный вопрос оказался другим:** когда по цене с живым RPI идут сделки, видимый размер
  уменьшается или нет? Если RPI исполняется первым, метка не смешана, а **перевёрнута** («съели»
  там, где стенка устояла). Плюс не проверено, не стоит ли RPI **внутри спреда** — тогда у этих
  монет неверен и наш лучший бид/аск, а с ним все расстояния в bps. Порядок: сначала пробник
  30–60 мин на 10 монетах по этим двум вопросам (ничего не пишем), потом решение о третьем потоке.

## [2026-09-16] finding | Флаг RPI есть в самой ленте — третий поток для метки не нужен

- Владелец принёс внешний разбор (`alpha-rpi-i-nahodki.md`), который нашёл то, что я пропустил:
  **в `publicTrade` есть булево поле `RPI`** — сделка сама помечена, исполнена она об RPI или нет.
- **Проверено дважды:** документация Bybit (`RPI | boolean | Whether it is a RPI trade or not`)
  и живой поток с сервера — поля реально приходят, ключи сделки
  `['BT', 'L', 'RPI', 'S', 'T', 'i', 'p', 's', 'seq', 'v']`. Скрипт `.tmp-ssh/trade_rpi_probe.py`.
- **Замер (30 с, 100 монет пула, 86 с торговлей):** 4 344 сделки, с флагом RPI — **109 (2.51 %)**;
  хотя бы одна RPI-сделка у 15 символов из 86. Надёжные счётчики: POWER 4.91 % (31/632),
  PENDLE 6.10 % (26/426), AKE 6.05 % (17/281), FIL 6.00 %, ASTR 4.55 %, LSK 0.80 % (3/375).
  Малые числа (BTW 3/11, COIN 2/7, META 1/6) — шум, не статистика.
- **Следствие: план меняется.** Починка метки «съели» **не требует третьего потока**: достаточно
  разбирать флаг сделки так же, как мы уже разбираем `BT` (`src/bybit/ws.rs`, `#[serde(rename =
  "BT", default)]` → рядом `RPI`), писать **бит 1 `attrs`** (бит 0 — блочность; старые файлы
  читаются как `rpi=false`) и в разметке считать «съели» по сделкам `rpi=false`, а RPI-объём вести
  отдельным полем. Третий поток (`rpi/`) остаётся только под **реальный размер** скрытого слоя.
- **Срочность:** уже записанную историю переразметить нельзя — в ней флага нет. Каждый день записи
  без флага остаётся смешанным навсегда.
- **Что в разборе не подтвердилось:** «колонка RPI доступен из `instruments-info`» — у LSKUSDT и
  BTCUSDT (linear) такого поля нет, только `riskParameters.priceLimitRatioX/Y`; заменяется
  измеренной долей RPI-сделок. «Приоритет RPI после обычной очереди на той же цене» — из чата, не
  из документации; с этой оговоркой страх «метка перевёрнута» снимается, проверяется на своих
  данных после включения флага. Подтвердилось: 50 уровней/100 мс и два размера в `orderbook.rpi`,
  скрытые скрещённые RPI, до 1 024 сделок в сообщении ленты и один `seq` в нескольких сообщениях.
- **Порядок принят из разбора (он лучше моего):** флаг RPI в `Trade` + бит в `attrs` — сразу;
  затем доля RPI-сделок по суткам из записанного; затем исключение RPI из исхода «съели»;
  третий поток — только на пары с заметной долей и только под размер.

## [2026-09-15] deploy | Флаг RPI: правка формата, ворота, раскатка на прод (В-53)

- **Сделано:** `bybit::ws::Trade.rpi` (`#[serde(rename = "RPI", default)]` рядом с `BT`); в формате —
  **бит 1 `attrs`** (`ATTRS_RPI`); `same_message` рвёт группу по смене флага, поэтому флаг остаётся
  поштучным, хотя живёт в заголовке группы (группа — одно сообщение биржи, а в сообщении до 1 024
  сделок с разными `RPI`); старые файлы читаются как `rpi = false`. Версия формата **не менялась (3)**:
  В-49 замораживал раскладку, а не список известных битов. `binlog-stats` печатает новую строку
  «RPI-сделок N (x %)» — доля по суткам считается из записанного, без новых потоков.
- **Разметка (`levels`/`markup`) флаг пока НЕ читает** — это отдельный шаг; сейчас копится история,
  без которой переразметка невозможна.
- **Ворота:** 754 теста (0 failed, 5 ignored), `clippy --all-targets -D warnings` и `fmt --check`
  чисто. Два новых теста: round-trip флага с разрывом группы и чтение файла без бита.
- **Грабли правки:** `edit` заменил заголовок В-52 текстом В-53 (грузил `old_string` = заголовок, а
  вставил целый абзац) — тело В-52 осталось без заголовка; починено скриптом `.tmp-ssh/fix_settled.py`
  (разбор по маркерам, дословная пересборка). Урок: для вставки нового абзаца брать `old_string`
  как **последнюю строку предыдущего абзаца**, а не заголовок следующего.
- **Раскатка (важно для следующих деплоев):** на боевом `139.99.91.22` **нет toolchain** вообще
  (`cargo`/`rustc` отсутствуют, `/opt/alpha/src` — исходники без `target`). Сборка — на **прежнем**
  сервере `13.140.29.171` (`root`, ключ `id_rsa`, toolchain 1.93.1, `/opt/alpha/src46` с готовым
  `target` 422 МБ): залить `tar czf src examples Cargo.toml Cargo.lock rust-toolchain.toml`, распаковать,
  `nice -n 10 cargo build --release` (39 с инкрементально), скачать `target/release/alpha`. Установка на
  боевой — **только через `sudo`** (`/opt/alpha` принадлежит root): `cp -a alpha-collector
  alpha-collector-prev-rpi` → `install -m 755` → `systemctl restart alpha-collector`.
- **Проверено на живой записи** (части `-p4`, 4 мин): RPI-сделки пишутся — LSK 53, AKE 80, POWER 78,
  PENDLE 4, FIL 2; доля от сделок совпала с подпиской до правки (AKE 4.9 % против 6.05 %, POWER 5.9 %
  против 4.91 %). Файл до правки читается как `RPI-сделок 0 (0.00 %)`. `gaps.csv` без разрывов,
  `NRestarts=0`, бинарник md5 `89e58e09bd6c33a041dec3bc90212c2a`, юнит активен с 22:47:56Z.

## [2026-09-16] decision | Прежний сервер закрыт (В-54) и попытка собрать Linux локально

- Владелец: «тот старый сервер больше не трогаем». Записано **В-54** вместе со следствиями:
  сборочный путь через `13.140.29.171` закрыт; оставшиеся там **1.5 ГБ топ-30** (09-14 и часть 09-15)
  **не выгружаются** — это осознанный отказ, а не забытый хвост; боевой процесс не затронут.
- **Что есть на рабочей машине** (проверено): rustc 1.93.1, но цель только `x86_64-pc-windows-msvc`;
  WSL не установлен, Docker и zig отсутствовали. Запись в `~/.rustup` и `~/.cargo` **режет песочница**
  (workspace-write) — `rustup target add` падает без развёрнутого доступа; расширение до
  `danger-full-access` работает.
- **Что поставлено:** цель `x86_64-unknown-linux-musl`, `cargo-zigbuild 0.23.4`, портативный
  `zig 0.15.2` — распакован в `.tmp-ssh/zig` (92 МБ, в репозиторий не идёт).
- **Докуда дошла кросс-сборка:** все зависимости (zstd-sys, ring, rustls, hftbacktest) **собрались**
  под musl; падает **только финальная линковка**. Причины по порядку вскрытия: (1) `cargo-zigbuild`
  сам ломает свои `.bat`-обёртки (`Failed to collect zig cc options`, битый `cmd`); (2) `zig` не
  понимает триплет `x86_64-unknown-linux-musl` (нужен `x86_64-linux-musl`) — cc-rs подставляет свой;
  (3) `zig` не может писать кэш в `AppData` — лечится `ZIG_GLOBAL_CACHE_DIR`; (4) `cmd`-обёртка
  **теряет кавычки** в путях с пробелом — заменена на собственный exe-шим
  (`.tmp-ssh/zigshim.rs`, компилируется `rustc`); (5) остаётся один аргумент в длинной форме
  `C:\visual projects\alpha\…` (собственный rlib крейта) — линкер режет его по пробелу
  (`C:\visual\: unrecognized file extension`).
- **Известные обходы, не применённые:** сборка из `cmd` с коротким путём `C:\VISUAL~1\alpha`
  (cargo видит `workspace_root` без пробелов, но собственную rlib-ссылку rustc всё равно выдаёт
  длинной); `--target-dir` без пробелов (помогает всем путям, кроме этой одной); замена префикса
  пути внутри шима (аргумент до линкера доходит уже без пробелов, ошибка сохраняется — то есть
  источник не в шиме).
- **Альтернативы для сборки Linux-бинарника:** (A) WSL Ubuntu на этой машине — нативная сборка без
  кросс-возни, нужны права администратора и, вероятно, перезагрузка; (B) `rustup` на боевом сервере
  — без новых машин, но занимает ~1.5–3 ГБ из 45 свободных (это ~10 часов записи) и грузит 2 ядра
  при холодной сборке; (C) CI/облако — отдельная тема. **Решение за владельцем, сборка не сделана.**
- Прод ни один из этих шагов не трогал: коллектор всё это время писал на прежнем бинарнике.

## [2026-09-16] deploy | Сборка Linux-бинарника переехала на боевой сервер (системный диск)

- Владелец: «на этой машине либо на новой сингапурской — там два жёстких тем более». Сделано по
  второму варианту, с оговоркой: toolchain и сборка живут **на `sda`** (`/home/ubuntu`), диск данных
  `sdb` (`/opt/alpha`) под запись не занимается.
- **Что поставлено на боевом:** `build-essential` (компилятора C на сервере не было вообще),
  rustup в `/home/ubuntu/.cargo`, toolchain **1.93.1** (как в `rust-toolchain.toml`).
- **Сборка:** исходники (tar `src examples Cargo.toml Cargo.lock rust-toolchain.toml`, 0.8 МБ) → SFTP →
  `/home/ubuntu/alpha-build` → `nice -n 10 cargo build --release`. Первая сборка ≈15 мин на 2 ядрах
  параллельно с записью (прод не задет). Занято на `sda`: `.cargo` 180 МБ + `target` 423 МБ.
- **Результат:** `/home/ubuntu/alpha-build/target/release/alpha`, 11.66 МБ, md5
  `630d466a47f45795420b6d12298a919d`; `lob --help` работает; **`binlog-stats` на боевом файле
  работает и показывает новый счётчик** — `LSKUSDT-2026-09-15-p4.binlog`, 2 190 809 записей,
  **«RPI-сделок 2 733 (0.12 %)»**. То есть флаг RPI не только пишется, но и читается сборкой из
  нового пути.
- **Перезапуск прода не делали:** исходники те же, что у работающего бинарника, менять нечего.
  Путь установки проверен раньше сегодня (`install` + `systemctl restart` через `sudo`), так что
  следующая правка кода поедет этим же порядком.
- **Кросс-сборка на рабочей машине закрыта как тупик** (все зависимости собирались, падала линковка
  из-за пробела в `C:\visual projects\alpha`): локальные следы попытки (zig 332 МБ, кэш 72 МБ,
  `target-linux` 521 МБ, `tl` 521 МБ) удалены — суммарно освобождено ≈1.4 ГБ.
- Заметка на будущее: `pgrep`/`paramiko` иногда «зависают» после долгой удалённой сборки — проверять
  результат отдельным подключением (`ls target/release/alpha`, `md5sum`), а не ждать вывод команды.

## [2026-09-16] measure | Сколько именно непрерывной записи 100 монет (17 ч 50 мин)

- Вопрос владельца: «у нас сейчас есть больше 10 часов непрерывной записи рынка 100 монет?» Ответ —
  **да, и заметно больше**. Проверено не по памяти, а по **birth-time файлов** (`stat -c '%w'`), это
  точная метка создания: первые части `.50` родились **2026-09-15 14:31:04.27Z** (LSK 14:31:04.271,
  1000BONK 14:31:04.278); сейчас **2026-09-16T08:21Z** ⇒ **17 ч 50 мин** записи.
- **Три технических перезапуска внутри** (части суток 09-15: 400 файлов основного потока и 300
  глубокого = 100 монет × 4 и × 3 части): 17:13:44 → 17:13:53 (включение двух потоков, окно ~9 с),
  18:36:33 (объединённый бинарник T45+T46), 22:47:50 (правка RPI). Внутри частей разрывов нет
  (`gaps.csv` пуст). Ролл суток в 00:00Z запись **не останавливает** — файлы просто сменяются.
- **Чего в этой записи нет:** глубина `.200` — только с 17:13:53 (первые 2 ч 43 мин пишется один `.50`);
  флаг RPI — только с 22:47:50, всё раньше «не размечено»; `verify` по закрытым суткам 09-15 **ещё не
  гонялся** (без `ok` читатели эти сутки не берут).
- **Замеренный расход: 4.4 ГБ за 17.8 ч ⇒ 5.9 ГБ/сутки** (раньше оценивал 7 ГБ/сутки по короткому
  прогону — теперь это факт по факту; из них `deep/` 1.6 ГБ). Свободно 42 ГБ ⇒ **≈7 суток** до
  заполнения; ротации по-прежнему нет — она первая в очереди.

## [2026-09-16] verify | Первая сверка боевых суток 09-15: 77 ok / 23 fail

- Прогнал `lob verify` по **закрытым суткам 2026-09-15** для **100 монет** (400 частей основного
  потока). Итог: **77 `ok`, 23 `fail`, ошибок запуска 0**.
- **Книга структурно чистая:** по всем 400 частям **`gaps = 0` и `invariants = 0`**; обновлений
  книги 54 212 281, сделок 11 278 352.
- **Роняют вердикт только сделки:** 70 583 вне диапазона книги + 18 795 нарушений = **89 378, то есть
  0.792 %** от всех сделок. Разброс огромный: `ASTRUSDT` падает на **одной** сделке из 33 913, а
  `LSKUSDT` — на 18 429 вне диапазона и 1 008 нарушений из 859 600. Правило сейчас — «любой ненулевой
  счётчик = fail» (fail-closed), поэтому монета с одной сделкой падает так же, как LSK.
- **Список fail (23):** ACE, AKE, ARB, ARK, ASTR, AVAAI, BR, BTW, CAP, CASHCAT, FF, FIL, LSK, MU,
  NBIS, POWER, SENT, SKHYNIX, TAO, UAI, USELESS, VVV, XMR.
- **Природа** — известная и измеренная 09-13 на топ-50 (те же LSK/BTW/MU): `orderbook.50` идёт с
  троттлингом 20 мс, `publicTrade` — в реальном времени, поэтому в момент сделки записанная книга
  может отставать, а принт быть внутри спреда или вне записанной глубины. **Это не регрессия** ни от
  нового сервера, ни от RPI-правки.
- **Грабли захода:** `/opt/alpha` — root, поэтому каталог сверки и лог создаются через `sudo`; в
  первом заходе цикл шёл по 400 файлам вместо 100 монет (части надо схлопывать в символы); в третьем —
  незакрытая кавычка в `echo` уронила bash уже после `mkdir`. Каталог сверки — **hardlink'и**
  (`/opt/alpha/verify-09015`, 400 файлов, 1.7 ГБ «на бумаге», физически 0 дополнительных байт), и он
  нужен именно отдельный: `session_binlog_for` отдаёт все части символа вместе с сегодняшней открытой,
  а маркер один на символ — смешивать закрытые сутки с идущей записью нельзя. Маркеры легли в каталог
  сверки, боевой root не тронут.
- **Цена вопроса:** без `ok` читатели (`levels`, `profiles`, …) эти сутки не берут, а архивация требует
  `ok`. Варианты (решение владельца): порог доли вместо «любой ненулевой» (сейчас критерий писался под
  живой поток с REST-сверкой и для файла никогда не калибровался); исключение 23 монет (теряем LSK, AKE,
  ARB, FIL); или сначала разобрать, где именно возникают необъяснимые сделки (лог и каталог сверки
  оставлены на сервере).

## [2026-09-16] decision | Разметка читает флаг RPI (В-55)

- Владелец: «4 — сделай». Реализовано: `TradeHit.rpi` (переносится из `Record.rpi` в
  `commands::lob::replay::trade_hit_from_record`), в трекере живого уровня RPI-объём идёт в
  **отдельный** счётчик `Live.rpi`, а не в `traded`. Поэтому метка исхода (`classify_outcome`,
  правило 70/20) и `TouchRecord.traded_during` перестают считать чужой объём **в одной точке правки**.
- Новые поля: `LevelRecord.rpi_lots`, `LiveLevel.rpi_lots`; `lob levels` пишет колонку `rpi_lots`
  между `traded_lots` и `outcome`.
- **Тест-доказательство** (новый `rpi_trade_does_not_count_as_visible_consumption_but_is_counted_separately`):
  150 RPI-лотов на уровне 200 (75 % — хватило бы на «съели») не меняют `traded_lots` (остаётся 20 от
  обычной сделки) и попадают в `rpi_lots = 150`; исход остаётся `Pulled`, тогда как до правки был бы
  `Eaten`. **Ворота:** 755 тестов (0 failed, 5 ignored), clippy `-D warnings` и `fmt --check` чисто.
- **Раскатки не было:** правка на чтение, коллектор её не использует; бинарник на сервере обновится
  вместе со следующей правкой коллектора.
- **Поправка в доке:** в `docs/findings/hft-underground-2026-09-16.md` §11 утверждал, что
  `traded_lots` включает исполнения об RPI — теперь это верно только для файлов до
  2026-09-15T22:47:56Z; добавлен §12b с поправкой (правило памяти: старое не удалять молча).
- **Грабли процесса:** `edit` вставил новый `#[test] fn` **внутри тела** предыдущего теста — в Rust
  это компилируется (вложенная функция), но тест не запускается, и счётчик остался 754 вместо 755.
  Заметить можно только по числу тестов; вынес скриптом `.tmp-ssh/unnest_test.py`. Впредь: вставляя
  новый тест, брать якорем **закрывающую скобку** предыдущей функции, а не её последний assert.

## [2026-09-16] git | Всё закоммичено и запушено: f5f89c3 (69 файлов)

- Владелец: «коммит пуш». Сделано: `f5f89c3` в `dev/wave-4-record-verify`, запушено в
  `github.com/Gogi213/alpha`. **69 файлов, +7445/−561** — весь накопленный с 09-15 слой: T45 (два
  потока глубины), T46 (контейнер архива), RPI-флаг (В-53), разметка с `rpi_lots` (В-55), пул на
  100 монет (`instruments.csv`), findings 09-15/09-16, тикеты 45/46, примеры-пробники в `examples/`,
  состояние в `CLAUDE.md` и памяти.
- **Найдено при этом:** в репозитории не было половины проекта — `src/binlog/archive*`,
  `src/commands/lob/archive*`, `examples/`, `.memory/skills/`, семь findings и два тикета лежали
  **untracked**. То есть «правки не закоммичены» было ещё мягко сказано: часть работы вообще не
  попадала в историю. Проверка перед коммитом: `git status --short`, `git ls-files`, `git check-ignore`
  на `data/`, `.tmp-*/`, `target-ci` — мусор и данные не попали, ключей и паролей в списке нет.
- **Грабли окружения:** `git push` **не работает из песочницы** — credential helper падает с
  `sh.exe: couldn't create signal pipe, Win32 error 5` и git пишет `could not read Username`.
  Это документированная граница (запрет именованных каналов), а не отсутствие учётных данных:
  с разовым `danger-full-access` тот же `git push` уходит нормально. Впредь пуш — сразу с
  расширенным доступом, без промежуточной попытки.
- Мелочь на будущее: в PowerShell `-like '??*'` — это вайлдкард (любые два символа), а не разбор
  колонки `git status`; для `??` проверять `$_.StartsWith('??')`. Я на этом минуту смотрел в
  «пустой» список untracked.

## [2026-09-16] measure | Цена RPI для метки: 286 уровней из 13.8 млн (0.002 %)

- Прогнал `lob levels` бинарником с В-55 по **77 монетам со статусом `ok`** за закрытые сутки
  09-15: **13 837 730 уровней**, CSV 1.4 ГБ, ~7 минут. Коллектор не трогал (ActiveEnterTimestamp
  так и 22:47:56, `NRestarts=0`) — правка на чтение.
- **Замер:** уровней с `rpi_lots > 0` — **806**; сумма `traded_lots` 8 181 044 427 против суммы
  `rpi_lots` 11 372 506 (0.14 %); вычитание RPI-объёма меняет метку у **286** уровней
  (`Eaten → Mixed` 248, `Eaten → Pulled` 21, `Mixed → Pulled` 17). Больше всех XPLUSDT (119).
- **Вывод и признание ошибки:** правка В-55 верна, но её влияние на статистику — в пределах шума.
  Моя прежняя тревога («невидимая часть ликвидности ломает метку») по эффекту не подтвердилась:
  RPI-сделки идут в основном **внутри спреда**, где нашего уровня нет, поэтому в «объём против
  видимого уровня» почти не попадают. Третий поток для метки не нужен; флаг полезен как отдельный
  признак. Впредь: прежде чем объявлять «дырку» в данных, мерить её эффект на существующих числах —
  я этого не сделал и раздул масштаб.
- Оговорка: флаг есть только с 22:47:56Z, то есть 286 смен — за ~1.2 ч из 24; экстраполяция ×20
  (≈5–6 тыс. меток) — оценка по пропорции, не замер.
- Побочно для планирования: **`lob levels` = 1.4 ГБ CSV на сутки для 77 монет** — артефакты анализа
  держать не на боевом диске данных. Разбор — `docs/findings/hft-underground-2026-09-16.md` §12c.

## [2026-09-16] fact | Почему `verify` ругается: расхождение кода с планом по порогу

- Вопрос владельца: «что ещё за фейл напомни». Разобрал по коду, а не по памяти.
- **Вердикт считает `VerifyStatus::of`** (`src/commands/lob/verify.rs:37`) и падает на любом ненулевом
  из трёх: `sequence_gaps`, `invariant_violations`, `trades_violations`. Важно:
  **`trades_out_of_range` в вердикт не входит вовсе** — в коде прямо сказано «порога нет, это свойство
  глубины» (`bybit/verify.rs:400`), потому что за границей 50 уровней смотреть нечего.
- **На 09-15:** структурная порча (`gaps`/`invariants`) — **0 монет из 100**; ненулевые
  `trades_violations` — **23 монеты**, они и дают `fail`. `trades_violations` — это «тест 3»: сделка
  внутри диапазона, но на цене, которую книга за это время **не держала** (лента в реальном времени
  против книги с троттлингом 20 мс).
- **Найдено расхождение:** в плане (ревизия 17а) для нарушений прописан порог **0.1 % (1000 ppm)** —
  это записано в комментарии к счётчикам, — а реализовано «любой ненулевой». Посчитал по логу сверки
  (`/home/ubuntu/verify-09015.log`, локально `.tmp-ssh/verify-09015.log`): по плановому порогу прошли
  бы **91 монета**, за порогом остались бы 9 — BTW 15 349 ppm, AKE 6 384, SENT 5 934, BR 5 166,
  UAI 4 463, LSK 4 363, POWER 4 243, USELESS 2 504, XMR 2 251.
- То есть «23 монеты вне анализа» — это не порча данных, а **нереализованный порог**: правку стоит
  делать не по моей выдумке, а по правилу, которое в плане уже написано. Требует решения владельца
  (меняет вердикт) и записи В-##.

## [2026-09-16] measure | Природа fail у 23 монет: разрешение фида, а не порча данных

- Владелец: «выясни природу этих 23 монет — ошибка/проблема/задержка сети или что-то ещё».
  Сделано по-настоящему: в `lob verify` добавлен **разбор причин** нарушения (блок, RPI, внутри
  спреда, близость уровня нужной стороны 1 тик / ≥2 тиков, давность обновления книги 20–100 / ≥100 мс),
  счётчики и тесты; прогон по 23 монетам (бинарник собран на боевом, коллектор не тронут).
- **Результат (18 795 нарушений):** блочных **0**; RPI 346 (1.8 %); внутри спреда 4 489 (24 %);
  в одном тике от уровня 3 896 (21 %); ≥2 тиков 14 899 (79 %); ≥20 мс с последнего обновления книги
  **136 (0.7 %)**, ≥100 мс — **0**. Структурно везде `gaps=0`, `invariants=0`.
- **Вердикт:** это не порча файла, не разрыв и не задержка сети. Это сделки по ценам, которых наша
  книга не видела ни мгновения, причём почти все — при свежей книге. Причина в природе фида:
  `orderbook.50` публикуется пачками с троттлингом ~20 мс, внутри окна заявки появляются и исчезают,
  а лента непрерывна. Доля нарушений растёт там, где книга меняется реже (BTW 1.53 %, AKE 0.64 %,
  LSK 0.44 %, XMR 0.23 %), у 77 ликвидных монет их нет вовсе.
- **Мои три прежние версии (блок-сделки, RPI, спред) — неверны**, все три проверил числом и отбросил.
  Хороший урок: гипотезу о природе данных надо проверять счётчиком, а не рассуждением.
- **Грабли реализации:** итог сверки собирается в `verify_and_mark` **по полям вручную** — новые
  счётчики сначала не попали в сумму, и прогон показал нули при ненулевых нарушениях. Стоит заменить
  на метод `VerifySummary::add(&other)`, чтобы список полей жил в одном месте.
- Практический вывод: 23 монеты пригодны для работы; `fail` у них — ложная тревога правила
  «любой ненулевой». Разбор — `docs/findings/hft-underground-2026-09-16.md` §12d.

## [2026-09-16] digest | Память приведена к 16.09; диагностика `verify` в код

- Владелец: «обновляй память по всему проекту и коммит пушься если надо». Digest сделан по полной
  схеме: `projects/active/alpha.md` (новый раздел «Состояние на 2026-09-16», «Что дальше» переписан,
  три новые грабли), `index.md` (блок состояния пересобран под бюджет boot-блока), `log.md`
  (записи этой сессии: RPI-правка, цена RPI, критерий, природа fail), `skills/ovh-vps-singapore.md`
  (ранбук сборки на боевом).
- **В код добавлено после `f5f89c3`:** `lob verify` печатает **состав нарушений**
  (`block/rpi/in_spread/adj1/far/no_side/stale20/stale100`) — `VerifyStats`, `VerifySummary`,
  `Verifier::classify_violation`, `TradePoint` (флаг RPI + метка биржи + сторона агрессора),
  суммирование в `verify_and_mark` и тест `violation_breakdown_separates_block_rpi_and_inside_spread`.
  Ворота: **756 тестов**, clippy `-D warnings` и `fmt --check` чисто.
- Всё это — на чтение: коллектор не перезапускался (`ActiveEnterTimestamp` так и 22:47:56Z,
  `NRestarts=0`), анализатор собран отдельным бинарником в `~/alpha-build`.
- Долг, который стоит закрыть: сумма `VerifySummary` собирается перечислением полей — заменить на
  `add`, иначе следующий новый счётчик снова «потеряется» (на этом уже спотыкались).

## [2026-09-16] git | Волна 4 слита в master (fast-forward)

- Владелец: «ну слито в мастер? не слито — слей». До этого главная ветка `master` отставала на
  **135 коммитов** (128 не-memory: plan 20, ticket 38 — 11, findings 8, ticket 42 — 6, ticket 41 — 5,
  deploy/pool/docs…); в репозитории есть только `master`, ветки `main` нет — это стоит помнить,
  чтобы не искать `origin/main`.
- Ворота перед слиянием: `cargo fmt --check` 0, `clippy -D warnings` 0,
  `cargo test --release` — **756 passed / 0 failed / 5 ignored**.
- Слияние **fast-forward** без конфликтов по построению (`origin/master` — предок ветки):
  `6c9b5f5..e7f58a7 master -> master`; `master` = `origin/master` = `dev/wave-4-record-verify` = `e7f58a7`,
  расхождение `origin/master..HEAD` = 0. Рабочей веткой осталась `dev/wave-4-record-verify`.
- Заодно снят последний фейк в памяти: строка «`rustup` на проде не поставлен и не нужен» в
  `skills/ovh-vps-singapore.md` (тулчейн поставлен 16.09, старый сервер закрыт В-54) и датированные
  числа объёма вынесены под оговорку «текущее на 16.09: 5.9 ГБ/сутки, 42 ГБ, ≈7 суток».

## [2026-09-16] fix | Порог `verify` — доля, а не счётчик: 14 монет вернулись (В-56)

- Владелец: «это же идиотизм, нахуя фейлить по этой причине?» → «исправляй этот механизм».
  Расхождение подтверждено по коду: порог **0.1 %** стоял и в плане §11, и в доккомментарии
  `VerifySummary`, но `VerifyStatus::of` сравнивала `trades_violations == 0` — до вердикта порог не
  доехал. Цена: 14 монет из 100 выпадали из анализа молча (`verify != ok` — fail-closed в
  `profiles`/`watch`/`touches`).
- **Сделано (коммит `438d07d`, `master` = `dev/wave-4-record-verify` = origin):** вердикт —
  целостность (`sequence_gaps`, `invariant_violations`) ровно ноль, тест 3 — доля меньше 0.1 %
  (`violations * 1000 < trades`, целочисленно, ровно 0.1 % — уже `fail`), ноль сделок проходит;
  константа `VIOLATION_SHARE_DENOM`.
- **Ворота:** 759 тестов (0 failed, 5 ignored), `clippy --all-targets -D warnings` и `fmt --check`
  чисто. Три новых теста проверены **мутацией**: `<` → `<=` роняет граничный тест, снятие
  `invariant_violations` — тест целостности.
- **На сервере маркеры перезаписаны** (бинарник собран в `~/alpha-build`, 1 м 21 с; прогон по 23
  бывшим `fail`): **14 `ok` / 9 `fail`** — ровно те девять, что выше порога (BTW 1.535 %, AKE
  0.639 %, SENT 0.593 %, BR 0.517 %, UAI 0.446 %, LSK 0.436 %, POWER 0.424 %, USELESS 0.250 %,
  XMR 0.225 %). Лог: `/home/ubuntu/verify-09015-v56.log`. Смоук «гейт открылся»: `lob touches` по
  NBISUSDT (был `fail`) дал `days=1 touches=10858`.
- **Артефакты:** `docs/findings/hft-underground-2026-09-16.md` §12e (все 23 монеты поимённо),
  `docs/plan/SETTLED.md` В-56, строки `lob verify` в `docs/COMMANDS.md` и `CLAUDE.md`.
- **Грабля прогона:** `pgrep -f "cargo build"` внутри ssh-команды матчит саму эту команду — цикл
  ожидания сборки не заканчивается; ждать надо по файлу-маркеру, а не по `pgrep`.

## [2026-09-16] design | Тикет 47: целостность отдельно, пригодность числом

- Заведён `.autopilot/2026-09-11-lob-density-ed3/tickets/47-verify-gate.md` — пересмотр механизма
  сверки, ждёт трёх ответов владельца: (1) оставлять ли слово `fail` для пригодности; (2) порог —
  гейт на чтении или колонка качества; (3) порог из природы фида (`trades/updates`) или плановые 0.1 %.
- Суть: `integrity` (разрывы `u`, инварианты, сверка с REST из `verify.csv` сайдкара) — ровно ноль и
  fail-closed; `usability` (доля невиданных цен + состав) — **число**, применяется на чтении флагом
  `--quality-max-violation-ppm`; маркер → `verify-<SYMBOL>.json` с `rule_version`, чтобы смена правила
  не переписывала смысл старых маркеров молча; метрика, влияющая на вердикт, — «зона риска» (сколько
  размеченных событий стоит рядом с невиданной сделкой), а не абстрактная доля сделок.

## [2026-09-17] plan | Большой кросс-линкованный план: база → накрутка

- Владелец завёл `docs/plan/dev-plan-2026-09-17.md` (20.6 КБ) — план работ по четырём этапам:
  **A** база и коллектор (срок ≈ 22.09), **B** база бэктестера, **C** база сигнала (отскоки),
  **D** накрутка. Опирается на четыре отчёта от 17.09: `docs/findings/bounce-spec-2026-09-17.md`
  (68 КБ, спека отскока по S/D с таймкодами), `transcripts-2026-09-17.md` (122 КБ, семь видео),
  `collector-audit-2026-09-17.md` (120 КБ, аудит коллектора: таблица проблем K1/V1–V13/C1–C9,
  §11 вопросы и §12 ответы владельца), `roadmap-2026-09-17.md` (26 КБ, цепочка до торговли и «сила
  плотности»). Решения владельца оттуда: порядок починки — «всё равно» (принят предложенный), линты —
  «как хочешь» (включать гейтом), журнал юнита — читать с `sudo`; открыты: глубокий поток `.200`
  и `fsync`. В `SETTLED.md` решения 17.09 ещё **не перенесены** (тикет A6 плана).
- План требует: исполнитель один, сборка одна за раз (В-39), тикет закрыт только по артефакту на
  диске + тесты + clippy.

## [2026-09-17] feat | A1: `lob archive` не трогает открытую часть; сторож убран по слову владельца

- Сначала A1 был сделан целиком (коммит `8e8e71c`): сторож диска `statvfs` на тике сессии,
  `session.json.{disk_free_bytes,disk_warn_bytes,disk_free_low}`, `--disk-warn-gib` (умолчание 15 ГиБ),
  зависимость `libc` для unix. **Владелец: «не нужны пока никакие сторожа. в ручном режиме буду
  управлять датой. пороги тоже не нужны»** — сторож снят целиком вместе с полями, флагом и
  зависимостью (коммит `42ded21`); диском управляет вручную, автоматики в коде нет.
- Осталось то, что не сторож, а страховка от ошибки в ручном режиме (аудит V8): при `closed = false`
  в `session.json` `lob archive` отказывает и на архивацию, и на удаление последней части символа —
  `unlink` открытого файла потерял бы дописанное. Тесты: открытая часть, закрытая сессия, «вчерашние
  сутки при открытой сегодняшней архивируются», нечитаемый `session.json` — отказ. Часть `session.json`
  читается **своей** структурой из трёх полей (`OpenSession`/`OpenPart`), а не всем `SessionSummary`:
  сторож обязан пережить смену формата файла.
- Ворота: **762 теста** (0 failed), clippy `-D warnings` и `fmt` чисто; обе новые проверки проверены
  мутацией (`<` → `<=`, замена сравнения части на `false`). Ранбук — `docs/COMMANDS.md` п. 6.

## [2026-09-17] feat | A2: `verify` закрытых суток таймером — 100 маркеров в боевом корне

- `tools/verify-day.sh` + `tools/systemd/alpha-verify.{service,timer}` (коммит `8f81912`). Таймер
  `OnCalendar=*-*-* 00:20:00 UTC`, `Persistent=true`; служба — тот же не-коллекторный бинарник
  `/opt/alpha/alpha-verify` (копия сборки `~/alpha-build/target/release/alpha`).
- Зачем скрипт, а не одна строка: `lob verify` считает вердикт по **всем** частям символа в `--root`,
  а боевой корень держит все сутки сразу → вердикт смешал бы дни при одном маркере на символ. Скрипт
  раскладывает вчерашние части hardlink'ами в `/opt/alpha/verify/<день>/`, гоняет verify оттуда и
  копирует маркеры в боевой корень (там их ищут `profiles`/`watch`), сводка — в
  `/opt/alpha/verify/<день>.log`.
- **Первый боевой прогон 17.09 08:35Z за сутки 09-16: `ok=94 fail=6`**, 100 маркеров в `/opt/alpha/root`
  (впервые — закрыт V1 аудита). Fail: LSK, AKE, BR, BTW, XMR, AVAAI. Для сравнения LSK: 11 543 935 сделок
  за сутки, нарушений 14 583 = 0.126 % (порог 0.1 %), состав: far 11 435, adj1 3 148, in_spread 2 981,
  rpi 1 212, stale20 89, block 0. Прогон 100 монет — **≈ 9 минут** (08:35:27 → 08:44:30Z).
- Грабля первого прогона: скрипт принял строку-шапку `symbol,tick_size,...` за символ (`missing=1`) —
  исправлено (шапка и `#`-строки пропускаются), исправленный скрипт доставлен на сервер; следующий
  ночной прогон 18.09 00:20Z уже чистый.
- Диск на 17.09 08:49Z: занято 11 ГБ, свободно **36 ГБ** (расход ≈ 6.5 ГБ/сутки ⇒ ≈ 5.5 суток).

## [2026-09-17] feat | A3 видимость потерь: K1, V5, V6, V4 (четыре коммита)

- **K1** (`6a96063`): отказ рукопожатия больше не молчит. `TransportError::Http{status, msg}`
  (403 ≠ 429) + `http_status()`/`message()`/`Display`; `ConnEvent::ConnectFailed{local_ts_ns,
  attempt, http_status, err}` уходит **на каждый инструмент** сокета (`Route::broadcast_connect_failed`),
  `feed::GapKind::ConnectFailed` → `commands::record::gaps::GapKind::ConnectFailed` (новая строка
  `gaps.csv`) + `session.json.connect_failed`. До правки `Err(_)` глотал всё: устойчивый 403/429
  выглядел как вечно тихий ретрай.
- **V5** (`62406dc`): `recv()` под таймаутом — новое поле `ConnConfig::recv_timeout` и
  `PoolConnConfig::recv_timeout`, у всех боевых вызывающих `2 × ping_interval` (40 с: `record.rs`,
  `feed/live.rs`, `pick/measure.rs`). Полуживой сокет (TCP жив, данных нет) раньше висел вечно.
  **Грабля:** сначала вывел таймаут из `ping_interval` прямо в `run` — тест «пинг каждые 5 мс»
  получил мёртвый сокет через 10 мс и упал (`sent` пуст); поле задаётся явно.
- **V6** (`62406dc`): `session.json.gap_rows_failed` (AtomicU64, `log_gap(&self)` + строка stderr
  только на первом отказе) — полный диск глушит и сам `gaps.csv`; финальный `flush` в `record.rs`
  печатает ошибку вместо `let _ =`. **Грабля теста:** `append_gap_row` сам делает `create_dir_all`,
  поэтому «несуществующий каталог» не отказ — брать надо путь-каталог.
- **V4** (`7982809`): вердикт `verify` читает `gaps.csv` того каталога, который сверяет
  (`VerifyStatus::of_with_gap_rows`, `VerifyReport.gap_rows`, в строке `gaps_csv=<n>`). В файловом
  режиме `sequence_gaps` всегда ноль — поля `u` в бинлоге нет. Скрипт `tools/verify-day.sh` копирует
  строки дня в рабочий каталог, иначе проверка — no-op.
- **Ворота A3:** 767 тестов, clippy `-D warnings`, `fmt` — чисто; мутации проверены (снятие вещания
  `ConnectFailed`, расширение таймаута, снятие счётчика, проглатывание ошибки журнала, `if false`
  вместо `gap_rows > 0`) — каждая роняет ровно свои тесты.
- **На сервере:** бинарник `/opt/alpha/alpha-verify` и скрипт обновлены (сборка 1 м 20 с), повторный
  прогон за 09-16 — **94 ok / 6 fail, missing=0** (шапка CSV больше не считается символом), ≈10 минут.
- **Находка:** в боевом `/opt/alpha/root/gaps.csv` **100 строк, все в 2026-09-17T01:51:19Z** —
  один разрыв сокета, `session.json.reconnects = 1`, `gaps = 100` (строка на инструмент). Значит
  ночной прогон за 09-17 пометит `fail` **все 100 монет** по V4 — это работает как задумано
  (PLAN §10: провал verify выбрасывает сутки целиком), но политику стоит подтвердить владельцем.
- **Осталось в A3:** V12 (смерть потока ввода-вывода видна на тике). Дальше A4 (мягкий `ShortRead`
  у читателей), A5 (память и линты), A6 (решения в SETTLED).

## [2026-09-17] feat | Этап A закрыт: V12, A4, A5, A6 (коммиты 4d17659…23f1e1d)

- **V12** (`4d17659`): `LiveFeed` хранил `JoinHandle` без привязки к инструментам — смерть шарда
  оставляла его монеты молча немыми. Теперь шард несёт глобальные индексы (`IoShard`), на тике
  `is_finished()` даёт по одной строке `gaps.csv` на инструмент (первый — `Disconnected`, остальные —
  `DisconnectedSameSocket`; `reconnects` растёт на один за шард), события ждут в `pending` и
  отдаются по одному на `next_event`. Тест `dead_io_shard_is_reported_once_on_the_tick`, мутация
  `dead = false` его роняет.
- **A4** (`90a108e`): читатели больше не падают на обрезанном хвостовом кадре. В `binlog::Reader`
  появился `read_frame_soft()` + флаг `truncated_tail()` (ShortRead = конец прочитанного),
  в `commands/lob/parts.rs` — обёртка с одной строкой stderr; переведены `watch`, `commands/lob/replay`,
  `profiles/sessions`, `binlog-stats` (2 замера) и `bybit/verify`. Строгий `read_frame` остался
  строгим **там, где пишется артефакт** (`--rewrite-out`): молча потерянный кадр выглядел бы полными
  сутками. Тесты: `soft_read_stops_at_a_truncated_tail_and_flags_it`, `verify_tolerates_a_truncated_tail_frame`.
- **A5** (`ef2a309`): `CLAUDE.md` приведён к фактам (v3 + `deep/`, **6.5 ГБ/сутки** вместо оценки
  5.02, 36 ГБ свободно, `verify` идёт таймером A2, сторожей нет — решение владельца, 770 тестов);
  `ARCHITECTURE.md` A4 — «отсортированный `Vec`» вместо «кольцевого массива» (C2);
  `depth-200-ab` — сайдкар подключён **только к `lob record`** (V3); V11: `unreachable!` → `bail!`
  в `session/args.rs` и доказательство индексации `books[slot][stream]` (комментарий + 3 `debug_assert!`).
  **Линты гейтом не включены — замер против:** `clippy::cast_possible_truncation` даёт **51**
  срабатывание; в плане записано как конвенция ревью.
- **A6** (`23f1e1d`, В-57): `.200` пишется дальше («копятся специально»), сторожей и порогов в коде
  нет (управление диском вручную), ротация/архивация вручную по регламенту; **`fsync` остался
  открытым** — решения владельца не было, в код ничего не добавлено.
- **Итог этапа A:** A1–A6 закрыты. **Но на проде коллектор всё ещё старый бинарник** (09-15 22:47) —
  K1/V5/V6/V12 вступят в силу только после пересборки и перезапуска `alpha-collector`;
  бинарник `/opt/alpha/alpha-verify` и скрипт ночной сверки уже обновлены.
- **Открытые вопросы владельцу:** `fsync`; политика V4 (ночной прогон 09-17 пометит `fail` все 100
  монет из-за одного переподключения в 01:51Z) — оставить строго, смягчить или развести вердикты
  (тикет 47).
- **Дальше — этап B (база бэктестера):** B1 (предрегистрация форм/фильтров, не ждёт кода) →
  B2 (вход от фронтрана, три стопа) → B3 (горизонты до 2 ч) → B4 (досрочный выход) → B5 (прогон-вердикт).

## [2026-09-17] feat | Этап B начат: В-58 (предрегистрация) и B2 (вход от фронтрана, три стопа)

- **B1 = В-58** (`783e380`): сетка форм зафиксирована до данных — **48 комбинаций** (вход от
  первого фронтранера / `P+1` × стоп `P+1`/`P`/`P−1` × тейк 1:1 × дедлайн 60 с/10 мин/1 ч/2 ч ×
  досрочный выход выкл/1/2/3 с). Формы T38 (трейл, лестница, `--post-only`) в сетку не входят.
  Фильтры — два независимых, комбинируются **И** (`size_usd ≥ N_abs`, `strength ≥ S%`); значения
  называет владелец после замеров C1/C2, каждое дополнительное значение = отдельное испытание.
  Деления на разведочную/подтверждающую нет (слово владельца). Строка `kind=prereg, trials=48` в
  `docs/plan/runs.csv` до первого прогона; цитаты владельца — в «Дополнениях» брифа.
- **B2** (`48510c0`, `89f10b6`, `9fcec8c`): `TouchRecord.frontrun_tick` — цена **ближайшего**
  лучшего уровня на том же наблюдении, что `frontrun_lots` (в трекере один `i64` рядом с префиксной
  суммой, без аллокаций); колонка `frontrun_tick` в `touches-*.csv`; вход плана — от неё (нет
  фронтрана → `P ± 1`, как T38); `--stop-mode before|at|behind` = `P+1`/`P`/`P−1`;
  тейк — 1:1 **от входа**. Аргументы CLI для `bounce_plan` собраны в `PlanShape` (clippy: 8 из 7).
- **Приёмка B2 на живой записи** (`data/always-on/20260912T201737Z`, ZECUSDT, 20 ч, 16 профилей):
  `net_fill` = **−0.0948 / −0.0945 / −0.0886** bps для `before / at / behind` — три формы дают
  разные числа (`behind` лучшая), но все отрицательные: вердикт T38 не переворачивается.
- **Ворота:** 772 теста, clippy `-D warnings`, `fmt` — чисто; мутация (`before` → −1) роняет тест
  геометрии плана.
- **Осталось в B:** B3 (горизонты до 2 ч: `m_10m/m_1h/m_2h`, `--deadline`), B4 (досрочный выход
  `--early-exit-secs` + причина выхода в CSV), B5 (прогон-вердикт по 48 формам на пригодных сутках).
- **Ждут слова владельца:** деплой коллектора (K1/V5/V6/V12 в коде, в проде старый бинарник);
  политика V4 (ночь за 09-17 пометит `fail` все 100 монет); `fsync`.

## [2026-09-17] debt | Аудиторский долг закрыт: C3, C4, хвост A4; линты замерены (коммит 94483e0)

- **C3**: греп-тесты горячего пути добавлены в `commands/lob/session.rs` и `session/sink.rs`
  (`live.rs`/`strategy.rs`/`react.rs` их уже имели). Проверка **пропускает строки-комментарии** —
  иначе падала бы на объяснениях, почему `HashMap`/`f64` тут нет; в `session.rs` банятся
  `HashMap`/`BTreeMap` и `SystemTime::now` (время — через трейт `Clock`; `Instant::now` разрешён:
  им меряются длительности), в `sink.rs` — только карты (`f64` там константа печати перцентиля).
- **C4**: инлайн-`mod tests` из `feed/replay.rs` вынесен в `feed/replay/tests.rs`. Три
  `#[cfg(test)]`-блока в `bybit/sign.rs` **не** тронуты: это не инлайн-тесты, а общие помощники
  (`for_test`, `ENV_LOCK`, `with_cleared_env`) для тестов `sign` и `probe` — аудит прочитал их
  как тесты; записано в плане.
- **Хвост A4**: мягкое чтение включено и в `lob/export.rs` — экспорт больше не падает на обрезанном
  хвосте живого корня.
- **V10 (линты) — замер, а не мнение:** `clippy::cast_possible_truncation` = **51** срабатывание,
  `clippy::indexing_slicing` = **≈290** диагностик (579 строк с подсказками). `indexing_slicing` я
  включил в `[lints.clippy]`, clippy сразу упал, и я его **снял**: гейт сегодня невозможен,
  разбор сотен мест — отдельная работа. Оба остаются конвенцией ревью, числа — в плане (A5).
- **Ворота:** 776 тестов, clippy `-D warnings`, `fmt` — чисто.
- **Что осталось из плана:** B3-хвост (длинные горизонты в `touch-profiles`, отложен к C4, чтобы не
  менять формат таблицы дважды), B4 (досрочный выход), B5 (прогон-вердикт), C1–C4 (нужны `N_abs`
  и `S%` от владельца), затем D. Аудиторские D7 (сайдкар REST в `lob session`), D8 (читатель
  `deep/`), D13 (индекс/seek, `ENOSPC`-тест) — тоже не сделаны.

## [2026-09-17] audit | Перепроверка плана, решений владельца и коммитов: один блокер (`kind=prereg`)

Запрос владельца «перепроверь план, мои каustomные решения и реализацию по коммиту»: три аудитора
по осям (этап A ↔ код, этап B + долг ↔ код, решения владельца ↔ код) + ворота капитана.

- **Ворота (мой прогон после хендоффа `6781b20`):** `cargo fmt --check` — 0; `clippy --release
  --target-dir target-ci --all-targets -- -D warnings` — 0; **776 passed / 0 failed / 5 ignored**;
  сборка чистая. CLI: `lob backtest … --deadline-secs 120` отвергнут («не из предрегистрированной
  сетки В-58 [60, 600, 3600, 7200]`, exit 1); `--early-exit-secs` в CLI **нет** (B4 не начат).
- **Подтверждено коммитами и кодом:** A2 (`tools/systemd/alpha-verify.timer` 00:20 UTC,
  `Persistent=true`; `verify-day.sh`; маркеры в `$ROOT`), A4 (`binlog/mod.rs:1795`
  `read_frame_soft`, `:1807` `truncated_tail`; строгий `read_frame` остался у `--rewrite-out`),
  A5 (доки; `ARCHITECTURE.md:114` «отсортированный Vec»; `depth-200-ab` — сайдкар только в
  `record`; `unreachable!`→`bail!`; три `debug_assert!`; линты **не** включены), A6 = В-57,
  архивная половина A1 (`commands/lob/archive.rs:207-215`), B1 (В-58: `1 × 3 × 1 × 4 × 4 = 48`,
  вход — не множитель; `runs.csv:10`), B2 (`levels.rs:328` `frontrun_tick` → вход
  `backtest.rs:869`, `--stop-mode` `:876`, тейк 1:1 `:880`; тест `backtest/tests.rs:358`),
  B3-половина (`markout.rs:52` `HORIZONS_LONG_MS` отдельно от `HORIZONS_MS:39`; колонки
  `touches.rs:135-137`; `TOUCHES_WIDTH` 29→32), долг `94483e0` (`mod hot_path_guard` в
  `session.rs` и `session/sink.rs`, тесты replay переехали, `export` читает мягко).
- **Сторож диска A1:** писался и **снят** — `8e8e71c` (libc/`statvfs`/`--disk-warn-gib`/
  `session.json.disk_free_bytes`) → `42ded21` (−228 строк). В дереве `statvfs|disk_free_bytes|
  disk-warn|libc::` — ноль вхождений: решение владельца соблюдено.
- **БЛОКЕР R1:** строка `kind=prereg` (`docs/plan/runs.csv:10`, коммит `783e380`) ломает **весь**
  журнал. `RunKind` (`src/lob/runs.rs:97-113`) варианта `prereg` не знает; `read_run_rows`
  (`:184-195`) парсит строго — свой тест `malformed_journal_is_none_not_silent_zero` (`:350-368`)
  утверждает «неизвестный kind → ошибка чтения»; `trials_from_runs_csv` (`:208-209`) через
  `.ok()` отдаёт `None`; `run_shortlist` (`commands/lob/shortlist.rs:1091`, `--runs-out` по
  умолчанию `docs/plan/runs.csv`) отказывает «журнал испытаний не читается — DSR не из чего
  дефлировать» — причём **после** тяжёлого разведочного прогона. Плюс `count_trials` (`:119-124`)
  `prereg` не считает, а колонки `trials` в CSV нет (4 колонки): «`trials=48`» существует только
  текстом `forms=48` в `detail`. Потребитель — **B5** (DSR по фактическому `N`). Фикс: вариант
  `RunKind::Prereg` (не испытание) + тест.
- **Прочие расхождения:** (R2) текст `runs.csv:10` перечисляет `entry(frontrun|P+1)` внутри
  произведения — читается как 96, хотя В-58:313 «вход не крутится» и число 48; (R3) живой прогон
  B2 (ZECUSDT −0.0948 / −0.0945 / −0.0886 bps) **без артефакта**: ни строки `runs.csv`, ни файла в
  `docs/findings/` — числа только в плане и памяти (нарушено «шаг закрыт только с артефактом на
  диске»); (R4) `dev-plan:75` обещает `frontrun_best_px`, в коде `frontrun_tick`; (R5) отказ
  `--deadline-secs` вне сетки тестом не покрыт (живьём работает); (R6) `markout::check_horizons:63`
  — guard **без боевого вызова** (единственный вызов — тест `markout/tests.rs:353`), docstring
  ссылается на `--deadline-secs`, который его не зовёт; (R7) `dev-plan:33` (A5) всё ещё требует
  `[lints.clippy] = "deny"`, хотя `:43-47` и `Cargo.toml` фиксируют конвенцию, а вывода замера
  (51 / ≈290) на диске нет.
- **Дрейф плана:** строка A1 (`dev-plan:29`) всё ещё требует `session.json.disk_free_bytes` и
  «сервер переживает 22.09»; номера строк устарели (`archive.rs:192-201`→`207-215`,
  `record.rs:908,914`→`934,944`); блоки «A5 — сделано» и «Линты» вставлены **внутрь** таблицы
  (`:35-47`) и рвут её; путь указан без `src/`.
- **A3 — недобор тестов:** нет теста на печать отказа финального `flush` (`record.rs:934-945`) и на
  рост `reconnects` от смерти шарда; `connect_failed` не выводится в stderr-сводку
  (`session.rs:1120-1133`).
- **Решения владельца — соблюдены** (сторожей/порогов нет; `deep/` пишется, читателя нет;
  архивация вручную, удаление только с `--delete-original` и маркером `ok`; `fsync` не добавлен;
  реальные ордера только по слову владельца; сетка зафиксирована до данных; деления на
  разведочную/подтверждающую нет). **В SETTLED не перенесены:** линты-гейт, журнал/journald,
  порядок починки, спот, «красивые числа» (D4 — решение «да», `round_zeros` не расширен),
  `N_abs` в долларах, знаменатель B, ордера + RTT `assumed`. `collector-audit §12 п.1`
  («сторож диска, срок 22.09») противоречит В-57 — источник устарел.

## [2026-09-17] fix+feat | R1 закрыт (журнал испытаний), B4 сделан (досрочный выход); коммиты `3582632`, `b73cc07`

- **R1 оказался двойным.** Строка `kind=prereg` в `docs/plan/runs.csv` гасила журнал не только
  неизвестным видом: она была записана **без экранирования**, а `detail` содержит запятые
  (`filters=AND(size_usd>=N_abs, strength>=S%)`) — 6 полей вместо 4, `deserialize::<RunRow>()`
  падает на числе колонок. Это нашёл **сам новый тест** `the_repository_journal_reads`, который
  читает боевой `docs/plan/runs.csv` (первая версия фикса только добавляла вариант в `RunKind` и
  этого не ловила).
- **Починено:** `RunKind::Prereg` (учёт, не испытание; `counts_as_trial` не считает) + строка
  закавычена по RFC 4180 (содержание предрегистрации не менялось) + строка `kind=amendment` с
  причиной правки. Итог: `trials_from_runs_csv(docs/plan/runs.csv)` снова читается, `lob shortlist`
  (DSR-дефляция) и будущий счёт `N` в B5 больше не падают. Плюс тест
  `prereg_row_is_bookkeeping_and_does_not_kill_the_journal`.
- **B4 сделан (коммит `b73cc07`).** `ExitReason::Early` + `still_at_level` в единственной стратегии
  `on_event` (работает и в `Backtest`, и в `LiveBot`): если через `X` секунд после входа лучшая цена
  стороны всё ещё на цене уровня `P` (точность половины тика) — выход по рынку. Порядок:
  стоп → трейл → тейк → **досрочный** → дедлайн. `TradePlan::Bounce` получил `early_exit_ns`,
  `level_px`, `tick_px` (стратегия не знает ни тика, ни уровня). CLI `--early-exit-secs X`,
  `X ∈ {1,2,3}`, без флага — выключен; `--early-exit-secs 5` проверен живьём (exit 1). В отчёт
  добавлена колонка `n_early`, причина каждого круга — в `BounceRun::fill_reason`.
- **Замер B4** (ZECUSDT, `20260912T201737Z`, стоп `behind`, дедлайн 60 с): `net_fill` −0.0886
  (выключен) / **−0.0909** (1 с) / −0.0907 (2 с) / −0.0937 (3 с) bps; досрочно 108/88/68 кругов,
  стопов 160/177/199 вместо 226; кругов 309 → 324 (ранний выход быстрее освобождает позицию —
  эффект ёмкости). Механизм работает, результат **хуже**: выход по рынку в прилипшем касании отдаёт
  больше, чем экономит. Артефакт `docs/findings/early-exit-2026-09-17.md`.
- **Хвосты аудита закрыты:** R3 — артефакт B2 `docs/findings/bounce-b2-stops-2026-09-17.md` (прогон
  воспроизведён: −0.0948 / −0.0945 / −0.0886 bps, круги 324/323/309, стопы 302/297/226);
  R5 — `deadline_ns_from_secs`/`early_exit_ns_from_secs` вынесены функциями и покрыты тестом;
  R6 — `markout::check_horizons` теперь вызывается боевым путём `--deadline-secs`; R4, R7 и дрейф
  плана (A1 со сторожами, A5 с `deny`-линтами, номера строк, сломанная таблица, `frontrun_best_px`)
  — исправлены в `dev-plan-2026-09-17.md`.
- **Ворота:** **782 теста** (0 failed, 5 ignored), clippy `-D warnings`, fmt — чисто; шесть новых
  тестов проверены мутацией (мутация `still_at_level → true` роняет отрицательный тест B4;
  мутация сетки `EARLY_EXITS_S = [1,2,4]` роняет тест отказа).
- **Открыто:** `trials=48` как машиночитаемое поле в журнале (сейчас текст `forms=48` в `detail`,
  журнал считает испытания по фактическим строкам); B5 (прогон-вердикт 48 форм) не начат.

## [2026-09-17] cleanup | Жёсткая чистка доков: одна структура, 37 файлов в `docs/archive` (коммиты `270e0a4`, `eac89b2`)

Запрос владельца: «давай жёстко почистим проект, проредим от лишних документов, старое неактуальное
уберём; все механизмы, которые что-то создают, должны создавать это в доке под определённой
структурой». Ничего не удалено — всё перенесено (`git` помнит).

- **Перенесено 37 файлов:** 9 сырых расшифровок (≈800 КБ, `docs/транскрипции/` →
  `docs/archive/transcripts/`; они уже сжаты в `docs/findings/transcripts-2026-09-17.md`),
  15 сгенерированных CSV-артефактов (`bounce-backtest-2026-09-13*`, `practitioner-numbers-*` →
  `docs/archive/artifacts/`), черновики первой редакции плана (`docs/plan/archive/`:
  `critique-1..7.json`, `OPEN_QUESTIONS.md`, `README.md` → `docs/archive/plan-2026-09-11/`), два
  старых пула из корня (`instruments-top20/top30-server.csv` → `docs/archive/pools/`), мёртвая доска
  автопилота (`.autopilot/index.html`, `dashboard.html` → `.autopilot/archive/ui-*.html`).
- **Структура и правило записаны в `CLAUDE.md`** (раздел «Доки: структура и правило артефактов»):
  `docs/{ARCHITECTURE.md, COMMANDS.md, plan/, adr/, findings/, archive/}` + таблица «что создаётся →
  куда»: текст-вердикт → `docs/findings/<тема>-<дата>.md`; машиночитаемый вердикт →
  `docs/findings/<команда>-<дата>[-<SYM>].<csv|json>`; сырое и промежуточное (levels/markout/touches,
  дашборд, архивы binlog) → `data/` (не документ, gitignored); журнал испытаний → `docs/plan/runs.csv`;
  **состояние проекта — только `CLAUDE.md`**, доки ссылаются на него, а не повторяют. Заменяющее
  переносит заменённое в `docs/archive/` тем же коммитом и правит ссылки.
- **`docs/COMMANDS.md`**: удалён устаревший блок «Состояние» (пул из десяти, олвейз-он
  `data/always-on/20260912T122440Z/`) — этот абзац ловили на дрейфе четыре раза; вместо него указатель
  на `CLAUDE.md`. `docs/plan/PLAN.md`: ссылка на никогда не существовавший
  `touch-profiles-2026-09-13.csv` исправлена на `<дата>`.
- **Ссылки проверены скриптом** (все `docs/…`-пути и markdown-ссылки по `git ls-files`) — битых нет;
  обновлены упоминания архивированного в `.autopilot/**`, `.claude/**`, `docs/**` и памяти.
- **Осталось на местах намеренно:** старые findings (`recording-2026-09-11`, `pilot-2026-09-11`,
  `audit-2026-09-12`, `collector-2026-09-12`, `RECON/REVIEW/REQUIREMENTS-2026-09-11`) — на них
  ссылаются doc-комментарии `src/` как на источник чисел; уедут, когда перестанут быть источником.
  `docs/archive/README.md` описывает архив и причину.
- **Ворота не гонялись:** чистка не касалась `src/` (только доки, `.autopilot`, `.claude`, память).
  Последний зелёный прогон — 782 теста на коммите `b73cc07`.

## [2026-09-17] cleanup | Раунд 2 чистки: 51 файл в архив, ссылки в коде обновлены

Владелец: «ещё один раунд сделай чистки на всякий». Перенесено (не удалено):
- **findings волн 1–3 (10):** `recording-2026-09-11`, `pilot-2026-09-11`, `audit-2026-09-12`,
  `collector-2026-09-12`, `pool-top50-2026-09-13`, `deploy-2026-09-14`,
  `binlog-format-2026-09-13` (перекрыт `binlog-v3-2026-09-13`), `depth-distribution-2026-09-15`,
  `format-columnar-2026-09-15`, `format-dictionary-2026-09-15` → `docs/archive/findings/`.
  Обновлены **doc-комментарии `src/`** (ws.rs, binlog/mod.rs, binlog_stats.rs, record.rs, export.rs,
  feed/live.rs, session/sink.rs, session/tests.rs, profiles.rs, profiles/accumulate.rs,
  `tests/collector_bench.rs`) и живые доки (`collector-audit-2026-09-17.md`, `interfaces.md`, PLAN, SETTLED).
- **черновики плана (3):** `RECON-2026-09-11`, `REVIEW-2026-09-11`, `REQUIREMENTS.md` →
  `docs/archive/plan-2026-09-11/` (ссылки поправлены в PLAN.md — 6 мест, `ws.rs` — 2, ADR 0008/0012).
- **тикеты T1–T33 (33) и передачки (5)** → `.autopilot/archive/{tickets-01-33,handoffs-2026-09-11}/`;
  живые тикеты `34…47` (13 файлов) остались — их цитируют `dev-plan` и `CLAUDE.md`.
- **Итог:** `docs/findings` 30 → **20**, `docs/archive` 49, `.autopilot/tickets` 47 → **13**.
- **Ворота после правки комментариев:** `fmt=0`, **782 теста** (0 failed, 5 ignored), `clippy=0`.
- **Грабля (новая, стоила двух проходов):** при массовой замене путей нельзя после замены полного
  пути (`docs/findings/X` → `docs/archive/findings/X`) менять отдельно basename `X` — он встречается
  уже внутри нового пути, и префикс удваивается (`…/archive/findings/…/archive/findings/X`; поймано
  в 4 файлах). Порядок: сначала basename, потом полный путь, и после — обязательная проверка на
  удвоенный префикс.
- **Осталось кандидатами на раунд 3 (нужно слово владельца):** `.autopilot/state.js` (108 КБ доски
  волн 1–4, на него ссылаются `CLAUDE.md` и `PLAN.md`), `examples/*_probe.rs` (6 разовых замеров),
  `.claude/` (скилл `plan-autopilot` + два агента), `tests/fixtures/BTCUSDT-2026-09-09.binlog`.

## [2026-09-17] cleanup | Аудит архива ПО СОДЕРЖАНИЮ: 4 файла вернулись в живое

Владелец: «все что в архиве — лежит там правильно? правомерно?» → «выясни по содержанию». Механическая
проверка ссылок дала 37 файлов в архиве, на которые ссылаются живые; проверка **по тексту** (что в
файле есть и чем оно перекрыто) показала, что 4 из них — не архив, а рабочий материал:

| вернулись в `docs/findings/` | почему (единственный источник) |
|---|---|
| `recording-2026-09-11.md` | обоснование разбора без DOM и `ConnSink`: `parse_p99` 400–850 мкс, очередь 918.6 vs 686.0 мкс → `src/bybit/ws.rs:30`, `conn.rs:453`, `feed/live.rs:205` |
| `pilot-2026-09-11.md` | 30 мин, пул 8: k-сетка, G0 net −9.006 bps, G-POWER-B 2.2551 vs 3.0570 (n=147); числа `399.5` → `ws.rs:30` |
| `collector-2026-09-12.md` | отсюда `record::ZSTD_LEVEL = 1` (zstd 1/3/6/9 = 7.245/7.307/6.908/6.839 Б/запись), гейт аллокаций (`session/sink.rs:223,330,479`), замер 761 инструмента (CPU 24.2 %, queue p99 27.8 мс), основание SETTLED В-34 |
| `binlog-format-2026-09-13.md` | v2-бюджет полей и «0 % ненулевых на 17–29 млн записей»; `binlog-v3` перекрывает v3-A/B, но не v2; читатель v2 `Reader::legacy_dead_fields` (`binlog/mod.rs:1635`) живой → 6 ссылок в `src/` |

**Законно остались в архиве (16):** `audit-2026-09-12` (все 5 находок закрыты В-35…В-37), `pool-top50-2026-09-13`
(перекрыт В-50 и разбором переезда), `deploy-2026-09-14` (старый сервер), `depth-distribution-2026-09-15`
(перекрыт `depth-200-ab`), `format-columnar/format-dictionary-2026-09-15` (отклонённые варианты, решения
в В-49/`Cargo.toml`), `REVIEW/REQUIREMENTS/OPEN_QUESTIONS/critique-1..7` (первая редакция плана).
**Первичное — в архиве по праву:** 9 расшифровок (таймкоды для цитат решений), CSV прогонов 09-13,
`RECON-2026-09-11` (`LEVELS_INITIAL_CAPACITY = 8`, `fast_topic` — на него ссылается `ws.rs` и PLAN),
старые пулы (пруф MD5).

**Правило уточнено в `CLAUDE.md`** (раздел «Доки»): архив = перекрытое **и** первичное (сырьё/пруфы);
если на файл опирается код или живое решение как на единственный источник числа — он рабочий и
возвращается в `docs/findings/`. Класть в архив по дате, а не по содержанию, — ошибка (моя, исправлена).
`docs/archive/README.md` переписан: две категории, критерий, список вернувшихся.
**Ворота:** `fmt` 0, **782 теста** (0 failed, 5 ignored), `clippy` 0 — после правки 6 ссылок в `src/`.

## [2026-09-17] audit+cleanup | Проверка реализации dev-plan и уборка проекта (сессия разбора транскрипций)

- Владелец: «проверить реализацию плана, там частично сделано; обновить память, подмести проект».
- **Проверено по коду:** A1 `archive.rs::still_writing`; A2 `tools/verify-day.sh` + `alpha-verify.timer`
  (00:20 UTC, в корне 94 ok / 6 fail за 09-16: AKE, AVAAI, BR, BTW, LSK, XMR); A3 `ConnectFailed` ×3,
  `recv_timeout`, `gap_rows_failed`; A4 `parts::read_frame_soft` (watch, replay); A5/A6 (В-57);
  B1 В-58; B2 `--stop-mode`, `frontrun_tick`; B3 `--deadline-secs`, `HORIZONS_LONG_MS`; B4
  `--early-exit-secs`. Ворота: **782 passed / 0 failed / 5 ignored**, clippy `-D warnings`, fmt — чисто.
  Не сделано: B5, C1–C4, D.
- **Сервер (только чтение):** юнит активен с 09-15 22:47Z, `NRestarts=0`, бинарник тот же 09-15 —
  A3 не выкачен; `session.json`: `reconnects=1`, `records_total=2.6e9`; `gaps.csv` — 100 строк
  `sequence_gap` «транспорт переподключился — шов покрытия» 2026-09-17T01:51:19Z по всем 100
  символам; `df` 14 ГБ занято / 34 свободно. Следствие V4: ночная сверка за 09-17 даст `fail` всем
  100 — нужна политика «шва» (владелец).
- **Уборка:** удалены `.tmp-ssh` (19 МБ, 130 одноразовых скриптов и копий исходников, zig-тулчейн —
  прод собирается на сервере с 16.09), `.tmp-gate`/`.tmp-b2` (CSV прогона B2 — отчёт сам говорит,
  что они временные), `.tmp-chrome2` (профиль браузера), содержимое `.tmp-shot` (каталог оставлен —
  `tools/shot.py` его не создаёт), пустой `does/not/`, `target/` (4.5 ГБ; сборка — в `target-ci`).
  Перенесены в `tools/server/`: `rpi_probe.py`, `rpi_pool_probe.py`, `alpha-collector.service`
  (на них ссылаются `hft-underground-2026-09-16.md` и `COMMANDS.md`); ссылки поправлены.
- **`data/` не трогал** (1.6 ГБ): упоминаются в доках `always-on`, `collector`, `recording-eco`,
  `pilot-battle`, `dashboard`, `collector-500`, `tmp-v2check`, `tmp-v3check`, `smoke-v3`,
  `session-debug`, `pilot-debug`, `moves`; **не упоминаются нигде** — `acceptance-20260911`,
  `acceptance-20260911T155044Z`, `backtest-fillmodel-debug`, `bybit`, `dashboard-check`, `live5e`,
  `pick-debug`, `pick22a`, `pick25`, `react-debug`, `recon5`, `shortlist-debug`, `smoke-v3-top50`,
  `tmp-lsk-v2`, `tmp-lsk-v3`, `top50-test` — записи, удалять только словом владельца.
- Память: `CLAUDE.md` (тесты 782, диск 34 ГБ, прод-бинарник и шов), `index.md`, эта страница,
  `dev-plan` §«Проверка реализации».

## [2026-09-17] feat+ops | В-59: сверка по смыслу; `alpha-verify` обновлён на сервере; смоук нового коллектора прошёл

- Владелец: «верифай надо не ебланский делать», «слить в master — ок», «начни со смоука, коллектор не
  ломай», «SIGTERM — не понял зачем; надо научить коллектор работать при любых обстоятельствах —
  смена/удаление/добавление монет, апокалипсис».
- **В-59** (`3614321`): `commands::lob::verify::GapTally` — `write_failed`/`book_invariant` роняют
  сутки (потеря целостности), `sequence_gap`/`parse_error`/`connect_failed` — швы, считаются
  (`seams=`), `step_change` — не потеря; строка вердикта `status= gaps_csv= seams= losses= step_changes=`;
  маркер по-прежнему одно слово. Три теста (шов → ok, потеря → fail, каждая причина в одной корзине).
  Ворота: 783 / 0 / 5, clippy, fmt. Доки: COMMANDS.md, CLAUDE.md, SETTLED В-59, dev-plan A7/A8.
- `master` ← `dev` fast-forward, оба запушены (`3614321`).
- **Сервер (сборка `~/alpha-build`, `nice -n 10`, 1 м 13 с; грабля: у `nohup sh -c` нет `~/.cargo/bin`
  в PATH — задавать явно).** `alpha-verify` → `3614321`, прежний `alpha-verify-prev-20260917`; на
  hardlink-копии `HYPEUSDT-2026-09-17.binlog` + строки дня из `gaps.csv`: `status=ok gaps_csv=1
  seams=1 losses=0` — шов посчитан, сутки живы. Смоук `alpha-smoke-3614321 lob session --minutes 5`
  (HYPE, LSK, NEAR, DOGE, AKE, корень `/opt/alpha/smoke`, владелец `ubuntu`): 611 214 записей за
  300 с, gaps 0, reconnects 0, `connect_failed=0`, `gap_rows_failed=0`, `io_threads_dead` в JSON есть,
  parse p99 102.4 мкс, queue p99 200.7 мкс, CPU 2.2 %, RSS 9.0 → 14.9 МиБ, `deep/` 5 файлов,
  `verify` ok ×5 (seams 0). Живой коллектор: `active`, час 44, не задет.
- **Не сделано (по слову владельца — перед деплоем):** A8 «коллектор при любых обстоятельствах»
  (dev-plan «Дополнения»: hot-remove/замена, отказ подписки, сеть, диск полон→свободен, SIGTERM =
  reboot, скачок часов, частичный `instruments.csv`). Деплой коллектора — после A8.
- Открыто: `N_abs`, `S%`, `fsync`, глубокий поток (пишется по В-57), старые каталоги `data/`.

## [2026-09-17] ops | Деплой коллектора `3614321` на сервер (владелец: «хоть щас, хоть потом»)

- `sudo cp` бэкап → `touch /opt/alpha/root/stop` → юнит `inactive` через 10 с, `session.json closed=true`
  (старый процесс: 44 ч, 2 653 675 550 записей, `gaps=100`, 6 ч 27 мин CPU, systemd «2.8G memory peak»
  — это page cache записанных файлов, RSS был 75–78 МБ) → `cp` нового бинарника → `systemctl start`
  → `active` с 19:17:41Z, pid 62981, `stop` снят стартом, части `-p2`, `instruments=100`,
  `connect_failed=0`, `gap_rows_failed=0`. Строк в `gaps.csv` штатная остановка не добавила (101 как было):
  дыра покрытия между частями не журналируется — заметка для A8.
- Дальше по плану: A8 (hot-remove/замена монет, отказ подписки, сеть, диск, SIGTERM/reboot, часы,
  частичный `instruments.csv`), затем B5.

## [2026-09-17] memory | Передача переписана начисто для другого исполнителя

- Владелец: «обнови память и доку разработки, чтобы другой ИИ передал». `index.md` §«Состояние» —
  заново (одна страница: что сделано, следующий шаг A8 → B5, прод, что ждёт владельца);
  `projects/active/alpha.md` §ПЕРЕДАЧА — заново (роль и правила, где встали, A8.1–A8.6 с местами в
  коде и порядком проверки, B5/C, решения владельца, как с ним работать, сервер, грабли сессии);
  `dev-plan` — шапка «Точка входа» со статусом каждого тикета. История ниже по странице не тронута.

## [2026-09-17] code | A8.1 сделан: снятие монеты и замена пула на идущей записи (`8e58c60`)

- Тикет `.autopilot/2026-09-11-lob-density-ed3/tickets/48-hot-remove-instruments.md`. `DynamicPool::remove`
  (`feed/mod.rs`, `feed/live.rs`): шард пересобирается без снятых, соседи получают новый сокет **с теми
  же индексами**; таймер потока решений вынесен в свой ОС-поток (`spawn_ticker`) — раньше жил на рантайме
  первого шарда и умер бы вместе с ним. `SessionCtx::close_symbol` + `FrameSink::close`/`ClosedSink`:
  батчи обоих потоков сброшены, дескрипторы отпущены, счётчики байт остаются; состояние остаётся в
  `states` с `active = false` — события и разрывы снятого индекса цикл пропускает, индексы не
  переиспользуются (вернувшееся имя — новый индекс и `-pN`), `session.json.pool_removals` (`symbol`,
  `ts_utc`), `instruments` без дубликатов. Перечитка `<root>/instruments.csv` только по **дописанному**
  файлу (разобрался + перевод строки в конце): оборванный не читается как «убрать монеты».
- Ворота: **791 тест, 0 failed, 5 ignored** (+8 новых), clippy `-D warnings`, fmt. Две новые проверки
  проверены мутацией (`mem::forget(stop)` → падают оба feed-теста; без `active = false` → три
  сценарных). Доки: `CLAUDE.md` (команды + грабли), `COMMANDS.md` (строка `lob session`, грабли пула,
  «снять монету / заменить пул»), `SETTLED.md` В-40 (дополнение), `dev-plan` (A8.1 сделан), `//!` session.rs.
- **Живой прогон** (локально `data/a81-live`, 3 монеты, один сокет, `--minutes 5`, 17.09): строка
  LSKUSDT убрана атомарной заменой файла в 19:56:04Z, снятие применено на тике 19:56:12.665Z (8 с),
  оба файла символа дописаны (31 245 и 23 761 Б), соседи после пересборки сокета писали дальше
  (190 287 и 187 029 Б), gaps 0 / reconnects 0 / frames_failed 0 / unrouted 0, `pool_removals` в
  `session.json`, `stop` → `closed=true` (151 с, 127 778 записей, CPU 1.1 %, parse p99 41 мкс, 640 514 Б).
- Грабля замера: `Get-ChildItem`/`FileInfo.Length` на **открытом** файле отдают метаданные каталога NTFS
  и показывают `0` до закрытия — «файлы соседей не растут, процесс повис» было ошибкой чтения; рост
  смотреть по закрытым файлам или `session.json`, а «висящий процесс» проверять файлом `stop`.
- Не сделано: смоук A8 на сервере и деплой — вместе с A8.2–A8.6; следующий — **A8.2 SIGTERM**.
  Пуш из песочницы опять упал (`Authentication failed`) — сработал разовый `danger-full-access`.

## [2026-09-17] fact | A8.3 (отказ подписки) виден уже сегодня

- Живой прогон A8.1: у HYPEUSDT/NEARUSDT снапшот книги пришёл заметно позже, чем у LSKUSDT (файлы
  соседей оставались 0 байт первые ~20 с, у LSK уже 31 КБ) — сам по себе это не отказ, но показывает,
  что отказ подписки биржей сегодня **невидим вовсе** (ни строки `gaps.csv`, ни счётчика); A8.3 —
  `success:false` → `GapKind::SubscribeFailed` — стоит делать раньше остальных подтикетов после A8.2.

## [2026-09-18] review | A8.1 (`8e58c60`) перепроверен: ворота 791/0/5, ревью «принять с правками»

- Владелец: «глянь, чё агент заебашил другой», «качественно сделал?». Ворота перегнаны: clippy, fmt,
  791 / 0 / 5 — совпадает. Живой коллектор не тронут (pid с 19:17:41Z, файлы растут); прогон A8.1 был
  локальным (`data/a81-live`).
- Ревью вторым читателем по трём осям (отчёт — в тикете 48, раздел «Ревью»): корректность и тесты —
  хорошо (8 тестов, мутация), горячий путь не задет (`active` — одно сравнение по индексу).
  **Три «важно» до раскатки на сервер:** (1) шов у соседей по сокету при снятии — тихий (нет строки
  `gaps.csv`; в проде это 99 монет); (2) нет барьера между `stop` старого шарда и `spawn` нового —
  перекрытие сокетов на одни индексы; (3) `add` после успешного `remove` в одной перечитке при отказе
  не повторяется до смены `mtime`. Косметика: обрезка ровно по границе строки; дедуп `instruments`.
- Порядок дальше (с учётом факта другого агента «отказ подписки сегодня невидим вовсе»): A8.3
  (`SubscribeFailed`) → A8.2 (SIGTERM) → правки ревью A8.1 → A8.4–A8.6 → смоук сценариев на сервере
  → деплой → B5.

## [2026-09-18] plan | A8 разложен на подтикеты с порядком после ревью A8.1

- Владелец: «окей, обнови план». `dev-plan` §Дополнения: строка A8 заменена таблицей A8.1 (сделан),
  A8.1b (правки ревью — до раскатки), A8.3 (следующий), A8.2, A8.4, A8.5, A8.6, A8.7 (смоук сценариев +
  деплой одним выкатом); строка порядка обновлена. `alpha.md` §ПЕРЕДАЧА п. 1 и `index.md` «Следующий
  шаг» — тот же порядок.

## [2026-09-18] code | A8.3 сделан: отказ подписки биржи виден и привязан к инструменту (`eeec710`)

- Тикет `.autopilot/2026-09-11-lob-density-ed3/tickets/49-subscribe-failed.md`. **Замер до кода**
  (живой сокет, в партию подписок подложен `ZZZFAKEUSDT`, временный `eprintln` в ветке `Other`,
  убран перед коммитом): `{"success":false,"ret_msg":"error:handler not found,topic:orderbook.50.ZZZFAKEUSDT",
  "conn_id":"…","req_id":"","op":"subscribe"}` — ответ **на каждый несогласованный топик отдельно**,
  топик назван в `ret_msg`, поля `args` нет; на `.200` и `publicTrade` того же фейка биржа не
  пожаловалась (отказ топик-специфичен). До правки это читалось как служебное сообщение, и инструмент
  молча стоял без данных — ни строки `gaps.csv`, ни счётчика.
- Реализация: `ws::Event::SubscribeFailed { topic, ret_msg }` + заимствующий `SubscribeAck`
  (аллокация только на отказе) + `ws::topic_symbol`; `conn::ConnEvent::SubscribeFailed` с привязкой к
  **инструменту топика** (`route.slot_of`; чужой топик → индекс сокета, как события без символа;
  `session.productive = true`), `feed::GapKind::SubscribeFailed` → строка `gaps.csv` класса
  `subscribe_failed`, счётчик `session.json.subscribe_failed` (и в строке сводки stderr),
  `GapTally::seams` (шов, В-59); одно-символьный `lob record` пишет ту же строку.
- Ворота: **795 тестов, 0 failed, 5 ignored** (+4 новых: разбор ответа, привязка к топику на сокете
  из двух инструментов + сосед жив, чужой топик → индекс сокета, счётчик+строка в сессии), clippy
  `-D warnings`, fmt. Доки: `CLAUDE.md` (швы, состояние A8), `COMMANDS.md` (строки `lob session` и
  `lob verify`), `dev-plan` (A8.3 сделан + заметка).
- **Живой прогон** (`data/sub-probe2`, HYPEUSDT + `ZZZFAKEUSDT`, один сокет, 51 с, остановка файлом
  `stop`): `2026-09-17T20:34:44Z,ZZZFAKEUSDT,subscribe_failed,"подписка не состоялась:
  orderbook.50.ZZZFAKEUSDT — error:handler not found,…"`, `subscribe_failed = 1`, `gaps = 1`,
  `connect_failed = 0`, `closed = true`.
- Открыто за тикетом: отказ сам не лечится (повтор подписки — только на переподключении сокета);
  «символ без данных N минут → снять из пула» — отдельное решение владельца, не изобретено.
  Следующий по плану — **A8.2** (SIGTERM = штатный `stop`), затем A8.1b (три правки ревью).

## [2026-09-18] code | A8.2 сделан: SIGTERM = штатная остановка, как файл `stop` (`0f3cd7a`)

- Тикет `.autopilot/2026-09-11-lob-density-ed3/tickets/50-sigterm-graceful-stop.md`. SIGTERM (его шлют
  `systemctl stop`/`restart`, перезагрузка и `kill` по умолчанию) раньше не обрабатывался: процесс
  умирал, запись рвалась на границе кадра (данные целы — `FrameSink` пишет кадр одним `write_all`), но
  `session.json` оставался `closed=false` и штатный путь (`flush_all` → финальная сводка) не выполнялся.
- Реализация: `StopHandle::stop_on_ctrl_c` → `stop_on_signals`; `wait_for_stop_signal` на Unix берёт
  `tokio::signal::unix::signal(SignalKind::terminate())` и в `tokio::select!` ждёт первый из `ctrl_c()` и
  `term.recv()`, затем `stop()` → `Item::Stop` → `None` от `Feed` → `finalize()` → `closed=true`; строка
  оператору называет сигнал. Второе нажатие Ctrl+C — по-прежнему аварийный выход 130. На Windows `cfg(unix)`
  — там Ctrl+C или файл `stop`. Шов для теста — `stop_on_signals_ready(Some(ready))` (метка после
  регистрации обработчика, иначе тестовый процесс убил бы себя сам).
- Тест `feed::live::tests::sigterm_reaches_the_stop_handle_like_ctrl_c` (`cfg(unix)`): тест посылает
  `kill -TERM` себе после метки готовности и требует `Item::Stop` из канала; цепочка «`Item::Stop` →
  `closed=true`» покрыта сценарным тестом сессии. **Unix-ветка тип-проверена на Linux-цель**
  (`cargo +1.93.1 check --target x86_64-unknown-linux-musl` на scratch-крейте с одним `tokio`): локально
  она под `cfg` не компилируется, а полный кросс-чек проекта упирается в C-код `zstd-sys` без
  кросс-компилятора. Грабля: musl-std стоит у тулчейна `1.93.1` (проект пинится `rust-toolchain.toml`),
  у `stable` его нет — `+1.93.1` обязателен.
- Ворота: **795 тестов, 0 failed, 5 ignored** локально (на Linux 796 — тест под `cfg(unix)`), clippy
  `-D warnings`, fmt. Доки: `CLAUDE.md` (команды/грабли/состояние), `COMMANDS.md` (строка `lob session`,
  ранбук «Коллектор на сервере» — `systemctl stop`/`restart` штатны, грабли Ctrl+C), `dev-plan` (A8.2
  сделан + заметка).
- Открыто за тикетом: живой `kill -TERM` на смоук-процессе на сервере и единственная проверка ветки на
  настоящем Linux — в A8.7; в проде до деплоя `3614321`, там SIGTERM рвёт запись (останавливать файлом
  `stop`). `lob record` (одно-символьная запись) свой цикл с `ctrl_c` не трогал — SIGTERM там нет.
  Следующий по плану — **A8.1b** (три правки ревью A8.1), затем A8.4–A8.6, A8.7 (смоук + деплой), B5.

## [2026-09-18] code | A8.1b, A8.4, A8.5, A8.6 закрыты: этап A8 остался на A8.7

- **A8.1b** (`1cf7743`, правки ревью A8.1, отчёт — тикет 48 §«Правки ревью закрыты»):
  (1) `DynamicPool::remove` возвращает индексы соседей пересобранного сокета — сессия пишет по строке
  `gaps.csv` `sequence_gap` «шов покрытия — пересборка сокета при снятии `<SYM>`» на каждого и
  сбрасывает доверие книги; (2) барьер `stop`→`spawn` (`join` старого потока до запуска нового; тест
  получил задержку закрытия старого транспорта 200 мс и требует «closed 0» раньше «open 1»);
  (3) отказ `add` после применённого `remove` повторяется на следующем тике без нового `mtime`
  (`pool_retry`, снимается после успешной перечитки; ошибки чтения по-прежнему ждут `mtime`).
  Обе правки проверены мутацией (без `join` — падает порядок; без `pool_retry` — падает повтор).
- **A8.4** (`70fa78c`, тикет 51): диск полон → снова свободен, перезапуск не нужен. Кода не
  потребовалось (`FrameSink::flush` уже откатывает к границе кадра); закрыто проверкой: двойник
  `FullThenFreeFile` с окном `ErrorKind::StorageFull` и тест на 6 кадров — `frames_failed = 3`, три
  строки `write_failed`, та же часть (`-p2` нет), файл читается целиком, `records_total` равен числу
  записей в файле, длина файла = `bytes_written`. Мутация `truncate_to` (откат невозможен) роняет тест.
- **A8.5** (`c5f6cc9`, тикет 52): обрыв сети на часы. Тест-коннектор с очередью `[HTTP 429, DNS-Io]`
  → снапшот: `ConnectFailed{attempt 1, status Some(429)}`, `ConnectFailed{attempt 2, status None,
  текст dns}`, затем `Message` со снапшотом и повторная подписка `orderbook.50.*`; сессионный тест —
  `ConnectFailed` → снапшот → `synced = true`, кадр на диске. Мутация «заморозить номер попытки»
  роняет. Грабля тест-двойника: `Option::take()` на кадры съедал сценарий у неудачной попытки — брать
  только когда отказа нет.
- **A8.6** (`9bf640a`, тикет 53): скачок часов (NTP) — зафиксированное поведение. Ряд середины строится
  по времени биржи (`cts`), локальная метка — только артефакты сессии: тест с меткой на 5 с назад даёт
  оба кадра на диске, `gaps = 0`, пустой `gaps.csv`, `records_total = 4`. Контракт `markout` —
  неубывающее время; на немонотонном ряде `partition_point` даёт срез за перелётом (цель 1600 → 1200,
  хотя «последний по времени» 1500), на неразделённом предикате ответ не определён — записано
  контрактом в доке модуля и тестом, молча не чинится.
- Ворота после четырёх тикетов: **802 теста, 0 failed, 5 ignored** (на Linux 803 — тест SIGTERM),
  clippy `-D warnings`, fmt. Доки: `COMMANDS.md` (шов соседей + барьер + повтор, диск полон, сеть,
  скачок часов), `dev-plan` (§Дополнения — все A8.x кроме A8.7 сделаны, «Точка входа» обновлена),
  тикеты 51–53 и раздел в 48.
- **Остался A8.7** — смоук сценариев на сервере во втором корне `/opt/alpha/smoke` одним бинарником
  (добавить/удалить/заменить монету, `kill -TERM`, обрыв сети `iptables`, заполнение диска) → отчёт
  `docs/findings/collector-resilience-<дата>.md` → деплой одним выкатом. Прод до этого — `3614321`.


## [2026-09-18] code | A8.7 сделан и задеплоен: этап A закрыт, в проде живёт бинарник с A8 (`de4c791`)

- Смоук сценариев на **живом Linux** во втором корне `/opt/alpha/smoke` (сборка из HEAD `29e8327`,
  `~/alpha-build`, бинарники `~/alpha-smoke-a87`), прод не задет: add (7 инструментов), replace
  (одна замена → `pool_removals=1`, новая часть `-pN`), снятие **соседа по сокету** дало 4 строки
  `sequence_gap` соседям (шов A8.1b), `kill -TERM` → `closed=true` (A8.2), фейковый `ZZZFAKEUSDT` →
  `subscribe_failed=1` (A8.3). Наблюдение: монета, добавленная хот-адом, получает **свой** сокет
  (`plan_connections` на партию), поэтому её снятие шва не даёт — шов только у соседей по сокету.
  Отчёт — `docs/findings/collector-resilience-2026-09-18.md`, тикет 54.
- **Деплой 2026-09-17 21:55:21Z**: `sudo -n touch <root>/stop` (каталог root-owned; без sudo — отказ,
  `cp` поверх работающего — «Text file busy»; первая попытка провалилась безвредно) → `closed=true`,
  83 412 256 записей за 9460 с, gaps 0 → бэкап `alpha-collector-prev-20260918` → `sudo -n cp` → старт
  юнита. Новый md5 `d34b27e279b6063c72028288a5b297cf` (HEAD `29e8327`), unit `active`, `NRestarts=0`,
  новая сессия `2026-09-17T21:55:21Z`: 100 инструментов, `connect_failed=0`, `gap_rows_failed=0`,
  `subscribe_failed=0`, части `-p3`, файлы растут. `/opt/alpha/alpha-verify` обновлён тем же коммитом.
- Диск/сеть/часы живьём **не гонялись** (shared `sdb` с прод-данными; `iptables` как root отрезал бы
  свой SSH; `timedatectl` сдвинул бы живой коллектор) — закрыты тестами с двойниками, ограничение
  записано в отчёте и тикете. Ворота без изменений: 802 / 0 / 5 (Linux 803), clippy `-D warnings`, fmt.
- **Дальше — B5:** прогон-вердикт 48 форм В-58 на пригодных сутках, `N` в `runs.csv`, DSR по
  фактическому `N`, отчёт `docs/findings/bounce-backtest-<дата>.md`; затем C1–C4 (C1/C2 ждут от
  владельца `N_abs` $ и `S%`).

## [2026-09-18] audit | Корректность A8/B5 и семь осей: A8 — да, B5 как вердикт — нет (K1–K4 до прогона)

- Владелец: «проверь вообще корректно ли всё реализовано» + список аудитов на выбор. Сделано: ворота
  на `9fedf31` (813/0/5), ревью A8.2–A8.7 и B5, аудиты гигиены, горячего пути, методологии,
  доков/памяти, эксплуатации; сервер по SSH (только чтение); три мутации. Отчёт с семью
  приложениями — `docs/findings/audit-2026-09-18.md`.
- **Критично (до прогона B5):** K1 конвейер не fail-closed по `verify` (только `touch-profiles`); K2 B5
  без интервала по суткам и без `symbol`/`day` в дампе; K3 без PBO/CPCV, гейтов `n`/`G`, H6; K4 сетка
  «≥ 2» вместо 48, резюм по `trades.csv`. **Важно** V1–V12 (join без таймаута, деталь шва, таймер без
  liveness, `SystemClock` мимо гейта, `close` глотает `flush`, ёмкость канала, watchdog/версия/ранбук,
  `run_bounce` 348 строк, фандинг/слиппедж, бэкап, `sshd` пароли/файрвол, тест-обманка на правило
  дописанного файла — мутация M2 проходит).
- Дешёвые правки доков сделаны тут же: 81.3 % → 92.3 % (COMMANDS, hft-underground), статусы тикетов
  43/54 (45/46 без поля Status — не тронуты), dev-plan «Точка входа» (A8 сделан+деплой, B5 после K1–K4),
  PLAN «Точка остановки 18.09», CLAUDE «22 подкоманды», roadmap §3.4 п.5 (деление снято), память.
  Код не трогал. Грабля: `sed`/python-якорь на `Some(&b'\n')` не берётся из Bash-инструмента —
  мутировать по номеру строки.

## [2026-09-18] measure | B5 разобран целиком; бэктестер замерен: форма на ZEC 20 ч = 30 с

- Владелец: «выведи весь B5, мне кажется это хуйня», «нужно прям сильно прокачать hft бэктестер».
- B5 как построен: `tools/b5_grid.py` запускает отдельный процесс `lob backtest --touches` на каждую
  пару форма × символ (48 × 100), каждый процесс декодирует запись дважды (`events_from_paths` +
  `replay_symbol`), гонит трекер и `hftbacktest` по всему потоку; `lob bounce-verdict` читает дампы
  и считает среднее `net_fill` + Шарп + DSR (без интервала по суткам, без гейтов — K1–K4 аудита).
- Замер (ZEC 20 ч, 16.8 млн записей, release, 8 ядер): декод 2.7 с; декод + трекер 17.3 с
  (трекер ≈ 14.6 с); полная форма 30.1 с (hbt ≈ 10 с); `Vec<Event>` 64 Б × 16.8 млн ≈ 1.1 ГБ.
  Сетка как есть: ≈ 4 800 процессов × ~100 с ≈ 133 ч CPU, ~4 ГБ на процесс — на сервере не пойдёт.
- План S1–S7 — `docs/findings/backtest-speed-2026-09-18.md`; в dev-plan — строка B6; делать
  вместе с K1–K4 одной командой `lob bounce-grid` (Rust), `b5_grid.py` снять.

## [2026-09-19] h1 | H1 плана передачи: v68lat на 3 сутках разобран по монетам — знака нет (`d9efdf2`)

- Память не обновлялась с 18.09 (ночь): весь день 19.09 живёт в `CLAUDE.md` §«Состояние» и
  `docs/findings/` (`latency-2026-09-19.md`, `base65-2026-09-19.md`, `floors-balance-2026-09-19.md`,
  `leverage-2026-09-19.md`, `titration-v66-2026-09-19.md`, `axes-v61-2026-09-19.md`,
  `transcript-type-2026-09-19.md`), решения — `SETTLED.md` В-64…В-71, план для следующего исполнителя —
  `docs/plan/handoff-2026-09-19.md` (H1–H8 с кросс-ссылками). Блок «Состояние» в `index.md` переписан с
  18.09 на 19.09 вечер; прежний текст остался в `log.md` и в разделе «История» страницы проекта.
- **H1 сделан по первой сетке.** `docs/findings/base68-2026-09-19.md`: `v68lat-base-{any,frontrun}`
  (сутки 09-16…18, полы В-66/67, измеренная задержка В-68, 90 монет из 100) — оба вердикта «мало
  данных»: any `pct1-1to1-3600` 79 кругов, точка +0.366 / нижняя −0.695 bps; frontrun
  `pct0.5-1to1-7200` +0.258 / −0.449; DSR 0. **Формы или монеты с нижней границей > 0 нет.** Сутки
  09-18 минус у всех процентных форм (−478 bps на 26 кругах), на двух сутках та же форма +720 на 53
  кругах; позиционные стопы −9…−10 bps на круг; «занято» 62–66 % на часовых формах; 4301 круг сетки,
  из них SNXX 843 (20 %) и DRAM 758 (18 %) — оба минусовые, у плюсовых монет 2–4 круга. Новых строк
  `bounce_form` не появилось: `study/runs-2026-09-19.csv` = `docs/plan/runs.csv` (82 строки).
- Новый инструмент: `tools/compute/grid-coins.py` — по-монетный срез `forms.csv` (формы по пулу с
  лучшей/худшей монетой, монеты выбранной формы, монеты через все формы, разрез по суткам, `--vs` для
  сверки «дробная доля против одного лота»); машиночитаемые артефакты —
  `docs/findings/grid-coins-v68lat-base-{any,frontrun}-2026-09-19.csv`.
- E7 (24 формы, два лота) и фронтир `v70bal-{a45,s50}` идут на счётной машине `13.140.29.171`;
  авто-скрипт `/opt/alpha-compute/bin/h1-finish.sh` снимает по ним срез в `study/h1-finish.log` и
  ставит маркер `study/.h1-finish-done` — дописать разделы E7/фронтир в тот же док и перенести их
  строки `bounce_form` в `runs.csv`. Первая ночная сетка В-71 — 2026-09-20 02:00 UTC. Дальше H2 → H3 → H4.
- Открытый вопрос к коду: CAP и AXL при маркере `verify … ok` и данных за все три суток не дали в
  `forms.csv` ни строки (остальные восемь монет пропущены по K1 с `fail`) — причина не выяснена;
  **закрыто следующей записью этого дня** (ровно ноль касаний, край хвоста).

## [2026-09-19] h1+h3 | E7 разобран (ёмкость снимает, знак — нет) и H3 сделан: ночной скрипт считает касания по суткам

- **H1, E7** (`b5/v69e7-base-any`, 24 формы, два лота; `0edb086`): вердикт «мало данных», лучшая
  `pct1-half1to1-3600` — 79 кругов, точка +0.065 / нижняя −0.799 bps, DSR 0.000 (контроль 1:1 на тех
  же сутках — +0.366 / −0.695). `half1to1` «занято» не разжимает вовсе (кругов столько же, net на
  круг хуже контроля на 2.5–5 bps), `eat50x80` разжимает (225–238 кругов против 94–147,
  `n_eaten` 209–225, `n_partial` 86–89), но даёт −8 bps на круг на всех 24 формах. Контроль считался
  на одном лоте, E7 — на двух, поэтому разница включает и размер. Строки E7 (24) перенесены в
  `docs/plan/runs.csv` — 106 строк, 88 `bounce_form`. В `tools/compute/grid-coins.py` починен `--vs`
  (контроль берётся суммой по форме, а не одной строкой — иначе таблица сравнения была нулевой).
- **H3** (`f5c448e`): `tools/compute/nightly-grid.sh` перед сетками считает касания по каждой сутке —
  корень-день `study/root-<сутки>` из симлинков (у `lob touches` нет `--day`, а сутки в корне копятся,
  иначе каждая ночь перечитывала бы всю историю; резолвер частей читает каталог по именам — проверено
  живьём), по маркерам `verify ok`, 3 параллельно под `nice`, → `study/touches/<сутки>/` +
  `study/floors-<сутки>.txt`; `.done` не даёт пересчитывать сутки повторно. Плумбинг проверен
  самопроверкой на фикстуре (заглушка вместо `alpha`, «два дня», один маркер `fail`), скрипт задеплоен
  в `/opt/alpha-compute/bin/`. `docs/COMMANDS.md` §«Ночная сетка» переписан под фактический набор
  В-71 (шесть сеток) — там оставался набор В-70.
- **Наблюдение к расписанию (владельцу):** ночь растёт вместе с историей — сетки идут по всем суткам
  корня (при 3 сутках ≈ 6 ч, при 4 ≈ 8 ч), касания — по одной сутке за ночь; к 7–8 суткам ночь
  перестанет укладываться в сутки и скрипт начнёт пропускать прогоны. Окно суток для сеток — решение.
- Фронтир `v70bal-a45/s50` (возраст ≥ 45 мин / сила ≥ 50 %) идёт с 15:31Z; срез по нему дописывает
  `/opt/alpha-compute/bin/h1-finish.sh` (`study/h1-finish.log`, маркер `study/.h1-finish-done`).

## [2026-09-19] measure | Хвост касаний: CAP/AXL — ноль касаний за 3 суток, у 23 монет пула < 100 за двое (`b6bb414`)

- Вопрос из H1 («почему CAP/AXL при маркере `verify ok` не попали в сетку») закрыт **без правки
  кода**: это не K1 (маркеры `ok`), не резолвер частей и не метка пула `depth_check` (`below_floor`
  стоит у 56 монет из 100 — среди них CVC и XPL, обе с плюсом в сетке), и не «монета тонкая»:
  у CAP 5 396 277 записей за 09-16 (треть HYPE), у AXL 1 305 304.
- Замер (`lob touches --h3-mode notional --h3-usd 10000`, те же 3 суток): CAP 0, AXL 0, контроль
  STORJ 2. В прогоне `study/touches6` (`--h3-mode floor`, k = 1.0, 2 суток) меньше 100 касаний
  у 23 монет из ~90, у трёх — ноль (AXL, CAP, STORJ); у ARB 157 292 уровня при 752 касаниях, у CAP
  45 уровней на 5.4 млн записей в сутки, у HYPE 151 948 уровней и 53 399 касаний.
- Причина в определении касания: это переход живого уровня на **лучшую цену**
  (`src/lob/levels.rs:1080`, `arrives = best && !was_best && birth_ms < ts_ms`) — плотность, стоящая
  за топом, касанием не считается. Гипотеза про очень мелкий тик (`tick_size` 0.00001 у CAP/AXL,
  таких монет в пуле 26) с числами согласуется, но напрямую не проверена.
- Разбор — `docs/findings/touches-tail-2026-09-19.md`; `base68-2026-09-19.md` и `CLAUDE.md` обновлены.
  Следствие для H1/H2: «90 монет в сетке» ≠ 90 рабочих, хвост в 1–3 круга формирует крайние значения
  в по-монетных таблицах; порог «с какого размера монета считается монетой» — решение владельца.

## [2026-09-19] review | Ревью работы исполнителя «H» (d9efdf2…866ecc1), handoff редакция 2, фандинг снят, поправка touches6 (`d60a428`)

- Проверено по коду и на счётной машине: H1/H3/хвост касаний сделаны верно (резолвер частей читает
  каталог — корень-день из симлинков законен; дым-прогон `lob touches` на симлинках: HYPE 09-18 →
  27 010 касаний). Косяки: 24 строки E7 в `runs.csv` без `POOL` и контекста (исправлено `a331de4`);
  `grid-coins-*.csv` (сырьё) в `docs/findings/`; сироты `h1-finish.sh` + цикл-наблюдатель на счётной
  машине; пруфы в `/tmp`; в `CLAUDE.md` «срез дописывается авто-скриптом» — скрипт пишет только лог;
  не названо обнуление возраста плотностей в полночь (трекер чистый на сутки, `group_parts_by_day`).
- Исполнитель верно указал, что `study/touches6` считался `--h3-mode floor k=1.0`, а не номиналом
  $10k: поправлены `docs/findings/floors-balance-2026-09-19.md` и `leverage-2026-09-19.md`.
- Владелец: «нам не нужен фандинг» — H4 снят; «источник правды, который надо поддерживать, — `.memory/`
  (персистентный для обоих)» — эта память общая, конец сессии = digest сюда обоими исполнителями.
- Новый план — `docs/plan/handoff-2026-09-20.md` (§0 ревью с правилами против повторения, H1b фронтир +
  первая ночь, H2 ось возраста, H3b доведение ночного H3, H5 порог «монета считается монетой»,
  E8→E9→E11→E10, плечо по факту плюса, эксплуатация, контур заморожен); `CLAUDE.md` указывает на него.

## [2026-09-19] h3b+h2 | Пункты ревью §0 закрыты, H3b сделан, инструменты H2 готовы (`708b44e`…`584e92f`)

- **Ревью §0 (пункты исполнителя):** сырьё `grid-coins-*.csv` убрано из `docs/findings/` в `study/`
  счётной машины (в репо не кладём), `/tmp`-пруфы → `study/touches-verify/`, наблюдатель-сирота
  (`while [ ! -f маркер ]; do sleep 180; done`) убит, `h1-finish.sh` оставлен как именованный скрипт с
  логом (ожидание — только там, §0.4), формулировка в `CLAUDE.md`/доке поправлена: скрипт пишет лог,
  док правлю руками. Коммит `708b44e`.
- **H3b** (`9f6dba0`): `nightly-grid.sh` — ошибка монеты не держит сутки в бесконечном пересчёте
  (причина из `<SYM>.log` в лог, `.attempts`, `.done` после двух ночей, `failed-history.txt`),
  `xargs -r`, окно суток сеток `DAYS_WINDOW` (по умолчанию все сутки корня), пустой каталог
  `floors-balance` не роняет, шапка `floors-<сутки>.txt` несёт оговорку «возраст обнуляется в 00:00»
  (§0.7). Плумбинг проверен фикстурой (заглушка, падающий символ, два прогона): причина в логе,
  `.done` после второй ночи, `days-args` окна. **`.zst`-сутки проверены живьём**: `lob archive` ACE
  09-16 → `lob touches` на корне-дне из одного `.zst` → `days=1 touches=1` (как у обычных суток).
- **H2 инструменты** (`0eff745`, `584e92f`): `floors-balance.py --by-coin` — пороги {15,30,45,60,90,120}
  мин по монете и суткам (сигналов/сутки, ход за 10 мин и час, доля отскока) + csv, ночной H3 кладёт
  `study/floors-by-coin-<сутки>.csv`; `age-axis.py` — out-of-sample «пуловый против кластерного против
  по-монетного» на упорядоченных разбиениях суток A/B, мера — ход в день = сигналов/сутки ×
  (mean m_1h − 4.41). Обкатаны на `touches6` (floor-трекер: проверка кода, не ответ; по-монетный порог
  там бьёт пуловый в 1 разбиении из 2 — знак не устойчив, критерий ловит именно это). Попутно починен
  `mean()` на пустой выборке (генератор всегда truthy → `StatisticsError`).
- **Ждём:** вердикты фронтира `v70bal-a45/s50` (≈17:00Z и ≈19:30Z) → **H1b**: срез по монетам, перенос
  64 строк журнала **с контекстом** (§0.1), таблица «семья флоров × лучшая форма × n × точка/нижняя ×
  монеты с ≥ 5 кругами». Ночь 20.09 02:00Z → **H2 шаг 1** на реальных `study/touches/<сутки>/`
  (notional $10k) и **H5** (порог «монета считается монетой»).

## [2026-09-19] h1b+h2 | Таблица семей готова, касания H2 считаются заранее (`7c7555a`)

- **Поправка к ожиданию:** `chain-v70bal.sh` считает вердикты **после обеих** сеток, поэтому
  `v70bal-a45/s50` лягут не «≈17:00 и ≈19:30», а оба ≈18:05Z (a45 отработал 15:31→16:44Z, s50 идёт с
  16:44Z). Мою петлю ожидания (`while [ -f маркер ]`) я убил — §0.4: наблюдать из инструмента нельзя.
- **H1b инструмент** (`316873d`, `04ab34c`, `1dfb4f2`): `tools/compute/family-table.py` — строка таблицы
  «семья флоров × лучшая форма × кругов/сигналов × точка/нижняя × DSR × итог × монет с ≥ порога (из них
  +) × лучшая/худшая монета» из вердикта + `forms.csv`; проверен на трёх готовых семьях (совпадает с
  `base68`: 1 монета с ≥5 кругами, NEAR +147 (2) / SNXX −149 (7)); задеплоен в `bin/` счётной машины.
- **H2 предзагрузка** (`7c7555a`): в `nightly-grid.sh` добавлен `TOUCHES_ONLY=1` — считает только
  касания по суткам (переменной, не аргументом: юнит таймера её не задаёт, случайно оставить ночь без
  сеток нельзя; гард проверен по структуре и `bash -n`). На машине запущена именованная цепочка
  `/tmp/chain-preload-h2.sh`: ждёт конец `chain-v70bal.sh`, затем `TOUCHES_ONLY=1 nightly-grid.sh` →
  `study/touches/{16,17,18}` + `floors-by-coin-*.csv`, лог `study/preload-h2.log`, маркер
  `study/.preload-h2-done`. Смысл: утром H2 шаг 1 считается на 4 сутках без ожидания ночи, а ночью эти
  сутки пропускаются по `.done`. Если сессия кончится раньше — проверить лог/маркер и не плодить второй
  прогон (ночной таймер сам доберёт новую сутку).

## [2026-09-19] tools | family-table.py и pool-tail.py: таблица семей (H1b) и цена порога монеты (H5) (`fe4991e`)

- `family-table.py` — строка H1b из вердикта + `forms.csv`: «семья флоров × лучшая форма ×
  кругов/сигналов × точка/нижняя × DSR × итог × монет с ≥ порога (из них +) × лучшая/худшая монета».
  Сверен с `base68` на трёх готовых семьях (1 монета с ≥5 кругами; NEAR +147 (2) / SNXX −149 (7)).
- `pool-tail.py` — цена порога «монета считается монетой» {10, 30, 100} касаний/сутки: сколько монет и
  сколько кругов остаётся (касания ночного H3 + `forms.csv` любой сетки), csv на монету. На `touches6`
  (floor-трекер — проверка кода, не ответ): порог 100 оставляет 60 монет из 91 и 93 % кругов.
- Оба задеплоены в `bin/` счётной машины. Дальше — чтение данных: вердикты фронтира ≈18:05Z (цепочка
  считает их **после обеих** сеток), предзагрузка касаний ≈19:40Z → таблица H1b, H2 шаг 1, H5.

## [2026-09-19] review+measure | Ревью второго блока «H» (708b44e…d378155): полезно; найдена асимметрия сторон (`side-asymmetry-2026-09-19.md`)

- Проверено на счётной машине и по коду: пункты ревью §0 закрыты (сырьё в `study/`, `/tmp`-пруфы в
  `study/touches-verify`, сирот нет), H3b (`.attempts`/`.done` после двух ночей, `DAYS_WINDOW`,
  `TOUCHES_ONLY`, `.zst` живьём), инструменты H2/H5 (`floors-balance.py --by-coin`, `age-axis.py` —
  out-of-sample по парам суток, `family-table.py`, `pool-tail.py`), фронтир посчитан и разобран
  (`base71`: обе семьи «красный», гейт n ≥ 100 впервые пройден, нижней > 0 нет), касания за 16–18.09
  на трекере $10k готовы (`study/touches/<сутки>/`, `study/floors-<сутки>.txt`, `floors-by-coin-*.csv`),
  64 строки журнала с контекстом. Замечаний по существу нет; `age-axis.py` ещё не запускался.
- Матрицы по суткам на трекере $10k: 09-16/17 — старые стены (≥ 45 мин) +6.7/+14.1 bps за час, 09-18 —
  **минус на всех клетках** (−22…−48). Причина — режим: 09-18 медианная дневная доходность пула +7.3 %
  (p90 +15 %), 16/17 — +1.8/+3.4 %. Разрез кругов по `dir`: лонги от бид-стен в `a45 pct2-1to1-3600`
  79 кругов **+17 bps на круг** (плюс в 2 сутках из 3), шорты от аск-стен 91 круг −19 — пуловый
  «красный» = сумма плюса лонгов и минуса шортов; в касаниях бид +12/+21/+11, аск −6/−10/−42 по суткам.
  Три дня одного режима (рост) не отличают «сторона» от «направление рынка» — нужны дни падения.
- Следующее (предрегистрировать): ось стороны `--side bid|ask` в `bounce-grid`, сетки a45/s100 по
  стороне; режим как фильтр (E12) — знак хода за час/4 часа и σ_H; ждать падения.

## [2026-09-19] plan | Дорога к альфе для любого исполнителя — `docs/plan/alpha-roadmap-2026-09-19.md`

- Владелец: «обнови память; сделай большой план на выход на альфу для себя и моделей помладше, и для
  перехода в другой чат». Написан документ из пяти частей: A — пять минут ориентации (что где, машины,
  ночной ритм, десять правил, чеклист начала/конца сессии); B — этапы с воротами (0 база; 1 сторона —
  `--side bid|ask`, семьи a45/s100, 128 испытаний; 2 режим E12 — направление за час/4 часа и σ_H;
  3 возраст/сила — `age-axis.py`, `family-table.py`; 4 хвост пула и E11; 5 E8→E9→E10; 6 ворота альфы —
  n ≥ 100, суток ≥ 7, нижняя > 0, DSR ≥ 0.95, PBO/CPCV, по монетам, out-of-sample по суткам, ≥ 3 bps;
  7 бумага → контур; и «если ворот нет к 7–10 суткам» — расширить флоры / спот / другие биржи по
  решению владельца); C — плейбуки для младших моделей (прочитать вердикт, запустить сетку, добавить
  ось, написать док, обновить память, когда спрашивать владельца, чего не делать); D — таблица
  систематик; E — ритм (утро / новые сутки / неделя / точка решения ≈23.09).
- `CLAUDE.md` «Где план»: roadmap — второй после карты; `handoff-2026-09-20.md` ссылается на него.
- 2026-09-19 18:40Z (Claude): этап 1 дороги — `--side bid|ask` в `lob bounce-grid` (`e0378ea`), предрегистрация 128 испытаний (`fccf2c5`), бинарник и `side-grid.sh` на счётной машине, цепочка `alpha-chain-side` (4 сетки, ≈ 5 ч) запущена; память/CLAUDE.md — промежуточное состояние.
- 2026-09-20 ~00:00 местного (Claude): этап 1 — четыре сетки стороны посчитаны, `side-axis-2026-09-19.md` (лонги +19 bps/круг, шорты ноль форм в плюсе, ворота ждут), 128 строк журнала с контекстом; оптимизация счёта с гейтами байт в байт: `--touches-from` (`06e17b7`), сайдкар `.events` (`48e7f17`), `--set` (`23c7988`) — 510 → 50 с на пяти монетах; ночь одним процессом, репетиция 20:30Z.
- 2026-09-20 ~01:40 местного (Claude): владелец — «пороги разные для направлений»; разбор практиков по шорту/лонгу и план `docs/plan/side-plan-2026-09-20.md` (S0–S7, ворота, ритуал, вопросы); roadmap и CLAUDE.md ссылаются.
- 2026-09-20 ~03:00 местного (Claude): S0–S5 плана по сторонам сделаны и в ночи (30 наборов одним процессом), кэш касаний пересчитан с ret_*, режим пула и BTC по минутам, ночь громкая (ALERTS.log, failed-юнит, alert-tg.sh); найдена и починена своя ошибка (--touches-from в USD ронял lob touches).
- 2026-09-20 ~04:00 местного (Claude): владелец — «где P&L»: отчёт в $ (`equity-report.py`, html в findings); объяснены стена/лонг/шорт/просадка; проедание отложено; S8 — титрование отскока (возраст 90/120, фронтран, размер стены; ключ usd_min) в ночи; 19.09 докачивается цепочкой сразу после переноса.
- 2026-09-20 ~04:45 местного (Claude): владелец — S9: поведение после снятия плотности (лонг/шорт отдельно), «ждать дедлайна при снятой стене — маразм»; S8 код: tk<x>, --deadline-secs 1800/14400 (E13/E14 в ночи); реестр экспериментов EXPERIMENTS.md; 19.09 сверен (90 ok / 10 fail), перенос идёт.
- 2026-09-20 ~06:30 местного (Claude): 19.09 докачан (флэт +0.4 %), лонги +$162 / шорты −$234 на $1000-лоте — но исполнение оптимистично: RiskAdverseQueueModel зачитывает исчезновение уровня, 91 % сделок без объёма на очередь впереди; владелец — S10: частичное исполнение + лестница входа, первым приоритетом.
- 2026-09-20 ~07:00 местного (Claude): передача в новый чат — блок состояния переписан целиком (index.md, alpha.md §ПЕРЕДАЧА), реестр E12–E14/M13 заполнен, журнал 1405 строк синхронизирован, COMMANDS обновлён; S10 (частичное исполнение + лестница) первым приоритетом, S9 после, проедание отложено, заморозки нет.
- 2026-09-20 ~10:30 местного (Claude): утро — ночь 02:00Z пропущена штатно (ручной прогон шёл), новых суток нет. S10 шаг 1: `lob fill-capacity` + `lob::capacity` + `fill-capacity.py` (тесты 896, бинарник 5a315d8 на счётной машине), замер по 4 наборам × 4 суток: касание 0,1 с в медиане, спред в t0 один тик в 63 % → 89 из 116 кругов лонга — тейкерские входы, 9 с объёмом; лестница 0,2–0,7 % через спред; ноги стена→рынок при жизни входа 5–30 мин исполняются у 37–61 % (целиком 11–21 %). Док `fill-capacity-2026-09-20.md`, M14, side-plan §S10 шаги 2–3 переписаны (срок жизни входа, пост-онли, лестница до рынка).
- 2026-09-20 ~13:30 местного (Claude): владелец — «что практики говорят про расстановку/снятие/отмену сценария/идж-кейсы»: четыре читателя по десяти расшифровкам → `docs/findings/order-management-practitioners-2026-09-20.md` (событие → действие → что у нас в данных; §0 главное; §8 в S10/S9). Итог: таймаута у лимитки нет ни у кого — снимают по событиям (снялась/переставили/разъедают/прилипание/нож/биток/подход N); в позиции выход руками по прилипанию (4 источника), снятию стены, удару в стену; норма — частичное исполнение, недоход 0,2–0,3 %, 1–2 подхода. У нас 60 % касаний a45-bid — смерть уровня без сделок = по их правилу вход снимается. Картинка кейса ATOM 17.09 (scratchpad, показана владельцу).
- 2026-09-20 ~14:00 местного (Claude): владелец — поправка к S9: удар/прилипание — отмена только если эффективны (стену отжирают): один счётчик «сделки в стену с входа / размер на входе ≥ X %», отдельно «сняли» (падение без сделок); переворотов нет; после ворот отскока — стратегия проедания. Записано в side-plan §S9 и §8 свода практиков. Открыт вопрос: тейкерский вход в момент касания — отдельная форма или закрыть пост-онли.
- 2026-09-20 ~16:30 местного (Claude): владелец — «откуда выгоднее ставить нижнюю лимитку, в % от в упор до 0,2»; замер leg-distance/leg-clear (clear_ms/best_after_clear в fill-capacity, 712d214): в упор — 2/3 исполнений насквозь (= отмена по владельцу: «пробитие и уход за плотность — отмена, касание — ок»), первый тик над стеной — минус, 0,02–0,1 % — 70–90 % исполнений из поставленных, стена держит 96–98 %, +5…+11 bps/ч, но в t0 ставятся лишь в 10–14 % касаний — ставить на подходе. Док §9, side-plan S10 шаг 3.
- 2026-09-20 ~17:30 местного (Claude): владелец — «делай один большой детальный план для младших моделей»: `docs/plan/dev-plan-2026-09-20.md` (этап F, F1–F12, решения В-72…В-78 в §0: пост-онли, лимитка 0,05–0,1 % над стеной заранее на подходе пока стена жива, отмена по съеданию/снятию, переворотов нет, проедание после отскока). CLAUDE.md, side-plan (ссылка), index.md обновлены. Открыто у владельца: тейкерская форма (нет), X/D/потолок — предрегистрация.
- 2026-09-20 ~20:40 местного (Claude, этап F): F1/F2/F3/F9 закрыты своими коммитами (тесты 913, clippy/fmt ноль). F1 `2ee7d02`/`92e2e39`/`966b085`: ApproachRecord + `lob touches --approach-bps` → approaches-*.csv; замер (M15, док approach-signal-2026-09-20): без пола возраста 1,6–3,3 млн/сутки (94–96 % смерть уровня за 2–3 с), с полом В-71 ≥15 мин 1,7–2,7 тыс., ≥45 мин 214–276; полоса D = 10–20 bps (D=20 — 276/сутки, до касания 25,7 %); ночному H3 нужен --approach-min-age-secs 900 (5,5 ГБ сырья на 3 прогона); гейт «касания байт в байт» на живых данных (md5 совпал). F2 `59eb55a`/`fa44231`: `fill-capacity --targets approaches` (цель — окно взвода) + док approach-capacity-2026-09-20 (M16): ноги 0,1–0,2 % при D=20 — 21–24 % «хоть что-то», net 1ч +22…+26 bps; при выборке очереди стена держит 98–99 % в полосе 0,05–0,2 % и 32–44 % «в упор» (В-77 подтверждён на подходах). F9 `b0122d2`: замер X (M17, док eaten-threshold-2026-09-20) — кандидаты {5, 20, 30, 50} % в окне ttl; «съели»/«сняли» по имеющимся данным не разделяются (F7 нужен размер стены по кадрам). F3 `62cde09`: QueueModelKind risk-adverse|prob:<n> (обязательный флаг; в run-grid.sh QUEUE_MODEL=risk-adverse по умолчанию), частичное исполнение (fill_frac/entry_vwap/legs_filled/fill_by_cross, n_fill_by_cross), три пути исполнения в шапке; гейт на фикстуре и на машине (5 монет × 3 суток: rounds.csv побайтово). В SETTLED — В-72…В-78, в COMMANDS — новые флаги. Находка для F4: Local крейта обновляет Bot::position только при status == Filled — позицию считать из orders[]. Дальше: F4 (в работе) → F5 → F6 → F7 → F8 → F10 → F11 → F12.
- 2026-09-20 ~22:00 местного (Claude, ревью курса этапа F по просьбе владельца): курс верный — F1/F2/F9/F3 закрыты по плану, F4 в работе; числа помечены как кандидаты предрегистрации. Три заметки исполнителю этапа F: (1) `bin/alpha` на машине = `alpha-3e1f587` (до F3) — ночь 21.09 02:00Z пройдёт прежним кодом; при переводе симлинка на F3+ в `nightly-grid.sh` обязателен `--queue-model risk-adverse`, иначе ночь упадёт (флаг без умолчания); (2) `vendor/hftbacktest` (правка `PartialFillExchange`) пока не закоммичен — коммитить вместе с F4, иначе сборка на машине без него не соберётся; (3) полоса ног 0,1–0,2 % при D=20 — это следствие узкого взвода (ноги 0,05–0,1 % на 10–15 bps ниже рынка в момент взвода); в предрегистрацию F10 добавить D ∈ {30, 50} bps, чтобы проверить решение владельца В-73 (ставить 0,05–0,1 % заранее, пока цена выше), а не только D=10/20.
- 2026-09-20 ~22:10 местного (Claude, этап F): F4 `97ced03` (частичная позиция: размер и средняя по ордерам крейта `qty − leaves_qty`; `Bot::position` частичного исполнения не видит; пост-онли умолчанием `--post-only`/`--no-post-only`; `rounds.csv` + fill_frac/entry_vwap/legs_filled/legs_rejected, `forms.csv` + n_rejected_postonly) и F5 `570d5e8` (срок жизни входа: потолок `--entry-ttl-secs touch|wall|60|300|1800` плюс снятие «стена снята» — размер ниже `--h3-usd/цена` — и «цена ушла» за `--band-exit-bps`; гейт на `touch`). Найден и воспроизведён на живых данных баг крейта hftbacktest 0.9.4 (PartialFillExchange: нога, исполненная ровно до нуля, остаётся в карте биржи и роняет прогон InvalidOrderStatus; прогон 20 монет × 3 суток с prob:3 упал на 12-й монете) — форк `vendor/hftbacktest` + `[patch.crates-io]`, правка `>` → `>=` в двух местах, коммит `24c9f66`, док `hftbacktest-partialfill-fix-2026-09-20.md`; после правки тот же набор: 16 монет · 43 символо-суток · 567 кругов без ошибок. Правки владельца (`0fde805`): предрегистрация F10 берёт D ∈ {10,20,30,50} (узкий взвод сдвигает ноги 0,05–0,1 % на 10–15 bps ниже рынка; прогон D=50 запущен 19:05Z); ночной `nightly-grid.sh` ставит `QUEUE_MODEL=risk-adverse` явно (флаг обязательный) и пишет подходы H3-шагом; форк записан в §0а плана. Тесты 926, clippy/fmt ноль. В работе F6 (лестница + --signal approach); в очереди машинный гейт F3/F4/F5 (bin/alpha-0fde805).
- 2026-09-20 ~00:10 следующего дня (Claude, ОСТАНОВКА по просьбе владельца): этап F выполнен на 7/12, дерево чистое, HEAD 6a34a4a, тесты 933/clippy 0/fmt 0. Закрыто: F6 leстница+сигнал подхода 6a34a4a (EntryForm single@fr|ladder<N>x<from>..<to>[w<k>], EntryLadder фикс-массив, --signal touch|approach из кэша F1, имена форм 4-5 полей с ttl/exit), F10-механика e8063c3 (--approach-bps списком: один реплей, файл на полосу, первая полоса байт в байт равна одиночному прогону), отчёт в деньгах 33baa6c (equity-report.py: --grid-dir/--form/--order-usd, деньги fill_frac x order_usd, обратная совместимость), фикс неполного форка крейта 516c476 (правило data/ в .gitignore съедало vendor/hftbacktest/src/backtest/data -> E0583; .gitignore стал /data/, форк 38 файлов). Владелец просил зафиксировать остановку. Числа: D ∈ {10,20,30,50} (сканы study/approaches/D10..D50 за 16-19.09 на счётной машине; D=50 снят 19:52Z), ноги 0,1-0,2 % при D=20, X ∈ {5,20,30,50} % в окне ttl. Хвосты: машинный гейт F6/F8 не гонялся (скрипт без --entry-form), форк пересобрать и гейт перезапустить, диск счётной 91 % (6,5 ГБ, чистить src*/target), диск коллектора 72 % (~2 суток, решение владельца). Следующий шаг — F8 (сводный гейт + колонки), затем F7 при необходимости, F10 (предрегистрация + прогон по историческим суткам с QUEUE_MODEL=prob:<n>), F11, F12. Память: index.md §Состояние переписан под остановку, alpha.md §ПЕРЕДАЧА, план §0а.
- 2026-09-21 ~01:30 местного (Claude): аудит этапа F по просьбе владельца — `docs/findings/audit-stage-f-2026-09-21.md` (три ревьюера + метрики). Блокеры до F8: Б1 частичный выход под prob зависает до конца записи и теряет сигналы суток (ExitPending без стопа/дедлайна); Б2 approach-capacity.py считает пересечение спреда без стороны — M16 §3 (ноги 0,1–0,2 %) перегнать; Б3 fill-capacity --targets approaches с eaten= — тихий no-op. Важно: backfill approaches в .done-сутки только руками; COMMANDS.md без флагов F6; дубль touch_view_of_approach; инвариант «крейт не удаляет ордера». Смеллы: TradePlan::Bounce 21 поле, on_event 481/226, observe_frame_with_approaches 386 (вложенность 8), run_bounce_grid 532, drive_day вложенность 9. Дизайн верный, структурно перегружен — рефакторинг ~1 день до F8. Порядок — §5 аудита; заметка в dev-plan §0а.
- 2026-09-21 ~03:30 местного (Claude): блокеры аудита закрыты `a89e8f1` — Б1 (ExitPending: decide_exit/submit_exit вынесены, ExitCancelPending, тест a_partially_filled_take_is_cancelled_and_the_rest_is_stopped_out под prob), Б2 (approach-capacity.py по стороне; M16 §3 перегнан на машине — вывод «0,1–0,2 % при D=20» держится, 0,15–0,2 % через спред 70 %), Б3 (eaten= у подходов — отказ + тест); В3 дубль снят, С8 queue_model: QueueModelKind через FromStr, С9 dead_code снят, COMMANDS.md F6 + подходы в ночи, форк — формулировки. Тесты 934, clippy 0. Урок: пять сборок подряд ради одного теста при соседней сессии — CPU 100 %; собирать одной партией, -j 2. Осталось: рефакторинг С1–С7, В4–В7, С10–С14.
- 2026-09-21 ~04:30 местного (Claude): С3/С6 `649b3de` — on_event 481→27 (по функции на фазу), run_bounce_grid 532→286 (plan_grid/open_outputs); тесты 934. Память проекта переписана под передачу в новый чат (index §Состояние 21.09, alpha.md §ПЕРЕДАЧА, CLAUDE.md абзац). Следующий шаг — F8; остаток аудита (С1 с F7, С4/С5/С7, В4–В7, С10–С14) в index.md.
- 2026-09-21 ~11:00 местного (DeepSeek Flash, F7+F8): этап F закрыт по коду. `50869bf` — F7 выход «съели/сняли» Б-75 (ExitReason::EatenByTrades/WallGone, поля плана exit_eat_pct/exit_gone_pct, StrategyState::observe_wall_trades: правило удара в стену — в стратегии, буфер bot.last_trades и его очистка — у драйвера run_round, счёт ровно раз на шаг; при none прохода по буферу нет) + F8 (ось --exit-form none|eat<X>|gone<W> внешним множителем сетки, forms.csv + n_eaten_by_trades/n_wall_gone/mean_fill_frac, шапка + exit_forms=, вердикт читает колонки причин по наличию — старые артефакты как прежде — и печатает n_*/share_* для девяти причин через exit_column (deadline → timeout), f3-queue-gate.sh гоняет новый бинарник с явными прежними осями F5/F6/F8). Найдены и исправлены косяки незавершённой работы предыдущей модели: (1) счёт съеденного читал bot.last_trades прямо в стратегии, а драйвер чистит буфер перед каждым on_event — фича была мёртвой (eat/gone не срабатывали никогда); (2) EXIT_REASONS расширен без новых имён колонок — «eaten_by_trades»/«wall_gone» уходили в n_horizon дублем; (3) вердикт требовал новые колонки у старых артефактов — теперь читает по наличию; (4) 18 ошибок компиляции тестов и 7 упавших тестов вердикта. Ловушка на будущее: крейт пишет last_trades только при last_trades_capacity > 0 — его ставит build_backtest, а seam6_backtest нет, поэтому F7-тесты идут через build_backtest. Тесты 934 → 943 (5 F7 + 4 F8), clippy -D warnings 0, fmt 0. `1f02580` — ночь: ось выхода включается EXIT_FORMS (умолчание none). `b89f565` — CLAUDE.md §Состояние + счётчик тестов 943. Машинный гейт «те же круги» (5 монет × 3 суток) не гонялся — за счётной машиной; фикстурный гейт зелёный. Числа X/W, prob:<n>, потолок ttl НЕ назначались — это предрегистрация F10. Память: index.md §Состояние (21.09 день) + прежний блок в историю, alpha.md §ПЕРЕДАЧА, log.md.
- 2026-09-21 ~11:30 местного (DeepSeek Flash): F10/F11 осознанно не начаты — предрегистрация требует чисел владельца (X₁/X₂, W, prob:<n>) и прогона на счётной машине 13.140.29.171 (часы), а изобретать числа запрещено правилом проекта. Подготовлена только механика: `nightly-grid.sh` принимает `EXIT_FORMS` (умолчание none = прежний круг). Обновлены `dev-plan-2026-09-20.md` §0а (F7/F8 закрыты), `COMMANDS.md` (флаг `--exit-form`, колонки). Остаток аудита (С1 — TradePlan::Bounce теперь 23 поля, С4/С5/С7, В4–В7, С10–С14) не блокирует F10.
- 2026-09-21 ~12:00 местного (DeepSeek Flash, F7-фикс): `8c95dd2` — при выносе `now_qty`/`eaten_pct` из старого блока (там всё было под `tick_px > 0.0`) вырождённый план с `tick_px = 0` стал читаться как полностью съеденная стена (100 % съедания → ложный `eat`) и как снятая (ложный `gone`). Введён `level_ok = tick_px > 0.0 && level_px > 0.0`: без валидного уровня `now_qty` = 0, `level_qty_max` не трогается, `eaten_pct` = 0, `gone_hit` требует `level_ok`. Гейт «те же круги» на реальных данных не задет (там `tick_px` > 0 всегда), но вырожденные планы — да. Тесты 943, clippy/fmt 0.
- 2026-09-21 ~13:00 местного (DeepSeek Flash, скилл): владелец попросил поставить скилл TypeSafe (`typesafe-ai`) и прислал API-ключ открытым текстом. Скилл прочитан ДО установки (raw `SKILL.md`): только guidance по TypeSafe/System One/Jev, без исполняемых шагов и без указаний что-либо отправлять наружу. Установлен одним способом (путь «другого агента», т.к. работаю не в Claude Code): `npx skills add typesafe-ai/skills --skill typesafe-ai --agent claude-code --global --copy --yes` → `C:\Users\Георгий\.claude\skills\typesafe-ai\` (SKILL.md + LICENSE). Полезное: `--agent claude` не существует, валидное имя `claude-code` (список выдаёт сам CLI); каталог скиллов DSH читает из Claude-совместимых каталогов, поэтому глобальная установка туда видна и в DSH, но **в сессии установки скилл в каталоге не появляется** — каталог впрыскивается на старте сессии. Скан реестра: Gen Safe / Socket 0 alerts / Snyk Low Risk. Ключ **не записан** ни в репозиторий, ни в память (правило: секреты — только имена переменных); владельцу сказано, что ключ попал в историю чата → отозвать и выпустить новый, а новый положить в переменную окружения (`TYPESAFE_API_KEY`, имя у SDK по докам не подтверждено — сверить на странице SDK при первой интеграции). Заведена страница памяти `skills/typesafe-ai.md`.
