# Memory Index — alpha

Этот файл **инжектится в каждую сессию автоматически** — здесь состояние проекта, не каталог.
Бюджет boot-блока 8000 символов ⇒ держать ~2,3 КБ, подробности уводить в страницы.

## Состояние (2026-09-17)

- `C:\visual projects\alpha` — Rust-бот по плотностям стакана Bybit: олвейс-он коллектор пула,
  разметка крупных уровней, профили по осям, вердикт бэктестом с моделью очереди.
- **План работ 17.09**: `docs/plan/dev-plan-2026-09-17.md` — этапы **A** база и коллектор (срок ≈ 22.09),
  **B** база бэктестера, **C** сигнал-отскоки, **D** накрутка; источники — четыре отчёта
  `docs/findings/*-2026-09-17.md` (спека отскока, транскрипции, аудит коллектора, роадмап).
  Идём по A: **этап A закрыт 17.09** (A1 архив не трогает открытую часть, A2 ночная сверка таймером,
  A3 видимость потерь K1/V4/V5/V6/V12, A4 мягкий хвостовой кадр у читателей, A5 документы+линты,
  A6 = В-57). **Дальше — этап B (база бэктестера: B1 предрегистрация → B2 вход от фронтрана и три
  стопа → B3 горизонты до 2 ч → B4 досрочный выход → B5 прогон-вердикт).**
- **Прод**: OVH `139.99.91.22` (Ubuntu 26.04, 2 vCPU / 3.7 ГБ, ключ `id_rsa`; данные — `sdb` 50 ГБ →
  `/opt/alpha`), юнит `alpha-collector.service` активен, `NRestarts=0`, стоп файлом `stop` (В-41).
  Пул — **100 монет**. Диск 17.09 08:49Z: занято 11 ГБ, свободно **36 ГБ** (≈6.5 ГБ/сутки ⇒ ≈5.5 суток);
  **диском владелец управляет вручную — сторожей и порогов в коде нет** (его решение 17.09).
- **`verify` в проде идёт таймером (A2)**: `alpha-verify.timer` 00:20 UTC → `tools/verify-day.sh` →
  hardlink-каталог `/opt/alpha/verify/<день>` → маркеры в `/opt/alpha/root`, сводка в
  `/opt/alpha/verify/<день>.log`. Первый прогон за **09-16: 94 ok / 6 fail** (LSK, AKE, BR, BTW, XMR,
  AVAAI), 100 маркеров в корне; 100 монет ≈ 9 мин. **Вердикт В-56**: целостность ровно ноль, «цена не
  держалась» — доля меньше 0.1 % сделок (до правки вердикт рубил на любом ненулевом и молча выкидывал
  14 монет из 100).
- **Два потока (T45)**: `.50` в основной файл, `.200` в `deep/` — копятся **сознательно** (не стриминг,
  читателя пока нет); **архив (T46)** `--level 19` → 81.3 %, вручную, только закрытых суток.
- **Открыто у владельца:** `fsync` (решения не было, в код ничего не добавлено — В-57); политика V4
  (ночной прогон за 09-17 пометит `fail` все 100 монет из-за одного переподключения в 01:51Z).
- **Коллектор на проде — старый бинарник** (09-15 22:47): K1/V5/V6/V12 заработают после пересборки и
  перезапуска `alpha-collector`; `/opt/alpha/alpha-verify` и скрипт ночной сверки уже обновлены.
- **Сборка:** toolchain и сборка — на боевом, на системном диске (`sda`, `/home/ubuntu`);
  бинарник для ручных команд — `/opt/alpha/alpha-verify`, коллектор — `/opt/alpha/alpha-collector`;
  прежний сервер `13.140.29.171` закрыт (В-54).
- **Git**: `master` = `dev/wave-4-record-verify`; 16.09 волна 4 слита fast-forward; 17.09 —
  В-56 (`438d07d`), A1 (`42ded21`), A2 (`8f81912`), K1 (`6a96063`), V5+V6 (`62406dc`), V4 (`7982809`),
  V12 (`4d17659`), A4 (`90a108e`), A5 (`ef2a309`), A6 (`23f1e1d`); 770 тестов, пуш — с разовым
  полным доступом.

## Где правда

`CLAUDE.md` · `docs/plan/{PLAN,SETTLED}.md` · `docs/COMMANDS.md` · `docs/findings/` ·
`.autopilot/2026-09-11-lob-density-ed3/` (тикеты, `interfaces.md`) ·
`C:\Users\Георгий\.dsh\PROFILE-STATE.md` (окружение DSH).

## Каталог страниц

- [projects\active\alpha.md](projects/active/alpha.md) — проект: состояние и история.
- [skills/](skills/) — [ovh-vps-singapore.md](skills/ovh-vps-singapore.md) (сервер, тюнинг, профиль),
  [dsh-task-board.md](skills/dsh-task-board.md) (доска задач DSH, снята 15.09).
- [log.md](log.md) — таймлайн. [MEMORY.md](MEMORY.md) — протокол. [SOUL.md](SOUL.md) — рамка.
