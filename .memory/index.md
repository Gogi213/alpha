# Memory Index — alpha

Инжектится в каждую сессию: здесь состояние, не каталог (бюджет boot-блока 8000 символов).

## Состояние (2026-09-17, ночь) — передача

- alpha — Rust-бот по плотностям стакана Bybit: олвейс-он коллектор пула, разметка крупных
  уровней, профили по осям, вердикт бэктестом с моделью очереди. **Реальных ордеров нет и не будет,
  пока не отлажены стратегия и коллектор** (владелец, 17.09).
- **Читать первым:** `projects/active/alpha.md`, раздел «ПЕРЕДАЧА» (где встали, что дальше по шагам,
  решения владельца, как с ним работать, грабли). План работ — `docs/plan/dev-plan-2026-09-17.md`
  (шапка «Точка входа»: статус каждого тикета). Состояние прода — только `CLAUDE.md` §«Состояние».
- **Сделано (все закоммичены, `master` = `dev/wave-4-record-verify` = `origin`, HEAD `8e58c60` в `log.md`):**
  этап A целиком (A1–A7), **A8.1 — снятие монеты и замена пула на ходу (тикет 48, `8e58c60`)**,
  B1 (В-58, 48 форм), B2, B4, половина B3; аудит коллектора и его долг;
  В-59 (сверка читает `gaps.csv` по смыслу: шов ≠ брак); **коллектор `3614321` задеплоен 17.09
  19:17:41Z** (смоук 5×5 мин прошёл, стоп файлом за 10 с, бэкап `alpha-collector-prev-20260917`);
  `alpha-verify` тот же коммит; **A8.1 в проде пока нет** — там `3614321`. Ворота: 791 тест / 0 / 5,
  clippy `-D warnings`, fmt. A8.1 проверен живьём (3 монеты, снятие применено за 8 с, соседи писали
  дальше, `session.json.pool_removals`, `stop` → `closed=true`).
- **Следующий шаг — A8.3 (отказ подписки биржей, сегодня невидим)**, затем A8.2 (SIGTERM = `stop`),
  A8.1b (три правки ревью A8.1: шов соседям, барьер stop→spawn, повтор `add`), A8.4–A8.6, A8.7 (смоук
  сценариев во втором корне `/opt/alpha/smoke` + деплой одним выкатом). **Потом B5** (прогон-вердикт 48 форм
  на пригодных сутках, DSR по фактическому `N`, отчёт `bounce-backtest-<дата>.md`). C1/C2 ждут двух чисел
  владельца — `N_abs` ($) и `S%` — после квантилей C1. Таблица статусов — `dev-plan` §«Точка входа».
- **Прод:** OVH `139.99.91.22` (`ssh -i ~/.ssh/id_rsa ubuntu@…`, из Git Bash задавать `HOME`),
  данные `sdb` → `/opt/alpha`, корень `/opt/alpha/root`, юнит `alpha-collector.service` активен,
  100 монет, ~6.5 ГБ/сутки, **34 ГБ свободно ≈ 5 суток, диск ведёт владелец вручную** (В-57,
  сторожей нет); `.200` пишется в `deep/` (В-57); ночная сверка `alpha-verify.timer` 00:20 UTC →
  маркеры в корне; сборка — `~/alpha-build`, `PATH=$HOME/.cargo/bin:$PATH nice -n 10 cargo build
  --release --bin alpha` (1–2 мин), исходники — `git archive` + scp.
- **Ждут владельца:** `N_abs` и `S%` (после C1); `fsync` (не нужен — конвенция); старые каталоги
  `data/` (16 без ссылок, список в `log.md`).

## Где правда

`CLAUDE.md` · `docs/plan/{PLAN,SETTLED,dev-plan-2026-09-17}.md` · `docs/COMMANDS.md` ·
`docs/findings/` · `.autopilot/2026-09-11-lob-density-ed3/` (тикеты, `interfaces.md`) ·
`C:\Users\Георгий\.dsh\PROFILE-STATE.md` (окружение DSH).

**Структура доков и правило артефактов (2026-09-17, коммит `270e0a4`):** `docs/` = `ARCHITECTURE.md`,
`COMMANDS.md`, `plan/`, `adr/`, `findings/` (живое) и `archive/` (заменённое); механизм пишет результат
по одному адресу из таблицы в `CLAUDE.md` §«Доки», сырое и промежуточное — в `data/`, **состояние
проекта — только в `CLAUDE.md`**. Заменяющее переносит заменённое в `docs/archive/` тем же коммитом
и правит ссылки; молча не удаляем. Чистка в два раунда (17.09): **88 файлов** в архив, `docs/findings`
30 → 20, живых тикетов `.autopilot` 47 → 13; ссылки в doc-комментариях `src/` обновлены, ворота после
этого зелёные (782 теста).

## Каталог страниц

- [projects\active\alpha.md](projects/active/alpha.md) — проект: передача, состояние, история.
- [skills/](skills/) — [ovh-vps-singapore.md](skills/ovh-vps-singapore.md) (сервер, тюнинг, профиль),
  [dsh-task-board.md](skills/dsh-task-board.md) (доска задач DSH, снята 15.09).
- [log.md](log.md) — таймлайн. [MEMORY.md](MEMORY.md) — протокол. [SOUL.md](SOUL.md) — рамка.
