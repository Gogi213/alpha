# Memory Index — alpha

Этот файл **инжектится в каждую сессию автоматически**. Поэтому здесь — состояние проекта,
а не каталог ради каталога. Обновлять в конце сессии (блок «Состояние»).

## Состояние (2026-09-15)

- **Что это**: `C:\visual projects\alpha` — Rust-бот по плотностям стакана Bybit. Коллектор
  пишет стакан по пулу, разметка крупных уровней, профили, вердикт бэктестом с моделью очереди.
- **Коллектор живёт на сервере** `13.140.29.171` (SSH с этой машины без пароля): юнит
  `alpha-collector.service` включён, **пул топ-30**, олвейс-он с 2026-09-14T18:20:59Z, формат
  **v3**. 0 разрывов, CPU 5.26 % ядра, RSS 22 МБ, **2.05 ГБ/сутки** при 40 ГБ свободных ≈ 20 дней.
  Выгрузки/ротации **нет** — это первое, что упрётся. `verify` серверных суток ещё не гонялся.
- **Пул**: локальный боевой `instruments.csv` — топ-50, заморожен (В-50, коммит `3da0b3f`);
  на сервере его первые тридцать строк (`instruments-top30-server.csv`).
- **Формат v3** принят и заморожен замером (В-48/В-49), раскатан на живой коллектор.
- **Вердикты 2026-09-13**: сделка-отскок `net_fill` < 0 на всех десяти монетах, трейл и лестница
  нуля не пересекают; ёмкость работала фильтром — «несколько позиций» без отбора входа хуже.
- **Следующие шаги**: закрыть первый серверный день (`binlog-stats` + `verify`, ожидаемы
  `fail` у LSK/BTW/MU); решить политику диска; затем T42 (переезды плотностей, `pending`).
- **Git**: ветка `dev/wave-4-record-verify`, всё запушено (128 коммитов ушло в origin 15.09).

## Где правда (не дублировать, а ссылаться)

- `CLAUDE.md` — память проекта для агента (инжектится отдельно, читать первым).
- `docs/plan/PLAN.md` → раздел «Точка остановки» — как продолжать.
- `docs/plan/SETTLED.md` — журнал решений В-1…В-50. `docs/plan/runs.csv` — прогоны.
- `docs/findings/` — боевые артефакты. `docs/COMMANDS.md` — команды и грабли.
- `.autopilot/2026-09-11-lob-density-ed3/` — тикеты, `interfaces.md` (карта построенного).
- `C:\Users\Георгий\.dsh\PROFILE-STATE.md` — окружение DSH (плагины, патчи, что не работает).

## Каталог страниц

- [projects\active\alpha.md](projects\active\alpha.md) — проект: состояние и история.
- [decisions/](decisions/) — решения и причины.
- [skills/](skills/) — как что делать здесь (команды, грабли, окружение).
- [user/](user/) — о владельце: предпочтения и стиль работы.
- [log.md](log.md) — таймлайн изменений памяти.
- [MEMORY.md](MEMORY.md) — протокол ведения памяти. [SOUL.md](SOUL.md) — рабочая рамка.
