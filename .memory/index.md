# Memory Index — alpha

Инжектится в каждую сессию: здесь состояние, не каталог (бюджет boot-блока 8000 символов).

## Состояние (2026-09-19, вечер — H1 плана передачи, HEAD `0edb086`) — передача

- alpha — Rust-бот по плотностям стакана Bybit (одна стратегия `lob::strategy::on_event` идёт в
  `Backtest` и `LiveBot`). **Реальных ордеров нет:** курс владельца 19.09 — искать альфу или намёк на
  неё; торговый контур (H8) заморожен, пока нижняя граница `net_fill` ≤ 0.
- **Читать первым:** `CLAUDE.md` (карта + §«Состояние») → `docs/plan/handoff-2026-09-19.md`
  (план для следующего исполнителя, H1–H8, кросс-ссылки и правила) → `docs/plan/dev-plan-2026-09-17.md`
  «Этап E» → `docs/plan/SETTLED.md` В-64…В-71 → `docs/COMMANDS.md` (команды, счётная машина, ключи).
  Числа живут в `docs/findings/`, состояние прода — только в `CLAUDE.md`; эта страница — ориентир.
- **19.09 (всё закоммичено):** задержки измерены живыми ордерами и вшиты в сетку — **В-68**
  (`--median-rtt-ns place=4200000,cancel=3980000,taker=5650000`, p95 есть; `lob latency`,
  `latency-2026-09-19.md`); полы **В-66/В-67** — номинал ≥ $10k **и сила ×поток ≥ 100 %** в момент
  касания, замеры по монетам; K1–K4/B5/B6 — `lob bounce-grid` + `lob bounce-verdict` (гейты `n ≥ 100`,
  кластеры ≥ 7, PBO/CPCV, DSR); **В-69** E7 (дробный выход `half1to1`/`eat50x80`, `--order-qty-mult`);
  **В-70/В-71** — 15 мин с постановки жёсткий флор возраста, выше — ось (H2); ночной автопрогон
  `alpha-grid-nightly.timer` 02:00 UTC на счётной машине (`tools/compute/nightly-grid.sh`).
- **H1 закрыт по первой сетке** — `docs/findings/base68-2026-09-19.md`: `v68lat-base-{any,frontrun}`,
  три суток 09-16…18, 90 монет из 100, оба вердикта **«мало данных»** (any `pct1-1to1-3600` — 79 кругов,
  точка +0.366 / нижняя −0.695 bps; frontrun `pct0.5-1to1-7200` — +0.258 / −0.449; DSR 0.000).
  **Формы или монеты с нижней границей > 0 нет.** Сутки 09-18 отрицательны у всех процентных форм
  (−478 bps на 26 кругах) — знак держится не во все сутки; позиционные стопы `at`/`behind`/`stack2`
  −9…−10 bps на круг; «занято» 62–66 % у часовых форм; из 4301 круга SNXX даёт 843 (20 %) и DRAM 758
  (18 %) — оба минусовые, у плюсовых монет 2–4 круга. По-монетный срез — новый
  `tools/compute/grid-coins.py` (+ `docs/findings/grid-coins-*-2026-09-19.csv`). **E7 разобран**
  (24 формы, два лота): лучшая `pct1-half1to1-3600` — 79 кругов, +0.065 / −0.799 bps, «мало данных»;
  `half1to1` «занято» не разжимает вовсе, `eat50x80` даёт 225–238 кругов вместо 94–147, но −8 bps на
  круг на всех 24 формах (контроль — 1:1 на одном лоте, разница включает и размер). Строки E7 (24)
  перенесены в журнал: `runs.csv` 106 строк, 88 `bounce_form`.
- **В работе на счётной машине:** фронтир `v70bal-a45/s50` (возраст ≥ 45 мин / сила ≥ 50 %) идёт с
  15:31Z; срез по нему дописывается в тот же док авто-скриптом `/opt/alpha-compute/bin/h1-finish.sh`
  (лог `study/h1-finish.log`, маркер `study/.h1-finish-done`). Первая ночная сетка В-71 — 2026-09-20
  02:00 UTC. **H3 сделан** (19.09, `f5c448e`): ночной скрипт перед сетками считает касания по каждой
  сутке через корень-день из симлинков → `study/touches/<сутки>/` + `study/floors-<сутки>.txt`
  (`.done`-пропуск, самопроверка плумбинга на фикстуре со заглушкой), описание — `docs/COMMANDS.md`
  §«Ночная сетка». Дальше: **H2** (ось возраста под флором), H4 (фандинг в издержки круга), E8→E9→E11→E10.
- **Касания есть не у всех (разобрано 19.09):** CAP и AXL при маркере `verify … ok` дают **ровно ноль
  касаний** под полами сетки за трое суток (контроль STORJ — 2). Это не K1 и не `depth_check` (метка
  `below_floor` стоит у 56 монет из 100, среди них CVC/XPL — обе с плюсом): это край длинного хвоста —
  у 23 монет меньше 100 касаний за двое суток. Касание в трекере = **переход живого уровня на лучшую
  цену** (`levels.rs:1080`), поэтому плотность, стоящая за топом, касанием не считается; разбор и
  замер — `docs/findings/touches-tail-2026-09-19.md`. Следствие: «90 монет в сетке» ≠ 90 рабочих, и
  хвост в 1–3 круга даёт крайние значения в по-монетных таблицах; порог «с какого размера монета
  считается монетой» — решение владельца.
- **Машины:** коллектор `139.99.91.22` (OVH Сингапур, `ubuntu`, `/opt/alpha`, ~6.5 ГБ/сутки, диск полон
  ≈23.09 — владелец вручную, сторожей в коде нет); счёт `13.140.29.171` (`root`, `/opt/alpha-compute`,
  `bin/run-grid.sh <метка> …`, ночной перенос суток 00:45 UTC). Локальный коллектор остановлен.

## Где правда

`CLAUDE.md` · `docs/plan/{handoff-2026-09-19,PLAN,SETTLED,dev-plan-2026-09-17}.md` · `docs/COMMANDS.md` ·
`docs/findings/` · `.autopilot/2026-09-11-lob-density-ed3/` (тикеты, `interfaces.md`) ·
`C:\Users\Георгий\.dsh\PROFILE-STATE.md` (окружение DSH).

**Структура доков и правило артефактов (2026-09-17, коммит `270e0a4`):** `docs/` = `ARCHITECTURE.md`,
`COMMANDS.md`, `plan/`, `adr/`, `findings/` (живое) и `archive/` (заменённое); механизм пишет результат
по одному адресу из таблицы в `CLAUDE.md` §«Доки», сырое и промежуточное — в `data/`, **состояние
проекта — только в `CLAUDE.md`**. Заменяющее переносит заменённое в `docs/archive/` тем же коммитом
и правит ссылки; молча не удаляем. Чистка в два раунда (17.09): **88 файлов** в архив, `docs/findings`
30 → 20, живых тикетов `.autopilot` 47 → 13; ссылки в doc-комментариях `src/` обновлены, ворота после
этого зелёные (782 теста).

## Каталог страниц

- [projects\active\alpha.md](projects/active/alpha.md) — проект: передача, состояние, история.
- [skills/](skills/) — [ovh-vps-singapore.md](skills/ovh-vps-singapore.md) (сервер, тюнинг, профиль),
  [dsh-task-board.md](skills/dsh-task-board.md) (доска задач DSH, снята 15.09).
- [log.md](log.md) — таймлайн. [MEMORY.md](MEMORY.md) — протокол. [SOUL.md](SOUL.md) — рамка.
