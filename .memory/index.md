# Memory Index — alpha

Инжектится в каждую сессию: здесь состояние, не каталог (бюджет boot-блока 8000 символов).

## Состояние (2026-09-20, утро — передача в новый чат; HEAD см. `git log -1`)

**Читать первым:** `CLAUDE.md` (карта + §«Состояние») → этот блок → **`docs/plan/side-plan-2026-09-20.md`** (план по
сторонам: S10 → S9 → S8 → S6; §7 утро, §8 вопросы владельцу) → **`docs/plan/EXPERIMENTS.md`** (реестр: что пробовали,
какими значениями, что вышло; главная гипотеза; оговорка об исполнении) → `docs/plan/alpha-roadmap-2026-09-19.md` →
`docs/COMMANDS.md`. `.memory/` — общая память обоих исполнителей, digest в конце сессии.

- **Что это:** Rust-бот по плотностям стакана Bybit; одна стратегия `lob::strategy::on_event` для `Backtest` и `LiveBot`.
  **Живых ордеров нет**; курс владельца — найти альфу; торговый контур заморожен.
- **Где мы (20.09):** сделка-отскок от старой стены. **Лонг от бид-стены** (стена ≥ $10k, стоит ≥ 45 мин, вход
  лимиткой у первого фронтранера, стоп 2 %, тейк 1:1, выход по дедлайну) на 16–19.09 (три дня роста + флэт):
  116 сделок, 44 % побед, **+$162 на $1000-лоте** (по дням +78/+93/−26/+16), просадка −$42; **шорт от аск-стены** —
  134 сделки, 31 %, **−$234** (ни одной формы в плюсе). Главная гипотеза **H1** — лонг **после просадки BTC за 4 ч**
  (`a45-bid-b4h-neg`, `pct2-1to1-3600`): 60 сделок, 58 %, +$172, просадка −$16. **Удержание — главная ось**
  (E14): 60 с −$198 · 10 м −$131 · 30 м +$7 · 1 ч +$187 · 2 ч +$155 · **4 ч +$296**; стоп/тейк почти не срабатывают
  (83/89 выходов по дедлайну), тейк в % (`tk`) хуже 1:1 — закрыт. Вердикты: лонг «красный» (n ≥ 100, нижняя
  −0.75 < 0), остальные «мало данных»; зелёного нет. Все три дня роста + флэт — **дней падения не было**.
- **ГЛАВНОЕ ОГРАНИЧЕНИЕ (S10, владелец 20.09, первый приоритет) — шаг 1 закрыт 20.09 (`lob fill-capacity`,
  `docs/findings/fill-capacity-2026-09-20.md`, M14):** касание в записи длится **0,1 с в медиане** (стена = лучшая цена —
  финальный шаг), вход снимается в его конце; спред в момент касания один тик в 63 % → лимитка у фронтрана **равна
  лучшему аску**: из 116 кругов лонга `a45-bid` (`pct2-1to1-3600`) **89 (77 %) — тейкерские входы** (крейт исполнил
  как рыночную; комиссия тейкера в P&L учтена, но сделка другая), 18 — мейкерские без единой продажи на тике
  (зачтено исчезновение уровня), **9 (8 %) — с объёмом**. Лестница практиков «0,2–0,7 %» к этим стенам мейкером не
  ставится (аск за 5 мин до касания в 13 bps от стены, ≥ 70 bps у 1 %). Исполняются ноги **между стеной и рынком**
  при сроке жизни входа дольше касания: 5 мин — хоть что-то у 37–51 % касаний, целиком 11 %; 30 мин — 48–61 % /
  15–21 % (верхняя оценка — слот не видит смерти стены); размер $200–1000, сторона, режим на «хоть что-то» не
  влияют. **Все P&L до S10 — тейкерские входы в момент касания, не мейкерская сделка спеки.** Шаг 2 (следующий):
  `--entry-ttl-secs` (сетка {60, 300, 1800} или число владельца, плюс снятие при смерти стены), пост-онли, очередь по
  объёму (`ProbQueueModel`/`PartialFillExchange`), частичная позиция (стоп/тейк/дедлайн от средней цены); гейт —
  при «крест-модели» и `ttl = касание` те же круги. Шаг 3 — лестница `fr..mkt`/`0..mkt` (`3@20..70` через спред
  в 87–100 %). Инструмент: `lob fill-capacity` (сырьё `study/capacity/v72/`, читатель `bin/fill-capacity.py`).
- **S9 (владелец):** поведение в сделке после снятия плотности, лонг и шорт отдельно — «ждать дедлайна/стопа при
  снятой стене — маразм»; сейчас стратегия после входа на стену не смотрит (кроме `eat`-форм E7). Замер по кругам →
  `eat`-формы на лонгах → правило «снята → безубыток / выход при возврате» в `lob::strategy`.
- **Сделано 19–20.09 (код, всё с тестами и гейтами «те же круги байт в байт»):** `--side`, `--touches-from`
  (касания из CSV ночного H3, реплей был 83 % времени), сайдкар `<бинлог>.events`, `--set` (наборы фильтров одним
  процессом: `age=,flow=,side=,frontrun,eaten=,usd_min=,ret10m/ret1h/ret4h/pool1h/pool4h/btc1h/btc4h _min/_max`),
  `--regime-from`, `ret_10m/1h/4h_bps` + `mids1m-*.csv` в `lob touches`, `ref-klines.py` (BTC/ETH свечи по REST),
  `regime.py` (режим по минутам; **утечка** закрытия минуты касания закрыта `7cd3502`), `tk<x>`, `--deadline-secs`
  (1800/14400), `--order-usd`. Тесты 883. Ночь: `nightly-grid.sh` = H3 касания суток → режим → **41 набор одним
  процессом** (~21 мин) + `tk` + `dl` + E7, вердикты, `study/verdicts.csv`; **ночь громкая** — `study/ALERTS.log`,
  ОК/ПРОВАЛ, failed-юнит, `bin/alert-tg.sh` через `ALERT_CMD` (токен — владелец, `/etc/alpha/alert.env`; пока нет).
  Скрипты на машину — только `scp … .new` + `mv`. Отчёт в деньгах — `tools/compute/equity-report.py`
  (`docs/findings/equity-2026-09-20.html`; интервалы 95 % на сделку/в день).
- **Научный контур:** предрегистрация до данных (`runs.csv` 1405 строк, 1380 `bounce_form`), реестр
  `EXPERIMENTS.md` (E01–E14, M01–M13, правило prereg ↔ строка, вердикт ↔ результат), интервал (нижняя граница по
  кластерам), DSR/PBO; одна главная гипотеза на ночь (H1), остальное разведка; заморозку **не** делаем (владелец:
  эмулировать позже); утечки ищем (одна найдена и закрыта).
- **Машины:** коллектор `139.99.91.22` (`ubuntu`, `/opt/alpha`, диск 29/49 ГБ на 20.09, +6.5 ГБ/сутки → полон
  ≈ 22.09 — **решение владельца**: чистить `deep/` и старые `.50` после переноса или расширять); счётная
  `13.140.29.171` (`root`, `/opt/alpha-compute`, `bin/alpha` → `alpha-2214ed3`, 19 ГБ свободно). Таймеры: сверка
  00:20Z, перенос 00:45Z, ночь 02:00Z (`alpha-grid-nightly.timer`). Ночь 20.09 02:00Z **упала штатно** («сетка ещё
  идёт» — шёл ручной прогон 19-го, `ALERTS.log` строка 02:00:44Z), ручной прогон кончился «ОК» 02:09Z; failed-юниты
  сброшены. `chain-day.sh` — докачать сутки сразу после переноса (маркер — число файлов дня = маркеров verify).
- **Вопросы владельцу:** диск коллектора (≈ 22.09); `DAYS_WINDOW` (ночь растёт ~5 мин/сутки); Telegram для алертов.
- **План разработки (20.09, вечер) — `docs/plan/dev-plan-2026-09-20.md`, этап F, для младших моделей.** Решения
  владельца В-72…В-78: вход только мейкером пост-онли (тейкерская форма — только по его слову, пока нет);
  лучший вход — лимитка 0,05–0,1 % над стеной, поставленная **заранее на подходе** и живущая, пока стена жива
  («да я с тобой согласен, я так и делал»); в упор — нет (2/3 исполнений насквозь = отмена), первый тик над
  стеной — минус; отмена в позиции — по эффективности: «съели» (сделки в стену с входа / размер ≥ X %) или
  «сняли» (размер упал без сделок), прилипание/удар без съедания — сидим; переворотов нет; проедание —
  отдельный блок после ворот отскока. Задачи: F1 `ApproachRecord` в трекере (сигнал подхода на `D` bps) →
  F2 ёмкость по подходам → F3 `ProbQueueModel`+`PartialFillExchange` (`--queue-model`) → F4 частичная позиция и
  пост-онли в стратегии → F5 `entry_ttl` «стена жива ∧ цена в полосе» + потолок {60,300,1800} → F6
  `--entry-form ladder<N>x<from>..<to>` + `--signal approach` → F7 `--exit-form eat<X>|gone<W>` по ленте →
  F8 сетка/вердикт (гейт «те же круги» при старых флагах) → F9 замер X и D → F10 предрегистрация + ночь →
  F11 чтение и деньги → F12 доки/SETTLED/память. Свод практиков — `order-management-practitioners-2026-09-20.md`
  (таймаута у лимитки нет ни у кого; снимают по событиям; 60 % касаний a45-bid — смерть уровня без сделок).
  Замеры входа — `fill-capacity-2026-09-20.md` §9 (`leg-distance.py`, `leg-clear.py`, `clear_ms` в CSV).
- **Следующий исполнитель начинает с:** утро (§7 side-plan), затем **`dev-plan-2026-09-20.md` F1** (сигнал
  подхода) и F3 (модель очереди) — ветки независимы; порядок и гейты — там. Ночь 20.09 02:00Z пропущена
  штатно (шёл ручной прогон), новых суток не было; счётная машина — `bin/alpha` → `alpha-5a315d8`.

## Где правда

`CLAUDE.md` · `docs/plan/{handoff-2026-09-20,PLAN,SETTLED,dev-plan-2026-09-17}.md` · `docs/COMMANDS.md` ·
`docs/findings/` · `.autopilot/2026-09-11-lob-density-ed3/` (тикеты, `interfaces.md`) ·
`C:\Users\Георгий\.dsh\PROFILE-STATE.md` (окружение DSH).

**Структура доков и правило артефактов (2026-09-17, коммит `270e0a4`):** `docs/` = `ARCHITECTURE.md`,
`COMMANDS.md`, `plan/`, `adr/`, `findings/` (живое) и `archive/` (заменённое); механизм пишет результат
по одному адресу из таблицы в `CLAUDE.md` §«Доки», сырое и промежуточное — в `data/`, **состояние
проекта — только в `CLAUDE.md`**. Заменяющее переносит заменённое в `docs/archive/` тем же коммитом
и правит ссылки; молча не удаляем. Чистка в два раунда (17.09): **88 файлов** в архив, `docs/findings`
30 → 20, живых тикетов `.autopilot` 47 → 13; ссылки в doc-комментариях `src/` обновлены, ворота после
этого зелёные (782 теста).

## Каталог страниц

- [projects\active\alpha.md](projects/active/alpha.md) — проект: передача, состояние, история.
- [skills/](skills/) — [ovh-vps-singapore.md](skills/ovh-vps-singapore.md) (сервер, тюнинг, профиль),
  [dsh-task-board.md](skills/dsh-task-board.md) (доска задач DSH, снята 15.09).
- [log.md](log.md) — таймлайн. [MEMORY.md](MEMORY.md) — протокол. [SOUL.md](SOUL.md) — рамка.
