# Memory Index — alpha

Этот файл **инжектится в каждую сессию автоматически** — здесь состояние проекта, не каталог.
Бюджет boot-блока 8000 символов ⇒ держать ~2,3 КБ, подробности уводить в страницы.

## Состояние (2026-09-16)

- `C:\visual projects\alpha` — Rust-бот по плотностям стакана Bybit: олвейс-он коллектор пула,
  разметка крупных уровней, профили по осям, вердикт бэктестом с моделью очереди.
- **Прод**: OVH `139.99.91.22` (Ubuntu 26.04, 2 vCPU / 3.7 ГБ, ключ `id_rsa`; данные — `sdb` 50 ГБ →
  `/opt/alpha`), юнит `alpha-collector.service` активен, `NRestarts=0`, стоп файлом `stop` (В-41).
  Пул — **100 монет** (топ-105 минус глобальный топ-5: BTC/ETH/XRP/SOL/ZEC).
- **Два потока (T45)**: `.50` в основной файл, `.200` в `deep/`. Пишем с **2026-09-15 14:31:04Z**,
  расход **5.9 ГБ/сутки** ⇒ **≈7 суток**; **ротации нет**.
- **Архив (T46)**: `lob archive --level 19` → `*.binlog.zst` = **81.3 %**; сырое — при `verify == ok`.
- **RPI** (В-53, В-55): флаг `publicTrade.RPI` пишется с 15.09 22:47:56Z, разметка его читает —
  объём об RPI идёт в отдельный `rpi_lots` и в метку исхода не входит. Влияние измерено: 286 меток
  из 13.8 млн (0.002 %) — в пределах шума. `priceLimit`/`allLiquidation` отклонены (В-51), сетап —
  из ручной торговли (В-52).
- **verify 09-15 (100 монет): 77 ok / 23 fail**, `gaps=0`/`invariants=0` везде. Причины fail
  разобраны: блок 0, RPI 1.8 %, спред 24 %, ≥2 тиков до уровня 79 %, отставание книги 0.7 % — это
  **разрешение фида** (`.50` пачками ~20 мс), а не порча; 23 монеты рабочие.
- **Дальше**: критерий `verify` (в плане порог 0.1 %, в коде «любой ненулевой» → 91 против 77; решение
  владельца открыто) → ротация (отложена, но 42 ГБ кончатся к ~09-22) → сухой прогон альфа-цепочки →
  **T42**. Первый вердикт — после 7 дневных кластеров (после 09-21).
- **Сборка:** toolchain и сборка — на боевом, на системном диске (`sda`, `/home/ubuntu`); прежний
  сервер `13.140.29.171` закрыт (В-54).
- **Git**: 16.09 волна 4 слита в **`master`** fast-forward (`6c9b5f5`→`e7f58a7`, +135 коммитов,
  из них 128 не-memory); `origin/master` = ветке `dev/wave-4-record-verify`; пуш — с расширенным доступом.

## Где правда

`CLAUDE.md` · `docs/plan/{PLAN,SETTLED}.md` · `docs/COMMANDS.md` · `docs/findings/` ·
`.autopilot/2026-09-11-lob-density-ed3/` (тикеты, `interfaces.md`) ·
`C:\Users\Георгий\.dsh\PROFILE-STATE.md` (окружение DSH).

## Каталог страниц

- [projects\active\alpha.md](projects/active/alpha.md) — проект: состояние и история.
- [skills/](skills/) — [ovh-vps-singapore.md](skills/ovh-vps-singapore.md) (сервер, тюнинг, профиль),
  [dsh-task-board.md](skills/dsh-task-board.md) (доска задач DSH, снята 15.09).
- [log.md](log.md) — таймлайн. [MEMORY.md](MEMORY.md) — протокол. [SOUL.md](SOUL.md) — рамка.
