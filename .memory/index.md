# Memory Index — alpha

Инжектится в каждую сессию: здесь состояние, не каталог (бюджет boot-блока 8000 символов).

## Правило общения с владельцем (21.09, вечер — обязательное для обоих исполнителей)

Владелец: «мне в основном нужны продуктовые и бизнесовые пояснения, без технической помойки. Если
НЕОБХОДИМО донести техничку в разборе, например механики — доноси, но разговаривать техничкой, будто я
хед инженер, не надо». Значит: первой фразой — цифра/результат/статус; потом «что это значит» и «что
дальше»; решения владельца — отдельной строкой с вариантами; хеши, имена юнитов, пути, маркеры, кэши —
не в ответ, а в память/доки; техника — только если спросил «почему», и тогда одним абзацем о механике.
Деньги и сделки важнее bps и токенов.

**TypeSafe (Jev) — граница после аудита 22.09 (В-85, отменяет «любое суждение через judge.py, порог 0,5» из В-81):**
правила, числа и статусы (семьи флоров, коллизия имени, статус ночи, число тестов, итог вердикта) проверяет **код** с
жёстким отказом, в том числе без ключа. Модель — только для семантики свободного текста (строка предрегистрации R3,
класс ошибки по трассировке, подсказки ревью) и печатается как «мнение модели <версия>», не рядом с метриками.
Точность судей меряет `tools/typesafe/eval-judges.py` (M20). Горячего пути не касается.

## Состояние (2026-09-24, ночь — АКТУАЛЬНОЕ; ветка `audit/design-fixes-2026-09-22`, HEAD — `git log -1`)

**Где что:** план — `docs/plan/dev-plan-2026-09-23.md` (этап G, §0а: G0/G11 частично сделаны, новые G12/G13); решения —
SETTLED В-86…В-94 (защита от прокидов В-88, позиция $500/депозит $2500 В-89, выключатель −1,5 % В-91, доллары только
с размером позиции В-93, перенос через полночь В-94); реестр — EXPERIMENTS E18–E23; разборы —
`titration-regime-2026-09-23.md`, `exit-titration-2026-09-23.md`, `crash-stress-2026-09-23.md`; дашборд
https://claude.ai/artifact/WfSGAfj2hhkRTckAnKXSRF (кейсы скрыты до пересчёта на $500). Хроника дня —
`projects/archive/state-history-2026-09-23.md`.

**Машины:** дек `deck@192.168.1.49` (`~/alpha`, `bin/alpha` → `alpha-e2f310c`); VPS `13.140.29.171` — сборка;
коллектор `139.99.91.22` пишет только `.50` (поток `.200` 15–22.09 перенесён на дек `~/alpha/deep/`, 18 ГБ, на
коллекторе удалён с разрешения владельца — G0 закрыт, диск 23 %). ssh с Windows — только
`-i /c/Users/Георгий/.ssh/id_rsa -o UserKnownHostsFile=/c/Users/Георгий/.ssh/known_hosts`.

**23.09 — стресс-тест и ревью:** обвал 10–11.10.2025 — стоп 2 % не держит (позиция в худшую минуту −28 % медиана,
−60 % худшая), выключатель BTC −1,5 %/1 ч сработал за 17 мин до провала (`crash-stress-2026-09-23.md`). Ревью 8
блоков нашло: перенос через полночь терял ~18 % сделок кандидата (исправлено, `--carry-root`, `e5c8847`); бэктест
торговал минимальным лотом (~$5), доллары отчёта были % × условная $1000 (исправлено — `--order-usd 500`, дашборд
печатает размер позиции). **Кандидатные +$515/+$272 (история/запись) — до этих исправлений, недействительны как
есть; пересчёт на $500 идёт (E23).**

**Решения владельца 23.09 (В-89…В-94):** позиция $500 «полная» / депозит $2500 / без плеча; без лимита числа
позиций (пока); выключатель −1,5 % и исключение монет с прокидами (≥10 %/15 мин/месяц) — рабочие варианты, не
заморожены; доллары в отчёте — только с размером позиции; проверка на реальном размере — обязательна до живого
запуска.

**Дальше:** волна 1 исправлений (трейл по модулю, лестница в целых лотах, размер по цене сигнала, минутная
просадка счёта) → пересчёт → дашборд; волна 2 (коллектор, надёжность живого бота, защита binlog, перенос логики
в `lob/` — A6/G13, честный выход по стакану); затем август, двойник `.200`/`.50`, предрегистрация.

**Ждут владельца:** числа защиты (лот, потолок, выключатель); август сейчас; microSD 1 ТБ на деке; мусор в корне
репо (`*.req/*.out`, `req.py` — не наш, не трогали).

## Где правда

`CLAUDE.md` · `docs/plan/{handoff-2026-09-20,PLAN,SETTLED,dev-plan-2026-09-17}.md` · `docs/COMMANDS.md` ·
`docs/findings/` · `.autopilot/2026-09-11-lob-density-ed3/` (тикеты, `interfaces.md`) ·
`C:\Users\Георгий\.dsh\PROFILE-STATE.md` (окружение DSH).

**Структура доков и правило артефактов (2026-09-17, коммит `270e0a4`):** `docs/` = `ARCHITECTURE.md`,
`COMMANDS.md`, `plan/`, `adr/`, `findings/` (живое) и `archive/` (заменённое); механизм пишет результат
по одному адресу из таблицы в `CLAUDE.md` §«Доки», сырое и промежуточное — в `data/`, **состояние
проекта — только в `CLAUDE.md`**. Заменяющее переносит заменённое в `docs/archive/` тем же коммитом
и правит ссылки; молча не удаляем. Чистка в два раунда (17.09): **88 файлов** в архив, `docs/findings`
30 → 20, живых тикетов `.autopilot` 47 → 13; ссылки в doc-комментариях `src/` обновлены, ворота после
этого зелёные (782 теста).

## Каталог страниц

- [projects\active\alpha.md](projects/active/alpha.md) — проект: передача, состояние, история.
- [skills/](skills/) — [ovh-vps-singapore.md](skills/ovh-vps-singapore.md) (сервер, тюнинг, профиль),
  [dsh-task-board.md](skills/dsh-task-board.md) (доска задач DSH, снята 15.09),
  [typesafe-ai.md](skills/typesafe-ai.md) (скилл TypeSafe/Jev: **корни скиллов DSH — `~/.agents/skills`,
  а НЕ `~/.claude/skills`**; скилл перенесён туда 21.09; батарея 21.09 — API работает правильно:
  контракт без ошибок, choice/noul 11/12 на своих метках, Brier 0.045, батч в 4× дешевле,
  неверный `type` даёт 400 (доки: 422); проверять через Node, PowerShell TLS на хосте не поднимает).
- [log.md](log.md) — таймлайн. [MEMORY.md](MEMORY.md) — протокол. [SOUL.md](SOUL.md) — рамка.
