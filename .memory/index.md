# Memory Index — alpha

Инжектится в каждую сессию: здесь состояние, не каталог (бюджет boot-блока 8000 символов).

## Состояние (2026-09-19, поздний вечер — ревью исполнителя «H», handoff редакция 2, HEAD `fe4991e`) — передача

- alpha — Rust-бот по плотностям стакана Bybit (одна стратегия `lob::strategy::on_event` идёт в
  `Backtest` и `LiveBot`). **Реальных ордеров нет:** курс владельца 19.09 — искать альфу или намёк на
  неё, по монетам; торговый контур заморожен, пока нижняя граница `net_fill` ≤ 0. **Фандинг не нужен**
  (владелец 19.09) — из плана снят.
- **Читать первым:** `CLAUDE.md` (карта + §«Состояние») → **`docs/plan/alpha-roadmap-2026-09-19.md`** (дорога к
  альфе с воротами и плейбуками — для любого исполнителя, в том числе младших моделей) → **`docs/plan/handoff-2026-09-20.md`**
  (план редакции 2: §0 ревью первого дня исполнителя «H», задачи H1b/H2/H3b/H5–H9 с явными
  входом/выходом/«готово, когда») → `dev-plan-2026-09-17.md` «Этап E» → `SETTLED.md` В-64…В-71 →
  `docs/COMMANDS.md`. Эта память (`.memory/`) — **общая персистентная передача для обоих исполнителей**
  (владелец 19.09): конец каждой сессии = digest сюда (`projects/active/alpha.md` §ПЕРЕДАЧА, этот блок,
  строка в `log.md`); локальная память Claude — вторична.
- **19.09, всё закоммичено:** задержки измерены живыми ордерами (`lob latency`, с сервера: сеть 2.7 мс,
  постановка 4.2, снятие 4.0, тейкер 5.7) → **В-68** тройка в сетке; **В-69** E7 (`half1to1`,
  `eat50x80`, `--order-qty-mult 2`); плечо по монете (`leverage-2026-09-19.md`: 30× при стопе 1 %);
  матрица флоров (`floors-balance-2026-09-19.md`: сила ≥ 100 % ∩ возраст ≥ 15 мин = 3 касания/сутки);
  **В-70/В-71** — 15 мин с постановки жёсткий флор, возраст выше — ось (H2); ночной набор шести сеток
  (`nightly-grid.sh`, 02:00 UTC) + H3 (касания по суткам через корень-день из симлинков).
- **Вердикты 19.09 — все «мало данных», знака нет:** `v68lat` any `pct1-1to1-3600` 79 кругов
  +0.366/−0.695 bps, frontrun `pct0.5-1to1-7200` +0.258/−0.449; E7 `pct1-half1to1-3600` +0.065/−0.799
  (`half1to1` «занято» не разжимает, `eat50x80` даёт 225–238 кругов, но −8 bps на круг). Сутки 09-18
  отрицательны у всех процентных форм; SNXX+DRAM = 38 % кругов, оба в минусе. `base68-2026-09-19.md`,
  срез по монетам `tools/compute/grid-coins.py`; CAP/AXL — ноль касаний по определению касания
  (`levels.rs:1080`), 23 монеты < 100 касаний за двое суток (`touches-tail-2026-09-19.md`).
- **Ревью 19.09 (Claude → «H», `handoff-2026-09-20.md` §0) — пункты исполнителя закрыты** (`708b44e`):
  сырьё `grid-coins-*.csv` из `docs/findings/` убрано в `study/` счётной машины, `/tmp`-пруфы →
  `study/touches-verify/`, наблюдатель-сирота убит (`h1-finish.sh` оставлен как именованный скрипт с
  логом), формулировка про него поправлена (скрипт пишет лог, док правлю руками). Строки E7 в
  `runs.csv` исправлены ревью (`a331de4`). В обратную сторону: `touches6` — трекер `--h3-mode floor
  k=1.0`, не номинал $10k: поправлены `floors-balance`/`leverage`, абсолютные числа возраста брать из
  ночного H3 (`study/floors-<сутки>.txt`). Возраст плотности обнуляется в 00:00 (трекер суточный) —
  систематика внесена в шапку `floors-<сутки>.txt`, учитывать в H2 (ревью §0.7).
- **H3b сделан** (19.09, `9f6dba0`): ночной скрипт — ошибка монеты не держит сутки в бесконечном
  пересчёте (причина в лог из `<SYM>.log`, `.attempts`, `.done` после двух ночей, `failed-history.txt`),
  `xargs -r`, окно суток для сеток `DAYS_WINDOW` (по умолчанию все сутки), пустой каталог не роняет
  `floors-balance`; архивные `.zst`-сутки проверены живьём (`lob touches` на корне-дне из одного `.zst`
  → `days=1 touches=1`). Критерий «две ночи без ручных шагов» — 21.09.
- **H2 шаги 1–2 — инструменты готовы, данных ждут ночи 20.09** (`0eff745`, `584e92f`):
  `floors-balance.py --by-coin` — пороги возраста {15,30,45,60,90,120} мин по монете и по суткам
  (сигналов/сутки, ход за 10 мин и час, доля отскока) + csv; ночной H3 кладёт это же в
  `study/floors-by-coin-<сутки>.csv`. `age-axis.py` — out-of-sample «пуловый против кластерного
  (колонка пула, до ответа) против по-монетного» на всех упорядоченных разбиениях суток A/B, мера —
  ход в день = сигналов/сутки × (mean m_1h − 4.41). Оба обкатаны на `touches6` (floor-трекер — проверка
  кода, не ответ). Плюс инструменты соседних задач: `family-table.py` (H1b — строка «семья флоров ×
  лучшая форма × n × точка/нижняя × монеты с кругами», из вердикта + `forms.csv`) и `pool-tail.py` (H5 —
  цена порога касаний/сутки {10,30,100} в монетах и кругах); все четыре задеплоены в `bin/` счётной машины.
- **Счётная машина `13.140.29.171`:** фронтир — `v70bal-a45-any` отработал 15:31→16:44Z (90/90), идёт
  `s50`; вердикты `chain-v70bal.sh` считает **после обеих** сеток (≈18:05Z), срез — именованный
  `h1-finish.sh` в `study/h1-finish.log` (наблюдателей из инструмента не держим — ревью §0.4; мою петлю
  ожидания убил). **Предзагрузка H2 идёт:** `/tmp/chain-preload-h2.sh` ждёт конец фронтира и гоняет
  `TOUCHES_ONLY=1 nightly-grid.sh` → `study/touches/{16,17,18}` + `floors-by-coin-*.csv`
  (лог `study/preload-h2.log`, маркер `study/.preload-h2-done`) — чтобы шаг 1 оси возраста считался
  вечером; ночью эти сутки пропускаются по `.done`. Журнал-копия
  `study/runs-2026-09-19.csv` (106 строк, синхронизирована с репо `a331de4`; вердикты фронтира допишут
  64 строки — переносить **с контекстом**, §0.1). Ночь 20.09 02:00Z — первый настоящий прогон H3
  (касания по 4 суткам + `floors-by-coin-*.csv`) и шесть сеток. **Ночь растёт с историей** (сетки по
  всем суткам: 3 суток ≈ 6 ч, 7–8 не уложатся) — механизм окна готов (`DAYS_WINDOW=N` в
  `nightly-grid.sh`, H3b), значение выбирает владелец (вопрос §4.3 плана).
- **Этап 1 дороги — ось стороны посчитана (19–20.09, Claude):** `--side` в `lob bounce-grid`, 128 испытаний, четыре сетки, `docs/findings/side-axis-2026-09-19.md`: лонги от старых бид-стен +19.2 bps/круг на 89 кругах (вердикт «мало данных», нижняя −1.16), шорты от аск-стен — 0 форм из 32 в плюсе; все три сутки рост, ворота ждут ≥ 7 суток и дней падения; сетки стороны — в ночи. **Счёт ускорен на порядок с гейтами байт в байт:** `--touches-from study/touches` (касания из CSV H3), сайдкар `<бинлог>.events`, `--set` (девять наборов одним процессом, `b5/nightly-<день>-base/<набор>/`); `nightly-grid.sh`/`side-grid.sh` переписаны, бинарник `bin/alpha-23c7988`, репетиция ночи 20:30Z — `study/nightly-2026-09-19.log`. HEAD `a93fef0`.
- **Намёк на альфу (19.09, поздний вечер, `side-asymmetry-2026-09-19.md`):** в семье «старые стены» лонги от
  бид-стен +12…+17 bps на круг (n 79–82, плюс 2 суток из 3), шорты от аск-стен −16…−19 — пуловый «красный»
  их сумма; все три сутки — рост (+1.8/+3.4/+7.3 %), сторона от режима пока неотличима. Дальше: ось
  `--side` в сетке (предрегистрация), режим E12 как фильтр, ждать дней падения. `age-axis.py` не запускался.
- **Машины:** коллектор `139.99.91.22` (OVH Сингапур, `ubuntu`, `/opt/alpha`, ~6.5 ГБ/сутки, диск
  27/49 ГБ на 19.09 → полон ≈23.09 — владелец вручную); счёт `13.140.29.171` (`root`,
  `/opt/alpha-compute`, `bin/run-grid.sh`, `bin/nightly-grid.sh`, `study/`, `b5/`). Ключи биржи — только
  окружение процесса, на диск не писать; живые сделки — число согласовать с владельцем до запуска.

## Где правда

`CLAUDE.md` · `docs/plan/{handoff-2026-09-20,PLAN,SETTLED,dev-plan-2026-09-17}.md` · `docs/COMMANDS.md` ·
`docs/findings/` · `.autopilot/2026-09-11-lob-density-ed3/` (тикеты, `interfaces.md`) ·
`C:\Users\Георгий\.dsh\PROFILE-STATE.md` (окружение DSH).

**Структура доков и правило артефактов (2026-09-17, коммит `270e0a4`):** `docs/` = `ARCHITECTURE.md`,
`COMMANDS.md`, `plan/`, `adr/`, `findings/` (живое) и `archive/` (заменённое); механизм пишет результат
по одному адресу из таблицы в `CLAUDE.md` §«Доки», сырое и промежуточное — в `data/`, **состояние
проекта — только в `CLAUDE.md`**. Заменяющее переносит заменённое в `docs/archive/` тем же коммитом
и правит ссылки; молча не удаляем. Чистка в два раунда (17.09): **88 файлов** в архив, `docs/findings`
30 → 20, живых тикетов `.autopilot` 47 → 13; ссылки в doc-комментариях `src/` обновлены, ворота после
этого зелёные (782 теста).

## Каталог страниц

- [projects\active\alpha.md](projects/active/alpha.md) — проект: передача, состояние, история.
- [skills/](skills/) — [ovh-vps-singapore.md](skills/ovh-vps-singapore.md) (сервер, тюнинг, профиль),
  [dsh-task-board.md](skills/dsh-task-board.md) (доска задач DSH, снята 15.09).
- [log.md](log.md) — таймлайн. [MEMORY.md](MEMORY.md) — протокол. [SOUL.md](SOUL.md) — рамка.
