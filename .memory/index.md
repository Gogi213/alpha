# Memory Index — alpha

Инжектится в каждую сессию: здесь состояние, не каталог (бюджет boot-блока 8000 символов).

## Правило общения с владельцем (21.09, вечер — обязательное для обоих исполнителей)

Владелец: «мне в основном нужны продуктовые и бизнесовые пояснения, без технической помойки. Если
НЕОБХОДИМО донести техничку в разборе, например механики — доноси, но разговаривать техничкой, будто я
хед инженер, не надо». Значит: первой фразой — цифра/результат/статус; потом «что это значит» и «что
дальше»; решения владельца — отдельной строкой с вариантами; хеши, имена юнитов, пути, маркеры, кэши —
не в ответ, а в память/доки; техника — только если спросил «почему», и тогда одним абзацем о механике.
Деньги и сделки важнее bps и токенов.

**TypeSafe (Jev) — граница после аудита 22.09 (В-85, отменяет «любое суждение через judge.py, порог 0,5» из В-81):**
правила, числа и статусы (семьи флоров, коллизия имени, статус ночи, число тестов, итог вердикта) проверяет **код** с
жёстким отказом, в том числе без ключа. Модель — только для семантики свободного текста (строка предрегистрации R3,
класс ошибки по трассировке, подсказки ревью) и печатается как «мнение модели <версия>», не рядом с метриками.
Точность судей меряет `tools/typesafe/eval-judges.py` (M20). Горячего пути не касается.

## Состояние (2026-09-23, вечер — АКТУАЛЬНОЕ; ветка `audit/design-fixes-2026-09-22`, HEAD — `git log -1`)

**Где что:** план — `docs/plan/dev-plan-2026-09-23.md` (этап G, §0а); решения — SETTLED В-86 «просадка» (не «пролив»),
В-87 шорт отложен, **В-88 защита от прокидов обязательна**; реестр — EXPERIMENTS E18–E21; разборы —
`docs/findings/titration-regime-2026-09-23.md`, `exit-titration-2026-09-23.md` (§5, §6); дашборд
https://claude.ai/artifact/WfSGAfj2hhkRTckAnKXSRF (кандидат, кейсы, по монетам). Хроника дня — `projects/archive/state-history-2026-09-23.md`.

**Машины:** дек `deck@192.168.1.49` (`~/alpha`, пользовательские юниты, `bin/alpha` → `alpha-e2f310c`, 120 ГБ свободно;
ночь 02:00 UTC, забор суток 01:15 UTC); VPS `13.140.29.171` — сборка (`/opt/alpha-compute/src-<хеш>`, target
`target-58e63d7`) и резервная ночь; коллектор `139.99.91.22` (`.50` + `deep/.200` 15–23.09, 18 ГБ, диск 69 %).
ssh с Windows — только `-i /c/Users/Георгий/.ssh/id_rsa -o UserKnownHostsFile=/c/Users/Георгий/.ssh/known_hosts`.
TypeSafe на деке — `~/.config/alpha/typesafe.env`, `judge.py` с `User-Agent` (Cloudflare 1010).

**Кандидат (не доказательство):** лонг от старой бид-стены (≥ $10k, ≥ 45 мин, подход D20, лестница F10) в просадке BTC
за 1 ч (≤ −0.21 %); стоп 2 %, трейл +1 %/откат 1 %, 4 ч; на стену не реагировать → история 189 сд. +$515 (PF 1.73, DD −$190),
запись 43 сд. +$272. Гладкий: ранний трейл 0.5/0.25 (DD −$105). Семья BTC 4 ч — короче под водой. Концентрация: 5 монет = 83 %.
Отвергнуто данными: тесные стопы, «съели 20 %», выход/трейл/безубыток по снятию стены, шорт.

**Требование владельца (В-88, 23.09):** «широкий стоп 2 % ставить нельзя — плотность проест и выстрел размажет»,
«3 % уже много с плечом», «перестраховаться от прокидов 3–5–10–15 %», «даже если за 3 месяца не поймаем — депозит может
сложиться в следующем» → защита по построению: лот от худшего случая, потолок экспозиции, выключатель (BTC/дневной убыток),
стоп на бирже, исключение монет с прокидами; числа — владельца. Прокиды часты (15 мин ≥ 10 %: 40 раз на истории, AKE −53 %).

**Следующий шаг (владелец не ответил на «запускаю?»):** стресс-тест обвала 10–11.10.2025 + симуляция портфеля (лот от
риска, потолок, выключатель) → худший день депозита; выход `eat80/eat100` и выключатель BTC; честный рыночный выход (проход по
стакану). Потом: август (~65 ГБ, 4.5 ч), двойник `.200/.50`, предрегистрация кандидата, G10 шаг 3 (общая фаза удержания).

**Код 23.09:** выходы `gone<W>tr<T>` (`58e63d7`) и `gone<W>be|bex` (`e2f310c`), память кругов `--round-memo` (`ce1b394`,
гейт 84/84 байт в байт; выигрыш 0–14 %); тесты 972, clippy 0. Грабли: Bash-heredoc ест `\` (правки — Edit/Write);
`src/commands/lob/backtest.rs` в CRLF (замены python-скриптом по LF не находят — Edit); вход в `oos-frozen` — `BIN=` для новых
форм выхода; пустой каталог результатов гейта = провал, не «OK».

**Ждут владельца:** числа защиты; август сейчас; microSD 1 ТБ на дек; удаление `.200` с коллектора; мусор в корне репо
(`*.req/*.out`, `req.py` от сторонних моделей — не наш, не трогали).

## Где правда

`CLAUDE.md` · `docs/plan/{handoff-2026-09-20,PLAN,SETTLED,dev-plan-2026-09-17}.md` · `docs/COMMANDS.md` ·
`docs/findings/` · `.autopilot/2026-09-11-lob-density-ed3/` (тикеты, `interfaces.md`) ·
`C:\Users\Георгий\.dsh\PROFILE-STATE.md` (окружение DSH).

**Структура доков и правило артефактов (2026-09-17, коммит `270e0a4`):** `docs/` = `ARCHITECTURE.md`,
`COMMANDS.md`, `plan/`, `adr/`, `findings/` (живое) и `archive/` (заменённое); механизм пишет результат
по одному адресу из таблицы в `CLAUDE.md` §«Доки», сырое и промежуточное — в `data/`, **состояние
проекта — только в `CLAUDE.md`**. Заменяющее переносит заменённое в `docs/archive/` тем же коммитом
и правит ссылки; молча не удаляем. Чистка в два раунда (17.09): **88 файлов** в архив, `docs/findings`
30 → 20, живых тикетов `.autopilot` 47 → 13; ссылки в doc-комментариях `src/` обновлены, ворота после
этого зелёные (782 теста).

## Каталог страниц

- [projects\active\alpha.md](projects/active/alpha.md) — проект: передача, состояние, история.
- [skills/](skills/) — [ovh-vps-singapore.md](skills/ovh-vps-singapore.md) (сервер, тюнинг, профиль),
  [dsh-task-board.md](skills/dsh-task-board.md) (доска задач DSH, снята 15.09),
  [typesafe-ai.md](skills/typesafe-ai.md) (скилл TypeSafe/Jev: **корни скиллов DSH — `~/.agents/skills`,
  а НЕ `~/.claude/skills`**; скилл перенесён туда 21.09; батарея 21.09 — API работает правильно:
  контракт без ошибок, choice/noul 11/12 на своих метках, Brier 0.045, батч в 4× дешевле,
  неверный `type` даёт 400 (доки: 422); проверять через Node, PowerShell TLS на хосте не поднимает).
- [log.md](log.md) — таймлайн. [MEMORY.md](MEMORY.md) — протокол. [SOUL.md](SOUL.md) — рамка.
