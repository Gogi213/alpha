# Memory Index — alpha

Инжектится в каждую сессию: здесь состояние, не каталог (бюджет boot-блока 8000 символов).

## Состояние (2026-09-21, вечер — **F8b закрыт (В-79), машинный гейт пройден; следующий шаг F10 — числа владельца**)

**Где мы.** Последняя строка аудита этапа F (**F8b**) закрыта двумя коммитами: `8cdeb38` (код+тесты+
F10-скрипт) и `33bfba4` (результат машинного гейта). Сборка **953 / 0 / 8 ignored**, clippy
`-D warnings` 0, fmt 0. Перенос на счётную (`alpha-sync.timer`) **включён и работал** — но там
**3.1 ГБ свободно при 3.3 ГБ/сутки**: перенос 09-21 и ночь упрутся в диск (детали — ниже).

**В-79 (владелец: 1 с).** `strategy::CANCEL_WAIT_NS = 1_000_000_000` — потолок ожидания подтверждения
отмены **одним механизмом** на `CancelPending` (вход) и `ExitCancelPending` (лимитка выхода). Вторая
половина механизма — **повтор снятия** на каждом шаге фазы (`StrategyState::cancel_open`: нога
`New`/`PartiallyFilled` **без запроса в полёте**). Повтор — не украшение: настоящая причина зависания
в том, что нога, чей запрос **постановки** летел в момент снятия входа, не снималась ни разу и
оставалась в рынке навсегда (фаза «отмена летит» ждала её вечно). Срабатывание называется, а не
прячется: `EntryCancelReason::CancelTimeout`, `StrategyState::exit_cancel_timeouts`, колонки
`n_entry_cancelled_cancel_timeout`/`n_exit_cancel_timeout` в `forms.csv`; круг освобождается тем же
правилом, что при подтверждённой отмене (позиция → `Holding`, иначе `Idle`).

**Тесты F8b (+10, всего 953):** потолок входа; повтор ноги с летящей постановкой (причина `Ttl`, не
`CancelTimeout`); потолок выхода (ровно одно срабатывание); гонка отмены/исполнения **на выходе** (Р3);
агрегат F7 (`ExitTally.eaten_by_trades`/`wall_gone`) в драйвере (Р4); строка `forms.csv` **по именам
колонок** (Р4 — `forms_row` вынесена из `write_day`); вердикт с **частью** колонок причин; границы
`EntryLadder` (`MAX_ENTRY_LEGS`, `weighted_avg_tick`) и лестница на всю ёмкость через `bounce_plan`
(С11). Приём тестов Р2/Р3: состояние «отмена летит» собирается полями, а незавершённая отмена — моделью
задержки `ExecLatency { place_ns: 1 мс, cancel_ns: 5 с, taker_ns: 1 мс }`; заполнение лимитки в гонке —
сделкой **выше** цены заявки (`Ordering::Less` в крейте исполняет безусловно, очередь не мешает).

**Байтовый дефект, пойманный гейтом:** `Iterator::sum` для `f64` стартует с `-0.0` — пустая сумма
печаталась `-0.000000` вместо прежнего `0.000000`; `sum_net_bps` складывает `fold(0.0, …)`.

**Машинный гейт «те же байты» — ПРОЙДЕН** (21.09 14:05–14:18Z, счётная машина `13.140.29.171`):
новый бинарник собран из этого дерева в `src-head` (`PATH=/root/.cargo/bin`, тулчейн 1.93.1, 1 м 09 с) →
`bin/alpha-8cdeb38`; `bin/f3-queue-gate.sh bin/alpha-8cdeb38 bin/alpha-2ee7d02` на наборе базы (RTT В-68,
лот от пула, 3 потока, `--h3-mode notional --h3-usd 10000 --min-flow-pct 100`, 1000BONK/1000PEPE/AAVE/
ADA/ATOM × 09-16…18) — **все 12 прежних колонок `rounds.csv` (32 круга) и все 22 колонки `forms.csv`
(60 строк) совпали**, срабатываний потолка нет (27 крестов пути 3). Протокол — `study/f3-gate-f8b.log`
на счётной; отчёт — `audit-stage-f-2026-09-21.md` §7.

**F10-скрипт (дыра закрыта):** `tools/compute/nightly-grid.sh` прокидывает `SIGNAL`/`ENTRY_FORMS`/
`ENTRY_TTL`/`BAND_EXIT_BPS`/`TOUCHES_FROM`/`REGIME_FROM` (умолчания — прежний круг: `touch`/`single@fr`/
`touch`/без полосы, гейт цел; `bash -n` на коллекторе ок). Наборы `approach-*` собираются окружением
одним процессом; для исторических суток `TOUCHES_FROM=study/approaches/D<d>` (там же рядом
`touches-<SYM>.csv`). Скрипт **задеплоен на счётную** (`/opt/alpha-compute/bin/nightly-grid.sh`, 755).

**Грабля ночи (найдена и починена 21.09, ночи 20.09 и 21.09 02:00Z потеряны).** Гейт «сетка предыдущей
ночи ещё идёт» смотрел `systemctl list-units "alpha-grid-*" | grep running` — под шаблон попадал **сам
`alpha-grid-nightly.service`** (скрипт исполняется как этот юнит, он в этот момент `active/running`) и
его таймер, поэтому **таймерная ночь объявляла занятой саму себя** и выходила с кодом 1 (юнит в
`systemctl --failed`); ручной пуск не из юнита работал — это и маскировало баг. Теперь гейт берёт
**только юниты сеток** (`alpha-grid-nightly-*`, их создаёт `run-grid.sh`), имя вырезается `grep -o`
(у failed-юнитов строка начинается с `●`, нумерация полей `awk` на них съезжает). Проверено на машине:
сейчас пусто, работающую сетку ловит, failed-юнит и сам `alpha-grid-nightly.service` — нет.
**Следующая ночь 22.09 02:00Z должна пройти; проверить утром `tail -3 study/ALERTS.log`.**

**Следующий шаг — F10 (предрегистрация + прогон). Числа НЕ назначены, ждут владельца:**
`D` (полоса взвода, кандидаты 10/20/30/50 по M15 — правка владельца 20.09: «ставить заранее» это
D=30–50), `ENTRY_FORMS`/`ENTRY_TTL`/`BAND_EXIT_BPS` (полоса ног 0,1–0,2 % при D=20, M16),
`X`/`W` для `eat<X>`/`gone<W>` (кандидаты 5/20/30/50 % из M17), `QUEUE_MODEL=prob:<n>`. Затем **F11**
(вердикты, `equity-report.py`, деньги, доля выходов eaten/gone) и **F12**.

**Осталось из аудита (не блокирует F10):** С1 `TradePlan::Bounce` (24 поля) → подструктуры; С4/С5
`levels.rs` (386 строк, вложенность 8); С7 `drive_day` (вложенность 9); С10 `Box::leak` ×3; С12–С14
(python). **Хвосты машин:** в корне репо чужие `*.req/*.out` (deepseek/kimi) — не коммитить.
**Правило сборок:** одна на партию, `-j 2`.

**Перенос на счётную (`alpha-sync.timer`, 00:45Z) — включён и работал:** 21.09 00:45:42Z сутки 09-20
ушли целиком (100 файлов, 3.5 ГБ, ~6 мин, `status=0`; лог `/opt/alpha/sync/<день>.log`).
**Перекачаны полностью: 09-16, 09-17, 09-18, 09-19, 09-20** — по каждой 100/100 символов пула,
размеры совпадают с коллектором (3.9 / 2.7 / 3.6 / 3.1 / 3.3 ГБ), маркеров `verify-*.status` 100.
**Не перекачаны: 09-15** (первые неполные сутки нового сервера, 545 МБ — синк стартовал 18.09) и
**09-21** (пишутся; поедут 22.09 00:45Z). Догнать: `sudo tools/sync-to-compute.sh 2026-09-15`.
**Места почти нет:** счётная 96 % — **3.1 ГБ** свободно ⇒ перенос 09-21 в 00:45Z упрётся в диск
(ночной сетке нужно ещё ~0.6 ГБ/сутки на касания + `b5/`).
Мусор на счётной под чистку (решение владельца): `study/touches2..6` 4.6 ГБ (итерации 18–19.09),
`study/levels-v66` 1.1 ГБ, `study/db-probe` 66 МБ → ≈5.8 ГБ; рабочие — `study/touches/<сутки>` 2.2 ГБ
(09-16…09-19; **09-20 не посчитан** — ночь пропустил баг гейта) и `study/approaches/D10…D50` 8.1 ГБ
(нужны F10). **Коллектор 84 % (7.5 ГБ, ~2.3 суток)** — `root/` 36 ГБ, 3.0–3.6 ГБ/сутки.

## Состояние (2026-09-21, вечер — **ревью закрытия этапа F: блокеров нет; следующий шаг F8b → F10**) — история

**Где мы.** Этап F закрыт по коду (F1–F9 + F7/F8 21.09). Вечером 21.09 по просьбе владельца —
**проверка качества сделанного**: сборка сверена (**943 / 0 / 8 ignored, clippy 0, fmt 0**), два ревьюера
по `git diff 780c9fc..8c95dd2` (стратегия/горячий путь; сетка/вердикт/скрипты). **Вердикт: блокеров нет**
— `649b3de` тождественный перенос `on_event`, `plan_grid`/`open_outputs` без смены логики, сторона F7 и
`level_ok` верны, семь запретов горячего пути не нарушены. Отчёт — `docs/findings/audit-stage-f-2026-09-21.md`
**§6** (Р1–Р4, смеллы, актуальные адреса хвостов, порядок).

**Закрыто вечером:** Р1 — `ExitForm::label` печатал `{:.0}` (`eat33.7` → имя `eat34` при пороге 33.7 %) →
`format!("eat{pct}")` как у `pct{x}`; тесты `eat33.7`/`gone12.5`/`gone-5`. TypeSafe проверен после
перезапуска (ключ в Process scope, живой `200`; поля `instructions`/`criteria`) — `skills/typesafe-ai.md`;
память DSH закоммичена `05d895d`.

**Следующий шаг — F8b** (полдня, строка в `dev-plan-2026-09-20.md` §0а): Р2+В5 — потолок ожидания
подтверждения отмены **одним механизмом** для `CancelPending` и новой `ExitCancelPending` (без него круг
висит до конца суток — то, ради чего делался Б1; число — измеренное `cancel` p95 В-68 × запас или
владельца, не изобретать); Р3 тест гонки отмены/исполнения на выходе; Р4 сквозной тест
`ExitTally.eaten_by_trades/wall_gone`; тест вердикта с частью новых колонок; С11 границы `EntryLadder`;
гейт «те же байты». Потом **F10**: **дыра** — `nightly-grid.sh` не передаёт `--signal approach`/
`--entry-form`/`--entry-ttl-secs`/`--band-exit-bps` (только `EXIT_FORMS`/`QUEUE_MODEL`/`APPROACH_BPS`),
наборы `approach-*` в ночи требуют правки скрипта; исторический прогон 16–19.09 — `--touches-from
study/approaches/D<d>` (проверить наличие `touches-<SYM>.csv` рядом). Затем F11, F12.

**Смеллы в С-список (§6):** `the_none_exit_form_ignores_the_wall_trades` проходит и без раннего `return`;
`level_qty_at_entry` — из плана, не живой размер на входе (комментарий); С1 `TradePlan::Bounce` — **24 поля**;
С10 `Box::leak` в трёх точках. Хвосты машин — как в блоке ниже (форк, диски, `*.req/*.out` в корне).

## Состояние (2026-09-21, день — **F7 и F8 сделаны, этап F закрыт по коду**; HEAD `b89f565`) — история

**Где мы.** Этап F (`docs/plan/dev-plan-2026-09-20.md`, таблица — §0а) закрыт по коду: F1–F9 сделаны
ранее, 21.09 добавлены **F7** (выход «съели/сняли», Б-75) и **F8** (ось `--exit-form`, колонки, вердикт).
Дерево чистое, тесты **943 / 0 / 8 ignored**, clippy `-D warnings` ноль, fmt ноль.

**Что сделано 21.09 (коммиты):**
- `50869bf` — **F7+F8** (один коммит: слой команд `PlanShape` нужен F7 для сборки).
  **F7:** `ExitReason::EatenByTrades`/`WallGone`, `TradePlan::Bounce::{exit_eat_pct, exit_gone_pct}`,
  `StrategyState::observe_wall_trades` — правило «что считать ударом в стену» живёт в стратегии
  (у бид-стены агрессор-продавец, у аск-стены покупатель, только цена уровня), а **буфер `last_trades`
  и его очистка — у драйвера** `run_round` (счёт ровно один раз на шаг, память не растёт с длиной
  круга); при `none` прохода по буферу нет — числа прежних прогонов не меняются. Порядок выходов:
  стоп → съедено всё (E7) → трейл → тейк → половина (E7) → `eat`/`gone` → прилипание → дедлайн.
  **F8:** ось `--exit-form none|eat<X>|gone<W>` (внешний множитель; `none` — прежнее имя формы, гейт),
  `forms.csv` + `n_eaten_by_trades`/`n_wall_gone`/`mean_fill_frac`, шапка + `exit_forms=`; вердикт
  читает колонки причин **по наличию** (артефакты до F7 читаются как прежде: нет колонки — ноль),
  печатает `n_*`/`share_*` для девяти причин (`exit_column`: `deadline` → `timeout`); `f3-queue-gate.sh`
  гоняет новый бинарник с явными прежними осями `--signal touch --entry-form single@fr
  --entry-ttl-secs touch --exit-form none`. Тесты: 5 F7 (печать, накопление 3+3, снятие без сделок,
  падение сделками ≠ `gone`, `none`) + 4 F8 (разбор форм, ось/имена, колонки/шапка, прежние имена).
- `1f02580` — ночь: ось выхода включается окружением `EXIT_FORMS` (умолчание `none`).
- `b89f565` — `CLAUDE.md` §«Состояние» и счётчик тестов 943.

**Ловушка, найденная тестами (помнить):** буфер `bot.last_trades` крейт пишет только при
`last_trades_capacity > 0`; его ставит `build_backtest` (`LAST_TRADES_CAPACITY = 64`), а самодельный
`seam6_backtest` в тестах — нет. F7-тесты обязаны идти через `build_backtest`, иначе лента пуста и
`eat`/`gone` молчат (первая версия тестов именно на этом и упала).

**Следующий шаг — F10** (предрегистрация и ночь): строки `prereg` в `runs.csv` + `EXPERIMENTS.md`
(E15), окружение `EXIT_FORMS`/`QUEUE_MODEL=prob:<n>`/`APPROACH_BPS`, прогон по уже снятым суткам
16–19.09 (`study/approaches/D10…D50`). **Числа `X`/`W`, `prob:<n>`, потолок ttl — предрегистрация, не
назначение.** Затем **F11** (вердикты, `equity-report.py`, деньги, доля выходов eaten/gone) и **F12**.

**Машинный гейт не гонялся:** «те же круги» на 5 монетах × 3 суток (`bin/f3-queue-gate.sh`) — на
счётной машине; фикстурный гейт (`risk_adverse_queue_model_keeps_the_old_bytes`) зелёный.

**Осталось из аудита (не блокирует F10):** С1 `TradePlan::Bounce` (теперь 23 поля) → подструктуры;
С4/С5 вложенность `levels.rs::observe_frame_with_approaches`; С7 `drive_day`; В4–В7; С10–С14.

**Правило сборок (урок 21.09, владелец):** одна сборка на партию правок, `cargo … -j 2`.

**Хвосты по машинам:** форк крейта пересобрать из архива с `516c476` и перезапустить гейт; диск
счётной 91 % — чистить `src*/target`; диск коллектора 72 % — **решение владельца**; в корне репо лежат
чужие `*.req/*.out` (deepseek/kimi) — не коммитить.

**Открыто у владельца:** тейкерская форма входа (В-72 — пока нет); диск коллектора; `D`/`X`/`W`/
`prob:<n>`/потолок ttl — предрегистрация.

## Состояние (2026-09-21, ночь — **аудит этапа F закрыт по блокерам**, передача в новый чат; HEAD `649b3de`) — история

**Где мы.** Этап F (`docs/plan/dev-plan-2026-09-20.md`, таблица — §0а) выполнен на 7 из 12 задач исполнителем
20.09; 21.09 по просьбе владельца проведён **аудит** (`docs/findings/audit-stage-f-2026-09-21.md`: три ревьюера +
метрики функций/файлов + дизайн-ревью) и **закрыты его блокеры и часть смеллов**. Дерево чистое, тесты
**934 / 0 / 8 ignored**, clippy `-D warnings` ноль, fmt ноль. Ночь 21.09 02:00Z идёт прежним бинарником
`alpha-3e1f587` (`QUEUE_MODEL=risk-adverse` в скрипте) — не задета.

**Что закрыто 21.09 (три коммита):**
- `a89e8f1` — **Б1** остаток позиции под частично исполненной лимиткой тейка ведётся по плану
  (`strategy.rs`: `decide_exit`/`submit_exit` вынесены из `on_event`, `ExitPending` решает рыночные причины
  для остатка, новая фаза `ExitCancelPending` снимает лимитку, остаток — тейкером через `Holding`; тест
  `a_partially_filled_take_is_cancelled_and_the_rest_is_stopped_out` под `prob`); **Б2**
  `tools/compute/approach-capacity.py` считает пересечение спреда по стороне (раньше — только бид);
  **Б3** `fill-capacity --targets approaches` с `eaten=` — отказ (+ тест); В3 одна `touch_view_of_approach`;
  С8 `--queue-model` типизирован (`QueueModelKind: FromStr`); С9 `dead_code` сняты; `COMMANDS.md` —
  флаги F6 (`--signal`, `--entry-form`), примечание про подходы в `.done`-сутках ночи; форк — формулировки.
- `829da16` — **M16 перегнан** на машине починенным читателем: вывод «ноги 0,1–0,2 % при D=20» держится
  (24–25 % «хоть что-то» на взводе, 45 % при жизни 30 мин у 0,1–0,15 %; у 0,15–0,2 % 70 % ног в момент
  взвода выше рынка); `approach-capacity-2026-09-20.md` §3 и план §0а обновлены.
- `649b3de` — **С3** `on_event` разложен по функции на фазу (`on_idle/on_entry_pending/on_holding/
  on_exit_pending/on_exit_cancel_pending/on_cancel_pending`, диспетчер 27 строк); **С6** `run_bounce_grid`
  532 → 286 строк (`plan_grid` — разбор флагов в `GridPlan`, `open_outputs` — артефакты). Логика не менялась.

**Осталось из аудита (не блокирует F8):** С1 `TradePlan::Bounce` 21 поле → подструктуры вход/выход/срок
(35 мест в коде и тестах — делать вместе с F7, когда добавятся формы выхода); С4/С5 вложенность в
`levels.rs::observe_frame_with_approaches` (386 строк, вложенность 8); С7 `drive_day` (вложенность 9);
В4 инвариант «крейт не удаляет ордера из карты» (тест/инкрементальный учёт); В5 потолок `CancelPending`;
В6 буфер `exits` в `run_round`; В7 золотой дамп драйвера; С10–С14 (`Box::leak` по оси форм, границы
`EntryLadder`, `equity-report.py --out` умолчание, `eaten-threshold.py::main` 380 строк, smoke у скриптов).

**Следующий шаг — F8** по плану (сетка и вердикт под новые формы): свести флаги, колонки, сводный гейт
«те же байты» (`--signal touch --entry-form single@fr --entry-ttl-secs touch --exit-form none
--queue-model risk-adverse --no-post-only` против золота `bounce_grid/golden/`), расширить
`tools/compute/f3-queue-gate.sh`; затем **F7** (выход `eat<X>`/`gone<W>` по ленте — вместе с С1),
**F10** (предрегистрация: D ∈ {10,20,30,50}, ноги 0,1–0,2 %, X ∈ {5,20,30,50} %, потолок ttl
{60,300,1800}; прогон по 16–19.09 с `QUEUE_MODEL=prob:<n>`), **F11** (вердикты + деньги), **F12**.

**Правило сборок (урок 21.09, владелец):** одна сборка на партию правок, `cargo … -j 2`; пять сборок подряд
ради одного теста при соседней сессии — CPU 100 %. Отлаживать на уже собранном бинарнике
(`target-ci/release/deps/alpha-*.exe <тест> --nocapture`).

**Хвосты по машинам:** форк крейта на счётной машине пересобрать из архива с `516c476` и перезапустить
гейт F3–F5 (`bin/f3-queue-gate.sh`); диск счётной 91 % (6,5 ГБ) — чистить `src*/target`; диск коллектора
72 %, ≈2 суток — **решение владельца** (чистить `deep/` и старые `.50` после переноса или расширять);
в корне репо лежат чужие `*.req/*.out` (deepseek/kimi) — не коммитить.

**Открыто у владельца:** тейкерская форма входа (В-72 — пока нет); диск коллектора; `D`/`X`/потолок —
предрегистрация, не назначение.

## Состояние (2026-09-20, ночь — **ОСТАНОВКА по просьбе владельца**; HEAD `6a34a4a`) — история

**Где мы:** этап F (`docs/plan/dev-plan-2026-09-20.md`, таблица состояния — §0а того же файла) выполнен
на **7 из 12 задач**. Дерево чистое, всё закоммичено, тесты **933 passed / 0 failed / 8 ignored**,
`clippy --all-targets -D warnings` — ноль, `fmt --check` — ноль. Следующая сессия начинает с **F8**.

**Закрыто (коммиты, каждая задача — свой):**

| задача | коммит | суть |
|---|---|---|
| F1 сигнал подхода | `2ee7d02`, `92e2e39`, `966b085` | `ApproachRecord` в трекере уровней; `lob touches --approach-bps D [--approach-min-age-secs A]` → `approaches-<SYM>.csv`; замер D (M15) |
| F2 ёмкость по подходам | `59eb55a`, `fa44231` | `fill-capacity --targets approaches` (цель = окно взвода `arm_ms…disarm_ms`); артефакт M16 |
| F3 очередь по объёму | `62cde09` | `QueueModelKind { RiskAdverse, Prob{n} }` + `--queue-model` (обязательный), `Fill`: `fill_frac/entry_vwap/legs_filled/fill_by_cross`, колонка `n_fill_by_cross`, три пути исполнения |
| F4 частичная позиция | `97ced03` | размер и средняя — по ордерам крейта (`qty − leaves_qty`; `Bot::position` частичного не видит); пост-онли **умолчанием** (`--post-only`/`--no-post-only`); `rounds.csv` + `fill_frac/entry_vwap/legs_filled/legs_rejected`, `forms.csv` + `n_rejected_postonly` |
| F5 срок жизни входа | `570d5e8` | потолок `--entry-ttl-secs touch\|wall\|60\|300\|1800` + снятие «стена снята» (размер < `--h3-usd / цена`) и «цена ушла» (`--band-exit-bps`); `n_entry_cancelled_*` |
| F6 лестница + сигнал подхода | `6a34a4a` | `EntryForm` (`single@fr` / `ladder<N>x<from>..<to>[w<k>]`), `EntryLadder` (фикс-массив ног), `--signal touch\|approach` (кэш подходов F1), имена форм на 4–5 полей (`FormFields` с `ttl`/`exit`) |
| F9 замер X («съели») | `b0122d2` | `tools/compute/eaten-threshold.py` + `docs/findings/eaten-threshold-2026-09-20.md` (M17) |
| F10-механика D-оси | `e8063c3` | `--approach-bps 10,20,30,50` **списком**: один реплей, файл на полосу (`approaches-<SYM>.csv` + `approaches-<SYM>-D<d>.csv`) |
| отчёт в деньгах | `33baa6c` | `equity-report.py` читает дампы (`--grid-dir/--form/--order-usd`), деньги по `fill_frac × order_usd` (В-78), обратная совместимость |
| форк крейта | `24c9f66` + **`516c476`** | `vendor/hftbacktest` + `[patch.crates-io]`, правка `PartialFillExchange` (`filled_qty >= leaves_qty`); **`516c476` обязателен**: без него форк был неполным (`.gitignore`-правило `data/` съедало модуль крейта → `E0583`) |
| доки/память по ходу | `3e1f587`, `39bd693`, `e8d524f`, `bd005ba`, `685daa6`, `0fde805`, `71cbe2b`, `0c44e44`, `bf907ea` | SETTLED В-72…В-78, COMMANDS (флаги F1–F5, отчёт), §0а плана, память |

**Числа замеров (не назначения):** полоса взвода **D ∈ {10, 20, 30, 50}** (D=20: 276 подходов/сутки к
стенам ≥ 45 мин, до касания 25,7 %, медиана ожидания 196 с; без пола возраста 1,6–3,3 млн/сутки → ночному
H3 нужен `--approach-min-age-secs 900`; владелец: узкий взвод сдвигает ноги на 10–15 bps ниже рынка);
ноги лестницы **0,1–0,2 %** при D=20 (21–24 % «хоть что-то», net 1ч +22…+26 bps), «в упор» 32–44 %
насквозь (В-77 подтверждён на подходах); `X` для `eat<X>` — **{5, 20, 30, 50} %** в окне, равном ttl
(M17: 20 % = 0,49 % по всей выборке в окне 300 с, 5,1 % где сделки были; окно 1800 с — 14,7 %/53,2 %);
markout 1 ч по B(300) падает монотонно (+23,9 → −5,3 bps), медиана положительна → `eat<X>` — защита
сценария, не улучшение среднего.

**ГЛАВНЫЙ БЛОКЕР, снятый по ходу (помнить при обновлении крейта):** в `hftbacktest 0.9.4`
`PartialFillExchange` роняет прогон `InvalidOrderStatus`, если нога исполнена сделкой **ровно до нуля**
(остаётся в карте биржи); воспроизведено на живых сутках (`prob:3`, падение на 12-й монете), после правки
тот же набор — 16 монет · 43 символо-суток · **567 кругов** без ошибок. Разбор —
`docs/findings/hftbacktest-partialfill-fix-2026-09-20.md`.

**Что делать первым в следующей сессии (по порядку):**
1. **F8** — свести F3–F7 во флаги/колонки `bounce-grid` (`--signal`, `--entry-form`, `--entry-ttl-secs`,
`--exit-form`, `--queue-model`), `forms.csv` + `n_partial/mean_fill_frac/n_rejected_postonly/
n_entry_cancelled_*/n_exit_eaten/n_exit_gone/n_fill_by_cross`, `rounds.csv` + `reason`; **сводный гейт**:
`--signal touch --entry-form single@fr --entry-ttl-secs touch --exit-form none --queue-model risk-adverse`
(+ `--no-post-only`) — прежние колонки `rounds.csv`/`forms.csv` построчно против «золота»
`src/commands/lob/bounce_grid/golden/`; расширить `tools/compute/f3-queue-gate.sh` под эти флаги.
2. **F7** (если F8 потребует) — выход `eat<X>`/`gone<W>` по ленте (`bot.last_trades`): «съели» =
сделки в стену с входа / размер ≥ X %, «сняли» = размер упал без сделок; исключение практиков
«развитие уже идёт» не кодировать.
3. **F10** — предрегистрация `runs.csv` (kind=`prereg`) по наборам `approach-a45-bid`,
`approach-a45-bid-b4h-neg` (H1), `approach-a15-bid`, контроль `touch-a45-bid`; D ∈ {10,20,30,50};
ttl {wall, 300, 1800} × стоп {pct1, pct2} × тейк 1to1 × дедлайн {3600, 7200} × выход {none, eat<X₁>,
eat<X₂>, gone50}; число испытаний — в `EXPERIMENTS.md` (E15, главная гипотеза: лестница на подходе,
лонг после просадки BTC, `eat<X₁>`); прогон по **уже снятым** суткам 16–19.09 с `QUEUE_MODEL=prob:<n>`
(ночь 02:00Z даст новые сутки сама).
4. **F11** — вердикты, `equity-report.py`, разбор по монетам/дням, доля кругов по путям исполнения,
доля выходов `eaten/gone`, сравнение с прежними «+$162» (тейкерскими).
5. **F12** — CLAUDE.md §«Состояние», SETTLED (если появятся решения), память.

**Открытые технические хвосты:** (1) машинный гейт для F6/F8 не гонялся (скрипт не знает `--entry-form`);
(2) форк пересобрать из архива с `516c476` и перезапустить гейт; (3) **диск счётной машины 91 %**
(6,5 ГБ) — почистить `src*/target` перед сборками; (4) диск коллектора `139.99.91.22` — 33/49 ГБ (72 %,
≈2 суток) — решение владельца (чистить `deep/` или расширять) ждёт.

**Что уже лежит на счётной машине:** `study/approaches/D10…D50` (4 суток × 89 монет, D=50 снят 19:52Z),
`study/capacity/v72` (M14), `study/touches/<сутки>`, бинарники `bin/alpha-2ee7d02` (F1),
`bin/alpha-3e1f587` (F2/ночь), `bin/alpha-b0122d2` (F3), `bin/alpha-patched` (форк), `bin/alpha-0fde805`
(сборка **упала** — неполный форк; пересобрать после `516c476`). Ночной скрипт обновлён (подходы H3-шагом
+ `QUEUE_MODEL=risk-adverse`); ночь 20.09 02:00Z на прежнем бинарнике — новых суток после неё не проверялось.

## Состояние (2026-09-20, вечер — до остановки) — история


**Этап F (`docs/plan/dev-plan-2026-09-20.md`) — состояние в §0а того же файла.** Сделано и закоммичено
вечером 20.09 (каждая задача — свой коммит, тесты/clippy/fmt зелёные):

- **F1 закрыта:** `ApproachRecord` в трекере + `lob touches --approach-bps D [--approach-min-age-secs A]`
  → `approaches-<SYMBOL>.csv` (`2ee7d02`, `92e2e39`, `966b085`). Замер D (`docs/findings/approach-signal-2026-09-20.md`,
  M15, 4 суток × 89 монет, D ∈ {10,20,30}): без пола возраста 1,6–3,3 млн подходов/сутки (94–96 % — смерть
  уровня в медиане 2–3 с, до касания 3,7–6 %); с полом В-71 ≥15 мин — 1,7–2,7 тыс./сутки; ≥45 мин —
  214–276/сутки. **Полоса для F10: D = 10–20 bps** (D=20 — 276/сутки, до касания 25,7 %, медиана 196 с;
  D=10 — 266/сутки, 20,8 %, 140 с). **Ночному H3 нужен `--approach-min-age-secs 900`**, иначе 5,5 ГБ
  сырья на три прогона. Гейт «касания те же байты» — md5 `touches-1000BONKUSDT.csv` совпал на живых
  данных (`alpha-712d214` = `alpha-2ee7d02`, 1142 касания).
- **F2 закрыта:** `lob fill-capacity --targets approaches` (`59eb55a`, `fa44231`) + артефакт
  `docs/findings/approach-capacity-2026-09-20.md` (M16). При D=20, слот `t0` (постановка на взводе):
  ноги 0,1–0,15 % — «хоть что-то» 21 %, целиком 7 %, условный net 1ч +22,1 (n=215); 0,15–0,2 % — 24 %/1 %,
  +26,1; 0,05–0,1 % — 6 %/2 %; «в упор» 1 %. **При выборке очереди стена держит 98–99 % в полосе
  0,05–0,2 % и лишь 32–44 % «в упор» — В-77 подтверждён на подходах.** Рабочая полоса ног для F6:
  **0,1–0,2 % при D=20**.
- **F9 закрыта:** замер X («съели») — `docs/findings/eaten-threshold-2026-09-20.md` (M17, `b0122d2`):
  в стену не продают вовсе у 77 % касаний; квантили доли съеденного в окне 300 с — 0,49 %/18,1 % (все),
  5,1 %/31,2 % (где сделки были), 3,2 %/30 % (семья a45); окно 1800 с — 14,7 %/53,2 % (сюда попадают
  цитаты {20, 50}). Markout 1 ч по B(300) падает монотонно (+23,9 → −5,3 bps), медиана положительна →
  `eat<X>` — защита сценария, не улучшение среднего. «Съели»/«сняли» по этим данным **не разделяются**
  (нужен размер стены по кадрам — вход в F7).
- **F3 закрыта** (`62cde09`): `QueueModelKind { RiskAdverse, Prob{n} }` + `fill_frac`/`entry_vwap`/
  `legs_filled`/`fill_by_cross` в `Fill`, колонка `n_fill_by_cross` и шапка `queue=/paths=` в `forms.csv`,
  обязательный `--queue-model` (умолчания в коде нет; в `run-grid.sh` — `QUEUE_MODEL` по умолчанию
  `risk-adverse`). Гейт: фикстура (все 22 прежние колонки + тело `rounds.csv`) и **машина** (5 монет × 3
  суток, риск-адверс: `rounds.csv` побайтово, 4 круга; сильнее — набор a45: см. лог сессии). Находка для
  F4: `Local` крейта обновляет `Bot::position` только при `status == Filled` — позицию считать из
  `orders[]` (`qty − leaves_qty`), иначе частичный вход не даёт круга.
- **F4** (частичная позиция + пост-онли, умолчание пост-онли с `--no-post-only` для гейта) — в работе;
  **F5–F8, F10–F12 не начаты**. Машинный скрипт гейта `tools/compute/f3-queue-gate.sh` требует правки:
  на счётной машине HYPE/DRAM/NEAR/LTC не `ok` по маркеру (рабочий список — первые монеты
  `study/touches/<сутки>/symbols.txt`, напр. 1000BONKUSDT 1000PEPEUSDT AAVEUSDT ADAUSDT ATOMUSDT).

**Обновление (20.09, поздний вечер): F4 и F5 закрыты, форк крейта.**

- **F4** `97ced03`: частичная позиция (средняя цена и размер — по ордерам крейта, `qty − leaves_qty`;
  `Bot::position` частичного исполнения не видит), пост-онли умолчанием (`--post-only`/`--no-post-only`),
  `rounds.csv` + `fill_frac/entry_vwap/legs_filled/legs_rejected`, `forms.csv` + `n_rejected_postonly`.
- **F5** `570d5e8`: срок жизни входа — потолок `--entry-ttl-secs touch|wall|60|300|1800` плюс снятие
  «стена снята» (размер ниже `--h3-usd / цена`) и «цена ушла» (`--band-exit-bps`); гейт — `touch`.
- **Форк крейта** `24c9f66`: `vendor/hftbacktest` + `[patch.crates-io]`; в `PartialFillExchange`
  0.9.4 нога, исполненная ровно до нуля, оставалась в карте биржи и роняла прогон
  `InvalidOrderStatus` — воспроизведено на живых сутках (20 монет × 3 суток, `prob:3`, падение на
  12-й монете), после правки тот же набор дал 16 монет · 43 символо-суток · 567 кругов, без ошибок.
  Разбор — `docs/findings/hftbacktest-partialfill-fix-2026-09-20.md`. **Помнить при обновлении крейта.**
- **Правки владельца 20.09 вечером** (`0fde805`): (1) полоса взвода — предрегистрация F10 берёт
  **D ∈ {10, 20, 30, 50}**: узкий взвод (D=20) сдвигает ноги 0,05–0,1 % на 10–15 bps ниже рынка,
  «ставить 0,05–0,1 % заранее» — это D=30–50; прогон D=50 запущен на счётной машине (19:05Z);
  (2) ночной `nightly-grid.sh` ставит `QUEUE_MODEL=risk-adverse` явно (флаг `--queue-model`
  обязательный, иначе ночь падает) и пишет подходы H3-шагом (`--approach-bps 20 --approach-min-age-secs 900`);
  (3) форк крейта записан в §0а плана.

**В работе:** F6 (лестница `ladder<N>x<from>..<to>` + `--signal approach`) — отдельный исполнитель;
машинный гейт F3/F4/F5 (`bin/alpha-0fde805`) поставлен в очередь после прогона D=50.


## Состояние (2026-09-20, утро — передача в новый чат) — история


**Читать первым:** `CLAUDE.md` (карта + §«Состояние») → этот блок → **`docs/plan/side-plan-2026-09-20.md`** (план по
сторонам: S10 → S9 → S8 → S6; §7 утро, §8 вопросы владельцу) → **`docs/plan/EXPERIMENTS.md`** (реестр: что пробовали,
какими значениями, что вышло; главная гипотеза; оговорка об исполнении) → `docs/plan/alpha-roadmap-2026-09-19.md` →
`docs/COMMANDS.md`. `.memory/` — общая память обоих исполнителей, digest в конце сессии.

- **Что это:** Rust-бот по плотностям стакана Bybit; одна стратегия `lob::strategy::on_event` для `Backtest` и `LiveBot`.
  **Живых ордеров нет**; курс владельца — найти альфу; торговый контур заморожен.
- **Где мы (20.09):** сделка-отскок от старой стены. **Лонг от бид-стены** (стена ≥ $10k, стоит ≥ 45 мин, вход
  лимиткой у первого фронтранера, стоп 2 %, тейк 1:1, выход по дедлайну) на 16–19.09 (три дня роста + флэт):
  116 сделок, 44 % побед, **+$162 на $1000-лоте** (по дням +78/+93/−26/+16), просадка −$42; **шорт от аск-стены** —
  134 сделки, 31 %, **−$234** (ни одной формы в плюсе). Главная гипотеза **H1** — лонг **после просадки BTC за 4 ч**
  (`a45-bid-b4h-neg`, `pct2-1to1-3600`): 60 сделок, 58 %, +$172, просадка −$16. **Удержание — главная ось**
  (E14): 60 с −$198 · 10 м −$131 · 30 м +$7 · 1 ч +$187 · 2 ч +$155 · **4 ч +$296**; стоп/тейк почти не срабатывают
  (83/89 выходов по дедлайну), тейк в % (`tk`) хуже 1:1 — закрыт. Вердикты: лонг «красный» (n ≥ 100, нижняя
  −0.75 < 0), остальные «мало данных»; зелёного нет. Все три дня роста + флэт — **дней падения не было**.
- **ГЛАВНОЕ ОГРАНИЧЕНИЕ (S10, владелец 20.09, первый приоритет) — шаг 1 закрыт 20.09 (`lob fill-capacity`,
  `docs/findings/fill-capacity-2026-09-20.md`, M14):** касание в записи длится **0,1 с в медиане** (стена = лучшая цена —
  финальный шаг), вход снимается в его конце; спред в момент касания один тик в 63 % → лимитка у фронтрана **равна
  лучшему аску**: из 116 кругов лонга `a45-bid` (`pct2-1to1-3600`) **89 (77 %) — тейкерские входы** (крейт исполнил
  как рыночную; комиссия тейкера в P&L учтена, но сделка другая), 18 — мейкерские без единой продажи на тике
  (зачтено исчезновение уровня), **9 (8 %) — с объёмом**. Лестница практиков «0,2–0,7 %» к этим стенам мейкером не
  ставится (аск за 5 мин до касания в 13 bps от стены, ≥ 70 bps у 1 %). Исполняются ноги **между стеной и рынком**
  при сроке жизни входа дольше касания: 5 мин — хоть что-то у 37–51 % касаний, целиком 11 %; 30 мин — 48–61 % /
  15–21 % (верхняя оценка — слот не видит смерти стены); размер $200–1000, сторона, режим на «хоть что-то» не
  влияют. **Все P&L до S10 — тейкерские входы в момент касания, не мейкерская сделка спеки.** Шаг 2 (следующий):
  `--entry-ttl-secs` (сетка {60, 300, 1800} или число владельца, плюс снятие при смерти стены), пост-онли, очередь по
  объёму (`ProbQueueModel`/`PartialFillExchange`), частичная позиция (стоп/тейк/дедлайн от средней цены); гейт —
  при «крест-модели» и `ttl = касание` те же круги. Шаг 3 — лестница `fr..mkt`/`0..mkt` (`3@20..70` через спред
  в 87–100 %). Инструмент: `lob fill-capacity` (сырьё `study/capacity/v72/`, читатель `bin/fill-capacity.py`).
- **S9 (владелец):** поведение в сделке после снятия плотности, лонг и шорт отдельно — «ждать дедлайна/стопа при
  снятой стене — маразм»; сейчас стратегия после входа на стену не смотрит (кроме `eat`-форм E7). Замер по кругам →
  `eat`-формы на лонгах → правило «снята → безубыток / выход при возврате» в `lob::strategy`.
- **Сделано 19–20.09 (код, всё с тестами и гейтами «те же круги байт в байт»):** `--side`, `--touches-from`
  (касания из CSV ночного H3, реплей был 83 % времени), сайдкар `<бинлог>.events`, `--set` (наборы фильтров одним
  процессом: `age=,flow=,side=,frontrun,eaten=,usd_min=,ret10m/ret1h/ret4h/pool1h/pool4h/btc1h/btc4h _min/_max`),
  `--regime-from`, `ret_10m/1h/4h_bps` + `mids1m-*.csv` в `lob touches`, `ref-klines.py` (BTC/ETH свечи по REST),
  `regime.py` (режим по минутам; **утечка** закрытия минуты касания закрыта `7cd3502`), `tk<x>`, `--deadline-secs`
  (1800/14400), `--order-usd`. Тесты 883. Ночь: `nightly-grid.sh` = H3 касания суток → режим → **41 набор одним
  процессом** (~21 мин) + `tk` + `dl` + E7, вердикты, `study/verdicts.csv`; **ночь громкая** — `study/ALERTS.log`,
  ОК/ПРОВАЛ, failed-юнит, `bin/alert-tg.sh` через `ALERT_CMD` (токен — владелец, `/etc/alpha/alert.env`; пока нет).
  Скрипты на машину — только `scp … .new` + `mv`. Отчёт в деньгах — `tools/compute/equity-report.py`
  (`docs/findings/equity-2026-09-20.html`; интервалы 95 % на сделку/в день).
- **Научный контур:** предрегистрация до данных (`runs.csv` 1405 строк, 1380 `bounce_form`), реестр
  `EXPERIMENTS.md` (E01–E14, M01–M13, правило prereg ↔ строка, вердикт ↔ результат), интервал (нижняя граница по
  кластерам), DSR/PBO; одна главная гипотеза на ночь (H1), остальное разведка; заморозку **не** делаем (владелец:
  эмулировать позже); утечки ищем (одна найдена и закрыта).
- **Машины:** коллектор `139.99.91.22` (`ubuntu`, `/opt/alpha`, диск 29/49 ГБ на 20.09, +6.5 ГБ/сутки → полон
  ≈ 22.09 — **решение владельца**: чистить `deep/` и старые `.50` после переноса или расширять); счётная
  `13.140.29.171` (`root`, `/opt/alpha-compute`, `bin/alpha` → `alpha-2214ed3`, 19 ГБ свободно). Таймеры: сверка
  00:20Z, перенос 00:45Z, ночь 02:00Z (`alpha-grid-nightly.timer`). Ночь 20.09 02:00Z **упала штатно** («сетка ещё
  идёт» — шёл ручной прогон 19-го, `ALERTS.log` строка 02:00:44Z), ручной прогон кончился «ОК» 02:09Z; failed-юниты
  сброшены. `chain-day.sh` — докачать сутки сразу после переноса (маркер — число файлов дня = маркеров verify).
- **Вопросы владельцу:** диск коллектора (≈ 22.09); `DAYS_WINDOW` (ночь растёт ~5 мин/сутки); Telegram для алертов.
- **План разработки (20.09, вечер) — `docs/plan/dev-plan-2026-09-20.md`, этап F, для младших моделей.** Решения
  владельца В-72…В-78: вход только мейкером пост-онли (тейкерская форма — только по его слову, пока нет);
  лучший вход — лимитка 0,05–0,1 % над стеной, поставленная **заранее на подходе** и живущая, пока стена жива
  («да я с тобой согласен, я так и делал»); в упор — нет (2/3 исполнений насквозь = отмена), первый тик над
  стеной — минус; отмена в позиции — по эффективности: «съели» (сделки в стену с входа / размер ≥ X %) или
  «сняли» (размер упал без сделок), прилипание/удар без съедания — сидим; переворотов нет; проедание —
  отдельный блок после ворот отскока. Задачи: F1 `ApproachRecord` в трекере (сигнал подхода на `D` bps) →
  F2 ёмкость по подходам → F3 `ProbQueueModel`+`PartialFillExchange` (`--queue-model`) → F4 частичная позиция и
  пост-онли в стратегии → F5 `entry_ttl` «стена жива ∧ цена в полосе» + потолок {60,300,1800} → F6
  `--entry-form ladder<N>x<from>..<to>` + `--signal approach` → F7 `--exit-form eat<X>|gone<W>` по ленте →
  F8 сетка/вердикт (гейт «те же круги» при старых флагах) → F9 замер X и D → F10 предрегистрация + ночь →
  F11 чтение и деньги → F12 доки/SETTLED/память. Свод практиков — `order-management-practitioners-2026-09-20.md`
  (таймаута у лимитки нет ни у кого; снимают по событиям; 60 % касаний a45-bid — смерть уровня без сделок).
  Замеры входа — `fill-capacity-2026-09-20.md` §9 (`leg-distance.py`, `leg-clear.py`, `clear_ms` в CSV).
- **Следующий исполнитель начинает с:** утро (§7 side-plan), затем **`dev-plan-2026-09-20.md` F1** (сигнал
  подхода) и F3 (модель очереди) — ветки независимы; порядок и гейты — там. Ночь 20.09 02:00Z пропущена
  штатно (шёл ручной прогон), новых суток не было; счётная машина — `bin/alpha` → `alpha-5a315d8`.

## Где правда

`CLAUDE.md` · `docs/plan/{handoff-2026-09-20,PLAN,SETTLED,dev-plan-2026-09-17}.md` · `docs/COMMANDS.md` ·
`docs/findings/` · `.autopilot/2026-09-11-lob-density-ed3/` (тикеты, `interfaces.md`) ·
`C:\Users\Георгий\.dsh\PROFILE-STATE.md` (окружение DSH).

**Структура доков и правило артефактов (2026-09-17, коммит `270e0a4`):** `docs/` = `ARCHITECTURE.md`,
`COMMANDS.md`, `plan/`, `adr/`, `findings/` (живое) и `archive/` (заменённое); механизм пишет результат
по одному адресу из таблицы в `CLAUDE.md` §«Доки», сырое и промежуточное — в `data/`, **состояние
проекта — только в `CLAUDE.md`**. Заменяющее переносит заменённое в `docs/archive/` тем же коммитом
и правит ссылки; молча не удаляем. Чистка в два раунда (17.09): **88 файлов** в архив, `docs/findings`
30 → 20, живых тикетов `.autopilot` 47 → 13; ссылки в doc-комментариях `src/` обновлены, ворота после
этого зелёные (782 теста).

## Каталог страниц

- [projects\active\alpha.md](projects/active/alpha.md) — проект: передача, состояние, история.
- [skills/](skills/) — [ovh-vps-singapore.md](skills/ovh-vps-singapore.md) (сервер, тюнинг, профиль),
  [dsh-task-board.md](skills/dsh-task-board.md) (доска задач DSH, снята 15.09),
  [typesafe-ai.md](skills/typesafe-ai.md) (скилл TypeSafe/Jev: **корни скиллов DSH — `~/.agents/skills`,
  а НЕ `~/.claude/skills`**; скилл перенесён туда 21.09; батарея 21.09 — API работает правильно:
  контракт без ошибок, choice/noul 11/12 на своих метках, Brier 0.045, батч в 4× дешевле,
  неверный `type` даёт 400 (доки: 422); проверять через Node, PowerShell TLS на хосте не поднимает).
- [log.md](log.md) — таймлайн. [MEMORY.md](MEMORY.md) — протокол. [SOUL.md](SOUL.md) — рамка.
