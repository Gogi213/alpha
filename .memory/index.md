# Memory Index — alpha

Инжектится в каждую сессию: здесь состояние, не каталог (бюджет boot-блока 8000 символов).

## Состояние (2026-09-17, вечер)

- alpha — Rust-бот по плотностям стакана Bybit: олвейс-он коллектор пула, разметка крупных
  уровней, профили по осям, вердикт бэктестом с моделью очереди.
- **Передача следующей сессии — `projects/active/alpha.md`, раздел «ПЕРЕДАЧА СЛЕДУЮЩЕЙ СЕССИИ»:
  читать первым.** Там: где встали, почему, что дальше по шагам, решения владельца, грабли сессии.
- План: `docs/plan/dev-plan-2026-09-17.md` (A база+коллектор → B бэктестер → C сигнал → D накрутка).
- **Сделано 17.09:** этап **A** целиком (A1–A6), **B1** = В-58 (предрегистрация 48 форм), **B2**
  (вход от первого фронтрана + `--stop-mode`), **половина B3** (длинные горизонты в `touches`,
  `--deadline-secs`), **долг аудита** (C3, C4, `export.rs`, замер линтов). **776 тестов**, clippy
  `-D warnings`, fmt; коммиты — в `log.md`, последние `94483e0`, `ae95958`, `48510c0`.
- **Где стоим:** B4 закрыт 17.09 (`b73cc07`), R1 (журнал испытаний) закрыт (`3582632`); B3-хвост
  (горизонты в `touch-profiles`) отложен **к C4**; **B5 (прогон-вердикт 48 форм) — следующий шаг**;
  **C1/C2 ждут чисел владельца — `N_abs`, `S%`**.
- **Аудит 17.09 и его исход (коммиты `3582632`, `b73cc07`):** ворота чисты (**782 теста**, clippy
  `-D warnings`, fmt). **R1 был двойным:** строка `kind=prereg` в `docs/plan/runs.csv` не только не
  имела варианта в `RunKind`, но и была записана без экранирования (запятые в `detail` → 6 полей
  вместо 4); журнал не читался целиком, `lob shortlist`/DSR падали. Починено: `RunKind::Prereg`
  (не испытание), строка закавычена, строка `amendment`, тест `the_repository_journal_reads` читает
  боевой журнал. **B4 сделан**: `--early-exit-secs {1,2,3}` («прилипание»), колонка `n_early`;
  на ZECUSDT выход хуже (−0.0909 / −0.0907 / −0.0937 против −0.0886 bps) — артефакт
  `docs/findings/early-exit-2026-09-17.md`. Хвосты R3–R7 и дрейф плана закрыты; открыто только
  машиночитаемое `trials=48`.
- **Дальше:** **B5** (48 форм на пригодных сутках, DSR по фактическому `N`, отчёт
  `bounce-backtest-<дата>.md`) → C1 → C2 → C3 → C4 (+B3-хвост); D1–D13 — только если вердикт не «нет».
- **Прод:** OVH `139.99.91.22` (ключ `id_rsa`, данные `sdb` → `/opt/alpha`), юнит активен,
  `NRestarts=0`, стоп файлом `stop` (В-41), пул **100 монет**; диск: 36 ГБ свободно,
  **6.5 ГБ/сутки** ⇒ ≈5.5 суток, **ведётся вручную — сторожей и порогов в коде нет** (решение
  владельца 17.09).
- **`verify` таймером (A2):** `alpha-verify.timer` 00:20 UTC → `tools/verify-day.sh` → маркеры в
  `/opt/alpha/root`, лог `/opt/alpha/verify/<день>.log`; 09-16 (вручную): **94 ok / 6 fail**.
  В-56 + V4: целостность ровно ноль **и ноль строк `gaps.csv`** по символу; «цена не держалась» —
  доля < 0.1 % сделок.
- **Ждут владельца:** деплой коллектора (K1/V5/V6/V12 в коде, в проде бинарник 09-15 22:47);
  политика V4 (ночь 09-17 пометит `fail` все 100 монет — одно переподключение `01:51:19Z`);
  `fsync`; `N_abs` и `S%`.
- **Сборка** на боевом (`sda`, `/home/ubuntu`); `/opt/alpha/alpha-verify` — ручные команды,
  `/opt/alpha/alpha-collector` — коллектор. Git: `master` = `dev/wave-4-record-verify`, push —
  разовым полным доступом.

## Где правда

`CLAUDE.md` · `docs/plan/{PLAN,SETTLED,dev-plan-2026-09-17}.md` · `docs/COMMANDS.md` ·
`docs/findings/` · `.autopilot/2026-09-11-lob-density-ed3/` (тикеты, `interfaces.md`) ·
`C:\Users\Георгий\.dsh\PROFILE-STATE.md` (окружение DSH).

**Структура доков и правило артефактов (2026-09-17, коммит `270e0a4`):** `docs/` = `ARCHITECTURE.md`,
`COMMANDS.md`, `plan/`, `adr/`, `findings/` (живое) и `archive/` (заменённое); механизм пишет результат
по одному адресу из таблицы в `CLAUDE.md` §«Доки», сырое и промежуточное — в `data/`, **состояние
проекта — только в `CLAUDE.md`**. Заменяющее переносит заменённое в `docs/archive/` тем же коммитом
и правит ссылки; молча не удаляем.

## Каталог страниц

- [projects\active\alpha.md](projects/active/alpha.md) — проект: передача, состояние, история.
- [skills/](skills/) — [ovh-vps-singapore.md](skills/ovh-vps-singapore.md) (сервер, тюнинг, профиль),
  [dsh-task-board.md](skills/dsh-task-board.md) (доска задач DSH, снята 15.09).
- [log.md](log.md) — таймлайн. [MEMORY.md](MEMORY.md) — протокол. [SOUL.md](SOUL.md) — рамка.
