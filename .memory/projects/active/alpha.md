---
title: alpha — бот по плотностям стакана Bybit
date: 2026-09-17
type: project
salience: 1
last_access: 2026-09-17
tags: [alpha, bybit, lob, collector]
---

# alpha — состояние проекта

## ПЕРЕДАЧА СЛЕДУЮЩЕЙ СЕССИИ (2026-09-17, вечер) — читать это первым

**В-59 и смоук (17.09, ещё позже).** `verify` больше не роняет сутки за шов переподключения
(`GapTally`: `write_failed`/`book_invariant` — потери, остальное — швы); коммит `3614321`, `master`
запушен; `alpha-verify` на сервере заменён (бэкап `-prev-20260917`), ночной таймер возьмёт его. Смоук
нового коллектора: `/opt/alpha/alpha-smoke-3614321 lob session --minutes 5` на HYPE/LSK/NEAR/DOGE/AKE
в `/opt/alpha/smoke` — 611 214 записей, gaps 0, `connect_failed`/`gap_rows_failed` = 0, `verify` ok ×5;
бинарник для деплоя — тот же файл. **Владелец: реальных ордеров не будет до отладки; перед деплоем —
A8 «коллектор при любых обстоятельствах»** (dev-plan «Дополнения»), SIGTERM — часть A8 (reboot).

**Проверка 17.09, поздний вечер (сессия разбора транскрипций).** Всё, что ниже заявлено сделанным,
проверено по коду и воротам (782 теста, clippy, fmt) — совпадает. Прод не трогали: бинарник
2026-09-15 22:47Z, A3-счётчиков в `session.json` нет; 09-17 01:51Z одно переподключение → 100 строк
`sequence_gap` «шов покрытия» — по В-59 (сделано в этой же сессии) шов сутки не роняет — сверка даст `ok seams=1`;
деплой коллектора — владелец. Диск: 34 ГБ свободно. Уборка сделана (`.tmp-*`, `does/`,
`target/`; серверные скрипты в `tools/server/`), `data/` (1.6 ГБ, 28 каталогов) оставлен —
16 из них ни один документ не упоминает, список в `log.md`, удалять — владелец.

**Где остановился.** Идём по плану `docs/plan/dev-plan-2026-09-17.md` (этапы A→B→C→D).
Закрыто: **этап A целиком** (A1–A6), **B1**, **B2**, **половина B3**, **аудиторский долг**
(C3, C4, хвост A4, замер линтов), **R1** (журнал испытаний) и **B4** (досрочный выход). Всё
закоммичено, ворота чистые: **782 теста**, clippy `-D warnings`, `fmt`. Последние коммиты (свежие
сверху): `b73cc07` (B4), `3582632` (R1 + артефакт B2 + хвосты аудита), `6781b20`/`c3d72be` (память),
`94483e0` (долг), `ae95958` (B3-половина), `9fcec8c`/`89f10b6`/`48510c0` (B2), `783e380` (B1 = В-58),
`23f1e1d`/`ef2a309`/`90a108e`/`4d17659`/`7982809`/`62406dc`/`6a96063` (A3–A6), `8f81912` (A2),
`42ded21` (A1), `438d07d` (В-56).

**Почему остановился именно здесь.** Очередь была такая: B3-хвост → B4 → B5 → C1…C4. B3-хвост
(длинные горизонты в `touch-profiles`) сознательно отложен **к C4**: там `horizons: [Vec<…>; 4]`
разворачивается в 12 колонок, и расширять таблицу дважды (сейчас и в C4) значило бы дважды
переделывать формат артефакта и его тесты; решение записано в плане. **B4 сделан 17.09**
(`b73cc07`), **B5 — следующий шаг**: он M-размера и упирается в вопрос, где считать (местная запись
или серверные сутки), поэтому остановка здесь естественная. C1/C2 **ждут двух чисел владельца**
(`N_abs` и `S%`), без них вердикт по силе не построить.

**Аудит 17.09 (второй проход) закрыт целиком:** блокер R1 починен, хвосты R3–R7 и дрейф плана —
тоже; открытым осталось только машиночитаемое `trials=48` в журнале. Подробности — раздел
«Аудит 17.09» внизу и строка `audit` + строка `fix+feat` в `log.md`.

**Что делать дальше, по шагам:**
1. **B5** (M): прогон всех 48 форм В-58 на пригодных сутках (местная запись
   `data/always-on/20260912T201737Z` + серверные сутки с `verify == ok` — на 17.09 таких нет:
   маркеры лишь за 09-16, `fail` у 6 монет); отчёт `docs/archive/artifacts/bounce-backtest-<дата>.md`
   с `net_fill` по формам, долями выходов и DSR по фактическому `N`; строка в `runs.csv`.
   Формула вызова проверена: `alpha lob backtest --touches --session-root <root> --symbol <S>
   --h3-mode floor --median-rtt-ns 20000000 --p95-rtt-ns 20000000 --order-qty-e9 10000000
   --stop-mode before|at|behind [--deadline-secs 60|600|3600|7200] [--early-exit-secs 1|2|3]`.
   **Решение владельца нужно:** гонять на местной записи (десять монет, 20 ч) или на сервере
   (`/opt/alpha/alpha-collector` + сутки 09-16 с маркерами `ok`); счёт испытаний — `trials_from_runs_csv`
   теперь читается и работает, но `N` в журнал писать прогонами, а не текстом.
2. **C1** (S): `size_usd = size_at_touch × price` в `touches`, флаг `--min-size-usd N`, квантили
   p10/p50/p90 `size_usd` по касаниям и по исходам — печатать **до** выбора `N`;
   владелец называет `N_abs` (единица — $ или лоты — уточнить у него; он отвечал «от N»).
3. **C2** (M): «сила плотности» = `size_at_touch / средний размер непустых уровней своей стороны`
   (скользящее среднее, окно `window_secs = 3600`, выборка раз в секунду, без аллокаций — как
   двухслотовая выборка В-45); колонка в `touches`, ось `strength` в `touch-profiles`; **владелец
   называет `S%`**.
4. **C3, C4** (S, M): сигнал одной функцией (`Signal::evaluate(touch, filters)` — общая для
   `lob watch` и `on_event`), затем вердикт по силе (стратификация, чтобы сила не повторяла
   «крупный в долларах») и **сюда же** B3-хвост (длинные горизонты в `touch-profiles`).

**Чего НЕ делать, не спросив владельца** (его решения уже приняты и их нельзя «улучшать»):
- **никаких сторожей диска и порогов** в коде — «в ручном режиме буду управлять датой, пороги тоже
  не нужны»; диск ведётся вручную (6.5 ГБ/сутки, 36 ГБ свободно на 17.09);
- **`.200`/`deep/` пишется дальше** — «они копятся специально, читателя нет, потому что у нас не
  стриминг»; читатель `deep/` — отдельная работа (D8);
- **архивация вручную** по регламенту `docs/COMMANDS.md` п. 6 (verify → archive → delete-original);
- **деление на разведочную/подтверждающую выборки отсутствует** (В-58);
- **сетка В-58 зафиксирована до данных**: 48 форм, а любое дополнительно рассмотренное значение
  фильтра/дедлайна — отдельное испытание в `runs.csv`. `--deadline-secs` вне {60,600,3600,7200}
  обязан падать (так и сделано).

**Четыре вопроса, которые ждут слова владельца (заданы 17.09, ответа нет):**
1. **Деплой коллектора**: K1/V5/V6/V12 лежат в коде, на проде всё ещё бинарник от 09-15 22:47.
   Нужна пересборка и перезапуск `alpha-collector` (рестарт начинает новую часть суток, потерь нет).
2. **Политика V4**: ночной прогон за 09-17 пометит `fail` **все 100 монет** — в боевом `gaps.csv`
   100 строк одного переподключения `2026-09-17T01:51:19Z` (`session.json: reconnects = 1`).
   Варианты: оставить строго (PLAN §10) / смягчить / развести вердикты (тикет
   `.autopilot/2026-09-11-lob-density-ed3/tickets/47-verify-gate.md`).
3. **`fsync`** (В-57): решения не было, в код ничего не добавлено; рекомендация аудита C1 — «не нужен».
4. **`N_abs` и `S%`** для C1/C2 (см. выше).

**Что проверить по времени, не забыв:**
- **18.09 00:20 UTC** — первый **автоматический** прогон `alpha-verify.timer` (вручную я гонял
  17.09: за 09-16 вышло 94 ok / 6 fail). Смотреть `sudo /opt/alpha/tools/verify-day.sh` и
  `/opt/alpha/verify/<день>.log`; ожидание — как раз пункт 2 выше.
- **после 09-21** — первые 7 суточных кластеров (`stats::G_MIN = 7`), то есть первые настоящие
  интервалы и первый вердикт.
- **диск**: 36 ГБ, расход 6.5 ГБ/сутки ⇒ ≈5.5 суток от 17.09; управление — ручное, владельца.

**Грабли этой сессии (стоили времени, повторятся):**
- `pgrep -f "cargo build"` внутри ssh-команды матчит саму эту команду — ждать сборку надо по
  файлу-маркеру, а не по `pgrep`; ssh-вызов с длинным ожиданием обрывается на 10 минутах.
- clippy держит предел 7 аргументов: 8-й параметр `bounce_plan` → собрать в структуру (`PlanShape`).
- добавление колонки в `touches-*.csv` требует править **и** `TOUCHES_WIDTH`, и массив строк.
- греп-тест горячего пути падает на комментариях, где слово-запрет объясняется — строки пропускать.
- `append_gap_row` сам делает `create_dir_all`: «несуществующий каталог» не отказ, для теста нужен
  путь-каталог.
- `clippy` кэширует диагностику по флагам: замер линтов надо делать с чистыми флагами, иначе
  «Finished за 0.6 с» врёт.
- приёмка «три прогона дают разные числа» проверяется только живым прогоном: `--stop-mode` на
  ZECUSDT дал −0.0948 / −0.0945 / −0.0886 bps (все отрицательные).

## Аудит 17.09 (второй проход) и его исход: план ↔ код ↔ коммиты ↔ решения владельца

Прогон по запросу владельца после хендоффа `6781b20`: три аудитора по осям + ворота капитана.
Отчёт — `docs/findings/plan-audit-2026-09-17.md`. **Всё найденное закрыто в тот же вечер**
(коммиты `3582632`, `b73cc07`); итоговые ворота после правок — **782 теста, 0 failed, 5 ignored**,
clippy `-D warnings`, fmt; шесть новых тестов проверены мутацией.

**Подтверждено аудитом:** A2, A4, A5 (доки/V11/линты не включены), A6 = В-57, архивная половина A1;
B1 (сетка 48, `1 × 3 × 1 × 4 × 4`, вход — не множитель), B2 (`frontrun_tick` → вход от фронтрана,
три стопа, тейк 1:1 от входа, `PlanShape`), B3-половина (`HORIZONS_LONG_MS` отдельно, колонки
`m_10m/m_1h/m_2h`, `TOUCHES_WIDTH` 29→32, `--deadline-secs` из сетки), долг `94483e0`. Сторож диска
A1 писался (`8e8e71c`) и снят (`42ded21`, −228 строк) — решение владельца соблюдено.

**R1 (блокер) — закрыт.** Строка `kind=prereg` в `docs/plan/runs.csv` гасила журнал **дважды**:
(1) `RunKind` не знал варианта `prereg`, а `read_run_rows` парсит строго (свой тест
`malformed_journal_is_none_not_silent_zero`), поэтому `trials_from_runs_csv` → `None` и
`run_shortlist` (`commands/lob/shortlist.rs:1091`) отказывал «журнал испытаний не читается — DSR не
из чего дефлировать» (потребитель — B5); (2) строка была записана **без экранирования**, а `detail`
содержит запятые → 6 полей вместо 4, и чтение падало и после добавления варианта. Починено:
`RunKind::Prereg` (учёт, не испытание), строка закавычена (содержание не менялось), строка
`amendment` с причиной правки, тест `the_repository_journal_reads` читает **боевой** журнал (он и
нашёл второй слой), тест `prereg_row_is_bookkeeping_and_does_not_kill_the_journal`.

**B4 — сделан (коммит `b73cc07`).** `ExitReason::Early` + `still_at_level` в `on_event`;
`TradePlan::Bounce` получил `early_exit_ns`, `level_px`, `tick_px`; порядок выходов
стоп → трейл → тейк → досрочный → дедлайн; CLI `--early-exit-secs {1,2,3}` (без флага — выключен,
чужое значение — отказ, `5` проверен живьём), колонка `n_early` в отчёте. Замер (ZECUSDT,
`20260912T201737Z`, стоп `behind`, дедлайн 60 с): `net_fill` −0.0886 (выкл) / −0.0909 / −0.0907 /
−0.0937 bps; досрочно 108/88/68; стопов 160/177/199 вместо 226; кругов 309 → 324 (эффект ёмкости,
не качества). Артефакт `docs/findings/early-exit-2026-09-17.md`.

**Хвосты аудита — закрыты:** R3 (прогон B2 без артефакта) → `docs/findings/bounce-b2-stops-2026-09-17.md`
(воспроизведён: −0.0948 / −0.0945 / −0.0886 bps, круги 324/323/309, стопы 302/297/226);
R4 (`frontrun_best_px` → `frontrun_tick` в тикете), R5 (отказ `--deadline-secs` вне сетки — функции
`deadline_ns_from_secs`/`early_exit_ns_from_secs` + тест), R6 (`markout::check_horizons` теперь
вызывается боевым путём `--deadline-secs`), R7 (строка A5 про `deny`-линты), дрейф плана (A1 со
сторожем, номера строк, сломанная блоком таблица). **Открыто:** `trials=48` как машиночитаемое поле
(сейчас текст `forms=48` в `detail`, журнал считает испытания по фактическим строкам).

**Не закрыто по решениям владельца:** в `SETTLED.md` не перенесены линты-гейт, журнал/journald,
порядок починки, спот, «красивые числа» (D4, `round_zeros` не расширен), `N_abs`, знаменатель B,
ордера + RTT `assumed` — живут только в findings (тикет A6 их не покрыл).

**Решения владельца соблюдены** (сторожей/порогов нет, `deep/` пишется, архивация вручную, `fsync`
не добавлен, реальные ордера только по слову, сетка до данных, деления на выборки нет).

## Что делаем

Олвейс-он коллектор пишет стакан Bybit по пулу инструментов; дальше — разметка крупных уровней,
профили по осям, вердикт бэктестом с моделью очереди. Архитектура под живого бота с первого дня:
одна стратегия `lob::strategy::on_event<MD, B>` идёт и в `Backtest`, и в `LiveBot`.

## Где правда

- `CLAUDE.md` (корень репо) — инжектится в сессию сам, читать первым.
- `docs/plan/PLAN.md` → «Точка остановки»; `docs/plan/SETTLED.md` → решения В-1…В-58.
- `docs/plan/dev-plan-2026-09-17.md` → **план работ A→B→C→D** со статусами по каждому тикету.
- `docs/findings/` → артефакты; `docs/COMMANDS.md` → команды и грабли; `docs/plan/runs.csv` → прогоны.
- `.autopilot/2026-09-11-lob-density-ed3/tickets/` → тикеты (41 done, 42 pending, 47 — пересмотр
  механизма `verify`).

## Состояние на 2026-09-17

- **План работ** — `docs/plan/dev-plan-2026-09-17.md`: этапы **A** база и коллектор (срок ≈ 22.09),
  **B** база бэктестера, **C** сигнал-отскоки, **D** накрутка. Источники — четыре отчёта от 17.09 в
  `docs/findings/`: `bounce-spec` (спека отскока по S/D, 68 КБ), `transcripts` (семь видео, 122 КБ),
  `collector-audit` (120 КБ; таблица K1/V1–V13/C1–C9, §11 вопросы, §12 ответы владельца), `roadmap`
  (26 КБ). Решения владельца 17.09 в `SETTLED.md` **не перенесены** — это тикет A6.
- **A1 сделан** (`42ded21`): `lob archive` отказывает на последней части незакрытой сессии
  (`session.json.closed = false`) — и на архивацию, и на удаление (`unlink` открытого файла потерял бы
  дописанное; аудит V8). Сторож диска был написан и **снят по слову владельца**: «не нужны пока никакие
  сторожа, в ручном режиме буду управлять датой, пороги тоже не нужны» — ни `statvfs`, ни полей в
  `session.json`, ни `--disk-warn-gib`, ни `libc` в дереве.
- **A2 сделан** (`8f81912`): `alpha-verify.timer` (00:20 UTC, `Persistent=true`) → `alpha-verify.service`
  → `tools/verify-day.sh`: вчерашние части hardlink'ами в `/opt/alpha/verify/<день>`, `lob verify` по
  каждой монете пула, маркеры в `/opt/alpha/root`, сводка в `/opt/alpha/verify/<день>.log`. Причина
  отдельного каталога: verify считает вердикт по всем частям символа в корне, а корень держит все сутки —
  иначе дни смешались бы при одном маркере на символ.
- **Первый боевой прогон (17.09 08:35Z, сутки 09-16): 94 ok / 6 fail** — LSK, AKE, BR, BTW, XMR, AVAAI;
  100 маркеров в боевом корне, впервые с начала записи (закрыт V1 аудита). 100 монет ≈ 9 минут. LSK:
  11.5 млн сделок, 14 583 нарушения = **0.126 %** (порог 0.1 %), почти всё — ≥2 тиков от нашего уровня.
  Грабля: первая версия скрипта приняла шапку CSV за символ → исправлено и доставлено.
- **Диск** (17.09 08:49Z): занято 11 ГБ, свободно **36 ГБ**, расход ≈ 6.5 ГБ/сутки ⇒ ≈ 5.5 суток;
  управление — вручную, без автоматики (решение владельца).
- **Этап A закрыт 17.09** (коммиты `6a96063`, `62406dc`, `7982809`, `4d17659`, `90a108e`, `ef2a309`,
  `23f1e1d`), 770 тестов, clippy/fmt чисто, каждая новая проверка проверена мутацией:
  - **A3 видимость потерь**: K1 — `TransportError::Http{status}` (403 ≠ 429), `ConnEvent::ConnectFailed`
    на каждый инструмент сокета, новый класс `GapKind::ConnectFailed` в `gaps.csv` +
    `session.json.connect_failed`; V5 — `recv()` под таймаутом (`ConnConfig::recv_timeout`, у боевых
    вызывающих `2 × ping_interval` = 40 с, иначе полуживой сокет висел вечно); V6 — `gap_rows_failed`
    в `session.json` (журнал потерь сам может не писаться) и печать отказа финального `flush`;
    V4 — вердикт `verify` читает `gaps.csv` того каталога, который сверяет (`gaps_csv=<n>`
    в строке); V12 — смерть ОС-потока шарда видна на тике (строка на инструмент, `reconnects`
    растёт на один за шард).
  - **A4**: `binlog::Reader::read_frame_soft()` + `truncated_tail()` — обрезанный хвостовой кадр
    это «файл ещё пишется», а не отказ; строгий `read_frame` остался там, где пишется артефакт
    (`--rewrite-out`).
  - **A5**: документы приведены к фактам (`CLAUDE.md`, `ARCHITECTURE.md` A4, `depth-200-ab`),
    V11 (`unreachable!` → `bail!`, доказательство индексации `books[slot][stream]` + `debug_assert!`);
    **линты `deny` не включены — замер: `cast_possible_truncation` даёт 51 срабатывание.**
  - **A6 = В-57**: `.200` пишется дальше, сторожей/порогов нет, ротация вручную; **`fsync` открыт**.
- **Важно:** коллектор на проде — **старый бинарник** (`/opt/alpha/alpha-collector` от 09-15 22:47):
  K1/V5/V6/V12 заработают только после пересборки и перезапуска; `/opt/alpha/alpha-verify` и
  `tools/verify-day.sh` уже обновлены.
- **Политика V4 ждёт слова владельца:** ночной прогон за 09-17 пометит `fail` **все 100 монет** —
  в боевом `gaps.csv` 100 строк одного переподключения 2026-09-17T01:51:19Z (`reconnects = 1`).
- **Дальше — этап B (база бэктестера):** B1 предрегистрация форм и фильтров (не ждёт кода) →
  B2 вход от фронтрана и три стопа → B3 горизонты до 2 ч → B4 досрочный выход → B5 прогон-вердикт.

## Состояние на 2026-09-16

- **Прод — новый сервер** OVH `139.99.91.22` (`os-sgp2`, Ubuntu 26.04, 2 vCPU / 3.7 ГБ, ключ `id_rsa`,
  данные на `sdb` → `/opt/alpha`). Юнит `alpha-collector.service` активен, `NRestarts=0`, остановка —
  файлом `stop` (В-41). Пул **100 монет** (топ-105 минус глобальный топ-5: BTC/ETH/XRP/SOL/ZEC).
- **Запись идёт с 2026-09-15T14:31:04Z** (проверено birth-time файлов; на 16.09 08:21Z — 17 ч 50 мин).
  Два потока (T45): `.50` в основной файл, `.200` в `deep/` (глубина с 17:13:53Z). Замеренный расход
  **5.9 ГБ/сутки** (4.4 ГБ за 17.8 ч), свободно 42 ГБ ⇒ **≈7 суток**. **Ротации нет**; владелец 16.09
  сказал, что пока это нормально, но упрётся первым именно оно.
- **Три перезапуска — все наши, не падения:** 17:13 (включили второй поток), 18:36 (бинарник T45+T46),
  22:47 (флаг RPI). `Restart=on-failure` при `NRestarts=0`; у каждой монеты ровно 4 части за 09-15 и
  1 за 09-16, части `-p5` нет.
- **RPI** (В-53, В-55): флаг `publicTrade.RPI` пишется битом 1 `attrs` с 22:47:56Z (md5 `89e58e09…`),
  разметка его читает — RPI-объём идёт в отдельном `rpi_lots` и в метку исхода не входит.
  **Влияние измерено и оказалось в пределах шума:** 806 уровней с RPI-объёмом из 13.8 млн, 286 смен
  метки (0.002 %). Причина — RPI-сделки идут в основном **внутри спреда**, где нашего уровня нет.
  Третий поток для метки не нужен; флаг полезен как отдельный признак.
- **`verify` по 09-15 (100 монет, 400 частей): 77 `ok` / 23 `fail`.** Структурно чисто у всех —
  `gaps = 0`, `invariants = 0`; падают только на `trades_violations` (0.79 % сделок). **Природа
  разобрана числом:** блочных 0, RPI 346 (1.8 %), внутри спреда 4 489 (24 %), ≥2 тиков до уровня
  14 899 (79 %), ≥20 мс с последнего обновления книги 136 (0.7 %). Это **разрешение сэмплированного
  фида** (`.50` публикуется пачками ~20 мс, лента непрерывна), а не порча и не задержка сети.
  23 монеты пригодны для работы.
- **Расхождение кода с планом:** в плане (ревизия 17а) для нарушений прописан порог **0.1 %**,
  а `VerifyStatus::of` рубит на любом ненулевом. По плановому порогу прошли бы 91 монета, за порогом
  осталось бы 9 (BTW, AKE, SENT, BR, UAI, LSK, POWER, USELESS, XMR). **Решение владельца открыто.**
- **Сборка Linux-бинарника** (В-54): прежний сервер `13.140.29.171` больше не трогаем; toolchain и
  сборка — на боевом, но на системном диске (`sda`, `/home/ubuntu`): build-essential + rustup 1.93.1 +
  `~/alpha-build` (~0.6 ГБ). Анализатор с В-55 собран и читает боевые файлы, коллектор не тронут.
- **Анализ на одном дне возможен:** `lob levels` по 77 монетам дал 13.8 млн уровней (CSV 1.4 ГБ в
  сутки). Числа считаются всегда; `none` встаёт только в границах интервалов и подтверждающих флагах —
  их требует ≥7 дневных кластеров (`stats::G_MIN = 7`), первый валидный прогон — после закрытия 09-21.
- **Git:** `dev/wave-4-record-verify`, `f5f89c3` запушен (69 файлов: T45, T46, RPI, доки, память);
  дальше — диагностика `verify` (состав нарушений) и findings §12c–§12d.
- **Git — слияние (16.09):** волна 4 сведена в `master` **fast-forward** (`6c9b5f5` → `e7f58a7`,
  +135 коммитов, из них 128 не-memory), `origin/master` = ветке; ворота перед слиянием —
  756 тестов, clippy `-D warnings` и `fmt --check` чисто. Главная ветка в репо называется `master`,
  ветки `main` нет.

## Состояние на 2026-09-15

- **Сервер `13.140.29.171`** (SSH без пароля): `alpha-collector.service` включён и активен,
  **пул топ-30**, олвейс-он с 2026-09-14T18:20:59Z, формат **v3** (5.64 Б/запись).
  За 4 ч: 72.9 млн записей, 0 gaps/reconnects/resyncs/frames_failed, CPU 5.26 % ядра,
  RSS 22 МБ, parse p99 122 мкс. Расход **2.05 ГБ/сутки**, свободно 40 ГБ ⇒ ~20 дней.
  **Ротации/выгрузки нет** (на сервере нет `crontab`/`rsync`) — упрётся первым.
  `verify-*.status` серверных суток ещё нет.
- **Пул**: локальный `instruments.csv` — топ-50, заморожен боевым часовым отбором (В-50,
  коммит `3da0b3f`). На сервере — первые 30 строк (`instruments-top30-server.csv`, MD5 сходится
  с `/opt/alpha/root/instruments.csv`).
- **Формат v3** (T43/T44, В-48/В-49) принят, заморожен замером, раскатан на живой коллектор;
  читатель понимает и v2.
- **Вердикты 2026-09-13**: сделка-отскок `net_fill` < 0 на всех десяти монетах; трейл-тейк
  улучшает, но нуля не пересекает; лестница даёт fill 99.9–100 %, но `net_fill` хуже;
  ёмкость (одна позиция за раз) работала **фильтром**. Разборы — `docs/findings/{bounce,trail,ladder}-2026-09-13.md`.
- **Git**: ветка `dev/wave-4-record-verify`; 15.09 отправлено 128 коммитов в origin
  (`github.com/Gogi213/alpha`), включая `963643b` (факт про живой топ-30).

## Проверка состояния — 2026-09-15 00:15Z (сессия «новый чат»)

Сверял память с реальностью, а не с текстом. Совпало: git (ветка `dev/wave-4-record-verify`,
дерево чистое, HEAD `a8dd7d8`, 128 коммитов в origin), сервер (`active`, старт
`2026-09-14T18:20:59Z`, `NRestarts=0`, 60 binlog-файлов = 30 монет × 2 суток, `gaps.csv` пуст,
`verify-*.status` — 0, диск 30G/73G занято, 40 ГБ свободно), локально (`alpha*` процессов нет,
`target-ci/release/alpha.exe` от 14.09 00:51, три каталога `data/always-on/*`).

**Сдвиг против памяти один**: первые серверные сутки **закрылись** — файлы `*-2026-09-14.binlog`
отротированы в 00:00 UTC, 436 МБ за 18:20→24:00Z (≈1.85 ГБ/сутки, сходится с 2.05 из замера);
сутки 09-15 идут. Значит `binlog-stats`/`verify` по суткам 09-14 можно запускать, но `verify`
по-прежнему ни разу не гонялся.

**Найденный дрейф документа** (не памяти): `docs/COMMANDS.md`, строки 29–40 — абзац «Состояние»
описывает боевой пул из **десяти** и идущий локальный олвейс-он `data/always-on/20260912T122440Z/`.
Факт: топ-50 заморожен (В-50, `3da0b3f`), местная запись остановлена владельцем. Правится
точечным `Edit`, пока не правился.

## Переезд на новый сервер — 2026-09-15 (сделан)

- Новый сервер OVH `139.99.91.22` (`vps-cab27b0c.vps.ovh.ca`, регион `os-sgp2`, Ubuntu 26.04,
  2 vCPU Haswell, 3.7 ГБ). **Второй диск `sdb` 50 ГБ был неразмечен** → ext4 `alpha-data` →
  `/opt/alpha` (fstab, 47 ГБ свободно); системный `sda` 40 ГБ отдельно.
- Перенесено с прежнего сервера: бинарник (md5 `e1d721a9…`), `/opt/alpha/src` — **байт-в-байт равен
  репозиторию** (133 файла `src/`), прежний серверный топ-30 (архив `instruments-top30-carried.csv`),
  systemd-юнит.
- Прод-пул: боевой часовой отбор `--window-secs 3600 --h3-k 1.0 --top 105`, из 105 строк вырезан
  **глобальный топ-5 по обороту (BTC, ETH, XRP, SOL, ZEC)** → **100 монет** (первая HYPE, дальше LSK,
  NEAR, DOGE, AKE, ENA, …, NBIS). Блеклистов не добавлялось; `instruments.csv` в репо обновлён.
- Юнит включён и активен с **2026-09-15T14:31:04Z**; старый коллектор погашен файлом `stop` (В-41):
  `closed=true`, 342 465 743 записи, 1.13 ГБ за 20.2 ч на топ-30 ⇒ 1.35 ГБ/сутки.
- Замеры 100 монет (600 с): CPU **12.21 % ядра**, RSS 9.4→38.4 МБ, parse p99 107.5 мкс,
  очередь p99 194.6 мкс, 2.49 Б/запись, 0 разрывов; диск **5.02 ГБ/сутки ⇒ 9 суток** на 47 ГБ
  (топ-50 — 2.95, топ-30 — 2.33; топ-5 монет = 24.9 % байтов).
- Тюнинг A/B (4 прогона): `busy_poll` **отклонён** (+45 % CPU на запись за ненужный p99); оставлены
  bbr, `netdev_max_backlog=25000`, `tcp_fastopen=3`, `tcp_slow_start_after_idle=0`, выключенные
  `unattended-upgrades`/apt-таймеры; привязка к ядру и `nice -5` отклонены (p99 хуже). Профиль
  `perf`: ядро/сисколлы ~25 %, libc memset 9.1 %, приложение ~17 % (`Book::apply` 5.1 %).
- Разбор — `docs/findings/deploy-2026-09-15-ovh-singapore.md`; детали окружения —
  `skills/ovh-vps-singapore.md`. Дополнения к `CLAUDE.md` («Состояние») внесены.

## T45 — два потока глубины (2026-09-15, в проде)

- Причина: 50 уровней — это 0.18 % цены у ликвидных и до 3.8 % у тихих, а `.200` по замеру даёт
  больше данных при **меньших** ресурсах (−19 % байт, −44 % CPU против `.50` на тех же записях),
  но публикуется реже: BBO 10 мс / `.50` 20 мс / `.200` 100 мс.
- Решение (индустриальное): **своя книга на каждый поток**, **раздельное хранение** — основной файл
  `<SYM>-<день>.binlog` как раньше (`.50`) и `deep/<SYM>-<день>.binlog` (`.200`, тот же v3, свой
  снапшот первым кадром); склейка — на чтении по `seq`. Так сделано потому, что склеенная книга
  бывает внутренне противоречива (быстрый поток уже впереди, глубокие уровни устарели).
- Каталог `deep/` невидим для всех существующих команд (резолверы читают корень) — поведение прежних
  команд не изменилось; `session.json.streams` несёт раздельные счётчики.
- Тикет `.autopilot/2026-09-11-lob-density-ed3/tickets/45-dual-depth-streams.md` (с критериями и
  отчётом). Ревью — `needs_revision` (находки про тесты/доку), ревизия закрыта, каждая новая проверка
  проверена мутацией. Гейты: **752 passed, 0 failed**, clippy/fmt чисто (на момент T46; после T45
  было 721).
- В прод включено 2026-09-15T17:13:53Z, затем 18:36:40Z — объединённый бинарник T45-ревизия + T46.

## Дрейф `.50` против `.200` — нет (замер)

- Прототип `examples/stream_drift.rs`: две книги по одной монете, быстрая подтягивается ровно к
  метке глубокого сообщения, сравнение на границах сообщений (сравнение внутри пачки даёт ложные
  расхождения — записано как ловушка).
- LSK (самая активная, ~1.5 ч, 8 927 сравнений): верх совпал **97.63 %**, по четвертям окна
  97.25 / 97.24 / 98.15 / 97.87 % — **ровно, без накопления**; расхождения ограничены (max 41 тик,
  «крупных» 1.67 %) и разнонаправлены. DOGE (17 006 сравнений): **99.99 %**, максимум 1 тик.
- Следствие для склейки: глубокий поток отстаёт на ~100 мс (интервал публикации), поэтому верх книги
  брать из `.50`, а `deep/` — только за пределами его окна, выравнивая по `seq`.
- Артефакт: `docs/findings/stream-drift-2026-09-15.md`.

## T46 — архив закрытых суток (2026-09-15, в проде)

- Закрытые сутки сжимаются целиком: контейнер `*.binlog.zst` = один zstd-поток над заголовком v3 и
  телами кадров; команда `lob archive --path … [--level 19] [--out …] [--delete-original]` с
  **обязательной** проверкой round-trip; удаление оригинала — только с флагом и при
  `verify-<SYMBOL>.status == ok`.
- Замер: на боевом v3 `CYSUSDT` **81.3 %** от оригинала; на файле после `--rewrite-out` — 90.3 %
  (уровень 1 — 97.7 %, то есть пересжатие покадрово дало бы всего −1.6 %, а контейнер — −7…−10 %).
  Чтение архива всеми командами работает; артефакты `levels`/`markout`/`verify` на архиве совпали с
  оригиналом по SHA256.
- Ограничение: контейнер принимает **только v3**, для старых v2-суток нужен шаг
  `binlog-stats --rewrite-out`. `xz` не добавляли (новая зависимость liblzma; чтение в 20–40 раз
  медленнее при выигрыше 2.8 п.п.).
- Артефакты: `docs/findings/archive-compression-2026-09-15.md`, тикет `46-archive-container.md`.

## Что дальше

1. **Критерий `verify`** — решение владельца: реализовать плановый порог 0.1 % (читаются 91 монета,
   9 остаются с явной причиной) или оставить 77. Правило уже записано в плане — надо его реализовать,
   а не изобретать.
2. **Ротация** — отложена владельцем 16.09; вернуться до ~09-22, иначе диск (42 ГБ при 5.9 ГБ/сутки)
   кончится ровно тогда, когда данные впервые станут пригодны для вердикта.
3. **Альфа-работа на том, что есть** — сухой прогон цепочки `levels` → `touches` → `markout` →
   `backtest --touches` на 09-15; числа считаются, выводы ждут 7 кластеров (после 09-21).
4. **T42** — переезды плотностей (`pending`).
5. Код-оптимизации из профиля (сисколлы/пробуждения, memset) — отдельным тикетом.
6. Датацентр: Bybit мигрирует мэтчинг в AWS Tokyo во второй половине ноября — переезжать вслед.

## Грабли, которые уже стоили времени

- Живой коллектор держит `target/release/alpha.exe` — сборка идёт в `--target-dir target-ci`.
- `--h3-mode floor|percentile` обязателен у `levels`/`markout`/`watch`/`profiles`/`shortlist`.
- Остановка олвейс-она — файл `<root>/stop` (В-41), Ctrl+C из оболочек агента не доходит.
- Параллельные `cargo build` в нескольких worktree дают CPU 100 % и OOM — сборка по одной.
- `verify` считает итог по символу **перечислением полей** в `verify_and_mark`: новый счётчик легко
  «теряется» в сумме и прогон показывает нули при ненулевых нарушениях — править нужно методом `add`.
- `git push` из песочницы DSH падает на именованных каналах (`could not read Username`), хотя ключи
  есть: пушить сразу с разовым `danger-full-access`, не тратя попытку.
- Новый `#[test]` вставлять якорем за **закрывающую скобку** предыдущей функции: вставленный внутрь
  тела он компилируется (вложенная функция), но не запускается — видно только по счётчику тестов.
- `verify` читает **все** части символа, а маркер пишет один на символ: чтобы сверить закрытые сутки,
  их раскладывают в отдельный каталог (hardlink'и — места не занимают).
