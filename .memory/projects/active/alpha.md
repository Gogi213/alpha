---
title: alpha — бот по плотностям стакана Bybit
date: 2026-09-20
type: project
salience: 1
last_access: 2026-09-23
tags: [alpha, bybit, lob, collector]
---

# alpha — состояние проекта

## ПЕРЕДАЧА СЛЕДУЮЩЕЙ СЕССИИ (2026-09-23, ~03:00 GMT+4 — две эпохи считаются; читать это первым; ветка `audit/design-fixes-2026-09-22`, HEAD `b844909`)

**Идёт прямо сейчас (Steam Deck `deck@192.168.1.49`, пользовательские юниты systemd):**
- `alpha-epoch-archive` — **эпоха «история»**: `~/alpha/bin/epoch-run.sh archive ~/alpha/epochs/e-archive 2026-09-01 … 2026-09-15`,
  лог `~/alpha/epochs-archive.log`. Импорт архива Bybit (ob200 + сделки) общей очередью по 6 монет; на 02:58 GMT+4 сутки
  01–07 импортированы (по 100 монет), 08 идёт; касания готовы 01–04 (конвейер: сутки → касания + замороженная F10 сразу).
  После импорта — ночной набор с тегом `e-archive` (сетки база/tk/dl/E7/скальп, вердикты, контроль рынка) и замороженная F10
  по всем суткам: `~/alpha/epochs/e-archive/b5/epoch-frozen/merged/<набор>/`, `study/bounce-verdict-epoch-frozen-<набор>.csv`,
  `study/placebo-epoch-frozen-<набор>.csv`, `study/placebo-e-archive-*.csv`. Прогноз ~05:30 GMT+4. Потолок — Wi-Fi дека
  (52 Мбит/с на приём, сигнал −62 дБм ⇒ ~7 МБ/с при ~3.8 ГБ архива на сутки пула).
- `alpha-epoch-collected` — **эпоха «запись»** (наши 16–21.09, поток `.50`): ночной набор с тегом `e-collected` + замороженная
  F10 с 16.09 (`FROM_DAY=2026-09-16`, `OOS_DIR=b5/epoch-frozen` в `~/alpha`), лог `~/alpha/epochs-collected.log`.
  **22.09 добавить утром** (дек заберёт сутки в 05:15 GMT+4, ночь в 06:00): перезапустить `epoch-run.sh collected`.
- Таймеры дека: `alpha-pull.timer` 01:15 UTC (забор суток с коллектора, pull — дек за NAT), `alpha-grid-nightly.timer`
  02:00 UTC (ночь: `NIGHT_JOBS=3`, `BASE_SPLIT=2`, срез `alpha.slice` 9G/10G). VPS-счётная `13.140.29.171` — ночь 02:00 UTC
  последовательно (резерв и сверка), бинарник `alpha-895c7e4`, на деке тот же.
- Дек **садился от батареи** 22:05 UTC — транзиентные юниты не переживают перезагрузку; импорт/касания возобновляемы,
  прогоны перезапущены руками. Держать на зарядке.

**Как проверить (с этой Windows-машины):** ssh только с явными путями —
`ssh -o UserKnownHostsFile=/c/Users/Георгий/.ssh/known_hosts -i /c/Users/Георгий/.ssh/id_rsa deck@192.168.1.49` (кириллический
HOME ломает умолчания). На деке: `systemctl --user list-units "alpha-*"`; `tail ~/alpha/epochs-*.log`; вердикты эпохи —
последняя строка вида в `study/verdicts.csv` дома эпохи; контроль — `grep контроль study/nightly-*.log`; F10 —
`tail ~/alpha/study/epoch-frozen.log` и `~/alpha/epochs/e-archive/study/epoch-frozen.log`.

**Сделано 22–23.09 (ветка `audit/design-fixes-2026-09-22`, запушена):**
- Аудит дизайна `docs/findings/design-audit-2026-09-22.md`, исправления и замеры `docs/findings/audit-fixes-2026-09-22.md`
  (§0–§9), решение **В-85** («правь всё»): живая ветка F10 — это удержание лонга 2 ч от старой бид-стены, не скальп-отскок.
- **Jev/TypeSafe:** правила и статусы — КОДОМ (страж R1/R2 с жёстким отказом и без ключа, чтение ночи, утренний статус,
  итог вердикта, число тестов), модель — только «мнение модели `jev-1.13.0`» над свободным текстом; точность M20: ошибки
  9/9, R3 7/8. Наводящая рамка `verdict-read` снята; `Judge.score` — список (карта давала 422).
- **Контроль «рост рынка»** (`tools/compute/placebo.py`, M19; в ночи после каждого вердикта и в `breakdown.py`): F10 +29.5 bps
  = +11.3 рост монеты + **+18.2 превышение**; H1 `a45-bid-b4h-neg` **+39.5** при контроле +2.9; шорты −17.5 при контроле −22.0
  (асимметрия сторон — в основном рынок). «Шарп 3.6» был t-статистикой; под $377 нужен капитал ~$8000 (пик 8 позиций).
- **Код** (тесты 966, clippy/fmt 0): `--early-exit-secs` (прилипание, В-58 п. 5), `--carry-age`/`--emit-day` (эффект ~1 %,
  кэши не пересчитываем), **`--touches-cache-only`** (сутки без кэша не реплеятся — это была причина OOM ночи: LSK со сверкой
  ok только за 21.09 уходила в реплей 6 суток, ~4 ГБ), шапка вердикта с единицей кластера, **`lob import-archive`**
  (архив Bybit → бинлог v3 тем же разбором WS, экспонента в CSV сделок разбирается точно). `pick_wall` в `loss-atoms.py`
  исправлен (M18b — выводы M18 держатся).
- **Ночь:** срок жизни входа `wall`+20 вместо `touch` (утечка будущего); скальп E17 **закрыт** (−5…−9 bps во всех 96 формах,
  прилипание не помогает); замороженный OOS E16 (`oos-frozen.sh`, официально с 23.09); маркеры журнала испытаний по
  конфигурации осей; `NIGHT_TAG`, `H3_DAYS`, `H3_JOBS`; параллельные стадии с семафором и общим срезом памяти.
- **Первая полная ночь на деке (16–21.09):** лонги от старых бид-стен на касании сверх рынка: `a45-bid` +12.4 (67 сд., 5/6
  суток), `a45-bid-b4h-neg` **+21.4** (32 сд., 6/6), `tk-…-b4h-neg` +25.9 (29, 6/6); зелёного нет. 21.09 (форма F10 его не
  видела): 13 сделок, превышение ≈ 0.
- **M21:** архив Bybit = поток `orderbook.200` (снимок + дельты, `u` подряд), сделки совпадают 1:1, середина 97–99 %,
  касания 84–95 %, шаг 100 мс против наших 20 мс; гейт импорта через весь конвейер на 21.09 — подходы к старым бид-стенам
  ATOM 17/17. ob200 есть с ~08–09.2025, раньше ob500.
- **L8 переезд:** дек — основная счётная (pull `tools/sync-from-collector.sh`, ключ дека `restrict,rrsync -ro` на коллекторе и
  VPS), гейт «те же байты» пройден; бинарник собирается на Ubuntu-VPS (на деке нет `cc`/`sudo`).

**Дальше (по порядку):** 1) утром прочитать обе эпохи — по каждой итог вердикта и превышение над контролем по наборам
(`a45-bid`, `a45-bid-b4h-neg`, `tk-*`, `a15-s10-bid`, `a45-ask`, скальп) и замороженной F10; **сравнить эпохи** (история
1–15.09 против записи 16–21.09 — первая проверка на днях, где подбора не было); 2) добавить 22.09 в «запись»; 3) доложить
владельцу по-бизнесовому (цифра → смысл → решение, GMT+4); 4) если лонги держатся на истории — расширить историю назад
(август и раньше, ob500) и предрегистрировать H1 главной; 5) L12 калибровка очереди (заявки ставит владелец), L13 снятие
стены, L14 признаки практиков, L15 хвосты (лот по цене сигнала, `LiveBot`, утечки в Python-разметке).

**Ждут владельца:** ключ TypeSafe на дек (судьи там молчат); дек включён и на зарядке ночью; кабель к деку (Wi-Fi —
потолок загрузки архива); `root/deep/` 14 ГБ на коллекторе.

**Грабли сессии:** кириллический HOME → ssh с явными `-i`/`UserKnownHostsFile`; heredoc Bash-инструмента съедает `\\` —
правки через Write/Edit или python-файл; `pkill -f` по строке из самой ssh-команды убивает сессию; работающий скрипт
заменять атомарно (`scp` в `.new` → `mv`); `OUT_BASE` — переменная `approach-scan.sh`, в других скриптах не использовать;
`study/root-<сутки>` при переносе на другую машину — битые симлинки, перелинковать; `grid.err` раньше дописывался между
запусками (теперь чистится `run-grid.sh`); на деке предел параллельности — память (процесс сетки до 2.9 ГБ), не ядра.


> Прежние передачи (17.09 … 22.09 вечер) — [`../archive/alpha-history-2026-09-22.md`](../archive/alpha-history-2026-09-22.md).
