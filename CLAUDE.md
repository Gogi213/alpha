# alpha — бот по плотностям стакана Bybit

## Что это

Rust-проект: олвейс-он коллектор пишет стакан Bybit по пулу инструментов (В-34), разметка
крупных уровней, профили по семи осям, вердикт бэктестом с моделью очереди. Архитектура —
под живого бота с первого дня: одна стратегия `lob::strategy::on_event<MD: MarketDepth,
B: Bot<MD>>` идёт и в `Backtest`, и в `LiveBot` крейта `hftbacktest` без правок.

## Где план и состояние

- **`docs/plan/review-fixes-2026-09-24.md` — ТЕКУЩИЙ список работ** (исправления по ревью 23.09: что исправить в коде,
  что готово, что пересчитать, как продолжить обе волны) — читать вторым после этого файла
- `docs/plan/dev-plan-2026-09-23.md` — план этапа G («титрование»: режим, выход, защита от прокидов, двойник, август;
  статус задач §0а, G12/G13 ссылаются на список выше)
- **`.memory/index.md`** — состояние и передача (общая память исполнителей, протокол `.memory/MEMORY.md`) — третьим
- `docs/plan/EXPERIMENTS.md` — реестр экспериментов E01–E24, M01–M21: `prereg` без строки здесь не зарегистрирован,
  вердикт без результата здесь не прочитан
- `docs/plan/SETTLED.md` — решения владельца В-1…В-95; `docs/plan/BUSINESS-TASK.md` — задача владельца, **не редактировать**
- `docs/findings/` — разборы; главные сейчас: `august-oos-2026-09-24.md` (кандидат вне выборки, E24), `recompute-u500-2026-09-24.md` (сентябрь на $500), `titration-regime-2026-09-23.md` (режим, E18), `exit-titration-2026-09-23.md`
  (выход E19, §5 защита после снятия E20, §6 безубыток E21), `design-audit-2026-09-22.md`, `audit-fixes-2026-09-22.md`
- Дашборд «Лонг в просадке»: https://claude.ai/artifact/WfSGAfj2hhkRTckAnKXSRF (кандидат на $500, счёт $2500, по монетам; кейсы — после пересчёта; сборка —
  `tools/compute/titration-dashboard-data.py` на деке → `titration-dashboard-build.py` + `titration-dashboard.html`)
- `docs/ARCHITECTURE.md` (A1–A9, обязательны), `docs/COMMANDS.md` (команды, грабли, ранбуки деки/VPS/коллектора)
- История: `docs/archive/claude-md-history-2026-09-23.md` (прежнее состояние и справка этого файла); планы этапа F
  и раньше, целиком заменённые и перенесённые в архив — `docs/archive/{dev-plan-2026-09-20,handoff-2026-09-19,
  handoff-2026-09-20,side-plan-2026-09-20,loss-plan-2026-09-22}.md`; `alpha-roadmap-2026-09-19.md` (дорога с
  воротами) не архивна, ещё в `docs/plan/`

## СОСТОЯНИЕ СЕЙЧАС (2026-09-24, 23:30 GMT+4) — читать первым

**Главное 24.09.** Кандидат (лонг от старой бид-стены в просадке BTC за 1 ч) пересчитан на позиции $500 со всеми
исправлениями ревью: сентябрь 01–23 — +$198 (+7,9 % на депозит $2500), просадка −2,5 % (`recompute-u500-2026-09-24.md`);
**на августе — месяце, которого правило не видело, — −$68 (−2,7 %), 768 сделок: кандидат не подтвердился** (E24,
`docs/findings/august-oos-2026-09-24.md`). Порог BTC 1 ч с сентября в августе срабатывал втрое чаще, входы в минусе.
«Просадка BTC за 4 ч» — в плюсе во всех трёх отрезках (история +$63, запись +$22, август +$112 в сделках, +3,1 % на
счёте) — направление для проверки, не кандидат (один из трёх вариантов, множественная проверка). База без фильтра —
знак меняется от отрезка к отрезку, просадка счёта −14 %. Кейсы на $500 (сентябрь): трейл +1 %/1 % ≈ трейл 2 %/0,5 %
≈ тейк 1,75 %, стоп 2 %, удержание 4 ч, на стену не реагировать. Дашборд (сентябрь, версия 11; режим/возраст/шорт
скрыты — на $500 не пересчитаны; август не добавлен): https://claude.ai/artifact/WfSGAfj2hhkRTckAnKXSRF

**Код.** Исправления по ревью 23.09 — волна 1 (R2 лот по цене сигнала, R3 лестница целыми лотами, R4 пыль, R6, R7
средняя цена свипа — вендор `Order::exec_notional`, B1 сироты, знак остатка шорта) и волна 2 (W1–W10: коллектор,
живой поток, бинлог, уровни, касания, сессия, дашборд, мёртвый код, дубли, сверка) готовы, независимо проверены,
слиты (`c16a876`) + B3 (разрезка `bounce_grid.rs`/`backtest.rs`) + раунд ревью 24.09 (5 блоков, находки закрыты):
**1002 теста / 0 / 7, clippy ноль**, на настоящих данных — байт в байт со старым бинарником. Список со статусами —
`docs/plan/review-fixes-2026-09-24.md`. Открыто: B2 (фильтр/план/защиты в общую стратегию `lob/` — живой бот = бэктест),
B4 (целые тики, `observe_wall_trades` внутрь `on_event`), B5 (пул буферов коллектора), **B6 ждёт числа** (глубина
рыночного свипа), B7 (импорт ob500 для старых обвалов). Сводки титрования печатают доллары по фактической позиции
(`3b80b43`), не условную $1000.

**Машины.** **Счёт — Steam Deck** `deck@192.168.1.49`, `~/alpha` (ssh только с явными `-i`/`UserKnownHostsFile` —
кириллический HOME). Эпохи на деке: `epochs/e-archive` (история 01–15.09, архив Bybit), `~/alpha/root` (запись с 16.09),
`epochs/e-aug` (август 2026, 76 монет, 41 ГБ), `epochs/e-crash` (обвал 10–11.10.2025). Бинарники: `bin/alpha-7bdf9a4`
(все денежные исправления — им считались u500r и август), `bin/alpha` → `alpha-e5c8847` (**ночная сетка дека считает
старым бинарником** — переключать ли, решает владелец). Диск дека 92 % (42 ГБ) — microSD 1 ТБ ждёт владельца. **Дек
засыпает при разряде батареи** (24.09 02:26–08:36 UTC) — держать на зарядке; запрет сна не ставить. У дека 4 ядра × 2
потока: «50 % простоя» = свободные SMT-потоки, ускорение параллельностью мало; на шаге `oos-frozen` кэш подходов D20
строится вторым проходом по стакану (~40 % времени суток) — кандидат на оптимизацию (проверить байт в байт).
**Сборка — на VPS** `13.140.29.171` (на деке нет `cc`), очередь `/opt/alpha-compute/.build.lock`, бинарник переносится
через эту машину; VPS — только сборки (данных записи и ночного счёта там больше нет, диск 49 %). **Коллектор**
`139.99.91.22`: с 24.09 08:57 UTC на `bd00ba5` (волны 1+2; откат — `/opt/alpha/alpha-collector-prev-20260924`); **пул 76
монет с 11:37 UTC** (В-95: 24 монеты без стен ни у лонга, ни у шорта — сняты с записи и из пулов бэктеста дека; копии
пулов `*.bak-20260924-before-dead`); пишет `.50` и `.200`; отправка на VPS выключена, дек забирает сам (01:15 UTC).
**Контроль:** одна сборка cargo на этой машине (хук `~/.claude/hooks/alpha_one_build.py`); `bash tools/alpha-status.sh`.

**Решения владельца** (`docs/plan/SETTLED.md` до В-95): позиция $500 / депозит $2500 / без плеча (В-89…В-91; но без
потолка числа позиций пик — $5–9 тыс., то есть плечо ×2–3,5 — см. август); выключатель BTC −1,5 %/1 ч и исключение
прокидов — рабочие варианты; доллары только с размером позиции (В-93); перенос через полночь (В-94); пул 76 (В-95);
шорт отложен (В-87). **Планы владельца:** после лонгов — тюнинг шортов ростом объёма и возраста стены; возможно
лонги на снижение издержек; затем лонги + шорты вместе ради числа сделок (`docs/findings/wall-availability-2026-09-24.{md,csv}`).

**Ждут владельца:** что дальше после провала кандидата на августе (предложение: проверить «BTC 4 ч» на ещё одном
независимом месяце — июль или октябрь — с предрегистрацией до счёта; либо пересмотреть вход); число для B6; окончательные
защиты; `bin/alpha` на деке; microSD. **Дальше по коду:** B2 → B4 → B6/B7; оптимизация кэша подходов в `oos-frozen`.

## Доки: структура и правило артефактов

```
docs/
  ARCHITECTURE.md   A1–A9 — обязательные ограничения
  COMMANDS.md       команды → артефакты, грабли
  plan/             PLAN.md, SETTLED.md (решения В-##), BUSINESS-TASK.md,
                    dev-plan-<дата>.md (план работ), runs.csv (журнал испытаний), candidates.csv
  adr/              ADR 0001–0012 — решения и их причины
  findings/         замеры и разборы, на которые опирается код или живое решение
  transcripts/      9 сырых расшифровок практиков с таймкодами (`отскок станок/dizzi`, `stanok_*`,
                    `dizzy_*`) — первоисточник базы отскока (В-64); цитаты [S mm:ss]/[D mm:ss] — по ним
  archive/          перекрытое (его целиком заменил живой док) и первичное (сырьё и
                    пруфы: расшифровки, CSV прогонов, старые пулы) — что именно, в
                    docs/archive/README.md
```

**Правило: механизм пишет результат по одному адресу из таблицы, а не «рядом с собой».**

| что создаётся | куда |
|---|---|
| разбор, вердикт, замер — текст | `docs/findings/<тема>-<YYYY-MM-DD>.md` |
| машиночитаемый артефакт вердикта (сводная таблица, кривые PnL) | `docs/findings/<команда>-<YYYY-MM-DD>[-<SYM>].<csv\|json>` |
| сырое и промежуточное (`levels-*`/`markout-*`/`touches-*.csv`, дашборд `index.html`+`*.json`, архивы binlog) | `data/` — это не документ, gitignored |
| журнал испытаний (одна строка = одно испытание) | `docs/plan/runs.csv` |
| «где мы» — состояние проекта (пул, сервер, диск, что сделано) | **только** `CLAUDE.md`, раздел «Состояние»; доки ссылаются на него, а не повторяют |

Изменение или замер, заменяющий прежний, **переносит** заменённое в `docs/archive/` тем же
коммитом и правит ссылки (`git grep` по старому пути — ноль). Удалять молча нельзя: память проекта
историческая. Ссылки в коде (`//!` в `src/`) — часть этой карты, их правит тот же коммит.

Обратное тоже правило: если на файл в архиве опирается **код** или живое решение как на
**единственный источник числа** — он не архивный, а рабочий, и возвращается в `docs/findings/`.
Так 2026-09-17 вернулись `recording-2026-09-11.md` и `pilot-2026-09-11.md` (обоснование разбора без
DOM и `ConnSink`), `collector-2026-09-12.md` (отсюда `record::ZSTD_LEVEL = 1`, гейт аллокаций, замер
761 инструмента), `binlog-format-2026-09-13.md` (v2-бюджет полей; читатель v2 `legacy_dead_fields`
живой). Класть в архив по дате, а не по содержанию, — ошибка.

## Команды

```bash
cargo build --release --target-dir target-ci          # target/release/alpha.exe занят коллектором
cargo test --release --target-dir target-ci 2>&1 | tail -5   # 1002 passed, 0 failed, 7 ignored (2026-09-24, после слияния волны 2 и ревью; на Linux +1 — тест SIGTERM под cfg(unix))
cargo clippy --release --target-dir target-ci --all-targets -- -D warnings   # ноль
cargo fmt --check
./target-ci/release/alpha.exe lob --help              # 26 подкоманд (lob export удалён 24.09), таблица — docs/COMMANDS.md
# олвейс-он коллектор (В-34): с копии бинарника, чтобы не держать target*; instruments.csv скопировать в --root
cp target-ci/release/alpha.exe data/always-on/alpha-collector.exe
./data/always-on/alpha-collector.exe lob session --pool-instruments instruments.csv --root data/always-on/<ts> --always-on
# штатная остановка: файл <root>/stop (В-41) — на ближайшем тике сброс писателей, session.json
# closed=true; на Unix так же (A8.2) работают SIGTERM/systemctl stop|restart и Ctrl+C из консоли
touch data/always-on/<ts>/stop
# докинуть монеты в идущую запись (T34): дописать строки в <root>/instruments.csv — подхват ≤ 10 с,
# свои <SYMBOL>-<день>.binlog и одно соединение на партию (несколько монет — одной записью файла)
grep '^ZECUSDT,' instruments.csv >> data/always-on/<ts>/instruments.csv
# снять монету / заменить пул (A8.1): переписать тем же форматом — файл обязан кончаться переводом
# строки (оборванный не читается), пиши атомарно: cp instruments.csv <root>/instruments.csv.new &&
# mv <root>/instruments.csv.new <root>/instruments.csv; на ближайшем тике ≤ 10 с файлы символа
# закрыты, соединение остановлено, событие в session.json.pool_removals
grep -v '^ZECUSDT,' data/always-on/<ts>/instruments.csv > /tmp/pool && \
  mv /tmp/pool data/always-on/<ts>/instruments.csv
# дашборд-сетка графиков (T41): вотчер живёт с копии data/dashboard/alpha-dashboard.exe;
# пишет index.html + data.json + coin-<SYMBOL>.json на монету (вся история записи)
./target-ci/release/alpha.exe lob dashboard --root data/always-on/<ts> --out data/dashboard [--watch 120] [--h3-k 10]
python tools/serve_dashboard.py data/dashboard        # → http://127.0.0.1:<порт>/index.html (с file:// coin-*.json не грузятся)
# проверка страницы: текстовый пробник (дёшево) и скриншот (когда чисел не хватает)
python tools/shot.py http://127.0.0.1:<порт>/index.html .tmp-shot/grid.png --text-only
python tools/shot.py http://127.0.0.1:<порт>/index.html .tmp-shot/grid.png [--solo SOLUSDT] [--hover 0.5,0.5]
```

Релизная сборка с нуля ~4 мин, инкрементально — секунды. Тесты офлайн (сеть — только за
`#[ignore]`). `data/` gitignored; `docs/plan/candidates.csv` коммитится отдельно.

## Структура

```
src/
  main.rs, lib.rs, alloc_count.rs   вход; счётчик аллокаций — гейт GC
  binlog/      формат лога: v3 (группы-сообщения, `block` битом, время на сообщение) и чтение v2
               (её пишет живой коллектор до перезапуска, В-48); Reader/Writer, дельты varint, zstd
  book/        книга, MarketDepth крейта, целые тики
  bybit/       адаптер площадки: conn (сокет, ресинк), ws (разбор), rest (вне событийного пути),
               verify + verify_sidecar (сверка с REST), trade_ws, sign, clock, probe
  feed/        trait Feed; replay (бинлог), live (N сокетов, один ОС-поток)
  lob/         чистая логика: levels, markout, costs, cells, shortlist, final_metrics, watch,
               strategy (единственная стратегия), runs, markup,
               touch_axes (корзины осей касаний В-44 и сетка профилей касаний — единственное место, T36/T37)
  stats/       bootstrap-t на весах Уэбба
  commands/
    record.rs              Recorder, run_record; record/{errors,gaps,paths,steps}
    lob/mod.rs             дерево модулей, LobCommand, dispatch, test_support
    lob/h3.rs, replay.rs, parts.rs, names.rs   порог H3 и CLI-аргументы; реплей → уровни/середина;
                           части и сутки сессии; имена сторон/исходов
    lob/session.rs         SessionCtx, run_session; session/{args,pool,sink,summary,resources}
    lob/pilot.rs           run_pilot; pilot/{k_grid,metrics,gates,inputs,chain}
    lob/profiles.rs        run_profiles; profiles/{axes,accumulate,coverage,sessions,table}
    lob/dashboard.rs       данные страницы (уровни + блок касаний, T36); dashboard_page.html — сама страница (include_str!)
    lob/touches.rs         run_touches — касания живых уровней → touches-<SYMBOL>.csv (T35, В-42)
    lob/touch_profiles.rs  run_touch_profiles — профили касаний → docs/findings/touch-profiles-<дата>.csv (T37, В-44)
    lob/pick/, react.rs, power.rs, backtest.rs, shortlist.rs, watch.rs, …
```

Тесты каждого модуля — в `<модуль>/tests.rs` (`#[cfg(test)] mod tests;`; для `mod.rs` —
`<каталог>/tests.rs`); `include_str!` в тестах — с `../`.

## Артефакты — коротко (полностью: `docs/COMMANDS.md`)

- `lob bounce-grid` — сетка форм: вход (`--signal touch|approach`, `--entry-form`), стоп (`--stop-form before|at|behind|
  midfr|stack2|pct<x>|s<a>`), тейк (`--take-form 1to1|tk<x>|t<b>|tr<a>x<t>|half1to1|eat<h>x<a>`), дедлайн (`--deadline-secs`
  60…14400), выход по стене (`--exit-form none|eat<X>|gone<W>|gone<W>tr<T>|gone<W>be|gone<W>bex`), наборы `--set имя:ключи`
  (`age flow side frontrun eaten usd_min <ось>_min/_max`, оси режима `btc1h btc4h pool1h pool4h`), память кругов
  `--round-memo on|off` (байт в байт, гейт `gate-g10.sh`) → `rounds.csv` + `forms.csv` по набору. **K1:** без маркера
  `verify-<SYM>.status == ok` — отказ. Стандартная задержка В-68: `--median-rtt-ns place=4200000,cancel=3980000,taker=5650000
  --p95-rtt-ns place=4790000,cancel=4550000,taker=6420000`; очередь F10 — `--queue-model prob:3`.
- `lob bounce-verdict --grid-dir` — вердикт (n ≥ 100, суток/кластеров, PBO/CPCV, DSR по журналу испытаний).
- `lob touches [--approach-bps D]` — касания/подходы; `lob import-archive` — сутки архива Bybit → бинлог v3; `lob verify`.
- Скрипты счёта (`tools/compute/`): `nightly-grid.sh` (ночь), `oos-frozen.sh` (замороженная F10; `SETS`, `FORM_EXIT`,
  `NO_MERGE`, сутки параллельно), `titrate*.sh` (режим / выход / защита / безубыток), `gate-g10.sh`, `placebo.py`
  (контроль «рост рынка» M19), `titration-read.py`, `exit-titration-read.py`.

## Правила, которые ловят ревью

Семь запретов горячего пути (ноль аллокаций на событие после прогрева, время только через
трейт `Clock`, без `async` с захватом состояния в потоке решений, без REST в событийном цикле,
сборка/подпись ордера до триггера, без `f64` для цены/размера, без `BTreeMap`/`HashMap` на
пути события) — полный текст в `interfaces.md`. Изобретённое число запрещено — измеримое
измеряется, назначаемое — коммитом/решением В-##. Расстояние только в bps. Шаг не закрыт,
пока нет названного артефакта на диске. Секреты — только имена переменных (`BYBIT_API_KEY`,
`BYBIT_API_SECRET`), значения не печатаются и не пишутся.

## Экономия контекста (В-39)

Один ревьюер по трём осям для тасков без вердикта/контракта/предрегистрации/горячего пути;
три — только для них; материал ревьюеру — дифф и тикет. Закоммитил тикет → память и передача →
клир. Правки — точечным `Edit`, файлы читать секциями, вывод команд через `tail`/`grep`,
страницу проверять `read_page`, скриншот — когда текст не отвечает. Длинный текст/HTML —
отдельным файлом (`include_str!`). Усилие `high`; выше — только дизайн и ревью вердикта.
**Исполнители — по одному:** на машине живёт коллектор, параллельные `cargo build` в
нескольких worktree дают CPU 100 % и OOM (2026-09-12) — один агент → сборка → слить → следующий.

Дополнено 2026-09-13 (владелец: «нужно продумать подход к экономии токенов»). Цена сессии ≈
(размер контекста) × (число шагов), поэтому:

- **Вид проверяется текстом, не картинкой.** `python tools/shot.py <url> <png> --text-only` печатает,
  что страница нарисовала (окно, полосок и касаний, упёрлось ли в потолок, сколько живых в кадре).
  `read_image` — только когда числа не отвечают, и в отчёте одной строкой «зачем понадобился».
- **Разведка незнакомого кода — субагентом или `grep` + секции**, не полным чтением: 1 строка ≈
  10–15 токенов, `dashboard.rs` целиком — ~25к.
- **Одна сборка на партию правок.** Для вида страницы Rust не нужен: шаблон + существующий `data.json`
  рендерятся питоном; сборка — только когда меняется Rust.
- **Дешёвый вывод:** `Select-String`/`tail`/`grep -c` вместо дампа, `job_output --wait` вместо цикла
  «сон–проверка», `Edit` вместо перезаписи файла целиком.
- **Каталог скиллов и workspace-инструкции оплачиваются каждым шагом** (в этой сессии ~150 скиллов):
  держать в них только то, что работает на каждом шаге, остальное — в файлы по требованию.

Дополнено 2026-09-13 после замера расхода (`dsh-cost-meter`, леджер `~/.dsh/storages/cost-meter/`):
день 2026-09-13 — 1133 вызова, **$1.95**: кэш-реплей $1.11 (57 %), output+reasoning $0.47 (24 %),
свежий вход $0.37 (19 %); тариф непиковый (пик ×2 его цены, окна 01:00–04:00 и 06:00–10:00 местного,
мы в них не работаем). Отсюда:

- **Шаг дороже сжатия.** Каждый вызов реплеит весь контекст, поэтому «батчить проверки в одну
  команду» сильнее любого сжатия вывода. Независимые команды — один вызов, не пять шагов.
- **Поиск по коду — индексом, не грепом по кругу.** `dsh-code-index` (tree-sitter, Rust поддержан)
  даёт `code_map`/`code_symbols`/`code_search`/`code_refs` — один вызов вместо цепочки `grep`→`read`.
- **Ответы коротко, без пересказа.** output+reasoning — четверть счёта: не повторять сделанное,
  не печатать вывод инструментов заново, ссылаться на путь.
- **Разведка незнакомого — субагентом:** его контекст не наш (4 README ≈ 30к в контексте против
  ~1.5к за отчёт).
- **Плагины, переписывающие историю, не ставить:** 99.8 % входа — кэш-чтение ($0.003/1M против
  $0.15/1M промаха), поэтому сдвиг префикса = полный пересчёт истории.

## Грабли — коротко (полностью: `docs/COMMANDS.md`)

- `--h3-mode floor|percentile` обязателен у `levels`/`markout`/`watch`/`profiles`/`shortlist`
  (умолчание `floor` есть только у `dashboard`); `percentile` требует часа прогрева
- `k` для пола `H3` — процедура пилота (В-30), не число; пилот 30 мин: `k` не определим, G0/G1/
  G-POWER-B красные — дальше решение владельца
- `lob probe` ставит реальные ордера — не гонять без надобности
- бинлог сессии ищется одним резолвером `commands::lob::session_binlog_for`; сутки и час — у
  каждой части (`session_parts_for`)
- олвейс-он: кадр на диске не реже 10 с, читатели не видят только хвост ≤ 10 с; `session.json`
  между часовыми записями — стартовый; остановка — файл `<root>/stop` (Ctrl+C из оболочек агента не
  доходит), на Unix — ещё SIGTERM (`systemctl stop`/`restart`, перезагрузка) и Ctrl+C из настоящей
  консоли: все три пути штатные (A8.2) — в проде с 17.09 21:55Z живёт бинарник с A8
- пул на ходу (A8.1): снятие строки применяется на ближайшем тике, только если файл **дописан**
  (разобрался и кончается переводом строки) — пиши атомарно (`mv` поверх), иначе оборванный файл
  читается как «убрать монеты»; снятый символ закрывает файлы обоих потоков, а соседи по его сокету
  переподключаются (шов `gaps.csv`, как у переподключения); вернувшийся в файл символ — новый индекс
  и новая часть (`-pN`), история снятого остаётся в `instruments`/`binlog_files`, момент — в
  `session.json.pool_removals`
- `lob dashboard` перечитывает все бинлоги при каждом расчёте (10 × 1.5 ч — 13 с); порог — из
  `instruments.csv` каталога записи; «жив/нет» — по росту бинлогов между двумя расчётами;
  `data.json` старого формата (без `touches`/`chart_file`) базой для «жив» не считается — первый
  расчёт новым бинарником честно печатает «не знаю». Блок касаний: `stack_levels` не показывается при
  метке `# debug` в `instruments.csv`; при заглушке `k = 1.0` он печатается как «49 из 50» — вырожден
  порогом, не отладкой (В-43). Страница грузит `coin-*.json` через `fetch` — только по `http`
  (`tools/serve_dashboard.py`), с `file://` плитки печатают «график не загружен»; `data.json`
  перечитывается раз в 30 с, `coin-*.json` — раз в 120 с у видимых плиток (`IntersectionObserver`)
- Bybit отдаёт `403` с части стран; с этой машины доступ есть
