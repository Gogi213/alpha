# tools/archive — снятые счётные скрипты

Сюда переносятся скрипты `tools/`/`tools/compute/`, которые перестали быть частью текущего
конвейера (правило `CLAUDE.md`: заменяющее переносит заменённое сюда тем же коммитом, не удаляя).
Ссылки на них в `docs/archive/` и `.memory/` — история, их не правим.

| файл | почему в архиве |
|---|---|
| `f10-verdicts.sh` | вердикты по `b5/f10fix-{D20,D50}/*` (F10-fix, план до 22.09) — семья `f10fix` снята заморозкой F10 (22.09, `oos-frozen.sh`); живых ссылок нет (2026-09-23) |
| `mark-e15-invalid.py` | разовая правка журнала испытаний (пометить прогон E15 21.09 `confirmatory → amendment`) — применена, журнал уже несёт правку; повторный запуск не нужен |
| `touches-all.sh` | разовый замер касаний всех монет для порога В-61 (18.09) — вытеснен ночным H3-шагом `nightly-grid.sh` (`study/touches/<сутки>/`), который считает то же и по расписанию |
| `chain-day.sh` | разовый триггер ночного скрипта сразу после переноса суток с коллектора (20.09, до переезда счёта на VPS) — перенос суток и ночь синхронизированы иначе после L8 (22.09), скрипт не вызывается |
