# 10 — `lob profiles`: таблица профилей

**Требования:** R01, R15, R55, R57, R58, R59, R65, R66
**Blocked by:** 03, 06, 09, 15
**Зона:** `src/commands/lob/profiles.rs`
**Волна:** 4
**Status:** blocked

## Что должно заработать

Появляется первый из трёх артефактов задачи. Сегодня его не производит ни одна
команда: `shortlist.rs` — библиотечный код, достижимый только своими
юнит-тестами, при собственном правиле проекта «каждое число получается одной
командой».

## Из брифа, дословно

> «`docs/findings/profiles-<дата>.csv`: строка на профиль, колонки раздела 4.
> Полная, без отсева — включая профили с плохим результатом и профили
> с недобором `n`. Отсев в таблице означал бы, что читатель видит только
> выигравших»

> «Отдельно и всегда — сырое движение середины без нормировки знаком»

> ««Сейчас» … читается как скользящее окно фиксированной длины … длина
> назначается до данных и печатается в обоих артефактах»

## Разделы спецификации

Истории 15–22, 39–41. Решения §5, §6.

## Критерии приёмки

- [ ] `lob profiles` производит `docs/findings/profiles-<дата>.csv`
- [ ] по каждому горизонту — флаг `unreachable` из гейта G-LAT
- [ ] строка на профиль; колонки: `n`, доли `eaten`/`pulled`/`mixed`, `m` на четырёх горизонтах с интервалом, `net`, `fill`, `net_fill` с нижней границей, час старта сессий
- [ ] **сырое движение середины без нормировки знаком** — отдельная колонка, всегда
- [ ] таблица полная: профили с плохим результатом и с недобором `n` остаются
- [ ] непригодная корзина отсутствует, а не присутствует нулём
- [ ] длина скользящего окна напечатана в файле
- [ ] тест: снести файл, прогнать команду, получить те же числа
- [ ] **GC**
