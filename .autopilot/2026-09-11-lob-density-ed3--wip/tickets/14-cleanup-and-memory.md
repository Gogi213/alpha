# 14 — Чистка репозитория и память проекта

**Требования:** R65, R70, R71, R73
**Blocked by:** 13
**Зона:** `data/`, `.autopilot/`, корень
**Волна:** 7
**Status:** blocked

## Что должно заработать

Репозиторий перестаёт нести данные, которые нельзя использовать, и дашборд
перестаёт утверждать то, чего диск не подтверждает.

## Из брифа, дословно

> «нужно чекнуть проект, сделать множество ревью на тему того что вообще
> в этом проекте оставляем что нет»

> «инструмент, в котором проверка новой идеи занимает минуты, а не переписывание
> кода»

## Разделы спецификации

`docs/plan/REVIEW-2026-09-11.md`, раздел 3.

## Критерии приёмки

- [ ] снесены `data/live5`, `live5b`, `live5c`, `live5d`, `live5f`, `live5g`, `live5h`, `data/smoke`, `data/pickdbg`, `data/bybit` — записи исключённого `BTCUSDT` и упавшие `pick` по отменённым правилам
- [ ] перед сносом подтверждено `cmp`, что полезное из `live5g` побайтово совпадает с `tests/fixtures/BTCUSDT-2026-09-09.binlog`
- [ ] `.autopilot/state.js` поле `dir` указывает на **существующий** каталог
- [ ] `.autopilot/index.html` либо симлинк на `dashboard.html`, либо свежая копия; расхождения нет
- [ ] `.autopilot/README.md` существует и содержит строку прогона с итогом
- [ ] дашборд показывает те же числа, что диск: ни одна цифра не подпёрта отсутствующим артефактом
- [ ] `CLAUDE.md` описывает: как поднять, как прогнать тесты, где что лежит, какие команды дают какой артефакт
- [ ] `git status` чист
