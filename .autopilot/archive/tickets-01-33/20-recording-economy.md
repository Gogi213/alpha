# 20 — Запись: доказать, что работает и экономна (короткие прогоны)

**Требования:** R38, R43, R79, R80
**Blocked by:** 19
**Зона:** `src/commands/lob/session.rs` (замер и отчёт), `src/feed/live.rs`, `src/bybit/{conn,ws}.rs` (профилирование разбора и починка узкого места), `docs/findings/recording-<дата>.md` (вердикт)
**Волна:** 8
**Status:** ready

## Что должно заработать

Владелец 2026-09-12: «нельзя делать длинные прогоны, пока мы не уверены, что
система работает хорошо и экономно по записи». Слепая приёмка: живой `lob
session` по 8 инструментам — разбор p99 = 1136.6 мкс по 221 406 кадрам против
суббюджета 200 мкс; на одном инструменте `lob react` даёт 68.7 мкс.

## Из брифа, дословно

> «систему нужно планировать такую чтобы была супер быстрая сразу»

> «Отпадает VPS … и потолок в 20 ГБ»

## Критерии приёмки

- [ ] серия из **трёх** сессий по 5 минут на пуле (`instruments.csv`), по одной, не параллельно; между ними — ничего; каждая ≤ 5 мин
- [ ] по каждой сессии и в сумме — таблица против бюджетов плана: байт/мин на инструмент и экстраполяция в ГБ/сутки против 1.5 ГБ/сутки (Decision 23а); CPU < 5 % ядра суммарно (PLAN 6, GC); RSS раз в 30 с — плоский (без роста между началом и концом больше, чем на один буфер); `gaps = 0`; `verify = ok` по всем; p99 разбора против 200 мкс
- [ ] p99 разбора под пулом: профиль (где время — разбор JSON, книга, запись бинлога, канал), одна починка узкого места без нарушения семи запретов, повторный замер в той же серии; если 200 мкс недостижимы — число и причина, суббюджет пересматривается по замеру (PLAN 3.1), общий 5 мс — нет
- [ ] вердикт `docs/findings/recording-<дата>.md`: «экономно / нет» по каждой строке бюджета, с числами; это первый файл в `docs/findings/`, не `debug` — три сессии по 5 минут после G-DEBUG данные о **записи**, не о рынке
- [ ] **GC**
