# 32 — Дашборд проекта на localhost: что пишется, что насчитано, где мы

**Требования:** R87 (дополнение владельца 2026-09-12: «мне нужна минимальная визуализация проекта … повесить что то на локалхост чтобы я хоть что то понимал»), R85, R62–R64 (форма результата), В-38 (контрольные точки 30 мин / 1 ч / 12 ч)
**Blocked by:** —
**Зона:** новый `src/commands/lob/dashboard.rs`, регистрация в `src/commands/lob/mod.rs` (только новая подкоманда), новый `tools/serve_dashboard.py` (или переиспользовать приём `.autopilot/sync.py` — `python -m http.server`), `CLAUDE.md`. Не трогать: `session.rs`, `feed/`, `bybit/`, `pick/**`, `profiles.rs`, `shortlist.rs`, `final_metrics.rs` (в рабочем дереве чужие незакоммиченные правки — работай в `git worktree` на HEAD с собственным `--target-dir`; `target/release/alpha.exe` основного дерева занят живым коллектором и не пересобирается).
**Волна:** 11
**Status:** done (ревью не проведено — лимит сессии; повторить в следующей)

## Что должно заработать

Владелец 2026-09-12: «мне нужна минимальная визуализация проекта потому что я пока
ничего не понимаю, только с твоих слов. надо повесить что то на локалхост чтобы я
хоть что то понимал». И там же: «первые сутки и 7 суток не нужны. меняем первые
сутки и 7 суток на полчаса-час-12 часов» — контрольные точки промежуточных
выводов (В-38). Живой олвейс-он каталог сейчас — `data/always-on/20260912T122440Z`
(8 инструментов, `session.json` раз в час, бинлоги — часть на сутки).

## Из брифа, дословно

> «мне нужна минимальная визуализация проекта потому что я пока ничего не понимаю,
> только с твоих слов. надо повесить что то на локалхост чтобы я хоть что то понимал»

> «первые сутки и 7 суток не нужны. меняем первые сутки и 7 суток на полчаса-час-12 часов»

## Критерии приёмки

- [ ] `lob dashboard --root <каталог олвейс-он> --out <dir> [--h3-mode floor] [--watch <секунды>]` пишет самодостаточный `index.html` (inline CSS/JS, без внешних CDN — открывается и с файла, и с localhost) плюс `data.json`; `--watch` пересчитывает и переписывает оба файла каждые N секунд (std, без новых зависимостей); страница сама перечитывает `data.json` раз в 30 с (`fetch` относительным путём — работает под `python -m http.server`)
- [ ] `tools/serve_dashboard.py` (или команда в `CLAUDE.md`): `python -m http.server <порт> --directory <out>` — одна команда, порт печатается; `CLAUDE.md` — как запустить и открыть
- [ ] **Блок 1 «Коллектор сейчас»**: жив/нет (по `session.json.updated_utc`/`closed` и по росту размера бинлогов между обновлениями), с какого момента, инструменты, записей, байт, КБ/мин, экстраполяция ГБ/сутки, `gaps`/`reconnects`/`resyncs`/`frames_failed`, RSS и CPU (последний `samples`), NTP offset из `clock.csv`; каждое число — с подписью простыми словами, что это и норма ли (пороги из `PLAN.md` 6.1/В-34 — не изобретать)
- [ ] **Блок 2 «Контрольные точки 30 мин / 1 ч / 12 ч»** (В-38): достигнута ли (по времени записи), и по достигнутым — вывод в одну фразу по данным ниже (уровней/час, доля `eaten`, `m_10s` по исходам, `net` с назначенным RTT 20 мс — В-37, пометка `assumed`); честная оговорка «данными по §7 не является: суток < G_MIN» — текстом, не мелким шрифтом
- [ ] **Блок 3 «По инструментам»**: таблица — записей, КБ/мин, уровней/час, `eaten`/`pulled`/`mixed` %, `m_10s` для `eaten` и `pulled`, время жизни (медиана); считается **самим дашбордом** из бинлогов через существующие `lob levels`/`markout` пути (`commands::lob::replay_symbol`/`lob::markout` — переиспользовать, не писать вторую разметку), на закрытом кадре (последние ≤ 10 с не видны — сказать)
- [ ] **Блок 4 «Где мы»**: гейты (G-DEBUG, G-POWER-A, G0, G1, G-POWER-B, G-LAT, GC) — статус и одна фраза, что значит; вердикт пилота 30 мин (`docs/findings/pilot-2026-09-11.md` — числа) и что он означает простыми словами; аудит — пять пунктов и их статус (таски 27–31); что дальше — читается из `docs/plan/PLAN.md` «Точка остановки» **не парсингом markdown, а вручную перенесённым в код текстом? — нет**: текст блока 4 — статический в `dashboard.rs` с датой, обновляется таском, который меняет состояние; изобретать статусы запрещено
- [ ] Язык — русский, простыми словами; каждый термин (уровень, `eaten`, markout, bps, `net_fill`) — одно предложение пояснения при первом появлении; никаких «на словах» — все числа из файлов
- [ ] Тесты: рендер на фикстуре каталога (две части, `session.json`, `clock.csv`) → `data.json` с ожидаемыми полями; контрольные точки по времени; страница валидный HTML (проверка тегов), без внешних URL
- [ ] Проверить живьём на `data/always-on/20260912T122440Z` (не трогать его файлы, писать в `data/dashboard/`), открыть в браузере — скриншот не нужен, но stdout `http.server` с 200 на `index.html` и `data.json`
- [ ] `cargo test --release`, clippy `-D warnings`, fmt — чисто; новых зависимостей нет
- [ ] **GC**
