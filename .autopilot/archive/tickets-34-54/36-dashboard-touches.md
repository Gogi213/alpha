# 36 — Касания на дашборде

**Требования:** R90 («то о чем говорят пацаны это важно очень»), владелец 2026-09-13: «обновляем
дашборд и делаем идею»; В-42, В-43, В-44
**Blocked by:** 35 (фикс В-43 должен быть закоммичен)
**Зона:** `src/commands/lob/dashboard.rs`, `src/commands/lob/dashboard_page.html`, `dashboard/tests.rs`,
`src/lob/touch_axes.rs` (новый) + `touch_axes/tests.rs`, `src/lob/mod.rs`, `CLAUDE.md`, `docs/COMMANDS.md`
(`touches.rs` править не пришлось).
**Не трогать:** `lob/levels.rs`, `replay.rs`, `session.rs`, `profiles/`, `shortlist`.
**Волна:** 12
**Status:** done (ревью — следующей сессией)

## Что должно заработать

На странице «Монеты и плотности» у каждой монеты — блок **«Касания: цена дошла до плотности»**,
из `ReplayDay.touches` (тот же реплей `replay_symbol`, второго прохода нет; касания уже в `stats`).
Простыми словами, термины поясняются при первом появлении (словарь пополнить: касание, отскок,
фронтран, подход).

1. **Итог**: касаний всего; исход — отскочила (уровень жив, цена ушла) / проели на касании
   (`ended_by_death`) — n и доли; `m` «в сторону отскока» на 4 горизонтах по исходам;
   `net` 10 с сделки-отскока не считается здесь (это бэктест T38) — только markout.
2. **По осям В-44** — таблица на ось: возраст (3 корзины), фронтран (3), круглость (3), номер
   касания (3), подход за 1 с (4, включая `<0`), длительность (3), сторона: n, доля отскоков,
   `m_10s` отскок, `m_10s` все. Корзины и границы — **из одного места**: `lob::touch_axes`
   (новый небольшой модуль в `src/lob/`, константы с цитатой источника в doc-комментарии) —
   T37 возьмёт те же; здесь их завести.
3. **Крест исход × возраст** — одна таблица 2 × 3.
4. **Картина за час**: на линии цены — метки касаний в момент `start_ms` (маленький треугольник:
   вверх у бид-уровня, вниз у аск), цвет по исходу (зелёный отскок / красный проели); наведение
   — цена, возраст, фронтран, подход, `m_10s`; фильтр «показывать касания» вкл/выкл; потолок как
   у полосок (`PICTURE_MAX_BARS`, самые крупные по размеру на касании).
5. Плитка монеты: «касаний за запись» и «отскочила %».
6. Оговорки текстом: `m` касания — база на момент касания (В-43); при отладочном пороге
   `stack_levels` вырожден — не показывать; суток < `G_MIN` — наблюдение, не вердикт.

## Критерии приёмки

- [x] `lob::touch_axes`: функции корзин для каждой оси В-44 + тесты границ (включительно/исключительно
      ровно как в В-44; `touch_index` 0 → «1-е», 1–2 → «2–3-е», ≥3 → «4-е+»)
- [x] `dashboard/tests.rs`: фикстура с одним касанием-отскоком и одним касанием-проездом →
      `data.json` несёт `touches.total = 2`, доли, строки осей с `n`, крест 2×3; без касаний —
      блок с нулями, не ошибка
- [x] страница: блок рендерится, метки на картине есть (тест на HTML — строка `touch` в шаблоне,
      JSON не закрывает `script`), внешних URL нет
- [x] живьём на `data/always-on/20260912T201737Z` (запись идёт — только чтение; вотчер уже
      крутится с копии бинарника — **не трогать**): один расчёт в `data/dashboard-check/` (не в
      `data/dashboard/`), показать числа блока для SOL
- [x] `cargo test --release --target-dir target-ci`, clippy `-D warnings`, fmt; одна сборка за раз
- [x] `CLAUDE.md` строка дашборда + `docs/COMMANDS.md`; `interfaces.md` «Из таска 36»
- [x] **GC**: горячий путь не тронут

## Что видно на живой записи (боевая десятка, `k = 1.0` заглушка, 1.47 ч)

SOL: 348 касаний (236 в час), отскочила 56 (16.1 %), проели на касании 292; `m_10s` отскоков
+1.25 bps, проели −0.43, все −0.16. Фронтран `(0,0.5)` отскакивает чаще (24.0 %) и лучше
(+1.34), чем `[0.5,inf)` (9.8 %, +1.06); 2–3-е касание отскакивает вдвое чаще 1-го (30.0 % vs
14.8 %); подход `[1,2.5)` bps — `m_10s` отскоков +2.08; касания от 1 с отскакивают чаще
(25.5 % / 23.1 %), но `[10s,inf)` даёт `m_10s` 0.00. Ось возраста при 1.5 ч записи сверху
пуста (0 старше часа); завал — медиана 49 из 50 (заглушка `k = 1.0`, как в T35);
`size_below_h3` = 9. Суток 1 < G_MIN — наблюдение, не вердикт. Расчёт десяти монет — 13 с;
LSK — 35 579 касаний, на картине 4000 меток из 24 143 (потолок `PICTURE_MAX_BARS`).
