# Передача таска 25 — №1 (потолок контекста, дерево зелёное: 624 passed, clippy 0, fmt чисто)

СДЕЛАНО: закрыты — `--always-on` (взаимоисключающий, `SessionPlan`, без дедлайна, `session.json.always_on/duration_s` фактические); ротация суток через `record::claim_part_with` с синтетическим снапшотом первым кадром (`Book` на инструмент, как `lob record`); `binlog_files` дописывается на ротации; `session.json` на старте/раз в час/на ротации/финал (`closed`), через tmp+rename; `clock.csv` — свой ОС-поток раз в `HOURLY_REFRESH_SECS` + финал; Ctrl+C через `feed::live::StopHandle` → `None` от `Feed` (тест на `stop()` как сигнал-заменитель, второе нажатие — exit 130); `reconnects`/`resyncs`/`frames_failed`/`bytes_written` в `session.json`, строка `gaps.csv` на каждый (`GapKind::WriteFailed` новый); ревью-24 (а)(б)(в) в `ws.rs` (`fast_topic_kind` якорем в начале + `probe_topic_kind`, `BadShape` по `classify()`); окно потери `record::FRAME_LOSS_WINDOW_SECS = 10` (из doc `FRAME_TARGET_RECORDS`) — тик `Event::Tick` с рантайма ввода-вывода раз в окно, сброс всех батчей на тике (тесты: тихий инструмент, ноль событий); `samples: [{ts_utc, rss_bytes, cpu_pct}]` в `session.json` (сэмплер больше не печатает); stderr — одна строка в час; `FrameSink` — кадр одним `write_all`, файл всегда на границе кадра (анализ живого каталога без правки читателей); zstd замерен 1/3/6/9 → `ZSTD_LEVEL = 1`. НЕ закрыты: живой прогон ≤ 5 мин с анализом по накопленному; findings-раздел; `CLAUDE.md`; `SETTLED.md` В-34.
ФАЙЛЫ: готово — `src/feed/mod.rs` (`Event::Tick`, `GapKind`), `src/feed/live.rs` (`Item`, тикер, `StopHandle`, `spawn_with_ticks`), `src/commands/record.rs` (`claim_part_with`, `FRAME_LOSS_WINDOW_SECS`, `WriteFailed`, `event_exch_ms` pub(crate), `ZSTD_LEVEL=1`), `src/binlog/mod.rs` (`Writer::get_ref`, `max_frame_bytes_on_disk` — формат не тронут), `src/bybit/ws.rs`, `src/commands/lob/session.rs` (ядро `run_session_loop(&mut dyn Feed, &mut SessionCtx)`), `src/commands/lob/pilot.rs` (`always_on: false`), `tests/collector_bench.rs` (бенч zstd, `#[ignore]`). Не начато: `docs/findings/collector-2026-09-12.md` (раздел таска 25 с числами zstd выше и живым прогоном), `CLAUDE.md`, `docs/plan/SETTLED.md`.
РЕШЕНИЯ: тик — вариант `feed::Event`, а не таймаут в `next_event`: горячий путь `blocking_recv` не тронут, реплей тиков не шлёт; `Stop` идёт FIFO по тому же каналу (ничего принятого не теряется), `StopHandle::stop` блокирующая (try_send на полном канале потерял бы остановку); сброс на **каждом** тике всех непустых батчей (гарантия ≤ окна; по возрасту дало бы ≤ 2 окна) — цена: кадры активных инструментов ≤ 10 с вместо ровно 1000 записей, байт/запись **надо перемерить живым прогоном** против 7.30 таска 24; `session.json` раз в час, не чаще (буква таска), плюс сразу на старте — `profiles`/`watch` видят каталог с первой минуты; книга в сессии вернулась ради снапшота ротации (док в `write_market_event`); zstd 1 — доминирует 3 по байтам и CPU, 6/9 не взяты (обоснование в doc `ZSTD_LEVEL`).
ТУПИКИ: `BufWriter` оставляет на диске длину без тела → `ShortRead` у читателей на живом файле — заменён на `FrameSink` (один `write_all` на кадр), буфер зарезервирован по `max_frame_bytes_on_disk(FRAME_TARGET_RECORDS+128)` — иначе гейт 10⁶ ловит 1 аллокацию роста; `write_market_event` обязана сама сбрасывать по порогу (тесты зовут её напрямую) — вынос сброса в цикл дал 9 аллокаций роста батча; `Instant::now()` в тесте `live.rs` ловит греп-гард — убран; heredoc bash с длинным python рвётся — скрипты правок класть в scratchpad файлом.
ДАЛЬШЕ: (1) `cargo build --release`; один живой прогон `timeout 330 ./target/release/alpha.exe lob session --pool-instruments instruments.csv --root data/always-on/<ts> --always-on` (timeout даёт не-ноль код — это не Ctrl+C; для проверки кода 0 нажать Ctrl+C руками или второй короткий прогон), во время него — `lob verify --symbol SOLUSDT --root <dir>`, `lob levels --symbol SOLUSDT --root <dir> --h3-mode floor --warmup-ms 0 --repeat-window-ms 3600000` (нужен `instruments.csv` в `--root` — скопировать), `lob markout ... --h3-mode floor`; потом прочитать `session.json` (`samples`, `bytes_written`/`records_total` против 7.30 Б/запись, `parse_p99_ns`, `reconnects`); (2) раздел «Таск 25» в `docs/findings/collector-2026-09-12.md` с числами zstd (1: 7.245/170нс, 3: 7.307/215, 6: 6.908/475, 9: 6.839/824 на 182 641 записи) и живого прогона; (3) `CLAUDE.md` — команда запуска/остановки always-on, грабля про `timeout`; (4) `SETTLED.md` В-34 словами владельца («как бы повесить олвейсон коллектор, но супер экономный», снимает R37/R38 в части сессий 5–15 мин, сутки — единица кластера). Шов — `run_session_loop` над `ScriptedFeed` в тестах `session.rs`.
