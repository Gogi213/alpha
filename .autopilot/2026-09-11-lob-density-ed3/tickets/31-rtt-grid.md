# 31 — Сетка задержек в бэктесте: точка безубыточности вместо одного RTT

**Требования:** R28–R31 (§4 `fill`, `net_fill` — «вот это и есть окупается»), R57, дополнение владельца 2026-09-12 (В-37)
**Blocked by:** 29
**Зона:** `src/commands/lob/backtest.rs`, `src/commands/lob/profiles.rs` (шапка `rtt=…`, колонки `net_fill` по сетке — после таска 30), `src/commands/lob/shortlist.rs` (шапка вердикта: `rtt_break_even_ms`, `rtt=assumed`), `src/lob/costs.rs` (аддитивно), `CLAUDE.md`. Не трогать: `session.rs`, `feed/`, `bybit/`, `pick/**`.
**Волна:** 12
**Status:** ready (после 29 и 30)

## Что должно заработать

Аудит п. 2: `net_fill` ни разу не посчитан — RTT на входе бэктеста, замера нет.
Владелец 2026-09-12 отказался от реального ордера («зачем ставить мои заявки
в стакан?»), предложил эмуляцию бэктестером и назначил задержку: «ну допустим
задержка на глаз 20 мс, пока у нас нет сервера байбита, просто примем за факт»
(В-37). Бэктестер задержку **берёт на вход**, поэтому честный вердикт без
замера — чувствительность: `net_fill` по сетке задержек и задержка, при которой
эдж исчезает.

## Из брифа, дословно

> «вот это и есть окупается»

> «ну допустим задержка на глаз 20 мс, пока у нас нет сервера байбита, просто
> примем за факт»

## Критерии приёмки

- [ ] `lob backtest`/`lob profiles`/`lob shortlist`: помимо обязательных `--median-rtt-ns`/`--p95-rtt-ns` — `--rtt-grid-ms <список>` (без умолчания в коде; в `CLAUDE.md` — сетка владельца, например `5,20,50,100,300`, и ссылка на В-37); для каждой задержки сетки считается `fill`/`net_fill` тем же `FillModel` (median = p95 = точка сетки, названо в doc); колонки `net_fill@<ms>` в `profiles-*.csv`, кривая — в `backtest-*-pnl.csv`
- [ ] шапка всех трёх артефактов печатает `rtt=assumed(20ms, В-37)` при значении из В-37 и `rtt=measured(...)` только если источник — `probe-*.csv`; источник различается флагом `--rtt-source assumed|probe:<путь>` (или эквивалент без умолчания), не угадыванием
- [ ] `rtt_break_even_ms` в шапке вердикта `shortlist`: наибольшая задержка сетки, при которой нижняя граница `net_fill` > 0 для вердиктного профиля; если такой нет — `none`; вердикт `Confirmed` при `rtt=assumed` печатается как `Confirmed(conditional: rtt<=<break_even>ms)`, не как безусловный
- [ ] тесты: фикстура, где при 5 мс `net_fill > 0`, а при 300 мс < 0 → `rtt_break_even_ms` между ними; шапка `assumed` vs `measured`; отсутствие сетки → ошибка, не молчаливый один RTT
- [ ] изобретённых чисел нет: 20 мс — В-37 (владелец), сетка — из `CLAUDE.md`/флага, не из константы
- [ ] `cargo test --release`, clippy `-D warnings`, fmt — чисто
- [ ] **GC**
