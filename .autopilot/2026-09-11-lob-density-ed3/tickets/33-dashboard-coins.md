# 33 — Дашборд о монетах и их плотностях (замена T32)

**Требования:** R88 (владелец 2026-09-12 вечер: «не такой дашборд я хотел. он обо всем и ни о
чем. я хотел дашборд о бизнес задаче конкретно — о монетах и их плотностях»), R87 (localhost),
В-37 (RTT assumed), В-38 (оговорка «суток < G_MIN»)
**Blocked by:** 32
**Зона:** `src/commands/lob/dashboard.rs` (переписан), `src/lob/markout.rs` (`base_before`/
`future_asof` — двоичный поиск, `sample_asof`), `src/lob/costs.rs` (`observation_at` — одна
конструкция `net`-наблюдения вместо копий в `pilot`/`dashboard`), `src/lob/levels.rs`
(`LiveLevel`, `LevelTracker::live_levels`), `src/commands/lob/mod.rs` (`ReplayStats.open`,
`tick_e9`/`step_e9`; `test_support::trade_frame`), `src/commands/lob/profiles.rs` (корзины
`pub(crate)`), `src/commands/lob/pilot.rs` (на `observation_at`), `CLAUDE.md`
**Волна:** 12
**Status:** done (ревью — следующей сессией; T32 ревьюирован: R-A/R-B/R-C, все BLOCKING
касались блока «Где мы» и порогов коллектора, которые T33 со страницы снял)

## Что должно заработать

Страница — про предмет задачи: плотность появилась, прожила, исчезла (проели/сняли), куда
ушла цена. По каждой монете пула.

## Критерии приёмки

- [x] плитка на монету: цена бид/аск, плотностей сейчас (бид/аск), уровней за запись, проели %, `net` 10 с; клик — монета
- [x] «Плотности сейчас»: живые уровни последнего кадра (`LevelTracker::live_levels`) — сторона, цена, расстояние bps, размер монет/$/×H3, возраст, проели уже %, который раз на цене; 40 самых крупных из N
- [x] «Картина за последний час»: SVG — середина раз в секунду, полоска на уровень (рождение→смерть), цвет — исход, живые пунктиром, толщина — ранг размера; фильтры исход / крупнейшие 100|50|20|5 % / жизнь от; диапазон цены — середина ± 25 bps (последняя корзина расстояния), потолок 4000 полосок; всё напечатано
- [x] «Что стало»: по исходам и «все» — n, доля, жизнь (медиана), `m` на `HORIZONS_MS`, `net` 10 с (`costs::observation_at`), n для net
- [x] «Где стоят и как живут»: маргиналы сторона/размер/расстояние/жизнь/повтор — те же корзины, что `lob profiles` (`shortlist::*_LABELS`, `profiles::*_bucket`)
- [x] строка про коллектор: жив по росту бинлогов между расчётами / `closed`, записано (по последнему кадру, не по стенным часам), монет, байт, разрывы из живого `gaps.csv`; суток < `G_MIN` — оговорка текстом; порог `H3` — с источником и меткой `debug`
- [x] со страницы убраны гейты, аудит, «где мы», пороги GC (были источником трёх BLOCKING ревью T32)
- [x] запись `index.html`/`data.json` через `.tmp` + rename; `--watch` переживает ошибку расчёта; прежний `data.json` чужого `--root` не база для «жив»
- [x] `markout::base_before`/`future_asof` — `partition_point` (тест равенства с линейным обходом); 8 монет × 5.5 ч — 15 с вместо ~2 мин
- [x] тесты: живые уровни/исходы/полоски на фикстуре с реальными тиками; атомарная запись и round-trip JSON; жив/нет по росту и чужой root; `closed`; монета без бинлога — строка с ошибкой; страница без внешних URL; `decimals_of_e9`; `gaps.csv`
- [x] живьём: `data/always-on/20260912T122440Z` → `data/dashboard/`, вотчер `--watch 120` с копии бинарника, http://127.0.0.1:8765/index.html, 200 на оба файла
- [x] `cargo test --release` 649 passed, clippy `-D warnings`, fmt — чисто; зависимостей нет
- [x] GC: горячий путь не тронут (`levels.rs` — метод чтения, `markout.rs` — та же семантика быстрее)

## Что видно на живой записи (5.5 ч, отладочный порог `h3_lots = 1`)

SOL: 12 737 уровней, проели 4.6 %, `net` −8.74 bps; проели → `m_10s` 2.54 bps, `net` −6.43;
сняли → `m` ≈ −0.1. Крупнейшие плотности сейчас — 1000–2000 SOL ($100–200k) на 6–40 bps от
середины, стоят 5–50 мин, проели 0 %. Картина: крупное снимают, не едят — тот же вывод, что
пилот. Порог отладочный: «плотность» = любая заявка в топ-50; честный порог — после
перезапуска коллектора на боевой десятке или `--h3-k`.
