# 30 — Ось «час» под олвейс-он: час рождения уровня, не час старта части

**Требования:** R41 (§6а «час старта каждой сессии печатается, и разведка обязана показать, зависит ли профиль от часа»), R84
**Blocked by:** —
**Зона:** `src/commands/lob/profiles.rs` (`accumulate_level`, `hour_dependence_test`, колонка `session_start_hours_utc`), `src/commands/lob/watch.rs` (`SessionTally.start_hour_utc` — только если тест на час читает его), `docs/plan/SETTLED.md` (В-36). Не трогать: `session.rs`, `shortlist.rs` (таск 29), `pick/**` (таск 27).
**Волна:** 11
**Status:** ready

## Что должно заработать

Аудит 2026-09-12 («Сессии → олвейс-он», п. 1): час для оси профиля берётся из
`part.start_hour_utc` (`profiles.rs:1116`, усреднение `:1177`); при олвейс-он
часть одна на сутки — час константа, ковариация в `hour_dependence_test` ноль,
вывод «зависимости от часа нет» при 24-часовом покрытии. Владелец: «и тут» —
чинить. Под олвейс-он «час сессии» = час рождения уровня (UTC, из `birth_ms`
записи), для сессий 5–15 мин это совпадает с часом старта с точностью до
границы часа.

## Из брифа, дословно

> «Час старта каждой сессии печатается, и разведка обязана показать, зависит ли
> профиль от часа»

## Критерии приёмки

- [ ] ось часа для профиля и для `hour_dependence_test` — час UTC рождения уровня (`LevelRecord.birth_ms`), не час старта части; колонка переименована честно (`level_hours_utc` или аналог) — читатели колонки (`shortlist::read_profile_table` матчит по имени) обновлены в этом же таске, если читают её
- [ ] `session_start_hours_utc` остаётся как информация о записи (час старта части), но в тест на час не входит; в doc сказано, почему
- [ ] тест: один каталог с одной частью на сутки, уровни в разных часах → тест на час видит разброс часов (не константу); для сессий 5–15 мин — прежний результат (тест на совместимость)
- [ ] `SETTLED.md` В-36: ось часа под олвейс-он — словами и ссылкой на §6а
- [ ] `cargo test --release`, clippy `-D warnings`, fmt — чисто
- [ ] **GC**
