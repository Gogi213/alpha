# 29 — PBO и CPCV в вердикте: заглушки `None` снять

**Требования:** R47 (§6.4 «PBO и CPCV проверяют процедуру отбора»), R68
**Blocked by:** —
**Зона:** `src/commands/lob/shortlist.rs` (сборка матрицы «испытания × периоды», вызов), `src/lob/final_metrics.rs` (только если API `pbo`/`cpcv_mean_oos_sharpe` не хватает — аддитивно), `src/lob/runs.rs` (если испытаниям нужен ряд по периодам). Не трогать: `profiles.rs`, `session.rs`, `pick/**`.
**Волна:** 11
**Status:** ready

## Что должно заработать

Аудит 2026-09-12, п. 3: `commands/lob/shortlist.rs:907-908` — `let pbo = None;
let cpcv_oos_sharpe = None;` при готовых и протестированных
`final_metrics::pbo(trials, partitions)` и `cpcv_mean_oos_sharpe(per_trade_net,
params)`. Причина в коде: нет матрицы «испытание × период». Владелец: «и тут
хуйня» — чинить. Периоды — календарные сутки окна (тот же кластер, что `G`);
испытания — профили сетки, каждое с рядом `net` по суткам.

## Из брифа, дословно

> «PBO и CPCV проверяют процедуру отбора»

## Критерии приёмки

- [ ] `shortlist` строит матрицу испытаний: строка — профиль сетки (те же, что идут в `runs.csv`), столбец — сутки окна, ячейка — `net` профиля за сутки (или `NaN` при `n = 0` за сутки — правило обработки названо в doc и в шапке)
- [ ] `pbo` считается по этой матрице с числом разбиений из существующего обоснования (`final_metrics` doc / `PLAN.md`), печатается в шапке `shortlist-<дата>.md` числом; `cpcv_oos_sharpe` — по ряду `net` выбранного профиля по суткам с параметрами `CpcvParams` из существующего обоснования; оба — `None` только при недостатке суток, и тогда шапка печатает **почему** (`pbo=n/a (days=1 < …)`), не молча
- [ ] вердикт: PBO выше порога из задачи/плана (найти; если порога нет — печатать число без вердикта и сказать это) не даёт `Confirmed`
- [ ] тесты: фикстура на ≥ 4 суток × ≥ 3 профиля с известным ответом (например, один профиль с постоянным `net > 0` → PBO низкий; случайные — высокий); шапка с `n/a` при 1 сутках
- [ ] числа не изобретать: число разбиений, порог, параметры CPCV — только из существующих doc/плана с ссылкой
- [ ] `cargo test --release`, clippy `-D warnings`, fmt — чисто
- [ ] **GC**
