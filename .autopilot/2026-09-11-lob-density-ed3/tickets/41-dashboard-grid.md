# 41 — Дашборд-сетка: график на монету по всей истории, ничего лишнего

**Требования:** владелец 2026-09-13: «дашборд нужно строить как сетку супер экономичных, но
достаточно интерактивных графиков … убирать ограничения по бокам и делать сетку, и кроме сетки ниче
там пока не оставлять … чтоб на графике можно было понять плотности и то о чем говорят пацаны»;
R87/R88 (localhost, о монетах и плотностях), R90; В-42…В-45
**Blocked by:** 35b (В-45 — `swept_lots`, `frontrun_lots` за секунду, `stack` в окне)
**Зона:** `src/commands/lob/dashboard.rs` (данные: файл на монету), `src/commands/lob/dashboard_page.html`
(страница целиком переписывается), `dashboard/tests.rs`, `tools/serve_dashboard.py` (только если нужно
отдавать несколько файлов — `http.server` уже умеет), `CLAUDE.md`, `docs/COMMANDS.md`.
**Не трогать:** `levels.rs`, `replay.rs`, `session.rs`, `touches.rs`, `profiles/`.
**Волна:** 12
**Status:** done (на боевом дашборде 2026-09-13 04:2x, ревью — после коммита)

## Что должно заработать

Страница = **сетка плиток по монетам на всю ширину окна** (`grid-template-columns: repeat(auto-fit,
minmax(<ширина>, 1fr))`, без `max-width` у контейнера) плюс одна строка сверху про коллектор
(жив, записано, монет, разрывы). Больше на странице ничего нет. В плитке — символ, цена, число
плотностей сейчас, касаний за запись / отскочила %, и **график**. Клик по плитке — она
разворачивается на всю ширину (остальные под ней), под графиком — таблицы касаний из T36 (исходы,
оси В-44, крест), кнопка «свернуть».

**График — `<canvas>`**, не SVG (десятки тысяч полосок × 10 монет). Слои:
- середина цены — линия (одна точка в секунду, как сейчас);
- плотности — горизонтальные полоски от рождения до смерти на своей цене; толщина — ранг размера
  (как сейчас), цвет — проели / сняли / смешанно / живая (пунктир до правого края);
- касания — метка на `start_ms` на цене уровня (треугольник вверх бид / вниз аск), цвет — отскок /
  проели; при наведении — цифры: цена, возраст, размер ×H3 и $, фронтран (В-45), сметено,
  подход 1 с, номер касания, длительность, `m_10s`;
- слой для T39 предусмотреть (горизонтальные пунктиры уровней, метки закола) — пустой массив в
  данных, рисовалка есть.

**Интерактив (экономный):** колесо — зум по времени вокруг курсора, перетаскивание — панорама;
диапазон — **вся история записи** (все сутки/части каталога), начальный вид — последний час;
двойной клик — сброс к последнему часу; наведение — тултип (поиск ближайшей полоски/метки по
предвычисленному индексу по времени, не перебором всего массива); фильтры общие на сетку (полоса
сверху): исход, крупнейшие 100/50/20/5 % по размеру, жизнь от, касания вкл/выкл; переключатель
«синхронизировать время у всех плиток» (по умолчанию выкл). Цена по вертикали — авто по видимому
окну (мин/макс середины ± последняя корзина расстояния 25 bps, как сейчас), полоски за диапазоном
не рисуются, их число печатается в углу.

**Данные:** `data.json` — только сводка (коллектор, список монет, плитки, таблицы касаний
развёрнутого вида); **на монету — `coin-<SYM>.json`**: середина `[[ts, price]…]` раз в секунду
по всей записи, полоски `[[b, d|null, p, side, outcome, x]…]`, касания `[[start, end, p, side,
outcome, age, x, fr, swept, approach, idx, dur, m10]…]` — компактные массивы, без имён полей в
каждой записи; пишутся атомарно (как `data.json`). Страница грузит все монеты при открытии и
перечитывает `data.json` раз в 30 с, `coin-*.json` — раз в 120 с (период вотчера) или по клику.
Потолок `PICTURE_MAX_BARS` снимается: рисуем всё, что в видимом окне; при > 4000 полосок в окне —
самые крупные (как сейчас), с надписью. Ожидаемый размер: сутки одной монеты при боевом пороге —
единицы МБ; при неделе записи — грузить `coin-*.json` только у развёрнутой/видимых плиток
(`IntersectionObserver`), остальные — по требованию.

**Оговорки текстом** остаются (В-43, В-45, «движение, не сделка», суток < G_MIN, `k` — заглушка) —
в развёрнутом виде под таблицами, не на сетке.

## Критерии приёмки

- [x] `build_dashboard` пишет `data.json` + `coin-<SYM>.json` на каждую монету атомарно; тест: фикстура
      двух монет → два файла, массивы нужной формы, середина по всей записи (не только час), полоски
      и касания за всю запись
- [x] страница без внешних URL; `canvas` на плитку; тест на HTML: строки `canvas`, `grid`, нет
      `max-width` у корневого контейнера; JSON не закрывает `script`; **на странице нет ни таблиц,
      ни фильтров, ни `<select>`/`<details>`** (владелец: «кроме сетки ниче там пока не оставлять»)
- [x] в браузере (Playwright, `tools/shot.py`: скриншот + состояние плиток + консоль): сетка 10 плиток
      на всё окно без пустых клеток (1920×1080 → 5×2, 1280×800 → 5×2); все 10 файлов грузятся, консоль
      пуста; зум/панорама/сброс работают; наведение печатает цифры касания; клик по шапке — одна монета
      на весь экран. Таблиц касаний на странице нет (см. отклонение ниже)
- [x] живьём на `data/always-on/20260912T201737Z` (только чтение): расчёт 35 с, `data.json` 0.3 МБ,
      `index.html` 0.35 МБ, `coin-SOLUSDT.json` 0.6 МБ, самый большой `coin-LSKUSDT.json` 8.6 МБ
      (96 000 крупнейших полосок из 3.27 млн, 77 125 касаний за 3.4 ч)
- [x] `cargo test --release --target-dir target-ci` (689 passed, 0 failed, 5 ignored), clippy
      `-D warnings` ноль, fmt чистый; одна сборка
- [x] `CLAUDE.md` строка дашборда + состояние (боевая десятка в `20260912T201737Z`), `docs/COMMANDS.md`
- [x] **GC**: горячий путь не тронут (страница и данные страницы)

## Отклонения от тикета (по слову владельца 2026-09-13)

- **Таблиц касаний на странице нет.** Тикет нёс их в развёрнутый вид; владелец сказал «кроме сетки
  ниче там пока не оставлять». Таблицы не потеряны — они остаются в `data.json` (`Coin.touches`,
  `cross_outcome_age`), их печатает любой разбор этого файла; на странице — только графики.
- **Фильтров и полосы сверху нет** (исход / крупнейшие N % / жизнь от / «одно время на всех» /
  легенда): легенда и фильтры — тот самый шум. Кодировка плотностей теперь сама себе легенда:
  цвет — сторона, толщина и яркость — ранг размера, белая линия — середина, треугольники —
  касания (зелёный отскок / красный проели, 4-е и дальше — контуром), справа профиль живых
  плотностей, в углу — предупреждение только когда полосок в окне больше потолка.
- **Клик по плитке** разворачивает монету на весь экран (без таблиц), второй клик возвращает сетку.
- Проверка страницы — `tools/shot.py` (Playwright): вне песочницы DSH, требует одобрения на запуск.
