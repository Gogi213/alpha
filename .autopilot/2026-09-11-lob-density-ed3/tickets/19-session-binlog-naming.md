# 19 — `lob session` пишет файлы, которые читают остальные команды

**Требования:** R12, R38, R59
**Blocked by:** 04, 18
**Зона:** `src/commands/lob/session.rs`, `src/commands/lob/pilot.rs` (мост копирования — снять), `src/commands/lob/mod.rs` (`replay_symbol`/поиск суточных файлов — только если нужно принять оба имени)
**Волна:** 7
**Status:** ready

## Что должно заработать

Слепая приёмка (G4): `lob session` пишет `<SYMBOL>.binlog` без даты, а
`verify`/`levels`/`markout`/`profiles`/`watch` ищут `<SYMBOL>-<дата>.binlog` и
падают «нет суточных файлов» на свежей сессии. Сценарий «запись → анализ»
руками обрывается; мост есть только внутри `pilot` (копия с переименованием).

## Из брифа, дословно

> «Данные набираются короткими сессиями — от пяти до пятнадцати минут по всему
> пулу одновременно»

> «инструмент, в котором проверка новой идеи занимает минуты, а не переписывание
> кода»

## Критерии приёмки

- [ ] `lob session` пишет `<SYMBOL>-<дата UTC старта>.binlog` — то имя, которое читают остальные команды; `session.json` без изменений
- [ ] `lob verify`/`levels`/`markout`/`profiles`/`watch` на свежем каталоге `lob session` работают без ручного переименования (тест на фикстуре: сессия → verify → levels)
- [ ] `pilot.rs` мост копирования с переименованием снят (или сведён к симлинку/ничему), цепочка `--debug` даёт те же артефакты
- [ ] старые каталоги `data/session-debug/…` с `<SYMBOL>.binlog` — читаются или честно отвергаются с сообщением (не тихий «нет файлов»)
- [ ] **GC**
