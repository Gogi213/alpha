# 27 — Пул по задаче: первые десять, глубина — строка отчёта, не критерий

**Требования:** R03–R06 (§2 пул), R09 (§3 ZEC), R66 (§9 «час живой глубины — проверка, а не критерий отбора»)
**Blocked by:** —
**Зона:** `src/commands/lob/pick/**` (`pool.rs`, `depth.rs`, `table.rs`, `mod.rs`), `docs/plan/SETTLED.md` (решение В-35), `CLAUDE.md` (грабли про пул). Не трогать: `session.rs`, `feed/`, `bybit/`, читатели.
**Волна:** 11
**Status:** ready

## Что должно заработать

Аудит 2026-09-12 (`docs/findings/audit-2026-09-12.md`, п. 1): порог глубины
`DEPTH_FLOOR_USD_E9` (`pick/depth.rs:249-260`) **отсеивает** кандидатов из пула —
восемь вместо десяти, выпадают `ZECUSDT` и `IOSTUSDT`. Задача §2: «первые десять
оставшихся» после исключений по правилам; §3: «ZEC не исключается, печатается
строкой отчёта»; §9: «час живой глубины — проверка, а не критерий отбора».
Владелец 2026-09-12 на этот пункт: «тут разберись, хуйня какая-то» — чинить.

## Из брифа, дословно

> «первые десять оставшихся»

> «час живой глубины — проверка, а не критерий отбора»

## Критерии приёмки

- [ ] отбор пула = исключения по правилам (BTC/ETH, некрипта, < 30 суток) → ранг → **первые десять**; глубина ниже `DEPTH_FLOOR_USD_E9` **не исключает** — печатается колонкой `depth_check` (`ok`/`below_floor`) в `instruments.csv` и `candidates.csv`, и строкой предупреждения в stdout
- [ ] `ZECUSDT` при прочих равных остаётся в пуле; тест на фикстуре: кандидат с глубиной ниже порога в первой десятке по рангу — в пуле с `depth_check=below_floor`
- [ ] `candidates.csv`: `excluded_reason` только для исключений по правилам; для непопавших по рангу — `rank_beyond_pool`; `selected_for_pilot` у ровно десяти
- [ ] `SETTLED.md` В-35: решение и ссылка на §2/§3/§9; `CLAUDE.md` «грабли» — строка про пул приведена к факту
- [ ] **боевой отбор**: один живой `lob pick --window-secs 3600` (час, публичный WS, без ключей; коллектор олвейс-он при этом не останавливать) → `instruments.csv` с десятью и `docs/plan/candidates.csv`; в отчёте — десять символов и `depth_check` каждого. Если час невозможен по ограничению инструмента — сказать, что именно, не сокращать окно молча
- [ ] **GC**
