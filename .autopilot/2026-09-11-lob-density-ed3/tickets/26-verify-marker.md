# 26 — Маркер сверки из `lob verify`: анализ олвейс-он суток без пилота

**Требования:** R85, R44, R57
**Blocked by:** 25
**Зона:** `src/commands/lob/verify.rs`, `src/commands/lob/pilot.rs` (только переиспользование `process_instrument`/записи маркера — одна функция, не вторая копия), `CLAUDE.md` (таблица артефактов, строка `lob verify`)
**Волна:** 10
**Status:** ready

## Что должно заработать

Ревью таска 25: `verify-<SYMBOL>.status` пишет только `lob pilot`
(`pilot::process_instrument`), `lob verify` — только stdout; таблица артефактов
`CLAUDE.md` утверждает обратное. `profiles`/`watch` пропускают сессию без маркера
`ok` (fail-closed, R44) — значит, накопленные олвейс-он сутки нельзя
проанализировать боевым путём без запуска `lob pilot`, который делает лишнее
(k-сетка, ставки, `runs.csv`). Владелец: «с того что заколлектилось уже делать
анализы».

## Из брифа, дословно

> «с того что заколлектилось уже делать анализы и так далее»

## Критерии приёмки

- [ ] `lob verify --symbol <S> --root <dir>` пишет `verify-<S>.status` (`ok`/`fail`) тем же кодом, что `lob pilot` (одна функция записи маркера; `pilot::process_instrument` её зовёт, не наоборот); вердикт — тот же `VerifyVerdict`, что печатается
- [ ] на каталоге с частями за несколько суток маркер — по всем частям символа (сверка читает `session_binlog_for`), одна строка stdout на часть с сутками
- [ ] тест: каталог из фикстуры → `lob verify` → файл `verify-<S>.status = ok`; битая часть → `fail`; после `ok` `lob profiles` без `--allow-unverified` читает сутки (тест на `sessions=`)
- [ ] `CLAUDE.md`: строка `lob verify` в таблице артефактов совпадает с фактом; `verify.csv` — сайдкар записи, не команда
- [ ] **GC**
