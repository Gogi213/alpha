# docs/archive — заменённое, но не забытое

Здесь лежит то, что **перестало быть актуальным**, но остаётся памятью проекта: замеры прошлых волн,
черновики плана, сырые расшифровки, артефакты старых прогонов, закрытые тикеты. Заведено 2026-09-17
жёсткой чисткой доков (два раунда, 88 файлов перенесено из рабочего дерева, ничего не удалено).

Правило: **заменяющее переносит заменённое сюда тем же коммитом и правит ссылки.** Молча не удаляем —
в `git` история есть, но ссылки в `src/` (doc-комментарии) и в `docs/` должны указывать на живое место.
Внутри архивных документов ссылки друг на друга могли устареть — это история, а не инструкция.

| каталог | что внутри | перенесено из |
|---|---|---|
| `findings/` | замеры волн 1–3: `recording-2026-09-11`, `pilot-2026-09-11`, `audit-2026-09-12`, `collector-2026-09-12`, `pool-top50-2026-09-13`, `binlog-format-2026-09-13` (перекрыт `binlog-v3-2026-09-13`), `deploy-2026-09-14`, `depth-distribution-2026-09-15`, `format-columnar-2026-09-15`, `format-dictionary-2026-09-15` (отклонённые форматы) | `docs/findings/` |
| `artifacts/` | `bounce-backtest-2026-09-13*.csv` (12), `practitioner-numbers-*.csv` (3) — машиночитаемые артефакты прогонов 09-13 | `docs/findings/` |
| `transcripts/` | 9 сырых расшифровок (≈800 КБ: `stanok_*`, `dizzy_*`, `отскок *`) — источник, из которого собран `docs/findings/transcripts-2026-09-17.md` | `docs/транскрипции/` |
| `plan-2026-09-11/` | `critique-1..7.json`, `OPEN_QUESTIONS.md`, `README.md` (ревью первой редакции плана) + `RECON-2026-09-11.md`, `REVIEW-2026-09-11.md`, `REQUIREMENTS.md` (черновики, из которых выросли `PLAN.md`/`BUSINESS-TASK.md`) | `docs/plan/`, `docs/plan/archive/` |
| `pools/` | `instruments-top20-server.csv`, `instruments-top30-server.csv` — прежние пулы (боевой — 100 монет) | корень репозитория |

Отдельно, вне `docs/`: `.autopilot/archive/` — доска автопилота (`ui-*.html`, старый `state`), закрытые
тикеты первой–третьей волны (`tickets-01-33/`, 33 файла: T1–T33) и передачки (`handoffs-2026-09-11/`,
5 файлов). Живые тикеты (`34…47`) и `state.js` остались на месте — на них ссылаются `CLAUDE.md` и
`docs/plan/PLAN.md`.

Живое в `docs/findings/` (2026-09-17): спека отскока, разбор транскрипций, аудит коллектора,
роадмап, аудит плана, замеры B2/B4 и вердикты T38 09-13 (`bounce`/`trail`/`ladder`/`touch-size-axis`),
`practitioners-2026-09-13` (словарь практиков — база живых правил), `practitioner-numbers-2026-09-13`,
`binlog-v3`, `depth-200-ab`, `stream-drift`, `archive-compression`, `hft-underground`, `deploy-2026-09-15`.
Структура `docs/` и правило «механизм → адрес артефакта» — `CLAUDE.md`, раздел «Доки».
