# Аудит этапа F (коммиты `7220a10..45f0d89`, 20.09) — корректность, дизайн, смеллы, сложность, длины файлов

**Заказ владельца (21.09):** «аудит того, что сделал [исполнитель этапа F], и раунд ревью на смеллы,
когнитивную сложность и длины файлов; и дизайн-ревью». Материал — дифф 39 файлов, +7803/−719 (без
`vendor/`: +~4000), план `dev-plan-2026-09-20.md`, доки M15–M17. Три ревьюера параллельно (горячий путь и
дизайн; слой команд; скрипты/форк/доки) + метрики функций скриптом; каждая находка ревьюеров проверена по
коду перед включением. Заявленные исполнителем гейты (тесты 933 / clippy 0 / fmt 0) на этой машине не
перегонялись (соседняя сессия держит сборку).

## 0. Вердикт

**Работа сильная и дисциплинированная** (гейты «те же байты» тестами, doc-комментарии «почему», честный
форк крейта с воспроизведением, ни одного изобретённого числа), **но в F8/F10 идти нельзя, пока не закрыты
три вещи:**

| # | что | где | почему блокер |
|---|---|---|---|
| Б1 | **Частичный выход зависает до конца записи** под `--queue-model prob:<n>`: лимитка тейка `PartiallyFilled` — фаза `ExitPending` ждёт без стопа/дедлайна/трейла, `run_round` крутит до `EndOfData`, `drive_bounce_with` ставит `incomplete` и **теряет остальные сигналы суток** | `src/lob/strategy.rs:972–996`; `src/lob/backtest.rs:1780` | любая сетка с моделью по объёму (F8/F10) даст неполные сутки молча; тесты не ловят — все prob-тесты выходят тейкером |
| Б2 | **Числа M16 (`approach-capacity`) для аск-стороны посчитаны с неверным направлением спреда**: `crossed = opp >= 0 and tick >= opp` без `side`, а скрипт глобит все наборы, включая `a45-ask`; §3 дока («полоса ног 0,1–0,2 %») — смесь bid и испорченного ask | `tools/compute/approach-capacity.py:120`; `approach-capacity-2026-09-20.md:54` | рекомендация ног для F6/F10 стоит на этих числах — перегнать с ветвлением по `side`, как в `fill-capacity.py`/`eaten-threshold.py` |
| Б3 | **`fill-capacity --targets approaches` с ключом `eaten=` — тихий no-op** вместо отказа, который обещает doc-комментарий (у подхода `size_max_before = size_at_arm` → `eaten_pct = 0`); зеркальная проверка в `bounce_grid.rs:1926` есть, здесь нет | `src/commands/lob/fill_capacity.rs:541` | контракт нарушен; M16 не задет (наборы без `eaten=`), но следующий прогон с ключом даст ложные числа; фикс — один `ensure!` + тест |

Плюс два **важных** без блокировки: ночной H3-шаг не дописывает `approaches-*.csv` в сутки, уже помеченные
`.done` (16–18.09), и при смене `D` их придётся пересчитывать руками — нигде не написано; флаги F6
(`--entry-form`, `--signal`) не описаны в `COMMANDS.md`.

## 1. Что сделано (факт)

| задача | коммит | суть |
|---|---|---|
| F1 сигнал подхода | `2ee7d02`, `92e2e39`, `966b085` | `ApproachRecord` в `LevelTracker` (взвод на `D` bps от живой стены, снятие по касанию/смерти/уходу за `2D`), `lob touches --approach-bps`, `approaches-<SYM>.csv`; замер M15 |
| F2 ёмкость по подходам | `59eb55a`, `fa44231` | `fill-capacity --targets approaches`; M16 |
| F9 порог X | `b0122d2` | `eaten-threshold.py`, M17 |
| F3 очередь по объёму | `62cde09` | `QueueModelKind`, `--queue-model risk-adverse\|prob:<n>`, `PartialFillExchange`; гейт на фикстуре и на машине |
| форк крейта | `24c9f66`, `516c476` | `vendor/hftbacktest`: две строки `>`→`>=` в `partialfillexchange.rs` + `#[allow(dead_code)]` в `npy/parser.rs`; `.gitignore` `data/`→`/data/` |
| F4 частичная позиция, пост-онли | `97ced03` | средняя цена по ордерам крейта, `--post-only/--no-post-only`, колонки `fill_frac/entry_vwap/legs_*` |
| F5 срок жизни входа | `570d5e8` | `--entry-ttl-secs touch\|wall\|N`, `--band-exit-bps`, причины снятия |
| F6 лестница + сигнал от подхода | `6a34a4a` | `--entry-form ladder<N>x<from>..<to>[w<k>]\|single@fr`, `--signal touch\|approach`, имя формы 4–5 полей |
| деньги, ночь, доки | `33baa6c`, `e8063c3`, … | `equity-report.py` с `fill_frac`, `QUEUE_MODEL` в ночи, `COMMANDS.md` F1–F5, `SETTLED` В-72…В-78, план §0а |

Не сделано: F7 (выход `eat<X>`/`gone<W>`), F8 (свод в сетку + сводный гейт), F10, F11, часть F12.

## 2. Метрики: длины файлов и функций

Файлы (строк; было → стало):

| файл | было | стало |
|---|---:|---:|
| `src/lob/backtest.rs` | 1857 | **2408** |
| `src/commands/lob/backtest.rs` | 1942 | **2364** |
| `src/commands/lob/bounce_grid.rs` | 1790 | **2283** |
| `src/lob/levels.rs` | 1401 | 1800 |
| `src/commands/lob/touches.rs` | 1050 | 1368 |
| `src/lob/strategy.rs` | 812 | 1247 |
| `src/commands/lob/bounce_verdict.rs` | 863 | 1003 |
| `src/commands/lob/fill_capacity.rs` | 567 | 768 |
| `tools/compute/equity-report.py` | 227 | 649 |
| `tools/compute/eaten-threshold.py` | — | 604 |

Функции (строк / прокси когнитивной сложности — число ветвлений с штрафом за вложенность; было → стало):

| функция | было | стало | вложенность |
|---|---:|---:|---:|
| `strategy::on_event` | 403 / 207 | **481 / 226** | 7 |
| `levels::observe_frame_with_approaches` (поглотила `…_with_touches`, та стала обёрткой в 13 строк) | 320 / 77 | **386 / 95** | 8 |
| `bounce_grid::run_bounce_grid` | 428 / 81 | **532 / 93** | 8 |
| `touches::run_touches` | 291 / 39 | 413 / 63 | 5 |
| `backtest::run_round` | 101 / 15 | 224 / 40 | 5 |
| `fill_capacity::run_fill_capacity` | 242 / 54 | 282 / 66 | 7 |
| `backtest::drive_bounce_with` | 172 / 46 | 206 / 52 | 8 |
| `commands::backtest::bounce_plan` | 148 / 24 | 193 / 26 | 4 |
| `bounce_grid::drive_day` | 89 / 51 | 97 / 51 | 9 |
| `eaten-threshold.py::main` | — | ~380 | — |

Три функции длиннее 400 строк и три файла длиннее 2200 — в проекте это было и до этапа (`run_bounce`
398, `dispatch` 326), но этап F нарастил самые тяжёлые места ещё на 20–25 %. `TradePlan::Bounce` — **21
поле в одном варианте enum**.

## 3. Находки по серьёзности

### Блокеры — см. §0 (Б1–Б3)

### Важно

| # | где | что | как |
|---|---|---|---|
| В1 | `tools/compute/nightly-grid.sh`, `touches_for_day` | `.done` проверяется до вызова с `$APPROACH`: сутки, посчитанные раньше (16–18.09 предзагрузкой), `approaches-*.csv` не получат; при смене `D` — тоже; backfill только руками (`approach-scan.sh`), нигде не сказано | комментарий + явный шаг «`rm .done` / `approach-scan.sh` при смене `D`» в `COMMANDS.md` §«Ночная сетка» |
| В2 | `docs/COMMANDS.md` | флаги F6 `--entry-form`, `--signal`, `--band-exit-bps` не описаны (F1–F5 описаны полно) | дописать до F8 |
| В3 | `src/commands/lob/backtest.rs:1829–1852` ↔ `fill_capacity.rs:195–217` | `touch_view_of_approach` продублирован побайтово с оговоркой «F2 трогать нельзя»; контракт «обязаны совпадать» ничем не проверяется — и уже разошёлся (Б3) | одна `pub(crate)` функция в `commands::lob` или тест структурного равенства |
| В4 | `src/lob/strategy.rs:83–99` (`entry_snapshot`) | нога, которой нет в `bot.orders()`, считается «не открытой» и её объём теряется; держится на невидимом инварианте «крейт не удаляет ордера из карты» | зафиксировать инвариант тестом/комментарием или копить `entry_qty/notional` инкрементально по событиям исполнения |
| В5 | `src/lob/strategy.rs:1051–1063` (`CancelPending`) | нет потолка ожидания подтверждения отмены (для фазы 2 живого бота — вечное «занято»); было и до F, но F5 — про срок жизни входа и это не закрыла | потолок по `Clock`, причина `CancelTimeout` |
| В6 | `src/lob/backtest.rs:1325` | `exits: Vec<…>` аллоцируется на каждый круг (было до F; F3/F4 при этом убрали `filled_legs.collect()` — плюс) | буфер в состоянии драйвера |
| В7 | тесты | нет автоматического гейта «прежний режим байт в байт» на уровне драйвера `backtest.rs` при `RiskAdverse` (есть на фикстуре `bounce_grid/tests.rs:1119` и точечные в `strategy`/`levels`); F3/F4 гейт на живых данных — руками | оставить как есть, если фикстурный гейт считается достаточным; иначе — золотой дамп в `tests/` |
| В8 | `tools/compute/approach-capacity.py` | весь код на уровне модуля, без функций, без ветвления по стороне (Б2) | по образцу `leg-distance.py` |
| В9 | `docs/findings/hftbacktest-partialfill-fix-2026-09-20.md` (шапка) и `vendor/hftbacktest/Cargo.toml` | «других правок нет» — а §3 того же дока называет вторую (`#[allow(dead_code)]`); нет оговорки «апстрим-фикс не проверяем офлайн» | поправить формулировки |

### Смеллы и сложность

| # | где | что | как |
|---|---|---|---|
| С1 | `strategy.rs:270–289` `TradePlan::Bounce` | 21 плоское поле: вход, выход, срок жизни, трейл, лестница, съедание вперемешку | три `Copy`-подструктуры `EntryPlan{entry_px, ttl, floor, band_exit, post_only, ladder}` / `ExitPlan{stop, take, take_frac, trail_*, eaten_*}` / `Deadline`; `match`-места (`entry_cancel_reason`, ветка `Holding`) станут читаемыми |
| С2 | `strategy.rs:836–876` | цепочка из 7 `else if` приоритетов выхода (стоп → съедено всё → трейл → тейк → половина → прилипание → дедлайн), порядок объяснён комментариями, но не тестируется отдельно | `fn resolve_bounce_exit(…) -> Option<(px, taker, reason, frac)>` + тест на порядок |
| С3 | `strategy.rs:726` `on_event` | 481 строка, сложность 226 — одна функция на четыре фазы | по функции на фазу (`on_idle`, `on_entry_pending`, `on_holding`, `on_exit_pending`), `on_event` — диспетчер |
| С4 | `levels.rs:1486–1502` | 6 уровней вложенности вокруг вызова уже чистой `observe_approach` (`for` → `match` → `Some` → `if let` → `if` → `if let`) | хелпер `try_observe_approach(...)` с ранними `return` |
| С5 | `levels.rs:1341` `observe_frame_with_approaches` | 386 строк, вложенность 8: касания + подходы + смерти + сила в одном проходе — проход один по замыслу (A-правило «один обход книги»), но тело разложимо | вынести тела веток (`on_seen_level`, `on_newborn`, `on_death`) в методы `Live`/трекера, проход оставить |
| С6 | `bounce_grid.rs:1749` `run_bounce_grid` | 532 строки: валидация флагов, подготовка символа, цикл суток, запись | `validate_args → SetPlan`, `prepare_symbol → SymbolCtx`, `run_symbol_day`, `write_outputs` |
| С7 | `bounce_grid.rs:1426` `drive_day` | вложенность 9 (`thread::scope` → `spawn` → `loop` → `match` → `lock` → `loop` → `if`) | `FormOrder::flush_ready`, шаг формы — функция |
| С8 | `bounce_grid.rs:470` | `queue_model: String`, разбор руками в `run_bounce_grid`; у `QueueModelKind::parse` уже сигнатура под `FromStr` | `impl FromStr`, поле типизировать как `ExecLatency` |
| С9 | `touches.rs:970–996` | `#[allow(dead_code)]` и комментарий «читателя в проде нет» у `ApproachRow`/`read_approaches_csv`, которые читают `bounce_grid`/`fill_capacity` | снять |
| С10 | `bounce_grid.rs:360, 362` | `Box::leak` для `label` растёт ещё и по оси `--entry-form` (было по трём осям) | `Arc<str>`/индекс формы вместо `&'static str` — когда сетка вырастет |
| С11 | `strategy.rs:214–244` `EntryLadder` | нет юнит-тестов на границы (`push` за `MAX_ENTRY_LEGS=8`, `weighted_avg_tick`/`outer_tick` при `n=0`) | добавить |
| С12 | `equity-report.py` | умолчание `--out` пишет HTML рядом со скриптом в отслеживаемый `tools/compute/` — против правила «по одному адресу» | умолчание в `docs/findings/` или обязательный `--out` |
| С13 | `eaten-threshold.py:223–604` `main` | ~380 строк последовательной сборки отчёта | `section_1…5()`; докстринг «ровно то же правило, что `fill_for`» неточен (при `q_t0 < 0` один роняет слот, другой пропускает тик) |
| С14 | python-скрипты этапа | нет ни одной самопроверки на фикстуре (у `f3-queue-gate.sh` — есть) | smoke на 1 монете × 1 сутках в `tools/compute/README` или в самом скрипте |

### Проверено и чисто

Гонка `CancelPending` (18.09) не сломана; GTX/`Expired` учтены по каждой ноге; средняя цена входа и сдвиг
стопа/тейка верны (полное исполнение по плану — сдвиг ровно 0 → гейт не нарушен); аллокаций на событие
не добавлено (одна убрана), `f64` только на границе крейта, сложность на кадр не выросла асимптотически;
`--post-only/--no-post-only` — «последний побеждает», умолчание вкл (В-72), тест есть; имена форм 4–5
полей читаются вердиктом, старые трёхпольные — тоже (`parse_form_fields`, тест); `--signal approach` без
кэша — явный отказ; новые колонки CSV — в конец, Python-читатели по именам; `QUEUE_MODEL` протянут во
все ночные вызовы; форк крейта: ровно заявленные строки + `dead_code`, правка узкая и верная (снимает
заявку при исполнении ровно до нуля, частичный путь не тронут), `git archive` после `516c476` полный,
`cargo check` проходит; `.gitignore` `/data/` побочных каталогов не открыл; M15–M17 ↔ доки ↔ `EXPERIMENTS`
— числа сходятся (кроме Б2); В-72…В-78 в `SETTLED` соответствуют плану; изобретённых чисел нет (`D=20`,
`age=900` в ночи — измеренные умолчания с override, откомментированы).

## 4. Дизайн-ревью

**Что удачно.**
- Разделение «сигнал → план → стратегия → драйвер → сетка» выдержано: подход добавлен как **новая
  запись трекера** (`ApproachRecord`), а не как костыль в касаниях; план сделки строится из любой из
  двух записей одной функцией; стратегия не знает, откуда сигнал.
- `observe_approach` — чистая функция с ранними выходами при сложном правиле (взвод/гистерезис/три
  причины снятия); правила снятия входа (`EntryCancelReason`) и выхода — раздельные счётчики, не «прочее».
- Гейты «прежний режим байт в байт» спроектированы **до** кода в каждой задаче (флаг = старое
  поведение) и покрыты тестами именно там, где легче всего сдвинуть числа.
- Форк крейта — минимальный, с причиной, воспроизведением и путём отката.

**Что не удачно.**
- **Рост через плоское расширение.** Каждая задача добавляла поля в `TradePlan::Bounce`, ветки в
  `on_event`, ветки в `run_bounce_grid` — вместо новых сущностей. Итог: 21 поле, 481-строчный
  автомат, 532-строчный оркестратор. Это не ошибка отдельной задачи, это отсутствие шага «разложить»
  между задачами; перед F7/F8 (ещё +2 формы выхода, +5 флагов, +10 колонок) его надо сделать, иначе
  F8 добавит четвёртую сотню строк туда же.
- **Асимметрия входа и выхода (Б1).** F5 дала входу срок жизни, F4 — частичное исполнение, но выход
  остался в старой модели «тейк-лимит либо исполнится, либо…» — с частичным исполнением у выхода тот же
  класс проблем, что и у входа, и его надо решать тем же механизмом (переоценка стопа/дедлайна на остаток).
- **Дублирование по запрету «F2 не трогать»** (В3): правило плана «не трогать закрытую задачу» породило
  две копии одной функции без теста равенства — и они уже разошлись. Правило нужно смягчить: общий код
  выносится, гейт закрытой задачи перегоняется.
- **Инварианты крейта в неявном виде** (В4): корректность средней цены держится на том, что `Local::orders`
  никогда не чистится. Такие вещи должны быть либо тестом на форке, либо инкрементальным учётом по
  событиям исполнения — тем более что крейт уже форкнут.

**Итог по дизайну:** архитектурно верно, структурно перегружено. Рефакторинг С1–С7 — **до F8**, объём
~1 день, риск низкий (гейты байт в байт есть).

## 5. Порядок закрытия (предложение)

1. Б1 — `ExitPending` с частичным исполнением: переоценка стопа/дедлайна/трейла на остаток, снятие
   лимитки тейка и добивание тейкером; тест с `prob` и `PartiallyFilled` тейком. **Полдня.**
2. Б2 — `approach-capacity.py`: ветвление по `side`, функции; перегнать M16, обновить §3 дока и
   строку `dev-plan §0а` «ноги 0,1–0,2 %». **2 часа.**
3. Б3 — `ensure!` на `eaten=` в `fill_capacity --targets approaches` + тест; В3 — одна функция вида
   подхода. **1 час.**
4. С1–С3, С6–С8 — разложить `TradePlan`, `on_event`, `run_bounce_grid`, типизировать `queue_model`;
   гейт «те же байты» после каждого шага. **1 день.**
5. В1, В2, В9, С9, С12 — доки и мелочи. **2 часа.**
6. Дальше — F7/F8 по плану.
