# Аудит соответствия бизнес-задаче — 2026-09-12

Независимый проход по `docs/plan/BUSINESS-TASK.md` (редакция 3) и дополнениям
владельца против кода на `9953b33` (630 passed, 0 failed, 5 ignored). Аудитор не
открывал `manifest.md`/`spec.md`/таски — судил по словам владельца и по коду.
Во время аудита шёл живой олвейс-он прогон `data/always-on/20260912T122440Z`
(16 мин, 8 инструментов, `verify = ok`).

## Соответствие по разделам задачи

| Пункт владельца | Вердикт | Доказательство | Чего не хватает |
|---|---|---|---|
| §2 исключения BTC/ETH, некрипта, < 30 суток — правилами | сделано | `src/commands/lob/pick/pool.rs:127-156` | `NON_CRYPTO_BASES` неполон по построению — названо |
| §2 «первые десять оставшихся», пул фиксируется | **противоречит** | порог глубины $2000/уровень фильтрует (`pick/depth.rs:249-260`); `candidates.csv`: `ZECUSDT`, `IOSTUSDT` без `selected_for_pilot` → пул 8, не 10 | §9: «час живой глубины — проверка, а не критерий отбора»; решения, снимающего это, в `SETTLED.md` нет |
| §3 «ZEC не исключается, печатается строкой отчёта» | **противоречит** | тот же фильтр выкидывает `ZECUSDT` из пула | ZEC защищён задачей поимённо |
| §2 пул заморожен на прогон | нет | `instruments.csv:1` «debug: окно 300 с — не годится для отбора»; в нём `ADAUSDT`/`ENAUSDT` | боевой `lob pick --window-secs 3600` не запускался |
| §2 `candidates.csv` с причиной на строку | сделано | 760 строк, `excluded_reason`, `coverage_top50_bps`, `suitable_baskets` | отладочный замер |
| §3 семь осей профиля | сделано (сознательно урезано) | `lob/shortlist.rs:440-497`: маргиналы + один крест инструмент×исход×расстояние | полного креста нет намеренно (В-18), не скрыто |
| §3 расстояние в bps | сделано | `DISTANCE_BOUNDS_BPS`, `distance_bucket_eligible` | — |
| §3 пригодность корзины по покрытию топ-50 | сделано | `shortlist.rs:427-438` | — |
| §3 границы корзин до данных | сделано | константы-массивы, не CLI | — |
| §4 `n`, доли, `m` с интервалом, `net` | сделано | колонки `profiles` | — |
| §4 сырое движение без нормировки знаком | сделано | `raw_*` | — |
| §4 `fill`, `net_fill` — «вот это и есть окупается» | **частично** | формулы `costs::net_fill_*`, `BacktestFillModel`; без RTT-флагов — `not_measured` | **RTT никогда не мерили**: `lob probe` не запускался; `data/backtest-fillmodel-debug/probe-SOLUSDT.csv` — синтетика |
| §4а размер заявки | сделано | `pick/order_size.rs` | — |
| §5.1 `profiles-<дата>.csv` | нет | в `docs/findings/` нет | сбор не начат; код готов |
| §5.2 `shortlist-<дата>.md` + заморозка | нет | там же | код готов |
| §5 окно «сейчас» в артефактах | сделано | В-31, шапка `window: …` | — |
| §6.1 деление 60/40 до анализа | сделано | `shortlist.rs:218-247`, минимум двое суток | `lob profiles` минимума не требует |
| §6.2 заморозка коммитом | сделано | `--preregistration/--freeze-out/--freeze-commit` | боевой заморозки не было |
| §6.3 каждый профиль — испытание в `runs.csv` | частично | `log_profile_trials` есть | на диске 8 строк пилота, ни одного испытания профиля |
| §6.4 DSR по фактическому числу испытаний | сделано | `final_metrics::dsr_for_trial_count` | — |
| §6.4 PBO и CPCV | **нет** | `commands/lob/shortlist.rs:907-908` — `None` | нужна матрица «испытания × периоды»; причина названа |
| §6.5 неподтверждённый профиль остаётся в отчёте | сделано | `ConfirmStatus` | — |
| §7 `n ≥ 100`, `G ≥ 7`, нижняя граница `net_fill > 0` | сделано | `G_MIN`, `net_fill_interval`, DSR-порог | боевых данных нет |
| §7 три исхода, `H6 = 3 bps` | сделано | `costs::GREEN_NET_BPS` | — |
| §8 никакого ML; пул не расширять; не торгуем | сделано | — | RTT не замерен |
| §9 исправить ПЛАН-2 | сделано | `pick/pool.rs:141-144` | — |
| §10 книга, исходы, markout, издержки, бэктест с очередью, GC | сделано | модули | гейт GC «ноль аллокаций» красный на 1–2/сообщение — названо |
| §11 пилот два часа | противоречит (решением владельца) | В-33: 30 мин | — |
| доп. «архитектура сразу под бота» | сделано | `lob/strategy.rs`, шов на `Backtest` крейта | — |
| доп. «супер быстрая сразу» | частично | G-LAT не объявлен (63 < 1000); триггер `react.rs` — смена лучшего тика, не плотность | реакционный путь плотности не измерен |
| доп. «`k` — сформулировать методологию» | сделано | `K_GRID` + G0/G1 | на данных `k` не определим |
| доп. 2026-09-12 «супер оптимизировать коллектор» | сделано, числами | `collector-2026-09-12.md` | один живой прогон до/после |
| доп. 2026-09-12 «олвейс-он, супер экономный» | сделано | `lob session --always-on`, живой прогон идёт | суточного прогона ещё не было |
| доп. «с накопленного делать анализы» | сделано | `verify`/`levels`/`markout`/`profiles` по живому каталогу | — |

## Пять вещей, которые владельцу надо знать

1. **Порог глубины $2000 режет пул, хотя задача запрещает** — остаются восемь из
   десяти; выпадают `ZECUSDT` и `IOSTUSDT`, а ZEC задача защищает поимённо.
   Единственное прямое противоречие в коде.
2. **`net_fill` — центральная величина задачи — ни разу не посчитана**: RTT не
   замерен (`lob probe` не запускался). Без этого вердикт по §7 невозможен.
3. **PBO и CPCV — `None`**: два из четырёх пунктов статконтракта §6.4 не работают.
4. **Ось «час старта сессии» под олвейс-оном мертва**: час берётся из части, часть
   одна на сутки — тест на зависимость от часа выродится в «зависимости нет».
5. **Пилот честен**: `k` не определим, G0/G1/G-POWER-B красные, все восемь
   инструментов в `runs.csv`, пороги под результат не двигались.

## Изобретённые числа

Не найдено. Все константы с обоснованием (задача, план, замер, коммит). Шесть
чисел — эксплуатационный выбор, признанный в doc: `MAX_RECORDS_PER_FRAME`,
`CHANNEL_CAPACITY`, `RECORD_PING_INTERVAL`, бэкофф, `RESOURCE_SAMPLE_SECS`,
`FRAME_LOSS_WINDOW_SECS`. Мелочь: doc `LevelTracker` (`src/lob/levels.rs:216-218`)
говорит про «предвыделенные ёмкости» `BTreeMap` — у него ёмкости нет.

## Сессии → олвейс-он: что рассогласовано

1. Час старта как ось — сломан (`profiles.rs:1116`, `:1177`); чинится переходом на
   час собственной метки уровня. Не сделано, нигде не записано.
2. Зачётный хвост пилота `window/2` (В-33) на многосуточной записи делает «прогревом»
   сутки; правило под дедлайн не переформулировано под запись без него.
3. Документы разошлись: §6а/§8 задачи требуют сессий и запрещают непрерывную
   запись; снято только В-34; `PLAN.md` — только «Точка остановки».
4. Потолок диска исчез вместе с обоснованием: ≈ 310–355 МБ/сутки на восьми, при
   десяти и месяце ~13 ГБ; ни потолка, ни удержания, ни ротации по объёму.
5. Заморозка 60/40 на одних сутках вырождается; `lob profiles` двух суток не требует.
6. Перенеслось без потерь: сутки — кластер, `G ≥ 7` календарными сутками,
   `sessions_outside_window`, полночь — новая часть со снапшотом, чтение живого
   каталога. Правило остановки «по размеру выборки» для олвейс-она не
   переформулировано — предел ставит только Ctrl+C.
