# Задержки торгового пути — живые прогоны `lob latency` (2026-09-19): сервер Сингапур и ноутбук владельца

Задача владельца: «измерить RTT, скорость постановки лимитки, скорость снятия, скорость исполнения
тейкера — на 10 баксов всё». Команда — `lob latency` (коммиты `35df968`, `6676045`; ступени и колонки —
`docs/COMMANDS.md`). Два прогона: **боевой с сервера** (главный, ниже первым) и отладочный с ноутбука
владельца. Ноутбук: `--symbol DOGEUSDT --notional-usd 10 --cycles 50`, mainnet, боевой счёт, 11:24Z. Точки — `data/bybit/latency-DOGEUSDT-20260919T112418Z.csv`
(1150 строк, gitignored). Ошибок ступеней — ноль, позиция в конце плоская, `qty = 114` DOGE
(≈ $10.05), 200 ног тейкера.

## Боевой прогон — с сервера коллектора `139.99.91.22` (OVH Сингапур), 2026-09-19 11:44Z

Тот же бинарник (`alpha-latency-6676045`, сборка на сервере из HEAD `6676045`), `--cycles 100`,
DOGEUSDT, $10 (114 монет), боевой счёт владельца, ключи — переменные окружения процесса на время прогона
(на диск сервера не писались). Ошибок ступеней — ноль, позиция плоская, 400 ног тейкера (≈ $1.4 комиссий).
Точки — `data/bybit/latency-DOGEUSDT-server-20260919T114457Z.csv` (2300 строк, gitignored; копия на
сервере `~ubuntu/latency/`). Первый смоук упёрся в `110072 OrderLinkedID is duplicate` — биржа помнит
`orderLinkId` счёта между прогонами; починено префиксом по времени старта (`6676045`).

| ступень | via | n | median | p95 | p99 | max |
|---|---|---|---|---|---|---|
| rest_time | rest | 100 | 5.15 | 7.55 | 12.11 | 18.27 |
| ws_ping | private | 100 | 2.74 | 3.22 | 3.48 | 3.51 |
| place_ack | rest | 100 | 6.32 | 13.73 | 22.53 | 37.10 |
| place_ack | ws | 100 | **4.20** | **4.79** | 6.60 | 6.74 |
| place_new | rest | 100 | 7.61 | 14.58 | 19.26 | 40.68 |
| place_new | ws | 100 | 5.55 | 6.12 | 6.54 | 8.19 |
| cancel_ack | rest | 100 | 5.97 | 9.73 | 14.04 | 14.27 |
| cancel_ack | ws | 100 | **3.98** | **4.55** | 5.01 | 5.42 |
| cancel_done | rest | 100 | 7.18 | 10.06 | 15.11 | 15.52 |
| cancel_done | ws | 100 | 5.34 | 5.99 | 6.20 | 6.55 |
| taker_ack | rest | 200 | 6.29 | 9.20 | 12.38 | 12.77 |
| taker_ack | ws | 200 | **4.13** | **4.85** | 6.18 | 38.02 |
| taker_exec | rest | 200 | 7.67 | 10.09 | 13.49 | 14.25 |
| taker_exec | ws | 200 | **5.65** | **6.42** | 11.07 | 39.60 |
| taker_filled | rest | 200 | 7.68 | 10.10 | 13.49 | 14.31 |
| taker_filled | ws | 200 | 5.66 | 6.45 | 11.08 | 39.61 |
| cycle_total | rest | 100 | 68.56 | 81.56 | 101.24 | 101.78 |

Разложение (медианы по парам одного цикла):

- **сеть по открытому сокету — 2.74 мс** (`ws_ping`, min 2.55); REST-круг — 5.15 (HTTP поверх, ~2.4 мс
  дороже сокета);
- **постановка лимитки по WS: 4.20 мс до ответа биржи**, из них сверх пинга — 1.45 мс (это и есть
  матчинг + ответ); **снятие — 3.98** (сверх пинга 1.2);
- **тейкер по WS: ответ 4.13, исполнение подтверждено приватным стримом через 5.65** (сверх пинга 2.9,
  p95 3.8); `execution` и `order Filled` — одной пачкой (0.01 мс);
- приватный стрим отстаёт от ответа WS trade на **1.35–1.50 мс** (p95 ≈ 1.9) — на ноутбуке было 2.3,
  разница в сети до стрима; кадры `New`/`Cancelled`/`execution` — одна и та же задержка;
- **REST медленнее WS на ~2 мс по медиане и вдвое-втрое по хвостам** (place_ack p99 22.5 против 6.6,
  max 37 против 6.7); хвосты WS — два выброса ~38 мс на 200 ног тейкера (циклы 44 и 72, 1 %), обе
  ступени `ack` и `exec` разом — то есть сеть/очередь, не матчинг.

## Что это меняет

- **В-37 (RTT 20 мс assumed, `--median-rtt-ns 20000000 --p95-rtt-ns 20000000`) — измерено:** с сервера по
  WS trade медиана **4.2 мс, p95 4.8** (ответ на постановку), для исполнения тейкера **5.7 / 6.4**.
  Допущение было консервативнее измерения в ~4 раза. Заменять флаги профилей/бэктеста на измеренные —
  решение владельца (В-##), число теперь есть.
- Бот — по WS trade, не REST (хвосты). Приватный стрим как источник «исполнено» — +1.4 мс к ответу.
- Разбор с ноутбука (ниже) — про сеть владельца, не про биржу; в модель не идёт.

## Прогон с ноутбука владельца (Windows), 11:24Z — сводка (мс)

| ступень | via | n | median | p95 | p99 | max |
|---|---|---|---|---|---|---|
| rest_time | rest | 50 | 206.97 | 210.19 | 216.19 | 216.19 |
| ws_ping | private | 50 | 203.16 | 204.14 | 206.01 | 206.01 |
| place_ack | rest | 50 | 208.52 | 217.46 | 470.09 | 470.09 |
| place_ack | ws | 50 | 203.17 | 203.83 | 214.79 | 214.79 |
| place_new | rest | 50 | 209.39 | 218.26 | 471.23 | 471.23 |
| place_new | ws | 50 | 205.45 | 206.21 | 217.38 | 217.38 |
| cancel_ack | rest | 50 | 208.23 | 215.95 | 225.10 | 225.10 |
| cancel_ack | ws | 50 | 203.47 | 204.10 | 204.48 | 204.48 |
| cancel_done | rest | 50 | 209.13 | 216.93 | 225.09 | 225.09 |
| cancel_done | ws | 50 | 205.76 | 206.50 | 206.60 | 206.60 |
| taker_ack | rest | 100 | 208.41 | 220.21 | 555.95 | 560.96 |
| taker_ack | ws | 100 | 203.23 | 203.75 | 205.67 | 206.54 |
| taker_exec | rest | 100 | 209.54 | 221.56 | 556.89 | 561.99 |
| taker_exec | ws | 100 | 205.67 | 206.34 | 206.52 | 206.54 |
| taker_filled | rest | 100 | 209.55 | 221.58 | 556.90 | 562.00 |
| taker_filled | ws | 100 | 205.68 | 206.35 | 206.52 | 206.54 |
| cycle_total | rest | 50 | 2280.06 | 2319.48 | 2980.07 | 2980.07 |

## Что это значит

1. **Сеть съедает всё: RTT ноутбук → Bybit ≈ 203 мс** (`ws_ping` min 202.6, медиана 203.2). Все ступени
   лежат в 203–210 мс, потому что каждая — один сетевой круг. Разность между ступенями — единственное,
   что здесь про биржу, а не про провайдера.
2. **Биржа ставит, снимает и исполняет за время, неотличимое от нуля на фоне сети:** `place_ack ws −
   ws_ping` = −0.05 мс (медиана). То есть матчинг + ответ укладываются в разброс пинга.
3. **Подтверждение приватным стримом приходит на 2.3 мс позже ответа WS trade** (`place_new − place_ack`
   медиана 2.30, p95 2.60; тейкер `taker_exec − taker_ack` 2.46 / 2.88; снятие 2.28). У REST та же
   разность 0.9–1.1 мс — не потому что стрим быстрее, а потому что REST-ответ сам приходит на ~5 мс позже.
   `taker_filled − taker_exec` = 0.01 мс: кадры `execution` и `order Filled` идут одной пачкой.
4. **WS trade быстрее REST на ~5 мс по медиане и радикально — по хвостам:** REST p99 470–560 мс (TLS/
   keep-alive провайдера или переоткрытие соединения `reqwest`), WS p99 205–217 мс. Бот идёт по WS
   trade — как и заложено (`bybit::trade_ws`, шаг 6.4).
5. Цикл 2.28 с = 11 последовательных сетевых кругов × 205 мс — расход самого замера, не биржи.
6. Стоимость прогона: 200 ног × $10 × 3.5 bps = $0.70 комиссий + спред DOGE (1 тик ≈ 1.1 bps × 100
   кругов ≈ $0.11).

## Чего ноутбучный прогон не даёт

Числа для В-37 и модели очереди — оно снято с сервера (раздел выше): 2.3 мс «ответ → стрим» на ноутбуке
стали 1.4 мс на сервере, а сеть — из 203 мс в 2.7.

Сам DOGE не важен — ступени про биржу, не про монету; выбран как ликвидный и дешёвый (114 монет на $10).
