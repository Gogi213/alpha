# План разработки — база, потом накрутка (2026-09-17)

Слово владельца: «сначала самую базу и коллектор, потом самую базу бэктестера, потом самую базу
отскоков; как только собираем это — накручивать среднее и сложное из того, что уже на бумаге».
Источники: `docs/findings/{bounce-spec,transcripts,collector-audit,roadmap}-2026-09-17.md`,
решения владельца в их последних разделах. Это план работ, не код; тикеты заводятся из него по
одному, исполнитель один, сборка одна за раз (В-39). Каждый тикет закрыт, когда названный
артефакт лежит на диске, тесты и `clippy -D warnings` чисты, ревью по В-39 пройдено.

Размер: **S** — часы, один файл; **M** — день, несколько модулей; **L** — больше дня или новый модуль.

## Карта документов

| Документ | Что в нём | Куда ушло |
|---|---|---|
| [transcripts-review-2026-09-17.md](transcripts-review-2026-09-17.md) | адаптированный промпт четырёх этапов | — |
| [bounce-spec-2026-09-17.md](../findings/bounce-spec-2026-09-17.md) | спецификация отскока по S/D с таймкодами, сверка с В-42…В-46, §12 — решения владельца | B1–B4, C1, D4 |
| [transcripts-2026-09-17.md](../findings/transcripts-2026-09-17.md) | семь остальных видео: идеи сильно/слабо/неясно, §3 — что не берём, §6 — рекомендации | B4, D1–D6, D9 |
| [collector-audit-2026-09-17.md](../findings/collector-audit-2026-09-17.md) | аудит коллектора: §7 таблица K/V/C, §8 расхождения с памятью, §12 решения | A1–A6, D7, D8, D13 |
| [roadmap-2026-09-17.md](../findings/roadmap-2026-09-17.md) | цепочка до торговли со статусами, §3 «сила плотности», §6 решения | B1–B5, C1–C4, D6, D10–D12 |
| `docs/plan/SETTLED.md`, `PLAN.md` §6/§12, `BUSINESS-TASK.md` §6–7 | решения, гейты, статконтракт | B1, B5 |

## Этап A — база и коллектор (эксплуатация, срок ≈ 22.09)

Цель: коллектор живёт без присмотра, каждая потеря видна, сутки пригодны для чтения.

| # | Тикет | Что именно | Где | Артефакт / готово, когда | Размер | Источник |
|---|---|---|---|---|---|---|
| A1 | Архив закрытых суток; сторож диска снят решением владельца | `lob archive` отказывается трогать часть, которую сессия ещё пишет (последняя часть символа по `session.json`); регламент: закрытые сутки → архив → `--delete-original`. Сторож диска (`statvfs`, `session.json.disk_free_*`, `--disk-warn-gib`, `libc`) был написан и **снят до выката** — «не нужны пока никакие сторожа, в ручном режиме буду управлять датой, пороги тоже не нужны» (В-57): в коде его нет | `commands/lob/archive.rs:207-215` (`still_writing`) | тест «открытая часть не архивируется»; сторожей и порогов в коде нет (В-57) | M | [аудит §7](../findings/collector-audit-2026-09-17.md) V2, V8; [roadmap §2](../findings/roadmap-2026-09-17.md) шаг 0 |
| A2 | `verify` в боевом корне по полуночи | таймер systemd / cron: после UTC-полуночи `lob verify` по всем 100 для вчерашних суток, статусы в `/opt/alpha/root`; сводка «ok/fail/причина» в лог | ранбук `COMMANDS.md`, юнит на сервере | 100 файлов `verify-*.status` за вчера; строка в `docs/COMMANDS.md` | S | [аудит §7](../findings/collector-audit-2026-09-17.md) V1, §Выводы 2 |
| A3 | Видимость потерь | (1) ошибка `connect()` → `ConnEvent::ConnectFailed{ts, err}` → строка `gaps.csv` + счётчик `connect_failed` в `session.json`, HTTP-статус рукопожатия сохраняется (403/429 отличимы); (2) таймаут приёма: нет сообщений дольше 2 × `LIVE_PING_INTERVAL` → `Disconnected`; (3) неудачные `append_gap_row` считаются; финальный `flush` в `record.rs` логируется; `Unrouted` пишется на диск на тике; (4) `verify_and_mark` учитывает строки `gaps.csv` по символу (V4); (5) смерть потока ввода-вывода видна на тике (V12) | `bybit/conn.rs:602-611, 983-985, 693`; `session.rs:489, 934-945`; `record.rs:934, 944` | тесты: мок-коннектор с отказом, «сервер замолчал»; смоук на сервере 1 час — новые счётчики в `session.json` | M | [аудит §7](../findings/collector-audit-2026-09-17.md) K1, V4, V5, V6, V12; §3–4 |
| A4 | Мягкий `ShortRead` у читателей | оборванный хвостовой кадр — вернуть прочитанное + предупреждение, как в `dashboard.rs:704-719` | `watch.rs:256`, `replay.rs:267`, `bybit/verify.rs:962`, `binlog_stats.rs` | тест с обрезанным файлом; команды не падают на живом корне | S | [аудит §7](../findings/collector-audit-2026-09-17.md) V7; §5 |
| A5 | Память и линты | `CLAUDE.md`: сервер пишет v3 + `deep/`, 6.5 ГБ/сутки, `verify` в корне; `ARCHITECTURE.md` A4 — отсортированный `Vec`; `depth-200-ab` — сайдкар только в `record`; линты `cast_possible_truncation` / `indexing_slicing` — **замер, а не гейт** (51 и ≈290 срабатываний, см. абзац ниже): в `[lints.clippy]` не включены, остаются конвенцией ревью; мелкие правки V11 (`unreachable!` → `bail!`, доказательство индексации в `conn.rs`) | docs, `Cargo.toml` | дифф docs; clippy чист (`-D warnings`) | S | [аудит §7](../findings/collector-audit-2026-09-17.md) V10, V11, C2, C3, C4; §8 |
| A6 | Решения владельца одной записью SETTLED | глубокий поток `.200` — писать/приостановить; `fsync` — конвенция «не нужен»; ротация — дата; порог сторожа A1 | `SETTLED.md` | запись В-## | S | [аудит §11–12](../findings/collector-audit-2026-09-17.md) п. 2–4; V9, C1 |

**A5 — сделано 17.09 (коммит `e1…`, см. `git log`).** Поправлено: `CLAUDE.md` (сервер пишет
v3 + `deep/`, расход **6.5 ГБ/сутки**, свободно 36 ГБ, `verify` идёт таймером A2, сторожей нет —
решение владельца, тестов 770); `ARCHITECTURE.md` A4 — «предвыделенный **отсортированный `Vec`**»
вместо «кольцевого массива» (аудит C2); `depth-200-ab` — сайдкар `verify_sidecar` подключён
**только к `lob record`**, в `lob session` его нет (аудит V3, отдельный тикет D7); V11 —
`unreachable!` в `session/args.rs` заменён на `anyhow::bail!`, а индексация `session.books[slot]`
в `conn.rs` получила доказательство в комментарии и три `debug_assert!` (книги и маршрут строятся
обходом одних и тех же `cfg.symbols`/`cfg.depths`).
**Линты гейтом НЕ включены — замер против (уточнено 17.09):** `-W clippy::cast_possible_truncation`
даёт **51** срабатывание, `clippy::indexing_slicing` — **≈290** диагностик (579 строк вывода с
подсказками); `indexing_slicing` я успел включить в `[lints.clippy]` и **снял**: `deny` ломает
сборку сразу, а разбор сотен мест — отдельная работа, не «мелкая правка A5». Оба остаются
конвенцией ревью (тот же смысл, что у существующих `#[allow(clippy::pedantic-…)]`).

**A6 — сделано 17.09, запись В-57.** Три из четырёх вопросов закрыты словом владельца: `.200`
**пишется дальше** («они копятся специально»), сторожей и порогов **нет** (управление диском
вручную), ротация/архивация — **вручную** по регламенту `COMMANDS.md`. `fsync` остался открытым:
владелец решения не называл, в код ничего не добавлено (рекомендация аудита C1 — «не нужен»,
но это не решение владельца и в В-57 записано именно так).

Не в базу (этап D): сайдкар REST в `lob session` (V3), читатель `deep/`, индекс/seek, `ENOSPC`-тест.

## Этап B — база бэктестера

Что уже есть: `lob backtest --touches` (T38) — `hftbacktest` с моделью очереди, `costs`, RTT В-37
`assumed`, `TradePlan` как данные (A6), формы `--trail-*`, `--grid-*`, `--post-only`. База — это
привести форму сделки к словам практиков и владельца, ничего лишнего.

| # | Тикет | Что именно | Где | Артефакт / готово, когда | Размер | Источник |
|---|---|---|---|---|---|---|
| B1 | Предрегистрация форм и фильтров | одна запись SETTLED (В-##) + «Дополнения» брифа: стоп ∈ {`P+1`, `P`, `P−1`}; вход от первого фронтранера; дедлайн ∈ {60 с, 10 мин, 1 ч, 2 ч}; досрочный выход при касании дольше X ∈ {1, 2, 3} с; фильтры `size_usd ≥ N_abs` И `strength ≥ S%` (знаменатель B); «красивые» числа; **без деления на разведочную/подтверждающую** (решение владельца, отступление от BUSINESS-TASK §6); число испытаний = произведение вариантов — в `runs.csv` до данных | `SETTLED.md`, `.autopilot/…/brief.md` | запись с числом испытаний | S | [спека §11–12](../findings/bounce-spec-2026-09-17.md); [транскрипции §5–6](../findings/transcripts-2026-09-17.md); [roadmap §2](../findings/roadmap-2026-09-17.md) шаг 1, §6 |

**B1 — сделано 17.09: запись В-58 + строка предрегистрации в `runs.csv` (`kind=prereg`, `trials=48`).**
Формы: вход от первого фронтранера (иначе `P+1`), стоп `P+1`/`P`/`P−1` (3), тейк 1:1 (1),
дедлайн 60 с/10 мин/1 ч/2 ч (4), досрочный выход выключен/1/2/3 с (4) ⇒ **48 комбинаций**;
формы T38 (трейл, лестница, post-only) в сетку не входят — отдельная предрегистрация, если
понадобятся. Фильтры — два независимых, комбинируются **И**: `size_usd ≥ N_abs` и `strength ≥ S%`;
значения называет владелец после замеров C1/C2, а каждое дополнительно рассмотренное значение
считается отдельным испытанием. Деления на разведочную/подтверждающую нет (слово владельца).
| B2 | Вход от фронтрана и три стопа | `TouchRecord` получает цену первого фронтранера (`frontrun_tick`, тот же кадр за 1 с до касания, В-45); вход — лимит на неё (если фронтрана нет — `P+1`, как сейчас); `--stop-mode before\|at\|behind` = `P+1` / `P` / `P−1`; тейк 1:1 от нового входа | `lob/levels.rs` (`TouchRecord`), `lob/strategy.rs` (`TradePlan`), `commands/lob/backtest.rs` | колонка в `touches-*.csv`; три прогона на одной монете печатают разные `net_fill`; тесты плана на синтетике | M | [спека §2, §4, §12 п. 1](../findings/bounce-spec-2026-09-17.md); [транскрипции §2](../findings/transcripts-2026-09-17.md) п. 3 |

**B2 — сделано 17.09.** `TouchRecord.frontrun_tick` (ближайшая лучшая цена на том же наблюдении
фронтрана, что и `frontrun_lots`; в трекере — один `i64` рядом с префиксной суммой, без аллокаций),
колонка `frontrun_tick` в `touches-*.csv`, вход плана — от неё (нет фронтрана → `P ± 1` тик, как в
T38), `--stop-mode before|at|behind` = `P+1`/`P`/`P−1`, тейк — 1:1 **от входа**. Тесты: три формы
стопа обязаны давать разные планы при неподвижном входе (мутация «`before` = −1» их роняет), и
форма T38 воспроизводится точно, когда фронтрана нет. **Прогон на живой записи** (`data/always-on/
20260912T201737Z`, ZECUSDT, 20 ч): `net_fill` = **−0.0948** (`before`) −0.0945 (`at`)
**−0.0886** (`behind`) bps — три формы дают разные числа, все отрицательные: вердикт T38 не
переворачивается, но вариация стопа работает. **Артефакт (закрыт хвост R3 аудита 17.09):**
`docs/findings/bounce-b2-stops-2026-09-17.md` — прогон воспроизведён 17.09 тем же бинарником,
числа те же (324/323/309 кругов, стопы 302/297/226), записана команда; строку `runs.csv` этот
прогон не добавляет: это инженерная проверка формы, а не испытание сетки В-58 (вердикт — B5).
| B3 | Горизонты до 2 ч | `--deadline` из B1; markout `m_10m/m_1h/m_2h` в `touches`/`touch-profiles` (середина раз в секунду уже есть, окно длиннее — только память); `within_touch` по-прежнему помечается | `lob/markout.rs` (`HORIZONS_MS` → расширенный набор для бэктеста, старые четыре не трогать), `touches.rs`, `touch_profiles.rs` | новые колонки; тест «горизонт длиннее суток — ошибка» | M | [спека §3, §12 п. 2](../findings/bounce-spec-2026-09-17.md); [roadmap §6](../findings/roadmap-2026-09-17.md) п. 3 |

**B3 — сделано наполовину.** Готово: `markout::HORIZONS_LONG_MS` = 10 мин / 1 ч / 2 ч **отдельным**
набором (старые четыре не тронуты — их число зашито в `m_*`/`within_touch_*`), `check_horizons`
с отказом «горизонт длиннее суток», `long_markouts_for_touch`, колонки `m_10m`/`m_1h`/`m_2h`
в `touches-*.csv` (`TOUCHES_WIDTH` 29→32) и флаг **`--deadline-secs`** со сверкой с
предрегистрированной сеткой В-58 {60, 600, 3600, 7200} — чужое значение это отказ, а не параметр
(сетка зафиксирована до данных). **Осталось:** те же три горизонта в `touch-profiles-*.csv` —
там `horizons: [Vec<FillObservation>; 4]` разворачивается в 4×3 колонки, и расширение до 7×3
(21 колонка) меняет формат таблицы; делать это имеет смысл вместе с C4 (стратификация по силе),
чтобы не плодить два формата — решение зафиксировать в тикете.
| B4 | Досрочный выход | `--early-exit-secs X`: если через X с после входа уровень всё ещё лучшая цена (касание не кончилось) — выход по рынку; отдельно причина выхода в CSV (`take/stop/deadline/early`) | `lob/strategy.rs`, `commands/lob/backtest.rs` | причина выхода каждого круга (`ExitReason::Early` + `BounceRun::fill_reason`); колонка `n_early` рядом с `n_stop`/`n_take`/`n_timeout`/`n_trail`; значение вне {1, 2, 3} — отказ | S | [спека §5, §12 п. 6](../findings/bounce-spec-2026-09-17.md); [транскрипции §2](../findings/transcripts-2026-09-17.md) п. 1–2 |
| B5 | Один прогон-вердикт базы | все комбинации B1 на всех пригодных сутках (местная запись 20260912 + серверные сутки со статусом `ok`); `runs.csv` с числом испытаний; отчёт `docs/findings/bounce-backtest-<дата>.md`: `net_fill` по формам, стоп/тейк/дедлайн/ранний в долях, DSR по фактическому `N` | `backtest.rs`, `lob/runs.rs` | отчёт + CSV; решение владельца «есть форма с `net_fill` > 0 или нет» | M | [roadmap §2](../findings/roadmap-2026-09-17.md) шаг 3, §6 п. 4; BUSINESS-TASK §6 п. 3–4 |

**B4 — сделано 17.09.** `ExitReason::Early` в единственной стратегии (`on_event`, работает и в
`Backtest`, и в `LiveBot`) + `still_at_level` (лучшая цена стороны на цене уровня `P` с точностью
половины тика; `TradePlan::Bounce` получил `early_exit_ns`, `level_px`, `tick_px` — тика стратегия
не знает). Порядок выходов: стоп → трейл → тейк-лимит → **досрочный** → дедлайн. CLI
`--early-exit-secs X`, `X ∈ {1, 2, 3}`, отсутствие флага — выключен (четвёртый вариант оси В-58);
чужое значение — отказ (`--early-exit-secs 5` проверен живьём, exit 1). В отчёт добавлена колонка
`n_early` (распределение причин), причина каждого круга — в `BounceRun::fill_reason`. Шесть новых
тестов, все проверены мутацией. **Замер на живой записи** (ZECUSDT, `20260912T201737Z`, стоп
`behind`, дедлайн 60 с): `net_fill` = −0.0886 (выключен) / **−0.0909** (1 с) / −0.0907 (2 с) /
−0.0937 (3 с) bps, досрочно закрыто 108/88/68 кругов, стопов 160/177/199 вместо 226 — механизм
работает, а результат **хуже**: выход по рынку в прилипшем касании отдаёт больше, чем экономит.
Кругов становится больше (309 → 324: ранний выход быстрее освобождает позицию) — эффект ёмкости,
не качества. Артефакт — `docs/findings/early-exit-2026-09-17.md`; строку `runs.csv` прогон не
добавляет (инженерная проверка формы, вердикт — B5).

**Хвосты аудита 17.09, закрытые здесь:** R5 (отказ `--deadline-secs` вне сетки не покрывался
тестом) — `deadline_ns_from_secs`/`early_exit_ns_from_secs` вынесены функциями и покрыты тестом,
мутация сетки тест роняет; R6 (`markout::check_horizons` — страж без боевого вызова) — теперь
вызывается из `deadline_ns_from_secs`, то есть на пути `--deadline-secs`; R3 (прогон B2 без
артефакта) — `docs/findings/bounce-b2-stops-2026-09-17.md`; дрейф плана (строка A1 со сторожами,
строка A5 с `deny`-линтами, номера строк, сломанная таблица, `frontrun_best_px`) — исправлен.
Открытым остаётся R1-хвост про `trials=48` как машиночитаемое поле: сейчас число живёт текстом
`forms=48` в `detail`, а журнал считает испытания по фактическим строкам.
## Этап C — база отскоков (сигнал)

Цель: сигнал = касание уровня, прошедшего два фильтра размера; та же функция в `lob watch` и в
`on_event`.

| # | Тикет | Что именно | Где | Артефакт / готово, когда | Размер | Источник |
|---|---|---|---|---|---|---|
| C1 | Абсолютный порог в долларах | `size_usd = size_at_touch × price` в `touches`; `--min-size-usd N` у `touches`/`touch-profiles`/`backtest`; квантили p10/p50/p90 `size_usd` по касаниям и по исходам (В-46 п. 3) печатаются до выбора `N` | `touches.rs`, `touch_profiles.rs`, `backtest.rs` | таблица квантилей в `docs/findings/`; владелец называет `N_abs` | S | [спека §12 п. 5](../findings/bounce-spec-2026-09-17.md); [roadmap §3.2](../findings/roadmap-2026-09-17.md) абсолютный фильтр, §6 п. 1 |
| C2 | Сила плотности, знаменатель B | скользящее среднее размера непустых уровней своей стороны в книге `.50` за период (константа существующая: `window_secs = 3600`), выборка раз в секунду, без аллокаций (как двухслотовая выборка В-45); `strength_pct = 100 × size_at_touch / mean_density`; колонка в `touches`; корзины — сначала квантили, границы — по видимой структуре, запись в SETTLED; ось `strength` в `touch-profiles` (маргинал + крест сила × исход) | `lob/levels.rs` (`LevelTracker` — новое поле состояния на инструмент/сторону), `lob/touch_axes.rs`, `touch_profiles.rs` | колонка + ось; `touch-profiles-<дата>.csv`; тест на синтетической книге (два уровня → сила 200 %) | M | [roadmap §3.2–3.3](../findings/roadmap-2026-09-17.md) вариант B, §6 п. 2; [транскрипции §2](../findings/transcripts-2026-09-17.md) п. 8 |
| C3 | Сигнал одной функцией | `Signal::evaluate(touch, filters) -> Option<Reason>`: `size_usd ≥ N_abs` И `strength ≥ S%`; `lob watch` печатает каждое касание с причиной отказа; `on_event` использует ту же функцию | `lob/strategy.rs`, `commands/lob/watch.rs` | вывод `watch` на живой монете; тест причин отказа | S | [roadmap §1](../findings/roadmap-2026-09-17.md) звено 4 |
| C4 | Вердикт по силе | B5 повторяется по корзинам силы и по `N_abs`: доля отскока, `m` на 60 с / 10 мин / 1 ч, `net_fill` по корзинам, стратификация по `size_usd` (чтобы сила не повторяла «крупный в долларах»); `runs.csv` | `backtest.rs`, `touch_profiles.rs` | `docs/findings/strength-<дата>.md`; решение: порог `S%` и «есть альфа / нет» | M | [roadmap §3.4](../findings/roadmap-2026-09-17.md); `touch-size-axis-2026-09-13.md` |
После C4 у нас есть: живой коллектор с видимыми потерями, бэктест в форме практиков, сигнал с
двумя фильтрами и ответ по `net_fill`. Всё ниже — только если ответ не «нет».

## Этап D — накрутка (по убыванию ожидаемого эффекта; оценки из `transcripts-2026-09-17.md` §2)

### Среднее

| # | Что | Откуда | Где | Размер |
|---|---|---|---|---|
| D1 | Знак переезда и `N` переставлений: повтор T42 с Δp со знаком; триггер плана B при `N ∈ {3, 5}` из цитат | [транскрипции §2](../findings/transcripts-2026-09-17.md) п. 4; В-46 | `lob/levels.rs`, `moves` | M |
| D2 | Подлинность плотности как оси: дисперсия приращений размера уровня; равноудалённость от спреда и её остановка; «моргаж» (снятие/постановка на той же цене без реакции); айсберг (исполнено на цене > убыло видимого) | [транскрипции §2](../findings/transcripts-2026-09-17.md) п. 5 | `lob/levels.rs`, `touch_axes.rs` | M–L |
| D3 | Окно реакции как ось (не только выход): агрессия за 1–3 с после начала касания | [транскрипции §2](../findings/transcripts-2026-09-17.md) п. 1 | `touches.rs` | S |
| D4 | «Красивые» числа: повторяющиеся цифры и «рядом с круглым» (граница — решение владельца) | [спека §12 п. 7](../findings/bounce-spec-2026-09-17.md) | `lob/levels.rs` (`round_zeros`) | S |
| D5 | Тейк частями: половина на середине хода 1:1, остаток по трейлу | [транскрипции §2](../findings/transcripts-2026-09-17.md) п. 10, §6 п. 5 | `strategy.rs` | S |
| D6 | Риск: лот от сквиза выхода по глубине книги; ≤ доля суточного объёма; доля депозита; лимит на монету и день; стоп торговли монетой при росте волатильности — модуль `risk` с тестами, сначала в бэктесте | [транскрипции §2](../findings/transcripts-2026-09-17.md) п. 6; [roadmap §1](../findings/roadmap-2026-09-17.md) звено 6 | новый `src/risk/` | L |
| D7 | Сайдкар REST в `lob session` (замер бюджета REST на 100 монет) | [аудит §7](../findings/collector-audit-2026-09-17.md) V3 | `session.rs`, `verify_sidecar.rs` | M |
| D8 | Читатель `deep/` и склейка 50+200 на чтении (если поток оставлен) | [аудит §7](../findings/collector-audit-2026-09-17.md) V9; T45 п. 5 | `parts.rs`, `replay.rs` | M |
| D9 | История инструмента как приор: доля отскоков за прошлые сутки — ось и фильтр | [транскрипции §2](../findings/transcripts-2026-09-17.md) п. 7 | `touch_profiles.rs` | S |
### Сложное

| # | Что | Откуда | Где | Размер |
|---|---|---|---|---|
| D10 | Сухой прогон на живом фиде: `on_event` над `Feed` с сокета, без ордеров; «бумажные» исполнения по модели очереди против ленты; отчёт реакции p99 (G-LAT) | [roadmap §1](../findings/roadmap-2026-09-17.md) звено 7; PLAN §1 | `feed/live.rs`, `strategy.rs`, новый `commands/lob/dryrun.rs` | L |
| D11 | Процесс-коннектор фазы 2 (`LiveBot` + iceoryx2, `trade_ws`) | PLAN §1; [roadmap §1](../findings/roadmap-2026-09-17.md) звено 7 | новый крейт/бинарь | L |
| D12 | Реальные ордера: `probe` минимальным лотом, затем торговля с лимитами и `kill switch` файлом — **только по слову владельца**, после отладки стратегии и коллектора | [roadmap §6](../findings/roadmap-2026-09-17.md) п. 5 | `probe.rs`, `trade_ws.rs` | L |
| D13 | Индекс «время → смещение кадра» в бинлоге; `ENOSPC`-тест; несколько позиций с отбором | [аудит §7](../findings/collector-audit-2026-09-17.md) C7, C8; T38 | `binlog/mod.rs` | M |
## Порядок и параллельность

A1–A2 — первыми (срок диска), A3–A5 следом, A6 — решениями владельца в любой момент. B1 не ждёт
кода — пишется параллельно с A. B2 → B3 → B4 → B5 последовательно (один исполнитель). C1 и C2
независимы, C3 после них, C4 — последний. D — после ответа C4, порядок внутри D — по таблице,
владелец может переставить.

## Что не делаем (подтверждено 2026-09-17)

Спот, чужие биржи, ликвидации, OI, BTC-контекст; деление на разведочную/подтверждающую;
реальные ордера до отладки; ML; «закол» без определения владельца.

## Аудит плана, решений владельца и коммитов (2026-09-17, вечер)

Отчёт — [`docs/findings/plan-audit-2026-09-17.md`](../findings/plan-audit-2026-09-17.md): три
исполнителя по осям (этап A ↔ код, этап B + долг ↔ код, решения владельца ↔ код) плюс прогон ворот
капитаном (`fmt`, `clippy -D warnings`, тесты, CLI-отказы живьём).

**Что подтвердилось:** A2, A4, A5, A6 = В-57, архивная половина A1; B1 (сетка `1 × 3 × 1 × 4 × 4 = 48`,
вход — не множитель), B2, B3-половина, аудиторский долг `94483e0`. Сторож диска A1 писался и
**снят** до выката решением владельца (в коде его нет).

**Блокер R1 (закрыт):** строка предрегистрации `kind=prereg` в `docs/plan/runs.csv` делала журнал
нечитаемым **целиком** — сразу двумя способами: (1) варианта `prereg` не было в `RunKind`, а
`read_run_rows` парсит строго, поэтому `trials_from_runs_csv` → `None`, и `lob shortlist` отказывал
«журнал испытаний не читается — DSR не из чего дефлировать»; (2) строка была записана **без
экранирования**, а её `detail` содержит запятые — 6 полей вместо 4. Починено: `RunKind::Prereg`
(учёт, не испытание) + закавыченная строка (содержание предрегистрации не менялось) + строка
`kind=amendment` с причиной + тест `the_repository_journal_reads`, который читает **боевой** журнал
и ловит оба слоя (он же и нашёл второй).

**Хвосты аудита:** R2 (текст строки `prereg` читался как 96 форм) — снят правкой строки;
R3 — артефакт B2; R4 (`frontrun_best_px`) — исправлен; R5, R6 — закрыты вместе с B4 (см. ниже);
R7 (строка A5 обещала `deny`-линты) — исправлена. Открыто: `trials=48` как машиночитаемое поле в
журнале (сейчас — текст `forms=48` в `detail`; журнал считает испытания по фактическим строкам).

**Решения владельца соблюдены** (сторожей/порогов нет, `deep/` пишется, архивация вручную, `fsync`
не добавлен, реальные ордера только по слову владельца, сетка зафиксирована до данных, деления на
выборки нет). В `SETTLED.md` перенесены не все: линты-гейт, журнал/journald, порядок починки,
спот, «красивые числа» (D4), `N_abs` в долларах, знаменатель B, ордера + RTT `assumed` живут только
в findings — тикет A6 их не покрыл.
