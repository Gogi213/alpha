# Settled objections

Формат: `id | возражение в одну строку | чем план на него теперь отвечает`

## Из critique-1.json (pass 1)

B1 | Ничто не проверяет корректность реконструированной книги; `gaps.csv` меряет покрытие, не верность | Новый шаг 0.5 `lob verify`: сверка с REST `/v5/market/orderbook` каждые 5 минут, инварианты на каждом событии, попадание каждой сделки по `cts` в диапазон книги. Стоит **до** недельной записи 0.6. Раздел Verification заполнен, пункт 1 — про это, пункт 2 — сквозной прогон на фикстуре
B2 | Гейт G2 непроверяем: границы слоёв не заданы, «любая ячейка» = десятки тестов без поправки, единица кластеризации не названа, знак не предрегистрирован | Decision 8 фиксирует **одну** ячейку (`pulled`, bid, 1-5 тиков, 10 с) и ожидаемый знак, тест односторонний — поправка на множественность не нужна, потому что тест один. Decision 9 задаёт единицу кластеризации: сутки UTC. Границы слоёв разведочной таблицы зафиксированы в шаге 2.2 до прогона, терцили волатильности считаются на первых сутках и не пересчитываются. Разведочная таблица явно не входит в гейты
B3 | Три определения не операционны: популяция для перцентиля, тождество уровня при пересоздании, база markout | H3 — перцентиль по **времени-взвешенной** выборке, иначе мелькающие уровни определяют порог. Шаг 1.1 — уровень рождается при первом превышении порога, умирает при падении ниже 20% максимума или уходе за топ-50; пересоздание на той же цене = новый уровень, объём через разрыв не переносится. Decision 11 — база markout берётся по последнему обновлению книги **строго до** начала исчезновения; для `eaten` база в момент исчезновения встраивает результат в измеритель. Decision 5 добавляет фильтр по стороне агрессора (`publicTrade.S`), Decision 6 — исключение блочных сделок по `BT`
B4 | Шаги 4.2 и 5.2 нарушают правило Goal «одна команда из репозитория»: hftbacktest внешний, статистики нет ни в одной зависимости | R3: у `hftbacktest` есть нативный Rust-крейт на crates.io, Python не нужен. Новый шаг 4.0 `lob export` — явная конвертация в 8 полей схемы. R4: статистического крейта нет, Decision 10 назначает собственный `src/stats/`, done-condition 5.2 — воспроизвести опубликованные значения DSR и PBO на контрольных примерах. Правило Goal больше не нарушается ничем
B5 | Формат лога заморожен в 0.2 без содержательной модели: снапшот при ротации, ресинк посреди файла, кадрирование zstd | Decision 7 задаёт модель целиком: сырые события, синтетический снапшот в начале каждых суток, байт вида записи, кадр zstd на блок фиксированного размера, схема — надмножество 8 полей `hftbacktest`. Done-condition 0.2 требует теста, читающего суточный файл с первого байта без обращения к предыдущим суткам, и теста на усечённый последний кадр. Раздел Data and state называет самодостаточность суток явно
B6 | Decision 4 объявляет пол по издержкам до недели записи, но ни один шаг его не делает; 3.1/3.2 стоят после разметки | Новый шаг 0.0 и гейт **G0**: пилотный час, грубый markout, сверка действующих комиссий, вердикт. Стоит перед 0.6. Шаг 3.1 остался, но считает уже точную версию на первичной ячейке. Decision 4 переписан так, чтобы называть отвергнутый вариант — тот, что был в ревизии 0

## Из critique-2.json (pass 2)

B1 | Гейт G0 задан противоречиво: проход требует markout >= 4 bps, красный требует «меньше издержек более чем втрое» (< 1.33 bps), полоса 1.33-4 bps без вердикта; минимума выборки нет | G0 переписан одним порогом: кандидат проходит при >= 200 зачтённых pulled-уровней И markout на 10 с >= круговых издержек, иначе красный. Формулировка «более чем втрое» удалена явно. Недобор уровней продлевает пилот до 6 часов один раз. G0 красный только если красные оба кандидата (H10)
B2 | Шаг 0.0 неисполним в своём порядке: зависит от 0.1-0.3, но требует правил уровня из 1.1, классификации 1.2 и базы markout из 2.1; не зависит от verify; перцентиль за скользящий час не существует в первый час пилота | Порядок переставлен: пилот стал шагом 3.1 и стоит после 1.1, 1.2, 2.1, меряя тем же кодом, что подтверждающий прогон. Второй разметчик не пишется. Прогрев окна задан в H3: пилот длится два часа, считается второй. Пилот по-прежнему стоит до недельной записи, ради чего Decision 4 и существует
B3 | Нет правила выравнивания REST-снапшота с WS-книгой, поэтому «ноль расхождений» недостижим по построению; нет правила при провале verify на уже записанной неделе | Тест 1 в 4.1 сопоставляет по u: книга проигрывается ровно до u снапшота, затем сравнение. Порог на неделе — доля расхождений < 0.01% проверок. H8 расширен: сутки, не прошедшие verify, выбрасываются целиком по тому же правилу, что и сутки с разрывом, потолок 10 суток
B4 | Кластеризация по суткам при 7 сутках даёт 5-7 кластеров: робастная дисперсия смещена вниз, односторонняя t = 3 на 6 степенях свободы это p ~ 0.012, а не 0.001; ни один модуль не владеет этой статистикой и не тестирует её | Decision 9 заменён: wild cluster bootstrap-t, 9999 реплик, распределение по G-1 степеням свободы, минимум 7 кластеров вместо 5. Оценщик переехал в src/stats/ (шаг 7.2), его done-condition требует номинального размера теста на синтетическом ряду с известной автокорреляцией. G2 формулируется через p <= 0.01, а не через t
B5 | Шаг 4.1 (замер RTT) не имеет модуля, подкоманды, теста и места в Contracts touched, и требует подписи HMAC-SHA256 и ключей, которых нет ни в зависимостях, ни в плане | Decision 12 и шаг 6.2: модуль src/bybit/probe.rs, подкоманда lob probe, зависимости hmac и sha2, ключи только из BYBIT_API_KEY и BYBIT_API_SECRET, в файлы попадают имена. Done-condition включает тест подписи против примера из документации. Утечка ключа добавлена в Failure and rollback

R3 прохода 2 | ОТКЛОНЁН. Критик сослался на коммиты «revision 3, pass-2 blockers settled» и потребовал объяснить пропажу critique-2.json и critique-3.json. Это коммиты снесённого репозитория из устаревшего снимка контекста; фактический git log содержит два коммита. Возражение построено на несуществующем состоянии

GC (по требованию пользователя, не из критики) | План проверял корректность и ничего не говорил про ремесло и экономность, хотя коллектор пишет неделю без присмотра | Decision 13 и раздел «Ремесло и экономность»: гейт GC на каждом шаге с кодом — clippy -D warnings, cargo fmt --check, ноль аллокаций на событие в горячем пути, байт на событие с порогом регрессии 10%, CPU < 5% ядра, плоский RSS, p99 разбора < 1 мс, ноль потерянных сообщений. Пороги измеряются на пилоте. Ревью диффа делает агент, который его не писал

## Из critique-3.json (pass 3)

B1 | Полярность markout не задана формулой: шаг 2.1 говорил «со знаком относительно стороны», Decision 8 предсказывал «вниз», G0 и G3 требовали положительного — при буквальном чтении оба невыполнимы по построению | Decision 14 задаёт формулу: m_h = сигма * (mid(t0+h) - mid(t0)) / mid(t0) * 10^4, где сигма = -1 для уровня на биде и +1 на аске. Положительное m означает «сигнал заработал в подразумеваемую сторону», предсказанный знак положителен во всех ячейках, все гейты читают одно. Verification пункт 4 — зеркальный тест: уровень на биде и на аске при зеркальном движении дают одинаковое m
B2 | Порог «ноль аллокаций на событие» недостижим с закреплённым транспортом: tungstenite-0.24.0/src/protocol/frame/mod.rs:180 делает Vec::with_capacity на каждый кадр безусловно; плюс не сказано, что именно меряет счётчик | Decision 13 разделён на два бюджета: транспорт <= 1 аллокация на кадр (известная и принятая цена tungstenite), путь разбор-и-запись — ноль на событие, измеряется проигрыванием 10^6 событий из заранее заполненного буфера. Замена транспорта явно вынесена в Out of scope этого прохода
B3 | local_ts ничей: инвариант exch_ts < local_ts проверяется в 6.1, то есть после недели записи, а дисциплину часов не устанавливает ни один шаг | Новый шаг 0.5 lob clock: смещение против NTP и против serverTime Bybit, clock.csv раз в час всё время записи, done-condition — |offset| < 5 мс без скачков > 1 мс и exch_ts < local_ts на 100% живой выборки. Строка про часы добавлена в GC. ASSUMPTION H12 задаёт хост и требует ставить local_ts по возврату из recv, до разбора. Шаг 0.1 получил это в done-condition
B4 | Пилот и гейт G0, закрывающий гипотезу окончательно, шли на книге, которую lob verify ещё не проверял; пункт про verify выпал из settled прохода 2 | Шаг 3.1 теперь зависит от 0.6 (verify) и 0.5 (часы). Цена — один час календаря, verify имеет done-condition на часе живого потока. Причина записана прямо в шаге, курсивом
B5 | Модель проскальзывания не задана ничем — ни формулой, ни числом, ни строкой предрегистрации, — а G3 решается целиком ею; done-condition «колонка существует» принимает любое значение | Decision 15: вход по цене уровня без улучшения, выход тейкером с издержкой в один тик против позиции плюс половина наблюдённого спреда в момент выхода. Добавлено в список Data and state, который коммитится до первого запуска 3.1

H3 прохода 3 (вопрос критика, решён не по умолчанию) | План меряет исход против markout, то есть плотность и поток; признаки истории уровня — траектория размера, время жизни, повторяемость, переставление — не входят ни в первичную ячейку, ни в разведку, ни в Out of scope. Треть задачи брифа испарилась молча | Отклонён предложенный критиком дефолт «объявить историю вне области». Бриф называет её отличием от ретейл-подхода прямым текстом. Decision 16: две предрегистрированные ячейки — C1 только исход, C2 исход и история (время жизни ниже медианы и повторяемость >= 2 за час), альфа 0.005 на ячейку по Бонферрони. Разность C2 - C1 с бутстрап-интервалом печатается всегда и вне гейтов: это и есть ответ на вопрос «добавляет ли история». Шаг 1.1 пишет шесть признаков истории колонками, каждый проверяется тестом. Заголовок плана переписан под настоящую задачу

R1 прохода 3 | Контейнер входа hftbacktest не был установлен: 8 полей известны с прохода 1, формат файла нет | Крейт принимает только numpy-структуры: read_npy_file, read_npz_file, есть свой write_npy. Плоский бинарь не принимается. Decision 17 фиксирует npy через писатель самого крейта, поэтому список зависимостей не растёт. Event это repr(C, align(64)), восемь полей ровно на 64 байта, последнее поле называется fval, а не faval

## Из critique-4.json (pass 4)

B1 | Инструмент фактически не выбирается: `[ASSUMPTION H7]` задавал ключ ранжирования, но не пул кандидатов и не исключения. Клауза «берётся первый после исключений» была в вариантах и потерялась при переносе в допущение. Ранг по глубине без пула даёт BTCUSDT и ETHUSDT — ровно то, что бриф вычёркивает строкой 61 | Decision 18 задаёт правилом, а не списком: пул — linear USDT-перпы, торгуются >= 30 суток, не токенизированная акция, дубликаты одного актива схлопываются; берётся **средняя треть по обороту за 24 часа** (это и есть «средний эшелон» брифа, отсекающий выеденный верх и безглубинный long-tail); внутри неё требуется времени-взвешенная медианная глубина топ-50 >= 10 размеров ордера; выжившие ранжируются по глубине, при равенстве по частоте событий, берутся два верхних. Правило не устаревает со сменой листингов, в отличие от перечня тикеров
B2 | Круг посчитан двумя несовместимыми способами: G0 сравнивал с мейкер-мейкер 4 bps, Decision 15 задавал выход тейкером (7.5 bps). Плюс тип входного ордера не назван: «вход по цене уровня» для уровня на биде означает продажу по цене бида, то есть переход спреда и круг около 11 bps, который бриф называет смертельным | Decision 19: вход мейкером **у своей стороны спреда** (шорт на лучший аск, лонг на лучший бид), выход тейкером на горизонте ячейки, круг = 7.5 bps плюс проскальзывание Decision 15. G0 сравнивает **с этим же числом**. `H4` переписан: 4 bps больше не порог нигде, потому что мейкер-мейкер требует держать до пассивного исполнения, чего горизонт 10 с не допускает. Формулировка Decision 15 исправлена
B3 | Шаг 6.3 бэктестит стратегию, которой в плане нет: ни триггера, ни стороны, ни правила выхода, ни лимита позиций, ни выбранной модели очереди; «PnL-кривая» удовлетворяется чем угодно; сам шаг отсутствует в списке предрегистрации | Decision 20 задаёт стратегию целиком: триггер — срабатывание условия ячейки, сторона по сигме, вход мейкером с временем жизни ордера 2 с, выход тейкером ровно на t0 + 10 с без стопа и цели, одна позиция за раз, модель очереди `RiskAdverseQueueModel`. Done-condition 6.3 требует печатать **число пропущенных сигналов раздельно по причине**. Стратегия внесена в список предрегистрации целиком. Клауза «одна позиция за раз», выпавшая из `H6` в ревизии 2, возвращена

R1 прохода 4 | Какие модели очереди даёт крейт и какая консервативнее | `RiskAdverseQueueModel` — позиция в очереди двигается только при сделках на этой же цене, документация прямо называет её консервативной. Есть также `ProbQueueModel` с семейством `PowerProbQueueFunc` 1-3 и `LogProbQueueFunc` 1-2, и `L3FIFOQueueModel` для L3-фида. Выбран `RiskAdverseQueueModel`: вероятностные требуют параметр формы, который пришлось бы подбирать, а подбор параметра под данные план запрещает
R2 прохода 4 | Откуда берутся тик, минимальный размер и шаг количества: проскальзывание меряется в тиках, `H3` в размерах сделки, `H6` в долларах, но метаданные не тянет ни один шаг | Шаг 0.4 расширен: `/v5/market/instruments-info` даёт `priceFilter.tickSize`, `lotSizeFilter.minOrderQty`, `lotSizeFilter.qtyStep`, `lotSizeFilter.minNotionalValue` в `instruments.csv`. `reqwest` уже в зависимостях, новой не появляется. Done-condition проверяет, что $200 не меньше `minNotionalValue` у обоих кандидатов

H1 прохода 4 (место на диске) | Кончившееся место — самый частый способ смерти недельного сборщика без присмотра, и оно не покрыто ни GC, ни разделом отказов, при том что неделя объявлена невосстановимой | Строка в GC: `lob record` не стартует, если свободного меньше, чем байт_в_сутки на оставшиеся сутки с трёхкратным запасом, и останавливается сам на пороге 10% с записью в `gaps.csv`. Потеря принимает форму `H8` — выброшенные сутки вместо молчаливой порчи недели
H2 прохода 4 (какой контраст печатать) | C2 есть подмножество C1, поэтому `C2 − C1` механически занижено ровно в (1 − w) раз, где w — доля C2 внутри C1; читатель принял бы заниженное число за вклад истории | Основным печатается **дизъюнктный** контраст `C2` против `C1 \ C2` — тот же исход, но без условия по истории, — со своим бутстрап-интервалом. Вложенная разность и сама `w` остаются вспомогательной строкой. Оба интервала берутся из одних и тех же кластерных бутстрап-реплик. Внесено в Goal, в шаг 5.2, в Decision 16 и в список предрегистрации
