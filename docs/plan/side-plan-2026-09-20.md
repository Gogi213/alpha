# Стороны раздельно: выводы и план для младших моделей (2026-09-20)

Решение владельца 20.09: «пороги должны быть разные для направлений — шорт и лонг очень разные».
Этот файл — что это значит по практикам и по нашим данным, и что делать по шагам. Читать после
`CLAUDE.md` → [roadmap](alpha-roadmap-2026-09-19.md) (этап 1 закрыт этим планом, этап 2 — здесь
же); правила — там же, часть A2 и C7. Ничего из этого файла не назначает чисел: пороги
титруются сеткой, числа-кандидаты в сетке — это сетка, а не решение.

## 1. Что говорят практики (файлы «отскок», цитаты с таймкодами)

| кто | где | что сказано | следствие |
|---|---|---|---|
| Dizzi | [D 10:17] | «Завалы и плотности отрабатывают, когда биток в боковике или в лонговом тренде. Если он в шортовом тренде — плотности не отрабатываем, их проедают. **Больше всего отскоков берём в лонг. В шорты отскоки работают гораздо хуже**» | режим рынка — фильтр для **обеих** сторон; лонг — основная сторона отскока |
| Dizzi | [D 33:08] | в аск-стену набивается «10–20 млн ликвидности шортистов», и их выносят | шорт от стоящей аск-стены — против толпы, стену съедают вместе с ней |
| Dizzi | [D 33:57] | «**единственный контекст, когда я готов шортить плотности — когда чел снялся**»; «не надо заходить ни в какие разъедания» | шорт практиков — от **снятия** стены, не от касания; стена, которую уже едят, — не вход (ни для кого) |
| Станок | [S 54:00] | лонг: «лучший вариант — биток уже пробивает в лонг либо в боковике»; «нож в лонг и ускорение — на участнике, на маркет-ойси» | режим для лонга; механика входа в лонг другая (в падающий нож на бид-стену), не зеркало |
| Станок | [S 1:26:00] | «шортисты, когда фиксируют позицию, — это покупка → выкуп» | у лонга есть топливо (покрытие шортов), у шорта от аск-стены — нет |
| Тайп | [T 11:13] | «шорта отрабатываешь побыстрее… покупочки тяжелее поднимать» | шорт быстрее → короче горизонт; лонг медленнее → длиннее |
| Тайп | [T 1:05:21] | «**всегда шорт — он хуже**. Если нету растяжки, он опаснее, агрессивнее, тяжелее отработать» | шорт только с «растяжкой» — цена перед стеной уже выросла |

Итого у практиков: **сторона — не параметр, а разные сделки.** Лонг от бид-стены = базовый отскок с
фильтром режима. Шорт от стоящей аск-стены = худший вариант; допустим только при растяжке и не в
лонговом тренде; настоящий шорт практиков — от снятия стены (другой сигнал).

## 2. Что говорят наши данные (3 суток, `side-axis-2026-09-19.md`)

- Лонги от бид-стен (`a45-bid`): лучшая форма +19.2 bps на круг на 89 кругах, 6 форм из 32 в плюсе.
- Шорты от аск-стен (`a45-ask`, `s100-ask`): **0 форм из 32** в плюсе, лучшая −7…−23 на круг.
- Все три сутки — рост (+1.6/+3.3/+7.1 %) — ровно «биток в лонговом тренде» по [D 10:17]: практики
  предсказали и знак лонгов, и провал шортов. Отсюда же: **различить «сторона» и «режим» можно только
  днями падения** — их в записи пока нет.
- Уже отдельное по сторонам: форма сделки (стоп, тейк, дедлайн) — вердикт каждой стороны выбирает
  сам (лонг взял дедлайн 1 ч, шорт — 2 ч, но у шорта выбор среди минусов).
- Ещё не отдельное: флоры входа (возраст, сила) и набор условий — одни на обе стороны.

## 3. Выводы

1. **Раздельные пороги — да, но не «те же оси с другими числами».** Для лонга главные оси уже
   есть (возраст, сила) плюс режим. Для шорта решают условия, которых в сетке нет: режим (не в
   лонговом тренде), растяжка (ход в стену до касания), состояние стены (не в разъедании).
2. **Шорт от стоящей аск-стены не обязан заработать.** Если после режима и растяжки он остаётся
   красным на ≥ 7 сутках — это не «плохо подобрали числа», это ответ практиков: шорт — от снятия
   стены, отдельный сигнал (§5, задача S7), делается после того, как лонг пройдёт ворота.
3. **Режим нужен обеим сторонам** и нужен раньше, чем титрование чисел по стороне: без него
   лонг на дне падения и шорт на дне роста — шум, который спрячет любой порог.
4. Всё, что можно посчитать без кода, считается **сегодня же ночью** (S0, S1): возраст по стороне и
   доля съедания стены — в ночной набор, одним процессом, +8 наборов ≈ +5 мин ночи.
5. Что требует кода — три небольшие задачи (S2–S4): доходность монеты до касания (10 мин / 1 ч /
   4 ч), режим пула по минутам, ключи наборов. Все — с воротами «без новых ключей круги те же байт в
   байт».
6. Числа в титровании — сетка кандидатов, не решение; ответ — вердикт по стороне на ≥ 7 сутках, с
   днями падения. Точка решения по дороге — ≈ 23.09; если дней падения к ней не будет, ждать.

## 4. Правила на каждый шаг (коротко)

- Предрегистрация в `docs/plan/runs.csv` **и** в `study/runs-2026-09-19.csv` на счётной машине —
  **до** запуска; текст — по образцу ниже, на английском, с `POOL,prereg`.
- Один процесс — `lob bounce-grid --set` (все наборы над одними событиями); касания — из кэша
  `--touches-from study/touches`; всё это уже в `nightly-grid.sh` — новые наборы **добавляются в
  него**, отдельных цепочек не заводить.
- Каждое новое условие в коде — тест «без него байты те же» на фикстуре + гейт на 5 монетах
  реальной записи (образец — `docs/COMMANDS.md`, `--touches-from`/`--set`).
- Строки `bounce_form` из журнала-копии переносить в репо **с контекстом** (образец — строки 172–483).
- Любой вывод — по монетам и по суткам, с режимом дня рядом (`side-axis.py --touches`).
- Числа не назначать. SETTLED не править. Живых ордеров нет.

## 5. Задачи (порядок = приоритет; оценка — для младшей модели)

### S0. Возраст по стороне — в ночной набор (30 мин, без кода)

- Вход: `tools/compute/nightly-grid.sh`, строка `run_sets …` (девять наборов).
- Делать: добавить шесть наборов — `a15-s10-bid:age=900,flow=10,side=bid`, `a15-s10-ask:…,side=ask`,
  `a30-bid:age=1800,side=bid`, `a30-ask`, `a60-bid:age=3600,side=bid`, `a60-ask` (a45 и s100 по
  стороне уже есть). Скопировать в `bin/` счётной машины (`scp`, `bash -n`).
- Предрегистрация (одна строка, обе копии журнала):
  `…,POOL,prereg,"side x age titration before data (side-plan-2026-09-20.md S0): age floors
  {900|1800|3600} s x side(bid|ask) x V-65 base forms=32 = 192 trials, added to the nightly set
  (a45 and s100 per side already registered 2026-09-19); notional>=10000 usd, latency V-68, entry_any,
  size = pool lot, all days in root, re-run nightly; question: does the long side hold lower>0 at
  some age floor, does any short set turn positive"`.
- Артефакт: утром `study/bounce-verdict-nightly-<день>-a{15-s10,30,60}-{bid,ask}.csv`; таблица —
  `family-table.py` со строкой на набор (образец вызова — docstring скрипта).
- Ворота: не решение, а чтение — на ≥ 7 сутках у лонгов есть ли пол возраста, где нижняя > 0.
- Готово, когда: первая ночь дала шесть вердиктов, 192 строки `bounce_form` перенесены с контекстом.

### S1. Состояние стены при касании — ключ `eaten_max` (2 ч, код + тест)

- Почему: [D 33:57] «не заходить в разъедания» — обе стороны; в `TouchRecord` уже есть
  `size_at_touch` и `size_max_before` (максимум до касания) → доля съедания
  `eaten = 100 × (1 − size_at_touch / size_max_before)`.
- Делать: в `src/commands/lob/bounce_grid.rs` — `FilterSet` получает `eaten_max_pct: Option<f64>`,
  парсер `--set` — ключ `eaten=<%>` (рядом с `"frontrun" =>`), фильтр в `signals_for` рядом с
  `p.side` (касание с `eaten > eaten_max` → `skipped += 1`), поле в `DayParams`, в шапку
  `forms.csv` — `eaten_max=…`. Тест в `bounce_grid/tests.rs`: набор `eaten=0` на фикстуре (там стена
  не съедалась — сигналы те же, что без ключа) и `eaten=-1` → все касания в `n_skipped`; парсер —
  кривое значение → отказ. `cargo test/clippy/fmt`, сборка на счётной машине (`docs/COMMANDS.md` п. 10),
  гейт: без ключа круги те же на 5 монетах (образец — `--set`).
- Наборы в ночь (после S0): `a45-bid-e30:age=2700,side=bid,eaten=30`, `a45-bid-e10:…,eaten=10`,
  `a45-ask-e30`, `a45-ask-e10` — 4 набора = 128 испытаний, предрегистрация по образцу S0.
- Ворота: чтение, как S0. Готово, когда: тесты, гейт, первая ночь, журнал.

### S2. Доходность монеты до касания — колонки в `lob touches` (3 ч, код + тест)

- Почему: «растяжка» [T 1:05:21] и «направление» [D 10:17] требуют хода цены **до** касания; в CSV
  касаний есть только подход за 1/10 с (`approach_*`) и ход после (`m_*`).
- Делать: в `src/commands/lob/touches.rs` — три колонки в конец `TOUCHES_COLUMNS` (ширина 58 → 61):
  `ret_10m_bps`, `ret_1h_bps`, `ret_4h_bps` = ход середины от `start_ms − T` к базе касания
  (`touch_base(&day.mids, …)` для обеих меток; знак — как есть, «плюс — вверх», не «в сторону
  отскока»: у оси режима знак абсолютный); пусто — если `start_ms − T` раньше первого среза суток
  (трекер суточный: первые 4 ч суток без `ret_4h` — систематика, записать в шапку док-а). Плюс
  минутный ряд середины — файл `mids1m-<SYMBOL>.csv` (`minute_utc,mid2x`) рядом с `touches-<SYMBOL>.csv`
  (1440 строк на сутки; `--out` даёт путь касаний — минутный рядом с ним, тем же именем-каталогом).
  Тест в `touches/tests.rs`: три колонки в шапке; на фикстуре `ret_10m` пусто (записи 76 с);
  минутный файл есть и монотонен. `read_touches_csv` менять не нужно (читает по именам).
- Пересчёт кэша: на счётной машине удалить маркеры `study/touches/<сутки>/.done` и `.attempts` за все
  сутки, запустить `TOUCHES_ONLY=1 /opt/alpha-compute/bin/nightly-grid.sh` юнитом (образец —
  `study/preload-h2.log`), ≈ 25 мин на сутки, 3 параллельно. Сетки ночи после этого работают как
  прежде (лишние колонки не читают).
- Готово, когда: тесты, кэш пересчитан за все сутки, `head -1` любого `touches-*.csv` кончается на
  `ret_4h_bps`, `mids1m-*.csv` лежат за все сутки.

### S3. Режим пула по минутам — `tools/compute/regime.py` (1 ч, скрипт)

- Вход: `study/touches/<сутки>/mids1m-*.csv` (S2).
- Делать: на каждую минуту суток — медиана по монетам `ret_1h` и `ret_4h` (bps, от минутного ряда),
  число монет; → `study/regime/<сутки>.csv` (`minute_utc,pool_ret_1h_bps,pool_ret_4h_bps,n_coins`);
  плюс строка суток (медианная дневная доходность — как `side-axis.py day_returns`, но по минутным
  рядам, а не по касаниям). Вызов — из `nightly-grid.sh` сразу после касаний суток (там, где
  `floors-balance.py`).
- Готово, когда: файлы за все сутки, `nightly-grid.sh` пишет их сам, докстринг с форматом.
- **Биток — не стаканом, а минутными свечами по REST** (владелец 20.09: «необязательно же стакан»).
  Bybit `GET /v5/market/kline?category=linear&symbol=BTCUSDT&interval=1&start=<мс>&end=<мс>&limit=1000`
  отдаёт закрытия по минутам **задним числом** (проверено 20.09 с обеих машин: `retCode 0`, за
  2026-09-16 00:00–01:00Z — 61 строка, `list` идёт от новых к старым; ключи не нужны; коллектор не
  трогать, диск ноль). Скрипт `tools/compute/ref-klines.py`: символы `BTCUSDT`, `ETHUSDT`, интервал
  1 мин, окна по 1000 минут, повтор при `retCode != 0` / сети (3 попытки, пауза 2 с), склейка без
  дыр (проверять, что минуты идут подряд; дыру — в лог) → `study/regime/ref-<SYMBOL>-1m.csv`
  (`minute_utc,open,high,low,close,volume`), дописывать только недостающие минуты (файл — источник
  истины, повторный запуск ничего не переписывает). Вызов — из `nightly-grid.sh` перед `regime.py`;
  первый запуск — за все сутки корня с 2026-09-16. `regime.py` (S3) кладёт в `study/regime/<сутки>.csv`
  рядом с медианой пула колонки `btc_ret_1h_bps`, `btc_ret_4h_bps`, `eth_ret_1h_bps`, `eth_ret_4h_bps`
  (от закрытия минуты касания к закрытию за T до неё; первые T суток — **не** пусто: свечи есть за
  прошлые сутки, у битка суточной границы нет). Ключи в S4: `btc1h_min/max`, `btc4h_min/max` (bps)
  — режим «по битку» буквально [D 10:17; S 54:00]; медиана пула остаётся как вторая мера.
  Уровень выше (стакан BTC потоком `orderbook.1`/`tickers`) не нужен, пока ось не показала знак.

### S4. Ключи режима и растяжки в `--set` (4 ч, код + тест + гейт)

- Делать: в `bounce_grid.rs` — рядом с касаниями строить `Vec<TouchContext>` (`ret_10m/1h/4h` монеты
  и `pool_ret_1h/4h` на минуту касания): из CSV — колонки S2 (`read_touches_csv` расширить
  `TouchRow` полем `context`), из реплея — из `day.mids` тем же расчётом (общая функция, тест «CSV =
  реплей» как для `TouchRecord`); режим пула — `--regime-from study/regime` (файл суток; нет файла →
  контекст пуст, ключи режима у набора → отказ). Ключи набора: `ret1h_min=`, `ret1h_max=`,
  `ret4h_min=`, `ret4h_max=`, `pool1h_min=`, `pool1h_max=`, `pool4h_min=`, `pool4h_max=` (bps);
  касание без значения (пусто) при заданном ключе → `skipped`. В шапку — ключи набора как есть.
- Тесты: без ключей — байты те же (расширить `filter_sets_match_separate_grids_byte_for_byte`);
  ключ `ret1h_min` на фикстуре (там пусто) → все в `n_skipped`; парсер. Гейт на 5 монетах: без
  ключей — те же `rounds.csv` (образец — `--set`).
- Готово, когда: тесты, гейт, бинарник на счётной машине, `docs/COMMANDS.md` строка.

### S5. Титрование по стороне — режим и растяжка (30 мин, предрегистрация + ночь)

- Наборы (все на семье `a45`, потому что у неё единственный плюс; числа — сетка кандидатов, шаг —
  из распределения `pool_ret_4h` за имеющиеся сутки: взять квартили, не круглые числа; до
  распределения — не регистрировать):
  - лонг: `a45-bid-p4h-q50:age=2700,side=bid,pool4h_min=<Q50>`, `…-q75:pool4h_min=<Q75>`,
    `…-p4h-neg:pool4h_max=0` (контроль «против режима»);
  - шорт: `a45-ask-p4h-q50:age=2700,side=ask,pool4h_max=<Q50>`, `…-q25:pool4h_max=<Q25>`,
    `a45-ask-r1h-q75:age=2700,side=ask,ret1h_min=<Q75 по ret_1h монет>` (растяжка),
    `a45-ask-both:…,pool4h_max=<Q50>,ret1h_min=<Q75>`.
  - 7 наборов × 32 = 224 испытания; предрегистрация — по образцу S0, с квартилями и датой их
    расчёта в тексте.
- Ворота этапа 2 (дорога): у лонга набор «по режиму» с нижней > 0 на ≥ 7 сутках, **включая дни
  падения**; у шорта — хоть один набор с нижней > 0. Ни того ни другого после 7 суток с днями падения
  — режим не спасает шорт от аск-стены → S7; лонг → этап 3 (возраст/сила по монете, H2).

### S6. Чтение и док (1 ч утром после первой ночи S5)

- `side-axis.py` расширить: `--set-list` (имена наборов через запятую) — таблица «набор × сторона:
  вердикт, по суткам, режим дня» из `study/regime/<сутки>.csv` (дневная строка), а не по касаниям.
- Док: `docs/findings/side-regime-<дата>.md` — по образцу `side-axis-2026-09-19.md`: вердикты, одна
  форма — две стороны по суткам с режимом дня, по монетам, «что значит и чего не доказывает».
- Память: `.memory/log.md` строка, `CLAUDE.md` «Состояние» абзац, `index.md` блок.

### S7. Шорт от снятия стены — спецификация, не код (после ворот лонга)

- Что это: сигнал практиков [D 33:57] — крупная **бид**-стена снята (не съедена), цена над ней ещё не
  ушла → шорт с целью «до следующей плотности/стопов лонгистов» [D 27:32 — «сбрили тех, кто стоял в лонг, а тех, кто в шорт, не сбрили»].
  Это **не** отскок от стоящей стены и не касание: у трекера смерть уровня без касания
  (`ended_by_death` есть только у касаний; снятие без касания сейчас — строка `levels-*.csv` со
  смертью «снят» — `death_name`), T42 `moves` — пары «сняли → родилось» (`lob touches --moves`).
- Делать сейчас: только замер по касаниям — сколько снятий бид-стен ≥ $10k в сутки, ход середины
  за 10 мин / 1 ч после снятия (markout от снятия, знак «вниз»), по монетам; скрипт по `levels-*.csv`
  — `tools/compute/removal-study.py`, док `removal-<дата>.md`. Сетка и форма — отдельной
  предрегистрацией **после** того, как лонг прошёл ворота (иначе два незакрытых фронта).

## 6. Ворота и ветвление (что значит результат)

| результат на ≥ 7 сутках с ≥ 1 днём падения | что это | дальше |
|---|---|---|
| лонг `a45-bid` (любой пол возраста / eaten): нижняя > 0 без режима | сторона — свойство стены | этап 6 (подтверждение: по монетам, out-of-sample по суткам, PBO/CPCV, DSR) |
| лонг в плюсе только при `pool4h ≥ Q` | режим — фильтр лонга | этап 6 с фильтром режима; шорт — S7 |
| лонг в минусе в дни падения, шорт «по режиму» в плюсе | сторона = направление дня | торговать «по направлению»: одна форма, сторона от режима; этап 6 |
| обе стороны ≤ 0 при всех наборах | сторона и режим — не оси | этап 3 (H2, по монетам) и E11 (приор по монете), затем «если ворот нет» в дороге |
| шорт от аск-стены ≤ 0 при растяжке и режиме | ответ практиков подтверждён | S7 (снятие), аск-стены как вход — закрыть, записать в SETTLED **решением владельца** |

## 7. Утро (каждый день, 15 минут)

```bash
ssh root@13.140.29.171 'cd /opt/alpha-compute && tail -30 study/nightly-$(date -u +%F).log | cut -c1-200'
ssh root@13.140.29.171 'cd /opt/alpha-compute && for f in study/bounce-verdict-nightly-$(date -u +%F)-*.log; do echo "$f: $(grep -a ИТОГ $f | grep -o "ИТОГ=[^·]*· .*кругов [0-9]*.*bps" | cut -c1-160)"; done'
ssh root@13.140.29.171 'cd /opt/alpha-compute && python3 bin/side-axis.py --prefix b5/nightly-$(date -u +%F)-base/ --verdict-label nightly-$(date -u +%F) --touches study/touches --top 3 | head -40'
```

Записать в `.memory/log.md` одной строкой: сутки в корне, режим дня (медиана %), лонг a45 — n / точка /
нижняя, шорт — лучший набор / знак, `FAILED` в логе (да/нет). Ничего не решать до 7 суток.

## 8. Вопросы владельцу (только числа и выборы; всё остальное — делать)

1. ~~Справочные инструменты в коллектор~~ — снято 20.09: биток берётся минутными свечами по REST (S3), стакан не нужен.
2. **Диск коллектора** — полон ≈ 22.09 вечером (29/49 ГБ на 20.09, +6.5 ГБ/сутки): чистить `deep/`
   (поток `.200`, читателя нет) и `.50`-сутки старше N после переноса на счётную машину — команда
   «чисти» + N; или расширить диск.
3. **Окно суток сеток** `DAYS_WINDOW` — пока все сутки (+5 мин ночи на сутки); можно отложить.

## 9. Чего не делать

- Не менять флоры/дедлайны/номинал руками «под шорт» — только наборами с предрегистрацией.
- Не заводить отдельные цепочки сеток — всё в `nightly-grid.sh` одним процессом.
- Не строить шорт от снятия (S7) раньше ворот лонга.
- Не читать вердикты «мало данных» как знак; не сравнивать наборы по точке без нижней границы и
  без разреза по суткам/монетам.
- Не писать в `SETTLED.md` и не трогать пул без слова владельца.

## 10. Систематики, которые ломают выводы (держать в голове)

- Трекер суточный: `ret_4h` пуст первые 4 ч суток, возраст обнуляется в 00:00 — лонги/шорты первых
  часов недосчитаны одинаково, но при ключах режима — выпадают.
- Все имеющиеся сутки — рост; любой «плюс лонгов» до первого дня падения — не свойство стороны.
- Концентрация по монетам: TRX даёт 27–48 кругов и минус на обеих сторонах, ATOM/ADA — плюс
  лонгов; читать «без лучшей монеты» всегда.
- «Занято» внутри набора: раздельные стороны дают больше кругов, чем `-any` (7875 против 7735) —
  сравнивать наборы только между собой, не с `-any`.
- Число испытаний растёт с каждым набором (сейчас 464 строк `bounce_form`); DSR у победителя будет
  низким по построению — ворота держатся на нижней границе и out-of-sample, не на DSR.
