# Исправления по ревью 23.09 — рабочий список (G12/G13)

Живой список работ после ревью 8 блоков 23.09 (стратегия, коммиты, логика и математика бэктеста, аналитика,
дашборд, мёртвый код, скрипты, архитектура). Статус правит исполнитель. Состояние проекта — только `CLAUDE.md`
«СОСТОЯНИЕ СЕЙЧАС»; здесь — задачи, порядок и как продолжить.

## 1. Что надо исправить в коде

| # | задача | зачем | где идёт | статус 24.09 05:00 |
|---|---|---|---|---|
| R2 | Размер позиции `--order-usd`/`--order-qty-from-pool` по цене сигнала, а не последнего касания записи (`bounce_grid.rs` ~2817) | деньги, заглядывание вперёд | волна 1, пачка | готово `a0b360a` (лот по цене каждого касания, `OrderSizing`); проверка идёт |
| R7 | Средняя цена исполнения при свипе: крейт перезаписывает `exec_price_tick` (`vendor/…/partialfillexchange.rs` ~233), драйвер умножает цену последнего уровня на весь объём (`lob/backtest.rs` ~1476, ~1516) | деньги | волна 1, пачка | готово `a0b360a` (вендор `Order::exec_notional`, разбор §8 `hftbacktest-partialfill-fix`); проверка идёт |
| R3 | Ноги лестницы в целых шагах лота (`strategy.rs` ~1942) | деньги, живой бот | волна 1, пачка | готово `243fce0` + отказ некратного `--order-qty-e9` в `a0b360a` (замечание проверки) |
| R4 | «Пыль» позиции: `position() <= 0.0` точно по f64 (`strategy.rs`) → допуск пол-лота | живой бот | волна 1, пачка | готово `243fce0`, проверено (замечание-нит: прямые тесты `sweep_orphans`/остатка драйвера на пыль — не добавлены) |
| R6 | Колонки `n_carried`/`carry_unverified` в forms.csv только при `--carry-root` (гейт `gate-g10.sh`) | контрольное сравнение | волна 1, пачка | готово `fad9358`, проверено |
| W1 | Коллектор (`bybit/conn.rs`): тишина соединения — **не рвать** (владелец 24.09: «ничего не рви, только следи»), предупреждение в журнал на эпизод + счётчик в сводке сессии; неудачная подписка → событие Disconnected; кадр без лишней копии. Коммит `0c43e6c` сделал разрыв по тишине — переделать | надёжность записи | волна 2 | переделано `7af2c0b` (тишина рынка — только предупреждение, 985 тестов на VPS); проверка нашла блокер — мёртвый транспорт (нет даже pong) не ловится, `silence_max_ns` застывает на первом срабатывании — доработка идёт |
| W2 | Живой поток (`feed/live.rs`): тик обязателен у всех конструкторов (не виснуть), Gap без `format!` на потоке решений | живой бот | волна 2, пачка W2+W3 | не начато |
| W3 | binlog: потолок длины кадра до чтения, предел `max_records_per_frame`, `read_upto` min; кодеки замеров → `binlog/experiments.rs` | нехватка памяти на порченом файле | волна 2, пачка W2+W3 | не начато |
| W4 | Разметка уровней (`lob/levels.rs`): утечка `births`, BTreeMap → отсортированный Vec, разрезка функции кадра на ~400 строк | горячий путь живого бота | волна 2, пачка W4+W5 | не начато |
| W5 | Касания (`commands/lob/touches.rs`): проверки флагов до реплея, const-assert, разрезка `run_touches`, связка имя↔значение колонок | чистота, байт в байт | волна 2, пачка W4+W5 | не начато |
| W6 | Сессия коллектора (`commands/lob/session.rs`): разрезка `impl SessionCtx`, `check_pool_file`, `run_session_loop`; время через `Clock` | чистота, без изменения поведения | волна 2, пачка W6+W7 | не начато |
| W7 | Модуль дашборда (`commands/lob/dashboard.rs`): писать `coin-*.json` сразу (пик памяти ~2,5 ГБ), накопители, дубли `names.rs` | память, чистота | волна 2, пачка W6+W7 | не начато |
| W8 | Мёртвый код: npy-экспорт (`lob export`, ~700 строк), мёртвые `allow`, шапка `stats/mod.rs` | чистота | волна 2, пачка W8–W10 | не начато |
| W9 | Дубли: разбор даты, поиск в instruments.csv, runs/gaps.csv, `day_file_path`, фикстуры session.json в тестах | чистота | волна 2, пачка W8–W10 | не начато |
| W10 | Сверка (`bybit/verify.rs`) и шортлист: дубли, потоковое чтение, разрезка `run_shortlist` | чистота | волна 2, пачка W8–W10 | не начато |
| B1 | «Сироты» ордеров: `adopt_orphans` заводит партию с `accounted: 0` → двойной учёт, позиция без стопа (`strategy.rs` ~688) | живой бот | после слияния волн | готово `cf2b8a6` (партия сирот получает уже учтённое; тест падает на старом коде) |
| B2 | Фильтр входа (режим BTC), план (стоп/тейк/лестница), размер позиции и защиты (выключатель, потолок) — в общую стратегию `lob/` (A6); сейчас в CLI и Python | живой бот = бэктест | после слияния, ~4–6 ч | не начато |
| B3 | Разрезка `bounce_grid.rs` (3000 строк) и `commands/lob/backtest.rs` (2400) на подмодули | чистота | после слияния | не начато |
| B4 | Цены и размеры в стратегии в целых (тики/лоты), `observe_wall_trades` внутрь `on_event`, контракт `clear_inactive_orders`, знак `flatten_residual` у шорта | живой бот | после слияния | частично: знак остатка шорта в страховке круга — `7bdf9a4`; целые тики/лоты, `observe_wall_trades`, `clear_inactive_orders` — не начато |
| B5 | Коллектор: `book::Update` с `Vec` на каждое сообщение (бюджет B2) — пул буферов | скорость | после слияния, нужен план | не начато |
| B6 | Честный рыночный выход по стакану (лимит свипа 100 тиков, `flatten_residual`) | деньги | после слияния | не начато |
| B7 | Импорт архива Bybit в формате ob500 (до 2025-08-20 включительно; с 2025-08-21 — ob200) | старые обвалы | когда дойдём | не начато |

## 2. Что готово

| что | коммит / где | проверка |
|---|---|---|
| Стресс-тест обвала 10–11.10.2025, выключатель BTC −1,5 % | `crash-stress-2026-09-23.md`, E22 | — |
| Перенос `.200` 15–22.09 на дек, чистка коллектора (диск 27 %), ночной забор тянет `.200` | `tools/sync-from-collector.sh` | — |
| Перенос через полночь `--carry-root` (В-94) | `e5c8847` | ревью, тесты |
| Реальный размер позиции `ORDER="--order-usd 500"` в `oos-frozen.sh` (В-89) | `bb622c0` | — |
| R1 трейл: прибыль знаковая, не по модулю | `52d2056`, `baee8e8` | проверено |
| P1 счёт депозита: минутная просадка (mark-to-market), одна позиция на монету, выключатель с направлением | `4d58ed8`, `9dc7d9e` | проверено дважды |
| S1 скрипты: 4 мёртвых в `tools/archive/`, `titrate-forms.sh` вместо трёх, `epoch-bootstrap.sh`, `_env.sh` (журнал), замок вердикта, stderr в логи, `_lib.py`, `sets-nightly.txt`, LF | `08c61ec`…`78e4202` | проверено дважды |
| D1 дашборд: 5 разделов, без лимита по умолчанию, один «пик», мост, глоссарий, даты из данных, клавиатура | `fb95883`, `53da32c` (+ доработка) | идёт проверка |
| Документы: SETTLED В-89…В-94, EXPERIMENTS E22/E23, CLAUDE.md, архив 5 планов и 15 тикетов | `7d206bb`…`b15031f` | — |
| Контроль: хук «одна сборка на машину» (`~/.claude/hooks/alpha_one_build.py`), очередь сборок на VPS (`.build.lock`), `tools/alpha-status.sh` (+ монеты без роста записи > 5 мин) | `d2db71e`, `0597c81` | хук проверен на 9 случаях |
| Правка clippy `trim().split_whitespace()` (другая сессия) | `5401feb` в волне 2 | — |
| Пачка R2/R3/R4/R6/R7: 993 теста / 0, clippy ноль; бинарник `alpha-a0b360a` (затем `alpha-7bdf9a4`) собран на VPS и лежит на деке (`bin/alpha` не переключён) | `fad9358`, `243fce0`, `a0b360a` | R3/R4/R6 — проверено; R2/R7 — идёт |

## 3. Что надо пересчитать или обновить

| # | что | зачем | когда |
|---|---|---|---|
| 1 | **Идёт 24.09 05:00:** бинарник `bin/alpha-7bdf9a4` на деке (было `a0b360a`; +B1, знак остатка шорта); скрипты — в `~/alpha/bin-stage/` (из `git archive`, +x); служба `alpha-chain-u500r` (`~/alpha/.chain-u500r.sh`, журнал `study/chain-u500r.log`) ждёт конца `alpha-titrate-v1-0924` (досчёт v1 старым бинарником после ночи), атомарно ставит скрипты и запускает `BIN=bin/alpha-7bdf9a4 DAY_JOBS=1 recompute-carry.sh u500r` (метка `u500` занята прогоном 23.09 на `e5c8847`). Было: Сборка на VPS (взять `/opt/alpha-compute/.build.lock`) → бинарник на дек; **все** изменённые скрипты на дек атомарно (scp во временный файл + `mv`): `oos-frozen.sh` теперь подключает `_env.sh`; `titrate-forms.sh`, `epoch-bootstrap.sh`, `_lib.py`, `sets-nightly.txt`, `crash-stress.sh`, `recompute-carry.sh`, `portfolio-sim.py`, `titration-dashboard-data.py`, `nightly-grid.sh`, `side-grid.sh`, `titrate*.sh` | без них пересчёт идёт по-старому | после пачки R |
| 2 | Кандидат на $500 со всеми исправлениями: `BIN=bin/alpha-7bdf9a4 bin/recompute-carry.sh u500r` (запускает цепочка п. 1) (история, запись, обвал, затем кейсы) — **после ночи дека** (02:00–02:40 UTC) или `DAY_JOBS=1`: сутки истории 04.09 и 14.09 падали по памяти при 4 параллельных сутках с переносом | главные цифры | после п. 1 |
| 3 | Счёт $2500: `portfolio-sim.py … --deposit-usd 2500 --position-usd 500 --max-pos 0,1,2,3,5 --day-stop-pct 0,2 --btc-kill-bps 0,150 --exclude-set прокиды=… --json` (варианты: кандидат tr1x1, гладкий tr0.5x0.25, BTC 4 ч tk1.75, без фильтра) | прирост, просадка, восстановление | после п. 2 |
| 4 | Дашборд: `titration-dashboard-data.py --position-usd 500 --deposit-usd 2500 …` → `titration-dashboard-build.py --merge protection=…` → публикация по той же ссылке | показать цифры | после п. 3 |
| 5 | Кейсы (стоп, выход, реакция на стену) на $500 — раздел «Надёжность» | полный дашборд | после п. 2, ~3–4 ч счёта |
| 6 | `docs/plan/runs.csv`/`candidates.csv`, CLAUDE.md, `.memory` — числа кандидата | старые +$515/+$272 недействительны | после п. 2 |
| 7 | Доля «оптимистичного» заполнения (`n_fill_by_cross`) у кандидата | насколько верить заполнению ~55 % на $500 | после п. 2 |
| 8 | Монеты-исключения скользящим окном (прокид ≥ 10 %/15 мин за прошлые 30 дней) | сейчас список выбран на тех же днях | после п. 2 |
| 9 | Проверка тишины соединений по записанным дням → перевыкатка коллектора с W1 (сборка на VPS, новый бинарник рядом со старым, `systemctl restart alpha-collector`, откат — старый бинарник) | исправление заработает | после волны 2 и слияния |
| 10 | Проверки стратегии: двойник `.200`/`.50` (G1), монеты того времени, старые обвалы (B7), август, предрегистрация | можно ли верить кандидату | после исправлений |

## 4. Как продолжить

**Новый чат (24.09 04:30):** оркестрации привязаны к сессии, в которой запущены, — из нового чата их не возобновить
по run ID. Первым делом: `bash tools/alpha-status.sh` и `git status --short -- src vendor docs` в основном дереве —
если пачка R2/R7/R3/R4/R6 прервалась, в `src/` останутся незакоммиченные правки прерванного исполнителя
(`bounce_grid.rs`, `strategy.rs`, их `tests.rs`, `docs/COMMANDS.md`): прочитать дифф, довести до конца по таблице 1
или откатить, затем одна сборка и тесты. Что уже закоммичено — `git log --oneline 967f181..HEAD`.

- **Волна 1** — основное дерево `C:/visual projects/alpha`, ветка `audit/design-fixes-2026-09-22`, сборки здесь (одна
  на машину). Оркестрация: скрипт `~/.claude/projects/C--visual-projects-alpha/<сессия>/workflows/scripts/alpha-fix-money-wf_82df131a-484.js`,
  run `wf_82df131a-484`. Если прервалось — пересоздать задачи по таблице 1 (R2, R7, R3, R4, R6 одной пачкой: правки
  по коммиту на задачу, одна сборка и тесты на пачку, 3 проверяющих параллельно без сборок, собирает один).
- **Волна 2** — рабочее дерево `C:/visual projects/alpha-wave2`, ветка `wave2-2026-09-24`; сборки **только на VPS**:
  `bash "C:/visual projects/alpha/tools/vps-check.sh" "C:/visual projects/alpha-wave2" all` (база на VPS — 981 тест,
  с W1 — 984). Скрипт оркестрации `…/alpha-wave2-wf_a77d9143-9f2.js` (задания W1…W10 с точными строками файлов),
  run `wf_a77d9143-9f2`; W1 в нём уже переписан под «не рвать». Пачки: W2+W3, W4+W5, W6+W7, W8+W9+W10.
- **Слияние:** после волны 1 — `git merge wave2-2026-09-24` в основную ветку (файлы почти не пересекаются), полный
  прогон тестов, затем B1–B6.
- **Грабли ночи:** возобновление оркестрации после правки скрипта перезапускает все вызовы после первого
  изменённого по порядку (кэш — по префиксу), даже законченные в других дорожках; лимит использования ~5 ч —
  две волны параллельно выбирают его за ~2,5 ч; heredoc в Bash съедает `\` — правки хука и Python — Edit/Write.
